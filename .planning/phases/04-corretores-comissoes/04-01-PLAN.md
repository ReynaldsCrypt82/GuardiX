---
phase: 04-corretores-comissoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260420_0016_corretores_schema.sql
  - supabase/migrations/20260420_0017_corretores_alter.sql
  - supabase/migrations/20260420_0018_corretores_rls.sql
autonomous: true
requirements:
  - COM-01
  - COM-02
  - COM-04
must_haves:
  truths:
    - "Tabela broker_profiles existe com colunas tenant_id, susep_number, monthly_goal, commission_rate_default, commission_rate_overrides, deleted_at — id é FK para profiles(id)"
    - "Tabela partners existe com colunas tenant_id, name, cnpj, contact_email, contact_phone, commission_rate_default, commission_rate_overrides, deleted_at"
    - "Tabela commission_entries existe sem deleted_at e sem updated_at — verdadeiramente imutável"
    - "Tabelas policies e consortium_quotas têm os novos campos partner_id (UUID, nullable, FK partners) e commission_paid_at (TIMESTAMPTZ, nullable)"
    - "RLS de commission_entries possui SELECT e INSERT, mas NÃO possui UPDATE nem DELETE policies — bloqueio é via ausência de policy"
    - "RLS de broker_profiles e partners restringe INSERT/UPDATE a role='admin' apenas"
    - "Hard DELETE em broker_profiles e partners é bloqueado por trigger prevent_hard_delete (commission_entries não tem trigger)"
    - "Schema aplicado no banco — supabase db query --linked -f executado para cada migration sem erro"
  artifacts:
    - path: "supabase/migrations/20260420_0016_corretores_schema.sql"
      provides: "broker_profiles + partners + commission_entries tables, índices, triggers"
      contains: "CREATE TABLE public.commission_entries"
    - path: "supabase/migrations/20260420_0017_corretores_alter.sql"
      provides: "ALTER TABLE policies + consortium_quotas add partner_id, commission_paid_at"
      contains: "ADD COLUMN partner_id"
    - path: "supabase/migrations/20260420_0018_corretores_rls.sql"
      provides: "RLS policies para 3 novas tabelas — sem UPDATE/DELETE em commission_entries"
      contains: "CREATE POLICY \"commission_entries_insert\""
  key_links:
    - from: "broker_profiles.id"
      to: "profiles.id"
      via: "FK 1:1 ON DELETE CASCADE"
      pattern: "REFERENCES public.profiles\\(id\\)"
    - from: "commission_entries.broker_id"
      to: "profiles.id (nullable)"
      via: "FK"
      pattern: "broker_id\\s+UUID\\s+REFERENCES public.profiles"
    - from: "commission_entries.partner_id"
      to: "partners.id (nullable)"
      via: "FK"
      pattern: "partner_id\\s+UUID\\s+REFERENCES public.partners"
    - from: "commission_entries.policy_id"
      to: "policies.id (nullable)"
      via: "FK"
      pattern: "policy_id\\s+UUID\\s+REFERENCES public.policies"
    - from: "commission_entries.quota_id"
      to: "consortium_quotas.id (nullable)"
      via: "FK"
      pattern: "quota_id\\s+UUID\\s+REFERENCES public.consortium_quotas"
    - from: "policies.partner_id"
      to: "partners.id"
      via: "FK nullable"
      pattern: "ALTER TABLE public.policies\\s+ADD COLUMN partner_id"
---

<objective>
Criar a fundação de dados da Phase 4: três novas tabelas (broker_profiles, partners, commission_entries) + dois ALTER TABLE em policies e consortium_quotas + todas as RLS policies necessárias. Aplicar as migrations no banco usando o workaround `supabase db query --linked -f` registrado no STATE.md.

Esta é a base obrigatória para Plans 02-04. Sem ela, Server Actions falham ao rodar e tipos divergem do schema real.

Purpose: Garantir que todas as tabelas e relações existem antes de qualquer código TypeScript depender delas. RLS aplica os limites de tenant e o ledger imutável (D-10).

Output: 3 arquivos SQL de migration + schema aplicado no banco linked do Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-corretores-comissoes/04-CONTEXT.md
@.planning/phases/04-corretores-comissoes/04-RESEARCH.md
@supabase/migrations/20260420_0011_seguros_schema.sql
@supabase/migrations/20260420_0012_seguros_rls.sql
@supabase/migrations/20260420_0013_consorcio_schema.sql
@supabase/migrations/20260420_0014_consorcio_rls.sql
@supabase/migrations/20260420_0002_rls_helpers.sql

<interfaces>
<!-- RLS helper functions disponíveis (já criados na Phase 1) -->

```sql
-- public.jwt_tenant_id() RETURNS UUID — STABLE
-- public.jwt_tenant_role() RETURNS TEXT — STABLE
-- public.jwt_tenant_slug() RETURNS TEXT — STABLE
-- public.set_updated_at() — trigger function (atualiza updated_at)
-- public.prevent_hard_delete() — trigger function (bloqueia DELETE físico)
```

Padrão estabelecido (de seguros/consorcio RLS):
- Tenant isolation: `tenant_id = (SELECT public.jwt_tenant_id())`
- Role check: `(SELECT public.jwt_tenant_role()) IN ('admin','corretor')`
- Wrapping com (SELECT ...) é OBRIGATÓRIO para query plan caching
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration de schema das 3 novas tabelas (broker_profiles, partners, commission_entries)</name>
  <files>supabase/migrations/20260420_0016_corretores_schema.sql</files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-02, D-04, D-10 — definição completa de campos)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Migration Strategy, Code Examples — Schema de commission_entries)
    - supabase/migrations/20260420_0011_seguros_schema.sql (padrão de CREATE TABLE com triggers e índices)
    - supabase/migrations/20260420_0013_consorcio_schema.sql (padrão consortium_groups que tem credit_value)
  </read_first>
  <action>
Criar `supabase/migrations/20260420_0016_corretores_schema.sql` com EXATAMENTE o seguinte conteúdo (preserve cada CREATE TABLE, INDEX e TRIGGER):

```sql
-- Phase 04 Plan 01 — Corretores & Comissoes Schema
-- References: 04-CONTEXT.md D-02, D-04, D-10
-- broker_profiles: 1:1 com profiles.id, soft delete
-- partners: standalone (sem login), soft delete
-- commission_entries: append-only, SEM updated_at, SEM deleted_at (imutável por design)

-- ========================================================================
-- broker_profiles — atributos de negocio do corretor (D-02)
-- id e FK 1:1 para profiles(id) — corretor existe como profile + broker_profile
-- ========================================================================
CREATE TABLE public.broker_profiles (
  id                        UUID PRIMARY KEY REFERENCES public.profiles(id) ON DELETE CASCADE,
  tenant_id                 UUID NOT NULL REFERENCES public.tenants(id),
  susep_number              TEXT,
  monthly_goal              NUMERIC(12,2) NOT NULL DEFAULT 0 CHECK (monthly_goal >= 0),
  commission_rate_default   NUMERIC(5,4) NOT NULL DEFAULT 0 CHECK (commission_rate_default >= 0 AND commission_rate_default <= 1),
  commission_rate_overrides JSONB NOT NULL DEFAULT '{}'::jsonb,
  deleted_at                TIMESTAMPTZ,
  created_at                TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at                TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_broker_profiles_tenant_id  ON public.broker_profiles(tenant_id);
CREATE INDEX idx_broker_profiles_deleted_at ON public.broker_profiles(deleted_at) WHERE deleted_at IS NULL;

CREATE TRIGGER trg_broker_profiles_updated_at
  BEFORE UPDATE ON public.broker_profiles
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

CREATE TRIGGER trg_broker_profiles_no_hard_delete
  BEFORE DELETE ON public.broker_profiles
  FOR EACH ROW EXECUTE FUNCTION public.prevent_hard_delete();

-- ========================================================================
-- partners — parceiros externos (D-04) sem login (entidade independente)
-- ========================================================================
CREATE TABLE public.partners (
  id                        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id                 UUID NOT NULL REFERENCES public.tenants(id),
  name                      TEXT NOT NULL,
  cnpj                      TEXT,
  contact_email             TEXT,
  contact_phone             TEXT,
  commission_rate_default   NUMERIC(5,4) NOT NULL DEFAULT 0 CHECK (commission_rate_default >= 0 AND commission_rate_default <= 1),
  commission_rate_overrides JSONB NOT NULL DEFAULT '{}'::jsonb,
  deleted_at                TIMESTAMPTZ,
  created_at                TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at                TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_partners_tenant_id  ON public.partners(tenant_id);
CREATE INDEX idx_partners_deleted_at ON public.partners(deleted_at) WHERE deleted_at IS NULL;

CREATE TRIGGER trg_partners_updated_at
  BEFORE UPDATE ON public.partners
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

CREATE TRIGGER trg_partners_no_hard_delete
  BEFORE DELETE ON public.partners
  FOR EACH ROW EXECUTE FUNCTION public.prevent_hard_delete();

-- ========================================================================
-- commission_entries — ledger append-only (D-10)
-- CRITICAL: SEM updated_at, SEM deleted_at, SEM trigger prevent_hard_delete
-- (RLS bloqueia UPDATE e DELETE pela AUSENCIA de policies)
-- ========================================================================
CREATE TABLE public.commission_entries (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id       UUID NOT NULL REFERENCES public.tenants(id),
  entry_type      TEXT NOT NULL CHECK (entry_type IN ('comissao','estorno','correcao')),
  broker_id       UUID REFERENCES public.profiles(id),
  partner_id      UUID REFERENCES public.partners(id),
  policy_id       UUID REFERENCES public.policies(id),
  quota_id        UUID REFERENCES public.consortium_quotas(id),
  amount          NUMERIC(12,2) NOT NULL,
  rate_used       NUMERIC(5,4),
  reference_month DATE NOT NULL,
  notes           TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  -- Garante que cada entry tem broker_id OU partner_id (xor logico)
  CONSTRAINT commission_entries_recipient_check CHECK (
    (broker_id IS NOT NULL AND partner_id IS NULL)
    OR (broker_id IS NULL AND partner_id IS NOT NULL)
  ),
  -- Garante que cada entry tem policy_id OU quota_id
  CONSTRAINT commission_entries_source_check CHECK (
    (policy_id IS NOT NULL AND quota_id IS NULL)
    OR (policy_id IS NULL AND quota_id IS NOT NULL)
  )
);

CREATE INDEX idx_commission_entries_tenant_id        ON public.commission_entries(tenant_id);
CREATE INDEX idx_commission_entries_broker_id        ON public.commission_entries(broker_id) WHERE broker_id IS NOT NULL;
CREATE INDEX idx_commission_entries_partner_id       ON public.commission_entries(partner_id) WHERE partner_id IS NOT NULL;
CREATE INDEX idx_commission_entries_policy_id        ON public.commission_entries(policy_id) WHERE policy_id IS NOT NULL;
CREATE INDEX idx_commission_entries_quota_id         ON public.commission_entries(quota_id) WHERE quota_id IS NOT NULL;
CREATE INDEX idx_commission_entries_reference_month  ON public.commission_entries(reference_month);

-- Sem trigger set_updated_at (nao ha updated_at)
-- Sem trigger prevent_hard_delete (nao ha deleted_at; RLS barra tudo via ausencia de policy)
```

Notas críticas:
- `broker_profiles.id` é FK para `profiles(id)` (1:1) — não usa `gen_random_uuid()` como default; o id vem do profile.
- `commission_entries` tem CHECK constraints que garantem XOR entre broker/partner e entre policy/quota.
- `commission_entries` NÃO tem `updated_at` nem `deleted_at` por design (D-10 — verdadeiramente imutável).
- Índices parciais (`WHERE column IS NOT NULL`) são otimização padrão do projeto.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE public.commission_entries" supabase/migrations/20260420_0016_corretores_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260420_0016_corretores_schema.sql` existe
    - Conteúdo contém literal `CREATE TABLE public.broker_profiles`
    - Conteúdo contém literal `CREATE TABLE public.partners`
    - Conteúdo contém literal `CREATE TABLE public.commission_entries`
    - Conteúdo contém literal `entry_type TEXT NOT NULL CHECK (entry_type IN ('comissao','estorno','correcao'))`
    - Conteúdo contém literal `commission_entries_recipient_check`
    - Conteúdo contém literal `commission_entries_source_check`
    - Conteúdo NÃO contém `updated_at` na definição de `commission_entries` (grep `commission_entries[^;]*updated_at` retorna 0)
    - Conteúdo NÃO contém `deleted_at` na definição de `commission_entries`
    - Conteúdo contém `REFERENCES public.profiles(id) ON DELETE CASCADE` na linha do `broker_profiles.id`
    - Conteúdo contém os 6 índices `idx_commission_entries_*`
  </acceptance_criteria>
  <done>Migration SQL completa, válida, com CHECK constraints e sem updated_at/deleted_at em commission_entries.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration de ALTER TABLE em policies e consortium_quotas</name>
  <files>supabase/migrations/20260420_0017_corretores_alter.sql</files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-05 — partner_id e commission_paid_at)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Migration Strategy — Alteracoes em tabelas existentes)
    - supabase/migrations/20260420_0011_seguros_schema.sql (estrutura atual de policies)
    - supabase/migrations/20260420_0013_consorcio_schema.sql (estrutura atual de consortium_quotas)
  </read_first>
  <action>
Criar `supabase/migrations/20260420_0017_corretores_alter.sql` com EXATAMENTE o seguinte conteúdo:

```sql
-- Phase 04 Plan 01 — ALTER TABLE policies + consortium_quotas
-- References: 04-CONTEXT.md D-05, D-09
-- Adiciona partner_id (FK nullable) e commission_paid_at (TIMESTAMPTZ nullable)

-- policies
ALTER TABLE public.policies
  ADD COLUMN partner_id UUID REFERENCES public.partners(id),
  ADD COLUMN commission_paid_at TIMESTAMPTZ;

CREATE INDEX idx_policies_partner_id
  ON public.policies(partner_id)
  WHERE partner_id IS NOT NULL;

CREATE INDEX idx_policies_commission_paid_at
  ON public.policies(commission_paid_at)
  WHERE commission_paid_at IS NOT NULL;

-- consortium_quotas
ALTER TABLE public.consortium_quotas
  ADD COLUMN partner_id UUID REFERENCES public.partners(id),
  ADD COLUMN commission_paid_at TIMESTAMPTZ;

CREATE INDEX idx_consortium_quotas_partner_id
  ON public.consortium_quotas(partner_id)
  WHERE partner_id IS NOT NULL;

CREATE INDEX idx_consortium_quotas_commission_paid_at
  ON public.consortium_quotas(commission_paid_at)
  WHERE commission_paid_at IS NOT NULL;
```

Esta migration depende da migration 0016 já existir (cria a tabela `partners`). Manter número 0017 garante ordem.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN partner_id UUID REFERENCES public.partners" supabase/migrations/20260420_0017_corretores_alter.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260420_0017_corretores_alter.sql` existe
    - Conteúdo contém literal `ALTER TABLE public.policies` com `ADD COLUMN partner_id UUID REFERENCES public.partners(id)`
    - Conteúdo contém literal `ALTER TABLE public.policies` com `ADD COLUMN commission_paid_at TIMESTAMPTZ`
    - Conteúdo contém literal `ALTER TABLE public.consortium_quotas` com `ADD COLUMN partner_id UUID REFERENCES public.partners(id)`
    - Conteúdo contém literal `ALTER TABLE public.consortium_quotas` com `ADD COLUMN commission_paid_at TIMESTAMPTZ`
    - Conteúdo contém os 4 CREATE INDEX (partner_id e commission_paid_at em ambas as tabelas)
    - Comando `grep -c "ADD COLUMN partner_id UUID REFERENCES public.partners" arquivo` retorna 2
  </acceptance_criteria>
  <done>Migration de ALTER TABLE adiciona partner_id e commission_paid_at em policies e consortium_quotas com índices parciais.</done>
</task>

<task type="auto">
  <name>Task 3: Criar migration de RLS policies (broker_profiles, partners, commission_entries)</name>
  <files>supabase/migrations/20260420_0018_corretores_rls.sql</files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-03 admin only, D-10 ledger immutability)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Pattern 1: Append-Only Ledger; Migration Strategy — RLS table)
    - supabase/migrations/20260420_0012_seguros_rls.sql (padrão de RLS com `(SELECT public.jwt_tenant_id())`)
    - supabase/migrations/20260420_0014_consorcio_rls.sql (padrão de role guard)
    - supabase/migrations/20260420_0002_rls_helpers.sql (funções jwt_tenant_id/jwt_tenant_role disponíveis)
  </read_first>
  <action>
Criar `supabase/migrations/20260420_0018_corretores_rls.sql` com EXATAMENTE o seguinte conteúdo. CRITICAL: NÃO criar policies de UPDATE ou DELETE para `commission_entries` — a ausência é a proteção (D-10).

```sql
-- Phase 04 Plan 01 — RLS: broker_profiles, partners, commission_entries
-- References: 04-RESEARCH.md Pattern 1 (Append-Only Ledger), Migration Strategy table
-- CRITICAL: commission_entries SEM UPDATE/DELETE policies — ausencia = bloqueio

ALTER TABLE public.broker_profiles    ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.partners           ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.commission_entries ENABLE ROW LEVEL SECURITY;

-- ========================================================================
-- broker_profiles
-- D-03: apenas admin pode INSERT/UPDATE; todos os roles autenticados leem
-- corretor pode ler somente o proprio (id = auth.uid())
-- ========================================================================
CREATE POLICY "broker_profiles_select" ON public.broker_profiles
  FOR SELECT TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
    AND (
      (SELECT public.jwt_tenant_role()) IN ('admin','financeiro','visualizador')
      OR id = (SELECT auth.uid())
    )
  );

CREATE POLICY "broker_profiles_insert" ON public.broker_profiles
  FOR INSERT TO authenticated
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  );

CREATE POLICY "broker_profiles_update" ON public.broker_profiles
  FOR UPDATE TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  )
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  );

-- ========================================================================
-- partners
-- D-04 / D-03: apenas admin INSERT/UPDATE; todos roles leem (D-12 dashboards)
-- ========================================================================
CREATE POLICY "partners_select" ON public.partners
  FOR SELECT TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
  );

CREATE POLICY "partners_insert" ON public.partners
  FOR INSERT TO authenticated
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  );

CREATE POLICY "partners_update" ON public.partners
  FOR UPDATE TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  )
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) = 'admin'
  );

-- ========================================================================
-- commission_entries — APPEND-ONLY (D-10)
-- SELECT: admin/financeiro veem tudo; corretor ve as proprias (broker_id = auth.uid())
-- INSERT: admin e corretor podem inserir
-- UPDATE: SEM POLICY — proibido
-- DELETE: SEM POLICY — proibido
-- ========================================================================
CREATE POLICY "commission_entries_select" ON public.commission_entries
  FOR SELECT TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (
      (SELECT public.jwt_tenant_role()) IN ('admin','financeiro','visualizador')
      OR broker_id = (SELECT auth.uid())
    )
  );

CREATE POLICY "commission_entries_insert" ON public.commission_entries
  FOR INSERT TO authenticated
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) IN ('admin','corretor')
  );

-- CRITICAL: NUNCA criar CREATE POLICY ... FOR UPDATE OR FOR DELETE em commission_entries
-- A ausencia destas policies = acesso negado por default (D-10 imutabilidade)
```

NÃO adicionar policies UPDATE/DELETE em commission_entries. NÃO usar `SELECT public.jwt_tenant_id()` sem o `(SELECT ...)` wrapper.
  </action>
  <verify>
    <automated>grep -c "CREATE POLICY" supabase/migrations/20260420_0018_corretores_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260420_0018_corretores_rls.sql` existe
    - Conteúdo contém literal `ALTER TABLE public.broker_profiles    ENABLE ROW LEVEL SECURITY` (ou similar com whitespace flexível)
    - Conteúdo contém literal `ALTER TABLE public.commission_entries ENABLE ROW LEVEL SECURITY`
    - Conteúdo contém exatamente 8 ocorrências de `CREATE POLICY` (3 broker_profiles + 3 partners + 2 commission_entries)
    - Conteúdo contém literal `CREATE POLICY "commission_entries_insert"`
    - Conteúdo contém literal `CREATE POLICY "commission_entries_select"`
    - Conteúdo NÃO contém `commission_entries_update` (zero ocorrências)
    - Conteúdo NÃO contém `commission_entries_delete`
    - Conteúdo NÃO contém literal `FOR UPDATE` na seção de `commission_entries` (grep `commission_entries.*FOR UPDATE` retorna 0)
    - Todas as `CREATE POLICY` que referenciam `jwt_tenant_id` ou `jwt_tenant_role` usam o pattern `(SELECT public.jwt_tenant_*())`
  </acceptance_criteria>
  <done>RLS aplicado nas 3 tabelas com a propriedade crítica: commission_entries não tem UPDATE/DELETE policy.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Aplicar migrations no banco via supabase db query --linked -f</name>
  <files>(no file modifications — runs SQL against Supabase)</files>
  <read_first>
    - supabase/migrations/20260420_0016_corretores_schema.sql (validar que existe e foi escrito corretamente)
    - supabase/migrations/20260420_0017_corretores_alter.sql
    - supabase/migrations/20260420_0018_corretores_rls.sql
    - .planning/STATE.md (workaround: "[Phase 03-01]: supabase db push contornado via supabase db query --linked -f por versioning collision")
  </read_first>
  <action>
Aplicar as 3 migrations sequencialmente usando o workaround documentado no STATE.md (collision de versioning impede `supabase db push` quando data prefix é igual).

Executar EXATAMENTE estes comandos, em ordem, parando se algum falhar:

```bash
supabase db query --linked -f supabase/migrations/20260420_0016_corretores_schema.sql
supabase db query --linked -f supabase/migrations/20260420_0017_corretores_alter.sql
supabase db query --linked -f supabase/migrations/20260420_0018_corretores_rls.sql
```

Após aplicar, validar via query inline que as tabelas existem:

```bash
supabase db query --linked --execute "SELECT to_regclass('public.broker_profiles') AS broker_profiles, to_regclass('public.partners') AS partners, to_regclass('public.commission_entries') AS commission_entries;"
```

Esperado: cada coluna retorna o oid (não NULL) — indica que a tabela existe.

Validar que os ALTER funcionaram:

```bash
supabase db query --linked --execute "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='policies' AND column_name IN ('partner_id','commission_paid_at') ORDER BY column_name;"
```

Esperado: 2 linhas retornadas (`commission_paid_at`, `partner_id`).

Validar que commission_entries NÃO tem policy de UPDATE:

```bash
supabase db query --linked --execute "SELECT cmd FROM pg_policies WHERE schemaname='public' AND tablename='commission_entries' ORDER BY cmd;"
```

Esperado: apenas linhas com `cmd` = `r` (SELECT) e `a` (INSERT). NÃO deve aparecer `w` (UPDATE) nem `d` (DELETE).

Se `supabase db query --linked -f` falhar com erro de auth/login, o operador deve rodar `supabase login` antes — flag `autonomous: false` no plan se isso acontecer durante execução. Caso falhe por collision de versioning, o workaround DOCUMENTADO já é o uso de `db query --linked -f` (este task) — re-tentar não resolve, escalar.
  </action>
  <verify>
    <automated>supabase db query --linked --execute "SELECT to_regclass('public.commission_entries')::text AS t;" 2>&1 | grep -v "NULL"</automated>
  </verify>
  <acceptance_criteria>
    - Comando `supabase db query --linked -f supabase/migrations/20260420_0016_corretores_schema.sql` retorna exit code 0
    - Comando `supabase db query --linked -f supabase/migrations/20260420_0017_corretores_alter.sql` retorna exit code 0
    - Comando `supabase db query --linked -f supabase/migrations/20260420_0018_corretores_rls.sql` retorna exit code 0
    - Query `SELECT to_regclass('public.broker_profiles')` retorna não-NULL
    - Query `SELECT to_regclass('public.partners')` retorna não-NULL
    - Query `SELECT to_regclass('public.commission_entries')` retorna não-NULL
    - Query em `information_schema.columns` retorna 2 linhas para `policies` (`partner_id`, `commission_paid_at`)
    - Query em `information_schema.columns` retorna 2 linhas para `consortium_quotas` (`partner_id`, `commission_paid_at`)
    - Query em `pg_policies` para `commission_entries` retorna apenas comandos SELECT (`r`) e INSERT (`a`) — NÃO retorna UPDATE (`w`) nem DELETE (`d`)
  </acceptance_criteria>
  <done>Schema aplicado no banco linked. As 3 tabelas existem, os ALTER foram aplicados, e commission_entries não tem policies UPDATE/DELETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → PostgreSQL | Inputs validados via Zod safeParse antes de qualquer DB call |
| Authenticated user (JWT) → RLS | tenant_id e role extraídos do JWT (app_metadata), nunca do FormData |
| Admin role → broker_profiles/partners writes | Role guard via `(SELECT public.jwt_tenant_role()) = 'admin'` na RLS |
| Anyone → commission_entries UPDATE/DELETE | BLOQUEADO pela ausência de policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | broker_profiles INSERT/UPDATE | mitigate | RLS WITH CHECK exige `jwt_tenant_role() = 'admin'` (Task 3) |
| T-04-02 | Tampering | commission_entries (mutate after insert) | mitigate | RLS sem policies UPDATE/DELETE — ausência = bloqueio (Task 3) |
| T-04-03 | Information Disclosure | broker_profiles cross-tenant | mitigate | Tenant isolation `tenant_id = (SELECT public.jwt_tenant_id())` em todas as policies |
| T-04-04 | Information Disclosure | commission_entries de outros corretores | mitigate | Policy `commission_entries_select` filtra por `broker_id = auth.uid()` para role corretor |
| T-04-05 | Tampering | commission_entries.recipient (broker XOR partner) | mitigate | CHECK constraint `commission_entries_recipient_check` no schema (Task 1) |
| T-04-06 | Tampering | commission_entries.source (policy XOR quota) | mitigate | CHECK constraint `commission_entries_source_check` no schema (Task 1) |
| T-04-07 | Elevation of Privilege | service_role bypass de RLS em commission_entries | accept | Pitfall 4 do RESEARCH.md — código de Server Action NUNCA usa createAdminClient para commission-entries; mitigação está no Plan 02 |
</threat_model>

<verification>
- 3 arquivos SQL existem em `supabase/migrations/` com prefixos 0016, 0017, 0018
- Schema aplicado no banco linked do Supabase
- Validações de pg_policies confirmam ledger imutável
- Após este plan, Plan 02 (Server Actions) e Plan 03/04 (UI) podem rodar sem erro de "table not found"
</verification>

<success_criteria>
- Tabelas `broker_profiles`, `partners`, `commission_entries` existem no schema `public`
- Colunas `partner_id` e `commission_paid_at` adicionadas em `policies` e `consortium_quotas`
- RLS habilitado nas 3 novas tabelas com policies que respeitam tenant isolation e role check
- Commission ledger é verdadeiramente imutável (sem UPDATE/DELETE policies)
- pg_policies confirma propriedade crítica de imutabilidade
</success_criteria>

<output>
After completion, create `.planning/phases/04-corretores-comissoes/04-01-SUMMARY.md` with:
- Migrations criadas e aplicadas
- Verificação pg_policies (apenas SELECT + INSERT em commission_entries)
- Tabelas confirmadas via to_regclass
- Próximo passo: Plan 02 (Server Actions + utils + Wave 0 tests)
</output>
