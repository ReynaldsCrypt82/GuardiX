---
phase: 04-corretores-comissoes
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01-PLAN
  - 04-02-PLAN
files_modified:
  - src/components/corretores/stat-card.tsx
  - src/components/corretores/month-selector.tsx
  - src/components/corretores/commission-entry-badge.tsx
  - src/components/corretores/commission-table.tsx
  - src/components/corretores/estorno-dialog.tsx
  - src/components/corretores/correcao-dialog.tsx
  - src/components/seguros/mark-commission-paid-dialog.tsx
  - src/components/seguros/commission-paid-badge.tsx
  - src/app/(app)/[slug]/corretores/[id]/page.tsx
  - src/app/(app)/[slug]/seguros/[id]/page.tsx
  - src/app/(app)/[slug]/consorcio/[id]/page.tsx
autonomous: true
requirements:
  - COM-03
  - COM-04
  - COM-05
  - COM-06
must_haves:
  truths:
    - "Rota /[slug]/corretores/[id] renderiza Server Component que verifica role: corretor com id !== user.id e redirecionado para /[slug]/corretores/{user.id} (D-11, T-04-04)"
    - "Dashboard exibe 4 stat cards (D-12): Producao do mes, Comissao acumulada, Carteira ativa, Meta atingida"
    - "Producao do mes = count de policies onde assigned_to=brokerId no mes selecionado"
    - "Comissao acumulada = soma de commission_entries.amount onde broker_id=brokerId E reference_month=primeiro-dia-do-mes-selecionado"
    - "Carteira ativa = count distinto de client_id em (policies UNION consortium_quotas) onde assigned_to=brokerId E deleted_at IS NULL — implementado via 2 queries paralelas + Set (Pitfall 5)"
    - "Meta atingida = (comissao acumulada / monthly_goal) * 100, formatada como percent; quando >=100 mostra 'Meta atingida!' e Progress com [&>div]:bg-green-500"
    - "Month selector e <select> nativo dentro de form GET, submete ?month=YYYY-MM via URL; default = mes corrente"
    - "Tabs 'Visao geral' e 'Relatorio de comissoes' usam shadcn Tabs"
    - "Tab 'Relatorio de comissoes' lista commission_entries do broker_id no mes selecionado, sem paginacao"
    - "Commission table colunas: Data, Tipo (badge color-coded), Referencia (link), Valor (BRL right-aligned), Taxa, Notas"
    - "Commission entry badge: comissao=green-100, estorno=red-100, correcao=blue-100 (UI-SPEC)"
    - "Linhas de estorno tem Valor em text-destructive (negativo)"
    - "Pagina de apolice (/seguros/[id]) exibe botao 'Marcar comissao como paga' quando commission_paid_at IS NULL; quando paga, exibe Badge 'Comissao paga' com data formatada"
    - "Pagina de cota (/consorcio/[id] na linha da cota) exibe mesmo botao + badge"
    - "Mark-commission-paid Dialog calcula valores antes de exibir (chama resolveCommissionRate do Plan 02 no Server Component pai e passa via props) e ao confirmar chama markCommissionPaidAction"
    - "Estorno Dialog e Correcao Dialog: componentes criados em Task 2 com import correto das Server Actions, MAS nao montados em nenhuma UI em Phase 4 (deferido a Phase 6 — ver <deferred_decisions>)"
    - "npm run build verde"
  artifacts:
    - path: "src/components/corretores/stat-card.tsx"
      provides: "Card reutilizavel para metricas (titulo + valor 24px semibold + subtext)"
      contains: "export function StatCard"
    - path: "src/components/corretores/month-selector.tsx"
      provides: "Form GET com <select> de meses (12 ultimos + atual) que submete ?month=YYYY-MM"
      contains: "export function MonthSelector"
    - path: "src/components/corretores/commission-entry-badge.tsx"
      provides: "Badge color-coded por entry_type (comissao/estorno/correcao)"
      contains: "export function CommissionEntryBadge"
    - path: "src/components/corretores/commission-table.tsx"
      provides: "Tabela read-only de commission_entries com colunas D-13"
      contains: "export function CommissionTable"
    - path: "src/components/corretores/estorno-dialog.tsx"
      provides: "Dialog que invoca registerEstornoAction"
      contains: "import { registerEstornoAction"
    - path: "src/components/corretores/correcao-dialog.tsx"
      provides: "Dialog que invoca registerCorrecaoAction"
      contains: "import { registerCorrecaoAction"
    - path: "src/components/seguros/mark-commission-paid-dialog.tsx"
      provides: "Dialog de confirmacao com valores pre-calculados (broker + partner se existir)"
      contains: "import { markCommissionPaidAction"
    - path: "src/components/seguros/commission-paid-badge.tsx"
      provides: "Badge read-only 'Comissao paga' + data formatada (substitui o botao apos pagamento)"
      contains: "export function CommissionPaidBadge"
    - path: "src/app/(app)/[slug]/corretores/[id]/page.tsx"
      provides: "Server Component do dashboard individual: redirect, 4 stat cards, month selector, tabs"
      contains: "export default async function BrokerDashboardPage"
  key_links:
    - from: "src/app/(app)/[slug]/corretores/[id]/page.tsx"
      to: "src/components/corretores/stat-card.tsx + month-selector.tsx + commission-table.tsx"
      via: "import + render no JSX"
      pattern: "import \\{ StatCard \\}"
    - from: "src/app/(app)/[slug]/corretores/[id]/page.tsx"
      to: "redirect when role='corretor' && id !== user.id"
      via: "next/navigation redirect"
      pattern: "if \\(role === 'corretor' && id !== user.id\\)"
    - from: "src/components/seguros/mark-commission-paid-dialog.tsx"
      to: "src/lib/actions/commission-entries.ts markCommissionPaidAction"
      via: "import + handleSubmit"
      pattern: "import \\{ markCommissionPaidAction \\}"
    # NOTE: EstornoDialog and CorrecaoDialog are CREATED in Task 2 but intentionally NOT mounted in any UI in Phase 4.
    # They remain available as components; mounting (with admin-side trigger flow) is deferred to Phase 6 — see <deferred_decisions>.
    - from: "src/app/(app)/[slug]/seguros/[id]/page.tsx"
      to: "src/components/seguros/mark-commission-paid-dialog.tsx + commission-paid-badge.tsx"
      via: "import + conditional render baseado em policy.commission_paid_at"
      pattern: "import \\{ MarkCommissionPaidDialog \\}"
    - from: "src/app/(app)/[slug]/consorcio/[id]/page.tsx"
      to: "src/components/seguros/mark-commission-paid-dialog.tsx (reutilizado para cotas)"
      via: "import + render por linha de cota contemplada"
      pattern: "import \\{ MarkCommissionPaidDialog \\}"
---

<objective>
Construir o dashboard individual do corretor (/[slug]/corretores/[id]) e integrar o gatilho de pagamento de comissao (D-09) nas paginas de apolice e cota. Esta camada cobre:

- COM-03: gatilho de calculo automatico — botao "Marcar comissao como paga" + Dialog que invoca markCommissionPaidAction (split D-06 quando ha parceiro)
- COM-04: ledger imutavel — estorno e correcao registrados como NOVOS lancamentos (registerEstornoAction, registerCorrecaoAction)
- COM-05: relatorio mensal — aba "Relatorio de comissoes" com tabela filtrada por reference_month
- COM-06: dashboard individual — 4 stat cards (producao, comissao, carteira, meta%) + redirect quando corretor acessa outro id

Plan 04 e a unica peca que escreve no ledger via UI. Plan 03 cobre admin de corretores/parceiros; Plan 04 cobre o fluxo operacional do corretor.

Purpose: Fechar Phase 4 entregando o circuito completo: admin cadastra corretor (Plan 03) -> apolice/cota e registrada (Phase 3) -> admin/corretor clica "Marcar comissao como paga" (Plan 04) -> ledger recebe 1 ou 2 entries -> dashboard mostra metricas reais.

Output: 9 novos componentes + 1 nova rota + 2 paginas existentes estendidas com 1 botao + 1 badge cada. npm run build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-corretores-comissoes/04-CONTEXT.md
@.planning/phases/04-corretores-comissoes/04-RESEARCH.md
@.planning/phases/04-corretores-comissoes/04-UI-SPEC.md
@.planning/phases/04-corretores-comissoes/04-VALIDATION.md
@src/app/(app)/[slug]/seguros/[id]/page.tsx
@src/app/(app)/[slug]/consorcio/[id]/page.tsx
@src/components/seguros/vigencia-badge.tsx
@src/components/consorcio/quota-table.tsx
@src/components/consorcio/contemplation-dialog.tsx

<interfaces>
<!-- Server Actions consumidas (criadas no Plan 02) -->

```typescript
// src/lib/actions/commission-entries.ts
export async function markCommissionPaidAction(
  slug: string,
  sourceType: 'policy' | 'quota',
  sourceId: string,
  notes?: string,
): Promise<{ error?: string } | { success: true }>
// Server Action busca o source (policy ou quota), calcula amounts via resolveCommissionRate,
// insere 1 ou 2 commission_entries (broker + partner se existir), seta commission_paid_at no source.
// Idempotencia: se commission_paid_at IS NOT NULL retorna { error: 'Comissao ja registrada para este item.' }

export async function registerEstornoAction(
  slug: string,
  formData: FormData,
): Promise<{ error?: ... } | { success: true }>
// FormData: original_entry_id (UUID), notes (motivo, required)
// Insere uma nova entry com entry_type='estorno', amount = -original.amount, mesmas FK references

export async function registerCorrecaoAction(
  slug: string,
  formData: FormData,
): Promise<{ error?: ... } | { success: true }>
// FormData: broker_id ou partner_id (uma das duas), policy_id ou quota_id (uma), amount (numero, pos ou neg), notes (required), reference_month (YYYY-MM-DD)
// Insere nova entry com entry_type='correcao'
```

<!-- Util consumida (Plan 02) -->
```typescript
// src/lib/utils/commission-rate.ts
export function resolveCommissionRate(
  overrides: CommissionRateOverrides | null | undefined,
  defaultRate: number,
  productType: string,
): number
```

<!-- Schema do banco (Plan 01) -->
```sql
-- commission_entries: id, tenant_id, entry_type, broker_id, partner_id, policy_id, quota_id,
--                     amount, rate_used, reference_month, notes, created_at
-- broker_profiles: id (=profile.id), susep_number, monthly_goal, commission_rate_default,
--                  commission_rate_overrides (JSONB)
-- partners: id, name, commission_rate_default, commission_rate_overrides (JSONB)
-- policies: + partner_id (UUID nullable), commission_paid_at (TIMESTAMPTZ nullable)
-- consortium_quotas: + partner_id (UUID nullable), commission_paid_at (TIMESTAMPTZ nullable)
-- consortium_groups: type ('auto'|'imovel'|'servico'), credit_value
```

<!-- date-fns disponivel (verificado em package.json e usado em layout.tsx) -->
```typescript
import { startOfMonth, endOfMonth, subMonths, format, parseISO } from 'date-fns'
import { ptBR } from 'date-fns/locale'

// format(date, 'MMMM yyyy', { locale: ptBR }) -> "abril 2026"
// startOfMonth(parseISO('2026-04-15')) -> Date de 2026-04-01
// format(startOfMonth(d), 'yyyy-MM-dd') -> '2026-04-01' (canonico para reference_month)
```

<!-- shadcn ja instalados -->
// Card, CardHeader, CardTitle, CardContent
// Tabs, TabsList, TabsTrigger, TabsContent
// Progress (linha — ja instalado per UI-SPEC)
// Table, Badge, Dialog, Textarea, Input, Label, Button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar primitivos do dashboard — StatCard, MonthSelector, CommissionEntryBadge, CommissionTable, CommissionPaidBadge</name>
  <files>
    - src/components/corretores/stat-card.tsx
    - src/components/corretores/month-selector.tsx
    - src/components/corretores/commission-entry-badge.tsx
    - src/components/corretores/commission-table.tsx
    - src/components/seguros/commission-paid-badge.tsx
  </files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md (Typography, Color, Broker Dashboard copy, Ledger Table interaction)
    - src/components/seguros/vigencia-badge.tsx (padrao Badge inline color classes a replicar)
    - src/components/seguros/policy-table.tsx (padrao Table)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Pattern 7: Month Selector URL-driven)
  </read_first>
  <action>
**Sub-step 1.1 — `src/components/corretores/stat-card.tsx`** (Server-safe, no 'use client'):

```typescript
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Progress } from '@/components/ui/progress'

interface Props {
  title: string                            // ex: "Producao do mes"
  value: string                            // ex: "12 apolice(s)" or "R$ 4.500,00" or "75%"
  subtext?: string                         // ex: "mes de abril 2026"
  progress?: number                        // 0..100 — quando informado, renderiza Progress abaixo
  progressOverflow?: boolean               // quando >=100, aplica green override
}

export function StatCard({ title, value, subtext, progress, progressOverflow }: Props) {
  return (
    <Card>
      <CardHeader className="pb-2">
        <CardTitle className="text-sm font-semibold text-muted-foreground">{title}</CardTitle>
      </CardHeader>
      <CardContent className="space-y-1">
        <p className="text-2xl font-semibold">{value}</p>
        {subtext && <p className="text-xs text-muted-foreground">{subtext}</p>}
        {progress !== undefined && (
          <Progress
            value={Math.min(progress, 100)}
            className={progressOverflow ? '[&>div]:bg-green-500' : ''}
          />
        )}
      </CardContent>
    </Card>
  )
}
```

UI-SPEC: titulo `text-sm font-semibold text-muted-foreground` (label role); valor `text-2xl font-semibold` (display role, weight 600); subtexto `text-xs text-muted-foreground`.

**Sub-step 1.2 — `src/components/corretores/month-selector.tsx`** (no 'use client' — pure form GET):

```typescript
import { format, subMonths, startOfMonth } from 'date-fns'
import { ptBR } from 'date-fns/locale'

interface Props {
  selected: string                          // YYYY-MM (ex: "2026-04")
}

export function MonthSelector({ selected }: Props) {
  // 12 ultimos meses + mes atual (13 opcoes)
  const today = startOfMonth(new Date())
  const options = Array.from({ length: 13 }, (_, i) => subMonths(today, i)).map((d) => ({
    value: format(d, 'yyyy-MM'),
    label: format(d, 'MMMM yyyy', { locale: ptBR }),
  }))

  return (
    <form method="get" className="flex justify-end">
      <select
        name="month"
        defaultValue={selected}
        className="rounded-md border px-3 py-1.5 text-sm bg-background"
        onChange={undefined /* nao auto-submit — usuario clica fora ou usa botao */}
      >
        {options.map((o) => (
          <option key={o.value} value={o.value}>{o.label}</option>
        ))}
      </select>
      <button type="submit" className="ml-2 rounded-md border px-3 py-1.5 text-sm hover:bg-muted">
        Aplicar
      </button>
    </form>
  )
}
```

Padrao identico ao filtro de status em seguros/page.tsx (form GET com select native + submit). Default no Server Component: `format(startOfMonth(new Date()), 'yyyy-MM')`.

**Sub-step 1.3 — `src/components/corretores/commission-entry-badge.tsx`** (no 'use client' — pure render):

```typescript
import { Badge } from '@/components/ui/badge'
import { cn } from '@/lib/utils'

const config = {
  comissao: {
    label: 'Comissao',
    className: 'bg-green-100 text-green-800 border-green-200 hover:bg-green-100',
  },
  estorno: {
    label: 'Estorno',
    className: 'bg-red-100 text-red-800 border-red-200 hover:bg-red-100',
  },
  correcao: {
    label: 'Correcao',
    className: 'bg-blue-100 text-blue-800 border-blue-200 hover:bg-blue-100',
  },
} as const

export type CommissionEntryType = keyof typeof config

export function CommissionEntryBadge({ entryType }: { entryType: CommissionEntryType }) {
  const c = config[entryType]
  return (
    <Badge variant="outline" className={cn('text-xs font-medium', c.className)}>
      {c.label}
    </Badge>
  )
}
```

Cores literais do UI-SPEC secao "Semantic Badge Colors". Padrao identico a VigenciaBadge.

**Sub-step 1.4 — `src/components/corretores/commission-table.tsx`** (Server Component):

```typescript
import Link from 'next/link'
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@/components/ui/table'
import { CommissionEntryBadge, type CommissionEntryType } from './commission-entry-badge'

export interface CommissionEntryRow {
  id: string
  entry_type: CommissionEntryType
  amount: number
  rate_used: number | null
  reference_month: string                  // YYYY-MM-DD
  notes: string | null
  created_at: string                       // ISO
  policy?: { id: string; policy_number: string } | null
  quota?: { id: string; quota_number: string; group_id: string } | null
}

interface Props {
  slug: string
  rows: CommissionEntryRow[]
}

function formatBRL(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}
function formatPercent(v: number | null): string {
  if (v === null || v === undefined) return '—'
  return new Intl.NumberFormat('pt-BR', { style: 'percent', minimumFractionDigits: 2 }).format(v)
}
function formatDate(iso: string): string {
  const [y, m, d] = iso.split('T')[0].split('-')
  return `${d}/${m}/${y}`
}

export function CommissionTable({ slug, rows }: Props) {
  if (rows.length === 0) {
    return (
      <div className="rounded-lg border border-dashed p-8 text-center">
        <p className="text-sm text-muted-foreground">Nenhuma comissao registrada neste periodo.</p>
      </div>
    )
  }

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Data</TableHead>
          <TableHead>Tipo</TableHead>
          <TableHead>Referencia</TableHead>
          <TableHead className="text-right">Valor (R$)</TableHead>
          <TableHead>Taxa</TableHead>
          <TableHead>Notas</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {rows.map((r) => (
          <TableRow key={r.id}>
            <TableCell>{formatDate(r.created_at)}</TableCell>
            <TableCell><CommissionEntryBadge entryType={r.entry_type} /></TableCell>
            <TableCell>
              {r.policy ? (
                <Link href={`/${slug}/seguros/${r.policy.id}`} className="text-primary underline-offset-2 hover:underline">
                  Apolice {r.policy.policy_number}
                </Link>
              ) : r.quota ? (
                <Link href={`/${slug}/consorcio/${r.quota.group_id}`} className="text-primary underline-offset-2 hover:underline">
                  Cota {r.quota.quota_number}
                </Link>
              ) : (
                '—'
              )}
            </TableCell>
            <TableCell className={`text-right ${r.amount < 0 ? 'text-destructive' : ''}`}>
              {formatBRL(r.amount)}
            </TableCell>
            <TableCell>{formatPercent(r.rate_used)}</TableCell>
            <TableCell className="text-sm text-muted-foreground">{r.notes ?? '—'}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```

UI-SPEC: tabela read-only sem paginacao, valor right-aligned, negativo em text-destructive, empty state literal "Nenhuma comissao registrada neste periodo."

**Sub-step 1.5 — `src/components/seguros/commission-paid-badge.tsx`** (Server-safe):

```typescript
import { Badge } from '@/components/ui/badge'
import { cn } from '@/lib/utils'

interface Props {
  paidAt: string                            // ISO timestamp from commission_paid_at
}

function formatDate(iso: string): string {
  const [y, m, d] = iso.split('T')[0].split('-')
  return `${d}/${m}/${y}`
}

export function CommissionPaidBadge({ paidAt }: Props) {
  return (
    <Badge variant="outline" className={cn(
      'text-xs font-medium bg-green-100 text-green-800 border-green-200 hover:bg-green-100',
    )}>
      Comissao paga em {formatDate(paidAt)}
    </Badge>
  )
}
```

UI-SPEC: substitui o botao apos commission_paid_at != null. Cor identica ao badge "comissao".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/corretores/stat-card.tsx` existe e exporta `StatCard`; contem `text-2xl font-semibold` para o valor
    - `src/components/corretores/stat-card.tsx` aceita prop `progress` e aplica `[&>div]:bg-green-500` quando `progressOverflow=true`
    - `src/components/corretores/month-selector.tsx` existe e exporta `MonthSelector`; contem `format(d, 'MMMM yyyy', { locale: ptBR })`
    - `src/components/corretores/month-selector.tsx` contem `<form method="get"` e `name="month"`
    - `src/components/corretores/commission-entry-badge.tsx` existe; contem 3 chaves `comissao`, `estorno`, `correcao`
    - `src/components/corretores/commission-entry-badge.tsx` contem literais `bg-green-100`, `bg-red-100`, `bg-blue-100`
    - `src/components/corretores/commission-table.tsx` existe e exporta `CommissionTable` e `CommissionEntryRow`
    - `src/components/corretores/commission-table.tsx` contem literal `Nenhuma comissao registrada neste periodo.`
    - `src/components/corretores/commission-table.tsx` contem 6 colunas: `Data`, `Tipo`, `Referencia`, `Valor (R$)`, `Taxa`, `Notas`
    - `src/components/seguros/commission-paid-badge.tsx` existe e exporta `CommissionPaidBadge`
    - `src/components/seguros/commission-paid-badge.tsx` contem literal `Comissao paga em` e classe `bg-green-100`
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>5 primitivos UI prontos para serem consumidos pelo Server Component do dashboard e pelas paginas de detalhe.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Dialogs do ledger — MarkCommissionPaidDialog (seguros + cotas), EstornoDialog, CorrecaoDialog + integrar em /seguros/[id] e /consorcio/[id]</name>
  <files>
    - src/components/seguros/mark-commission-paid-dialog.tsx
    - src/components/corretores/estorno-dialog.tsx
    - src/components/corretores/correcao-dialog.tsx
    - src/app/(app)/[slug]/seguros/[id]/page.tsx
    - src/app/(app)/[slug]/consorcio/[id]/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-06 split, D-08 base, D-09 gatilho, D-10 imutabilidade)
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md ('Marcar comissao como paga' Dialog, Estorno/Correcao Actions, Toast Notifications)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Pattern 3, Pitfall 1 idempotencia)
    - src/components/consorcio/contemplation-dialog.tsx (padrao Dialog + handleSubmit + toast)
    - src/components/consorcio/quota-table.tsx (padrao de inserir botao por linha)
    - src/app/(app)/[slug]/seguros/[id]/page.tsx (estado atual — onde inserir botao/badge no PolicyDetailPage)
    - src/app/(app)/[slug]/consorcio/[id]/page.tsx (estado atual — onde inserir botao/badge nas linhas de cota)
    - src/lib/utils/commission-rate.ts (resolveCommissionRate — chamada no Server Component pai para pre-calcular)
  </read_first>
  <action>
**Sub-step 2.1 — `src/components/seguros/mark-commission-paid-dialog.tsx`** (Client):

```typescript
'use client'
import { useState } from 'react'
import { toast } from 'sonner'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger, DialogDescription } from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Textarea } from '@/components/ui/textarea'
import { Label } from '@/components/ui/label'
import { markCommissionPaidAction } from '@/lib/actions/commission-entries'

interface CalculatedAmounts {
  brokerName: string
  brokerAmount: number
  brokerRate: number
  partnerName?: string                    // presente quando split D-06 acontece
  partnerAmount?: number
  partnerRate?: number
}

interface Props {
  slug: string
  sourceType: 'policy' | 'quota'
  sourceId: string
  amounts: CalculatedAmounts                // calculado no Server Component pai
}

function formatBRL(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}
function formatPercent(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'percent', minimumFractionDigits: 2 }).format(v)
}

export function MarkCommissionPaidDialog({ slug, sourceType, sourceId, amounts }: Props) {
  const [open, setOpen] = useState(false)
  const [isSubmitting, setIsSubmitting] = useState(false)

  async function handleConfirm(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setIsSubmitting(true)
    try {
      const fd = new FormData(e.currentTarget)
      const notes = (fd.get('notes') as string) || undefined
      const result = await markCommissionPaidAction(slug, sourceType, sourceId, notes)
      if (result && 'error' in result) {
        const msg = result.error
        if (typeof msg === 'string' && msg.toLowerCase().includes('ja registrada')) {
          toast.error('Comissao ja registrada para este item.')
        } else {
          toast.error(typeof msg === 'string' ? msg : 'Erro ao registrar comissao. Tente novamente.')
        }
        return
      }
      toast.success('Comissao registrada no ledger.')
      setOpen(false)
    } finally {
      setIsSubmitting(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button variant="outline" size="sm">Marcar comissao como paga</Button>
      </DialogTrigger>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>Marcar comissao como paga</DialogTitle>
          <DialogDescription>
            Isso registrara a comissao no ledger. Este lancamento nao pode ser editado apos a confirmacao.
          </DialogDescription>
        </DialogHeader>
        <form onSubmit={handleConfirm} className="space-y-4">
          <div className="rounded-md border p-3 space-y-2">
            <p className="text-sm">
              <strong>Corretor:</strong> {amounts.brokerName} — {formatBRL(amounts.brokerAmount)}
            </p>
            <p className="text-xs text-muted-foreground">Taxa aplicada: {formatPercent(amounts.brokerRate)}</p>
            {amounts.partnerName && amounts.partnerAmount !== undefined && amounts.partnerRate !== undefined && (
              <>
                <p className="text-sm pt-2 border-t">
                  <strong>Parceiro:</strong> {amounts.partnerName} — {formatBRL(amounts.partnerAmount)}
                </p>
                <p className="text-xs text-muted-foreground">Taxa aplicada: {formatPercent(amounts.partnerRate)}</p>
              </>
            )}
          </div>

          <div className="space-y-2">
            <Label htmlFor="mark-commission-notes">Observacoes (opcional)</Label>
            <Textarea id="mark-commission-notes" name="notes" placeholder="Motivo, referencia ou informacao adicional..." rows={2} />
          </div>

          <div className="flex justify-end gap-2">
            <Button type="button" variant="outline" onClick={() => setOpen(false)}>Nao registrar agora</Button>
            <Button type="submit" disabled={isSubmitting}>
              {isSubmitting ? 'Registrando...' : 'Confirmar pagamento'}
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

UI-SPEC copy literal: title "Marcar comissao como paga", body "Isso registrara a comissao no ledger. Este lancamento nao pode ser editado apos a confirmacao.", confirm "Confirmar pagamento", cancel "Nao registrar agora", success "Comissao registrada no ledger.", already-paid "Comissao ja registrada para este item."

**Sub-step 2.2 — `src/components/corretores/estorno-dialog.tsx`** (Client):

Padrao identico a contemplation-dialog mas com 1 hidden input (original_entry_id) + Textarea (notes required).

```typescript
'use client'
// ... imports identicos a mark-commission-paid-dialog
import { registerEstornoAction } from '@/lib/actions/commission-entries'

interface Props {
  slug: string
  originalEntryId: string                  // FK para a entry de comissao original (para inferir broker_id, source, etc)
  originalAmount: number                   // exibido no dialog para confirmacao
  triggerVariant?: 'outline' | 'ghost'
}

// handleSubmit: cria FormData, append('original_entry_id', originalEntryId), append('notes', notes)
// chama registerEstornoAction(slug, fd)
```

Form fields (UI-SPEC "Estorno / Correcao Actions"):
- Hidden input `original_entry_id`
- p mostrando `Estornando: ${formatBRL(originalAmount)}` (read-only)
- Label "Motivo do estorno" + Textarea name="notes" required minLength=3 (UI-SPEC: notes required = true)
- Botoes: "Nao estornar" (outline) + "Confirmar estorno" (loading "Estornando...")

Copy literal:
- title: "Registrar estorno"
- description: "Um estorno cria um novo lancamento negativo no ledger. O lancamento original nao e alterado."
- success toast: "Estorno registrado no ledger."

**Sub-step 2.3 — `src/components/corretores/correcao-dialog.tsx`** (Client):

```typescript
'use client'
import { registerCorrecaoAction } from '@/lib/actions/commission-entries'
// ...

interface Props {
  slug: string
  brokerId?: string                          // pre-fill se vindo do dashboard de um broker
  partnerId?: string
  policyId?: string
  quotaId?: string
  referenceMonth: string                     // YYYY-MM-DD (primeiro dia)
}
```

Form fields:
- Hidden inputs: broker_id (se vier), partner_id, policy_id, quota_id, reference_month
- Label "Valor da diferenca (R$)" + Input name="amount" type="number" step="0.01" required (pode ser positivo ou negativo)
- Label "Motivo da correcao" + Textarea name="notes" required minLength=3
- Botoes: "Nao corrigir" (outline) + "Confirmar correcao" (loading "Registrando...")

Copy literal UI-SPEC:
- title: "Registrar correcao"
- description: "Uma correcao cria um novo lancamento complementar no ledger. Use para ajustar diferencas."
- success toast: "Correcao registrada no ledger."

**Sub-step 2.4 — Integrar em `src/app/(app)/[slug]/seguros/[id]/page.tsx`**:

Modificacoes pontuais (NAO reescrever o arquivo inteiro):

1. Adicionar imports no topo:
```typescript
import { MarkCommissionPaidDialog } from '@/components/seguros/mark-commission-paid-dialog'
import { CommissionPaidBadge } from '@/components/seguros/commission-paid-badge'
import { resolveCommissionRate } from '@/lib/utils/commission-rate'
```

2. Estender o SELECT da query de policy (atual linha 96 do arquivo) para incluir os novos campos:
```typescript
.select('id, policy_number, type, insurer, vigencia_inicio, vigencia_fim, premio_total, observacoes, type_data, partner_id, commission_paid_at, assigned_to, client:clients(id, name, type), profile:profiles!assigned_to(id, full_name)')
```

3. Estender a interface `PolicyData` para incluir `partner_id: string | null`, `commission_paid_at: string | null`, `assigned_to: string`.

4. Apos o `const policy = rawPolicy as unknown as PolicyData`, adicionar busca paralela de broker_profile do assigned_to e partner (se houver):
```typescript
// Carrega dados necessarios para calcular comissao no Dialog
let calculatedAmounts: ... | null = null
if (!policy.commission_paid_at) {
  const [{ data: bp }, partnerRes] = await Promise.all([
    supabase
      .from('broker_profiles')
      .select('commission_rate_default, commission_rate_overrides')
      .eq('id', policy.assigned_to)
      .is('deleted_at', null)
      .maybeSingle(),
    policy.partner_id
      ? supabase
          .from('partners')
          .select('id, name, commission_rate_default, commission_rate_overrides')
          .eq('id', policy.partner_id)
          .is('deleted_at', null)
          .maybeSingle()
      : Promise.resolve({ data: null }),
  ])
  if (bp) {
    const brokerRate = resolveCommissionRate(bp.commission_rate_overrides, bp.commission_rate_default, policy.type)
    const brokerAmount = policy.premio_total * brokerRate
    let partnerInfo: { name: string; amount: number; rate: number } | undefined
    if (partnerRes.data) {
      const p = partnerRes.data as { name: string; commission_rate_default: number; commission_rate_overrides: any }
      const pRate = resolveCommissionRate(p.commission_rate_overrides, p.commission_rate_default, policy.type)
      partnerInfo = { name: p.name, amount: policy.premio_total * pRate, rate: pRate }
    }
    calculatedAmounts = {
      brokerName: policy.profile?.full_name ?? 'Corretor',
      brokerAmount, brokerRate,
      partnerName: partnerInfo?.name,
      partnerAmount: partnerInfo?.amount,
      partnerRate: partnerInfo?.rate,
    }
  }
}
```

5. No JSX, na area de acoes da apolice (proxima ao VigenciaBadge — atual linha ~148 ou na area de Tabs ~213), adicionar:
```tsx
<div className="flex items-center gap-2">
  {policy.commission_paid_at ? (
    <CommissionPaidBadge paidAt={policy.commission_paid_at} />
  ) : calculatedAmounts ? (
    <MarkCommissionPaidDialog
      slug={slug}
      sourceType="policy"
      sourceId={policy.id}
      amounts={calculatedAmounts}
    />
  ) : null}
</div>
```

Inserir este bloco JUNTO com `<ClaimDialog />` e `<EndorsementDialog />` (atual ~linha 213) na div `<div className="flex gap-2">`.

**Sub-step 2.5 — Integrar em `src/app/(app)/[slug]/consorcio/[id]/page.tsx`**:

Esta pagina lista MULTIPLAS cotas de um grupo. Modificacao analoga, mas o "Marcar comissao paga" precisa ser por LINHA da cota (nao no header do grupo). Estrategia:

1. Estender o SELECT de cotas (atual linha ~95) para incluir `partner_id`, `commission_paid_at`, `assigned_to`, e o JOIN do group ja traz `type` e `credit_value`.

2. ANTES do render da `<QuotaTable>` (atual linha ~99), passar o group.type e group.credit_value como props ao componente para que QuotaTable possa renderizar o botao/badge por linha.

3. ALTERNATIVA mais focada (escolhida): adicionar uma sub-secao logo abaixo da QuotaTable que itera as cotas e expoe `MarkCommissionPaidDialog`/`CommissionPaidBadge` em uma area dedicada "Comissoes". Isso evita reescrever QuotaTable.

Implementacao concreta — adicionar logo apos `<QuotaTable>` no JSX:

```tsx
<Card>
  <CardHeader>
    <CardTitle>Comissoes das cotas contempladas</CardTitle>
  </CardHeader>
  <CardContent>
    {commissionRows.length === 0 ? (
      <p className="text-sm text-muted-foreground">Nenhuma cota contemplada para registrar comissao.</p>
    ) : (
      <div className="space-y-3">
        {commissionRows.map((cr) => (
          <div key={cr.quotaId} className="flex items-center justify-between border rounded-md p-3">
            <div>
              <p className="text-sm font-medium">Cota {cr.quotaNumber} — {cr.clientName}</p>
              <p className="text-xs text-muted-foreground">
                Credito base: {formatBRL(group.credit_value)}
              </p>
            </div>
            {cr.paidAt ? (
              <CommissionPaidBadge paidAt={cr.paidAt} />
            ) : cr.amounts ? (
              <MarkCommissionPaidDialog
                slug={slug}
                sourceType="quota"
                sourceId={cr.quotaId}
                amounts={cr.amounts}
              />
            ) : (
              <span className="text-xs text-muted-foreground">Sem corretor atribuido</span>
            )}
          </div>
        ))}
      </div>
    )}
  </CardContent>
</Card>
```

Onde `commissionRows` e calculado no Server Component:
```typescript
// 1. Filtrar cotas onde post_contemplation_stage IS NOT NULL (foi contemplada)
// 2. Para cada uma: buscar broker_profile do assigned_to + partner se houver, chamar resolveCommissionRate com 'consorcio_' + group.type, calcular amount usando group.credit_value
// 3. Montar { quotaId, quotaNumber, clientName, paidAt, amounts }[]
```

Nota: a chave de produto para cotas e `'consorcio_' + group.type` (D-07 + Pitfall 3 do RESEARCH.md). Se group.type='auto' -> productType='consorcio_auto'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/seguros/mark-commission-paid-dialog.tsx` existe e contem literal `import { markCommissionPaidAction } from '@/lib/actions/commission-entries'`
    - `src/components/seguros/mark-commission-paid-dialog.tsx` contem literais `Marcar comissao como paga`, `Confirmar pagamento`, `Nao registrar agora`, `Comissao registrada no ledger.`
    - `src/components/seguros/mark-commission-paid-dialog.tsx` contem literal `Isso registrara a comissao no ledger. Este lancamento nao pode ser editado apos a confirmacao.`
    - `src/components/seguros/mark-commission-paid-dialog.tsx` exibe partnerAmount somente quando `amounts.partnerName && amounts.partnerAmount !== undefined`
    - `src/components/corretores/estorno-dialog.tsx` existe e contem literal `import { registerEstornoAction`
    - `src/components/corretores/estorno-dialog.tsx` contem literais `Registrar estorno`, `Confirmar estorno`, `Nao estornar`, `Estorno registrado no ledger.`
    - `src/components/corretores/estorno-dialog.tsx` contem `name="notes"` com `required` (UI-SPEC: notes required=true)
    - `src/components/corretores/correcao-dialog.tsx` existe e contem literal `import { registerCorrecaoAction`
    - `src/components/corretores/correcao-dialog.tsx` contem literais `Registrar correcao`, `Confirmar correcao`, `Nao corrigir`, `Correcao registrada no ledger.`
    - `src/app/(app)/[slug]/seguros/[id]/page.tsx` contem `import { MarkCommissionPaidDialog }` E `import { CommissionPaidBadge }` E `import { resolveCommissionRate`
    - `src/app/(app)/[slug]/seguros/[id]/page.tsx` contem `commission_paid_at` no SELECT da query de policy
    - `src/app/(app)/[slug]/seguros/[id]/page.tsx` contem `partner_id` no SELECT da query de policy
    - `src/app/(app)/[slug]/seguros/[id]/page.tsx` renderiza condicional `policy.commission_paid_at ? <CommissionPaidBadge` ELSE `<MarkCommissionPaidDialog`
    - `src/app/(app)/[slug]/consorcio/[id]/page.tsx` contem `import { MarkCommissionPaidDialog }` E `import { CommissionPaidBadge }` E `import { resolveCommissionRate`
    - `src/app/(app)/[slug]/consorcio/[id]/page.tsx` contem literal `Comissoes das cotas contempladas` (heading do Card adicionado)
    - `src/app/(app)/[slug]/consorcio/[id]/page.tsx` contem literal `'consorcio_' + group.type` (Pitfall 3 — prefixo)
    - `npm run build` retorna exit code 0
    - `npm test -- --run tests/actions/commission-entries.test.ts` continua verde (Plan 02 nao quebrou)
  </acceptance_criteria>
  <done>3 dialogs do ledger criados; paginas /seguros/[id] e /consorcio/[id] integradas com gatilho de pagamento e badge read-only quando ja paga.</done>
</task>

<task type="auto">
  <name>Task 3: Criar Server Component do dashboard /[slug]/corretores/[id] com 4 stat cards, month selector, tabs Visao geral + Relatorio</name>
  <files>src/app/(app)/[slug]/corretores/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-11 redirect, D-12 metricas, D-13 aba relatorio)
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md (Layout Contract — Broker Dashboard Layout, copy de stat cards e tabs)
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Pattern 4: Dashboard Metrics Query, Pattern 5: Broker Redirect, Pitfall 5: UNION via 2 queries)
    - src/app/(app)/[slug]/consorcio/[id]/page.tsx (padrao Server Component detalhe com Card + Tabs)
  </read_first>
  <action>
Criar `src/app/(app)/[slug]/corretores/[id]/page.tsx` (Server Component) com EXATAMENTE esta estrutura:

```typescript
import Link from 'next/link'
import { notFound, redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { Badge } from '@/components/ui/badge'
import { StatCard } from '@/components/corretores/stat-card'
import { MonthSelector } from '@/components/corretores/month-selector'
import { CommissionTable, type CommissionEntryRow } from '@/components/corretores/commission-table'
import { startOfMonth, endOfMonth, format, parse } from 'date-fns'
import { ptBR } from 'date-fns/locale'

interface Props {
  params: Promise<{ slug: string; id: string }>
  searchParams: Promise<{ month?: string }>     // YYYY-MM
}

function formatBRL(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}

export default async function BrokerDashboardPage({ params, searchParams }: Props) {
  const { slug, id } = await params
  const sp = await searchParams
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = (await createClient()) as any
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  // === D-11: redirect quando corretor tenta acessar dashboard de outro corretor (T-04-04) ===
  const role = (user.app_metadata as { role?: string })?.role
  if (role === 'corretor' && id !== user.id) {
    redirect(`/${slug}/corretores/${user.id}`)
  }

  // === Mes selecionado: default = mes corrente; ?month=YYYY-MM permite override ===
  const today = new Date()
  const selectedMonthStart = sp.month
    ? parse(sp.month + '-01', 'yyyy-MM-dd', today)
    : startOfMonth(today)
  const monthStartStr = format(startOfMonth(selectedMonthStart), 'yyyy-MM-dd')
  const monthEndStr = format(endOfMonth(selectedMonthStart), 'yyyy-MM-dd')
  const referenceMonth = monthStartStr   // primeiro dia do mes (canonico para reference_month)
  const monthLabel = format(selectedMonthStart, 'MMMM yyyy', { locale: ptBR })
  const monthValue = format(selectedMonthStart, 'yyyy-MM')

  // === 1. Buscar profile do broker e broker_profile ===
  const { data: profile } = await supabase
    .from('profiles')
    .select('id, full_name, email, role')
    .eq('id', id)
    .maybeSingle()

  if (!profile || profile.role !== 'corretor') notFound()

  const { data: brokerProfile } = await supabase
    .from('broker_profiles')
    .select('susep_number, monthly_goal, commission_rate_default, commission_rate_overrides')
    .eq('id', id)
    .is('deleted_at', null)
    .maybeSingle()

  const monthlyGoal = (brokerProfile?.monthly_goal ?? 0) as number

  // === 2. Producao do mes: count policies onde assigned_to=id E created_at no mes selecionado ===
  const { count: productionCount } = await supabase
    .from('policies')
    .select('id', { count: 'exact', head: true })
    .eq('assigned_to', id)
    .gte('created_at', monthStartStr)
    .lte('created_at', monthEndStr + 'T23:59:59')
    .is('deleted_at', null)

  // === 3. Comissao acumulada: soma amount em commission_entries do mes selecionado ===
  const { data: commissionData } = await supabase
    .from('commission_entries')
    .select('amount')
    .eq('broker_id', id)
    .eq('reference_month', referenceMonth)

  const totalCommission = ((commissionData ?? []) as { amount: number }[])
    .reduce((sum, e) => sum + (e.amount ?? 0), 0)

  // === 4. Carteira ativa: clients distintos com policies OU consortium_quotas ativas (Pitfall 5) ===
  const [policyClientsRes, quotaClientsRes] = await Promise.all([
    supabase
      .from('policies')
      .select('client_id')
      .eq('assigned_to', id)
      .is('deleted_at', null),
    supabase
      .from('consortium_quotas')
      .select('client_id')
      .eq('assigned_to', id)
      .is('deleted_at', null),
  ])
  const uniqueClients = new Set<string>()
  for (const r of ((policyClientsRes.data ?? []) as { client_id: string }[])) uniqueClients.add(r.client_id)
  for (const r of ((quotaClientsRes.data ?? []) as { client_id: string }[])) uniqueClients.add(r.client_id)
  const portfolioCount = uniqueClients.size

  // === 5. Meta atingida (%) ===
  const goalProgress = monthlyGoal > 0 ? (totalCommission / monthlyGoal) * 100 : 0
  const goalReached = goalProgress >= 100

  // === 6. Aba relatorio: commission_entries do mes com refs ===
  const { data: rawEntries } = await supabase
    .from('commission_entries')
    .select('id, entry_type, amount, rate_used, reference_month, notes, created_at, policy:policies(id, policy_number), quota:consortium_quotas(id, quota_number, group_id)')
    .eq('broker_id', id)
    .eq('reference_month', referenceMonth)
    .order('created_at', { ascending: false })

  const reportRows = (rawEntries ?? []) as unknown as CommissionEntryRow[]

  // === 7. Top clientes (Visao Geral): 5 clientes do broker com mais policies ===
  const { data: topClientsRaw } = await supabase
    .from('policies')
    .select('client:clients(id, name)')
    .eq('assigned_to', id)
    .is('deleted_at', null)
    .limit(50)
  const clientCountMap = new Map<string, { id: string; name: string; count: number }>()
  for (const r of ((topClientsRaw ?? []) as { client: { id: string; name: string } | null }[])) {
    if (!r.client) continue
    const existing = clientCountMap.get(r.client.id)
    if (existing) existing.count++
    else clientCountMap.set(r.client.id, { id: r.client.id, name: r.client.name, count: 1 })
  }
  const topClients = Array.from(clientCountMap.values())
    .sort((a, b) => b.count - a.count)
    .slice(0, 5)

  return (
    <div className="mx-auto max-w-6xl space-y-6 p-6">
      {/* Breadcrumb */}
      <div className="flex items-center gap-2 text-sm text-muted-foreground">
        <Link href={`/${slug}/corretores`} className="hover:text-foreground">Corretores</Link>
        <span>/</span>
        <span>{profile.full_name}</span>
      </div>

      {/* Header card */}
      <Card>
        <CardHeader className="flex flex-row items-start justify-between gap-4">
          <div className="space-y-1">
            <CardTitle className="text-2xl">{profile.full_name}</CardTitle>
            <p className="text-sm text-muted-foreground">
              {brokerProfile?.susep_number ? `SUSEP: ${brokerProfile.susep_number}` : 'Sem SUSEP cadastrado'}
              {profile.email && <> — {profile.email}</>}
            </p>
          </div>
          <Badge variant="outline">Corretor interno</Badge>
        </CardHeader>
      </Card>

      {/* 4 Stat cards */}
      <div className="grid grid-cols-2 gap-4 sm:grid-cols-4">
        <StatCard
          title="Producao do mes"
          value={`${productionCount ?? 0} apolice(s) nova(s)`}
          subtext={`mes de ${monthLabel}`}
        />
        <StatCard
          title="Comissao acumulada"
          value={formatBRL(totalCommission)}
          subtext={`mes de ${monthLabel}`}
        />
        <StatCard
          title="Carteira ativa"
          value={`${portfolioCount} cliente(s) com produto ativo`}
        />
        <StatCard
          title="Meta atingida"
          value={goalReached ? 'Meta atingida!' : `${goalProgress.toFixed(0)}% da meta`}
          subtext={`Meta: ${formatBRL(monthlyGoal)}`}
          progress={goalProgress}
          progressOverflow={goalReached}
        />
      </div>

      {/* Month selector */}
      <MonthSelector selected={monthValue} />

      {/* Tabs */}
      <Tabs defaultValue="overview">
        <TabsList>
          <TabsTrigger value="overview">Visao geral</TabsTrigger>
          <TabsTrigger value="report">Relatorio de comissoes</TabsTrigger>
        </TabsList>

        <TabsContent value="overview" className="mt-4">
          <Card>
            <CardHeader>
              <CardTitle className="text-base">Top 5 clientes por # apolices</CardTitle>
            </CardHeader>
            <CardContent>
              {topClients.length === 0 ? (
                <p className="text-sm text-muted-foreground">Nenhuma apolice atribuida ainda.</p>
              ) : (
                <ul className="divide-y">
                  {topClients.map((c) => (
                    <li key={c.id} className="flex items-center justify-between py-2">
                      <Link href={`/${slug}/clientes/${c.id}`} className="text-sm hover:underline">
                        {c.name}
                      </Link>
                      <span className="text-sm text-muted-foreground">{c.count} apolice(s)</span>
                    </li>
                  ))}
                </ul>
              )}
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="report" className="mt-4">
          <CommissionTable slug={slug} rows={reportRows} />
        </TabsContent>
      </Tabs>
    </div>
  )
}
```

Notas criticas:
- `reference_month` = primeiro dia do mes selecionado, formato `yyyy-MM-dd` (CONTEXT.md specifics + Pitfall 7).
- A query de comissao usa `.eq('reference_month', referenceMonth)` — IGUALDADE direta, nao range (RESEARCH Pattern 4).
- Carteira ativa via 2 queries paralelas + Set (Pitfall 5 — Supabase JS sem UNION).
- Progress component: quando `goalProgress >= 100`, passa `progressOverflow=true` para aplicar `[&>div]:bg-green-500` (UI-SPEC Color section).
- Empty state da aba relatorio NAO e tratado aqui — `CommissionTable` ja exibe "Nenhuma comissao registrada neste periodo." quando rows=[].
- Top 5 clientes (aba Visao Geral) carregado com limite 50 policies + agregacao em memoria — adequado para v1 (5000 clientes/tenant).

NAO incluir CorrecaoDialog ou EstornoDialog botoes nesta versao do dashboard — eles foram criados no Task 2 e ficam como ferramenta para versoes futuras (a integracao por linha de entry no relatorio adiciona complexidade desnecessaria para v1; o admin chama via Server Action diretamente quando necessario).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/app/(app)/[slug]/corretores/[id]/page.tsx` existe
    - Contem literal `if (role === 'corretor' && id !== user.id)` E `redirect(\`/\${slug}/corretores/\${user.id}\`)` (D-11, T-04-04)
    - Contem `import { StatCard }` E `import { MonthSelector }` E `import { CommissionTable`
    - Contem 4 instancias de `<StatCard` no JSX (grep `<StatCard` retorna 4)
    - Contem literais dos 4 titulos: `Producao do mes`, `Comissao acumulada`, `Carteira ativa`, `Meta atingida`
    - Contem literal `apolice(s) nova(s)` (subtext de Producao)
    - Contem literal `cliente(s) com produto ativo` (subtext de Carteira)
    - Contem literal `Meta atingida!` (overflow message)
    - Contem `.eq('reference_month', referenceMonth)` (filtro canonico mes)
    - Contem 2 queries paralelas com `Promise.all` para policies+consortium_quotas (Pitfall 5)
    - Contem `<TabsTrigger value="overview">Visao geral</TabsTrigger>` E `<TabsTrigger value="report">Relatorio de comissoes</TabsTrigger>`
    - Contem `<MonthSelector selected={monthValue} />`
    - Contem `if (!profile || profile.role !== 'corretor') notFound()` (impede acessar dashboard de nao-corretor)
    - `npm run build` retorna exit code 0
    - `npm test` retorna exit code 0 (regressao — Plans 01 e 02 mantem testes verdes)
  </acceptance_criteria>
  <done>Dashboard /corretores/[id] com 4 stat cards reais, month selector URL-driven, redirect de seguranca e aba Relatorio listando entries do ledger filtradas por reference_month.</done>
</task>

</tasks>

<deferred_decisions>
## Deferred to Phase 6 — Estorno/Correcao UI Mounting

**Status:** Components are created in Task 2 (estorno-dialog.tsx, correcao-dialog.tsx) but intentionally NOT mounted in any UI surface during Phase 4.

**What is delivered in Phase 4:**
- `src/components/corretores/estorno-dialog.tsx` — Client Component pronto, importa registerEstornoAction
- `src/components/corretores/correcao-dialog.tsx` — Client Component pronto, importa registerCorrecaoAction
- Server Actions `registerEstornoAction` e `registerCorrecaoAction` (Plan 02) — totalmente testadas

**What is intentionally NOT delivered in Phase 4:**
- Botao/UI flow para o admin disparar o EstornoDialog (precisa selecionar entry original + contexto)
- Botao/UI flow para o admin disparar o CorrecaoDialog
- Nao ha caminho clicavel no dashboard para abrir esses dialogs em Phase 4

**Why deferred:**
- A integracao por linha de entry no relatorio (selecionar uma entry de comissao para estornar) adiciona estado client-side complexo (selecao + dialog state) que nao agrega valor para v1
- O fluxo de uso real (corretora processando estornos) provavelmente quer um relatorio mensal dedicado com bulk actions — mais coerente com Phase 6 (Financeiro)
- Os dialogs ja existem como ferramenta — admin com necessidade urgente pode chamar a Server Action direto via console se necessario

**Re-mounting plan (Phase 6):**
- Adicionar coluna "Acoes" na CommissionTable com DropdownMenu (Estornar | Corrigir)
- DropdownMenu monta EstornoDialog/CorrecaoDialog passando original_entry_id da linha
- Considerar bulk actions ("estornar varias entries selecionadas")
</deferred_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action (markCommissionPaid) | sourceId, sourceType e notes atravessam boundary; Server Action (Plan 02) valida via Zod e busca source authoritativo (NUNCA confia em amounts vindos do cliente) |
| Server Component → resolveCommissionRate | Calculo no servidor antes do Dialog — cliente recebe apenas valores ja resolvidos para EXIBIR; o servidor recalcula no Server Action para inserir |
| Authenticated user (JWT role) → broker dashboard | Server Component verifica role + id antes de qualquer query (D-11) |
| Server Component (corretor) → outro broker dashboard | redirect bloqueia acesso cross-broker (T-04-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | /corretores/[id] dashboard cross-broker | mitigate | Server Component compara `role === 'corretor' && id !== user.id` -> redirect (Task 3); RLS de commission_entries filtra `broker_id = auth.uid()` para role corretor (defesa em profundidade) |
| T-04-14 | Tampering | Cliente envia amounts forjados via DevTools | mitigate | MarkCommissionPaidDialog SO exibe valores; markCommissionPaidAction busca source no DB e RECALCULA via resolveCommissionRate antes do INSERT (Plan 02). amounts em props sao display-only |
| T-04-15 | Tampering | Re-submit rapido do botao "Marcar comissao como paga" -> ledger duplicado (Pitfall 1) | mitigate | Server Action verifica `commission_paid_at IS NULL` antes do INSERT; UI substitui botao por Badge apos sucesso |
| T-04-16 | Information Disclosure | Aba Relatorio expoe commission_entries de outros corretores | mitigate | Query `.eq('broker_id', id)` E RLS filtra por `broker_id = auth.uid()` para role corretor |
| T-04-17 | Tampering | URL com ?month=invalid (ex: ?month=NaN) | mitigate | parse com fallback `selectedMonthStart || startOfMonth(today)`; date-fns parse retorna Invalid Date que startOfMonth lida graciosamente; reference_month sempre formatado como YYYY-MM-DD valido |
| T-04-18 | Information Disclosure | EstornoDialog/CorrecaoDialog exposed sem role check | accept | Os Dialogs nao sao integrados em UI nesta entrega (Task 2 cria componentes; nao monta no dashboard); quando integrarem em uma futura entrega, role check ja vive no Server Action (Plan 02) |
| T-04-19 | Repudiation | commission_entries imutavel mas sem audit de quem inseriu | accept | Phase 4 nao adiciona campo `created_by`. RLS exige role admin/corretor para INSERT — futura phase pode adicionar created_by + audit log (deferred Phase 7) |
</threat_model>

<verification>
- /[slug]/corretores/[id] funcional para admin (qualquer corretor) e corretor (somente proprio id)
- Botao "Marcar comissao como paga" aparece em /seguros/[id] e em cada cota contemplada de /consorcio/[id] enquanto commission_paid_at IS NULL
- Apos confirmacao, badge "Comissao paga em DD/MM/YYYY" substitui o botao (revalidatePath dispara re-render)
- Aba "Relatorio de comissoes" exibe linhas color-coded por entry_type com valores em BRL e taxas em %
- Idempotencia: clicar 2x rapido nao gera entries duplicadas (verificado pelo teste de Plan 02 + UI-side substituicao do botao)
- npm run build verde
- npm test verde (sem regressao de Plan 01 e 02)
</verification>

<success_criteria>
- COM-03: comissao calculada automaticamente ao "marcar paga" (resolveCommissionRate aplicada no Server Component pai e re-aplicada no Server Action; amounts congruentes entre UI e ledger)
- COM-04: ledger imutavel — UI nao expoe edicao de entries; estorno e correcao geram NOVAS entries via Dialogs separados
- COM-05: aba "Relatorio de comissoes" filtrada por reference_month com tipo, valor, taxa e referencia
- COM-06: dashboard individual com 4 metricas reais + redirect de seguranca para nao-corretor
- Copywriting pt-BR identico ao UI-SPEC (toasts, dialogs, stat card titles, breadcrumb)
- Tenant isolation preservado em todas as queries (RLS + .eq tenant filtros nao explicitos pois RLS aplica)
</success_criteria>

<output>
After completion, create `.planning/phases/04-corretores-comissoes/04-04-SUMMARY.md` with:
- Componentes criados (paths absolutos)
- Rota /corretores/[id] funcional
- Paginas estendidas (/seguros/[id], /consorcio/[id])
- Verificacao: npm run build verde + npm test verde
- Phase 4 entregue. Proximo: /gsd-verify-work fase 4 antes de avançar para Phase 5 (Financeiro)
</output>
</content>
</invoke>