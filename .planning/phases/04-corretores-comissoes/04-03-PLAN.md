---
phase: 04-corretores-comissoes
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN
  - 04-02-PLAN
files_modified:
  - src/components/auth/sidebar-shell.tsx
  - src/app/(app)/[slug]/corretores/page.tsx
  - src/app/(app)/[slug]/parceiros/page.tsx
  - src/components/corretores/broker-profile-dialog.tsx
  - src/components/corretores/broker-list-table.tsx
  - src/components/parceiros/partner-dialog.tsx
  - src/components/parceiros/partner-table.tsx
  - src/components/parceiros/partner-delete-confirm.tsx
autonomous: true
requirements:
  - COM-01
  - COM-02
  - COM-06
must_haves:
  truths:
    - "Sidebar exibe item 'Corretores' com icone UserCog entre Consorcio e Configuracoes (D-14, UI-SPEC)"
    - "Sidebar exibe sub-item 'Parceiros' aninhado sob 'Corretores' apontando para /[slug]/parceiros"
    - "Rota /[slug]/corretores renderiza tabela com colunas Nome, SUSEP, Meta mensal, Producao do mes, Acoes"
    - "Producao do mes na tabela = count de policies onde assigned_to = profile.id no mes corrente"
    - "Botao 'Ver dashboard' em cada linha leva para /[slug]/corretores/{id}"
    - "Quando profile.role='corretor' nao tem broker_profile, linha exibe CTA 'Completar perfil de corretor' que abre Dialog (nao pagina)"
    - "Dialog de perfil chama upsertBrokerProfileAction (do Plan 02) com FormData incluindo profile_id, susep_number, monthly_goal, commission_rate_default e 9 campos commission_rate_overrides"
    - "Rota /[slug]/parceiros renderiza tabela com colunas Nome, CNPJ, E-mail de contato, Taxa padrao, Acoes"
    - "Botao 'Novo parceiro' abre Dialog que chama createPartnerAction"
    - "Dialog de parceiro inclui 9 campos de override de taxa em grid grid-cols-3 gap-4"
    - "Acao 'Excluir' em parceiro abre AlertDialog que chama softDeletePartnerAction"
    - "Listagem de corretores e parceiros pagina por 25 itens (?page=N) — padrao seguros/page.tsx"
    - "Empty state de corretores: 'Nenhum corretor cadastrado ainda' com CTA 'Ir para Usuarios' apontando para /[slug]/configuracoes/usuarios"
    - "Empty state de parceiros: 'Nenhum parceiro externo cadastrado' com CTA 'Cadastrar primeiro parceiro' que dispara o Dialog"
    - "Toast de sucesso usa sonner top-right com copy exata do UI-SPEC ('Perfil de corretor atualizado.', 'Parceiro cadastrado com sucesso.', 'Parceiro removido.')"
    - "npm run build executa sem erros de TypeScript ou ESLint"
  artifacts:
    - path: "src/components/auth/sidebar-shell.tsx"
      provides: "Sidebar com item 'Corretores' (UserCog) + sub-item 'Parceiros'"
      contains: "label: 'Corretores'"
    - path: "src/app/(app)/[slug]/corretores/page.tsx"
      provides: "Server Component — listagem de corretores com producao do mes"
      contains: "export default async function CorretoresPage"
    - path: "src/app/(app)/[slug]/parceiros/page.tsx"
      provides: "Server Component — listagem de parceiros"
      contains: "export default async function ParceirosPage"
    - path: "src/components/corretores/broker-profile-dialog.tsx"
      provides: "Dialog 'Completar perfil de corretor' — submete via upsertBrokerProfileAction"
      contains: "import { upsertBrokerProfileAction }"
    - path: "src/components/corretores/broker-list-table.tsx"
      provides: "Tabela client-aware com DropdownMenu de acoes (Ver dashboard, Editar perfil)"
      contains: "export function BrokerListTable"
    - path: "src/components/parceiros/partner-dialog.tsx"
      provides: "Dialog dual-mode 'Novo parceiro' / 'Editar parceiro' — chama createPartnerAction ou updatePartnerAction"
      contains: "import { createPartnerAction"
    - path: "src/components/parceiros/partner-table.tsx"
      provides: "Tabela de parceiros com DropdownMenu (Editar, Excluir)"
      contains: "export function PartnerTable"
    - path: "src/components/parceiros/partner-delete-confirm.tsx"
      provides: "AlertDialog que chama softDeletePartnerAction"
      contains: "import { softDeletePartnerAction"
  key_links:
    - from: "src/components/corretores/broker-profile-dialog.tsx"
      to: "src/lib/actions/broker-profiles.ts upsertBrokerProfileAction"
      via: "import + handleSubmit chama action com FormData"
      pattern: "import \\{ upsertBrokerProfileAction \\} from '@/lib/actions/broker-profiles'"
    - from: "src/components/parceiros/partner-dialog.tsx"
      to: "src/lib/actions/partners.ts createPartnerAction"
      via: "import + handleSubmit"
      pattern: "import \\{ createPartnerAction"
    - from: "src/components/parceiros/partner-delete-confirm.tsx"
      to: "src/lib/actions/partners.ts softDeletePartnerAction"
      via: "import + onConfirm"
      pattern: "import \\{ softDeletePartnerAction \\}"
    - from: "src/app/(app)/[slug]/corretores/page.tsx"
      to: "src/components/corretores/broker-list-table.tsx"
      via: "import + render <BrokerListTable rows=... />"
      pattern: "import \\{ BrokerListTable \\}"
    - from: "src/app/(app)/[slug]/parceiros/page.tsx"
      to: "src/components/parceiros/partner-table.tsx"
      via: "import + render <PartnerTable rows=... />"
      pattern: "import \\{ PartnerTable \\}"
    - from: "src/components/auth/sidebar-shell.tsx"
      to: "/${slug}/corretores"
      via: "Link href no array navItems"
      pattern: "href: `/\\$\\{slug\\}/corretores`"
---

<objective>
Construir a camada de UI administrativa da Phase 4: telas de listagem de corretores e parceiros, dialogs de cadastro/edicao e extensao do sidebar. Esta camada cobre COM-01 (cadastrar corretor — Dialog 'Completar perfil'), COM-02 (cadastrar parceiro — Dialog) e a porta de entrada de COM-06 (botao 'Ver dashboard' em cada linha de corretor).

A camada e puramente de apresentacao + invocacao das Server Actions ja construidas no Plan 02. Nao toca o ledger, nao calcula comissao, nao toca apolices/cotas — esses fluxos vivem no Plan 04.

Purpose: Dar ao admin telas funcionais para gerenciar corretores internos e parceiros externos, alimentando os dados que o Plan 04 vai usar para calcular comissoes (broker_profiles.commission_rate_default, partners.commission_rate_overrides etc.).

Output: 2 rotas (/[slug]/corretores e /[slug]/parceiros) + 5 componentes (1 tabela + 1 dialog por entidade + 1 alert-dialog para soft delete) + sidebar atualizada. npm run build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-corretores-comissoes/04-CONTEXT.md
@.planning/phases/04-corretores-comissoes/04-RESEARCH.md
@.planning/phases/04-corretores-comissoes/04-UI-SPEC.md
@src/components/auth/sidebar-shell.tsx
@src/app/(app)/[slug]/seguros/page.tsx
@src/components/seguros/policy-table.tsx
@src/components/consorcio/contemplation-dialog.tsx
@src/components/consorcio/group-edit-dialog.tsx

<interfaces>
<!-- Contratos das Server Actions criadas no Plan 02 (consumidas aqui) -->

```typescript
// src/lib/actions/broker-profiles.ts
export async function upsertBrokerProfileAction(
  slug: string,
  formData: FormData,
): Promise<{ error?: Record<string, string[]> | { _form: string[] } } | { success: true }>
// FormData fields esperados:
//   profile_id (UUID, required)
//   susep_number (string, optional — vazio = NULL)
//   monthly_goal (number coerced from string)
//   commission_rate_default (number 0..1 coerced from string)
//   override_auto, override_vida, override_residencial, override_empresarial,
//   override_saude, override_outros, override_consorcio_auto,
//   override_consorcio_imovel, override_consorcio_servico (numbers, all optional)
// O Server Action reconstrói o JSONB commission_rate_overrides a partir dos campos override_*

// src/lib/actions/partners.ts
export async function createPartnerAction(
  slug: string,
  formData: FormData,
): Promise<{ error?: Record<string, string[]> | { _form: string[] } } | { success: true; id: string }>
// FormData fields: name, cnpj?, contact_email?, contact_phone?, commission_rate_default,
//                  override_auto, ..., override_consorcio_servico (mesmas chaves)

export async function updatePartnerAction(
  slug: string,
  partnerId: string,
  formData: FormData,
): Promise<{ error?: ... } | { success: true }>

export async function softDeletePartnerAction(
  slug: string,
  partnerId: string,
): Promise<{ error?: string } | { success: true }>
```

<!-- Componentes shadcn ja instalados — usar diretamente (nao reimplementar) -->
// src/components/ui/dialog.tsx — Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger
// src/components/ui/alert-dialog.tsx — AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogTrigger
// src/components/ui/table.tsx — Table, TableHeader, TableBody, TableRow, TableHead, TableCell
// src/components/ui/dropdown-menu.tsx — DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem
// src/components/ui/input.tsx, label.tsx, button.tsx, badge.tsx, sonner.tsx (toast)

<!-- Padrao de Dialog estabelecido em group-edit-dialog.tsx + contemplation-dialog.tsx:
     - 'use client'
     - useState para open + isSubmitting
     - handleSubmit(e): e.preventDefault() -> setIsSubmitting(true) -> fd = new FormData(form) -> action(slug, fd)
     - on error: toast.error(...)
     - on success: toast.success(...) -> setOpen(false) -> setIsSubmitting(false)
     - DialogTrigger asChild com Button -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender sidebar com 'Corretores' + sub-item 'Parceiros'</name>
  <files>src/components/auth/sidebar-shell.tsx</files>
  <read_first>
    - src/components/auth/sidebar-shell.tsx (estado atual — ja tem Configuracoes com children, padrao a replicar)
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md (secao Layout Contract: 'Sidebar addition for Phase 4')
    - .planning/phases/04-corretores-comissoes/04-RESEARCH.md (Pattern 6: Sidebar Extension; Open Questions item 2)
  </read_first>
  <action>
Editar `src/components/auth/sidebar-shell.tsx` para adicionar duas entradas no array `navItems`:

1. Item "Corretores" — entrada principal com sub-item "Parceiros" (mesma estrutura `children` que ja existe para Configuracoes).
2. Posicionar ENTRE "Consorcio" (linha ~54 atual) e "Configuracoes" (linha ~56 atual).

Usar EXATAMENTE este shape (preservando o padrao do array atual):

```typescript
{
  label: 'Corretores',
  href: `/${slug}/corretores`,
  icon: <UserCog size={16} />,
  children: [
    {
      label: 'Parceiros',
      href: `/${slug}/parceiros`,
    },
  ],
},
```

Notas criticas:
- `UserCog` JA esta importado de `lucide-react` na linha 4 do arquivo (verificar antes de editar — nao adicionar import duplicado).
- O item "Corretores" tem `children` — pelo pattern atual (linhas 92-95), isso renderiza o label como `<div>` nao-clicavel. ISSO E UM BUG VISIVEL NO ARQUIVO ATUAL: Configuracoes nao e clicavel apesar de ter href. **Resolver no Task 1**: alterar a logica de render para ainda permitir click no item PAI quando `href` esta presente, mesmo se `children` existir. Substituir o `else if (item.children)` (linhas 92-95) por:
  ```tsx
  ) : item.children && !item.href ? (
    // Apenas label nao-clicavel quando NAO tem href proprio
    <div className="flex items-center gap-2 rounded-md px-3 py-2 text-sm text-foreground">
      {item.icon}
      {item.label}
    </div>
  ) : (
    // Item com href (e talvez children) — renderiza Link normal
    <Link
      href={item.href}
      className={[
        'flex items-center gap-2 rounded-md px-3 py-2 text-sm transition-colors',
        isActive(item.href)
          ? 'bg-primary text-primary-foreground font-semibold'
          : 'text-foreground hover:bg-muted',
      ].join(' ')}
    >
      {item.icon}
      {item.label}
    </Link>
  )
  ```
  E ajustar Configuracoes para ter `href: ''` (ou remover href) — assim Configuracoes continua nao-clicavel mas Corretores fica clicavel. ALTERNATIVA mais segura (preferida): manter Configuracoes intocada e simplesmente adicionar Corretores SEM children no array principal, e adicionar Parceiros como ITEM IRMAO (entrada propria) imediatamente apos Corretores. Esta segunda alternativa NAO MUDA a logica de render do componente — apenas estende o array.

**Acao final escolhida (preferida — minimiza risco):** Adicionar DUAS entradas independentes no array, sem usar `children`, ambas dentro da secao "Produtos":

```typescript
// Inserir entre 'Consorcio' (atual idx 4) e 'Configuracoes' (atual idx 5):
{
  label: 'Corretores',
  href: `/${slug}/corretores`,
  icon: <UserCog size={16} />,
},
{
  label: 'Parceiros',
  href: `/${slug}/parceiros`,
  icon: <Users size={16} />,
},
```

`Users` JA esta importado na linha 4. Usar `Users` (nao UserCog) para Parceiros distingue visualmente o sub-conceito.

Resultado: array `navItems` ganha 2 entradas; nenhuma logica de render muda; sidebar mostra Corretores + Parceiros como itens-irmaos sob a secao "Produtos".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth/sidebar-shell.tsx` contem literal `label: 'Corretores'`
    - `src/components/auth/sidebar-shell.tsx` contem literal `label: 'Parceiros'`
    - `src/components/auth/sidebar-shell.tsx` contem literal `href: \`/\${slug}/corretores\``
    - `src/components/auth/sidebar-shell.tsx` contem literal `href: \`/\${slug}/parceiros\``
    - `src/components/auth/sidebar-shell.tsx` contem `<UserCog size={16} />` na entrada Corretores
    - Imports de `UserCog` e `Users` ja existem na linha 4 — ZERO novos imports adicionados
    - `npm run build` retorna exit code 0 (zero erros TS/ESLint)
    - `grep -c "label: 'Corretores'\|label: 'Parceiros'" src/components/auth/sidebar-shell.tsx` retorna pelo menos 2
  </acceptance_criteria>
  <done>Sidebar lista Corretores (UserCog) e Parceiros (Users) entre Consorcio e Configuracoes; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Dialog 'Completar perfil de corretor' + tabela de corretores + rota /[slug]/corretores</name>
  <files>
    - src/components/corretores/broker-profile-dialog.tsx
    - src/components/corretores/broker-list-table.tsx
    - src/app/(app)/[slug]/corretores/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-01, D-02, D-03, D-14, Claude's Discretion)
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md (secoes Corretores List Page, Broker Profile Dialog, Broker List Table — copy exata em pt-BR)
    - .planning/phases/04-corretores-comissoes/04-02-PLAN.md (interfaces — assinatura de upsertBrokerProfileAction)
    - src/app/(app)/[slug]/seguros/page.tsx (padrao Server Component listagem com paginacao 25/?page=N)
    - src/components/seguros/policy-table.tsx (padrao Table + DropdownMenu acoes)
    - src/components/consorcio/contemplation-dialog.tsx (padrao Dialog + FormData + Server Action — copiar shape de handleSubmit)
    - src/components/consorcio/group-edit-dialog.tsx (padrao Dialog DUAL-MODE create/edit com pre-fill)
  </read_first>
  <action>
**Sub-step 2.1 — Criar `src/components/corretores/broker-profile-dialog.tsx`** (Client Component):

Padrao identico a `contemplation-dialog.tsx` mas com 14 campos. Suporta DOIS modos:
- **Create mode** (broker_profile ainda nao existe): triggerLabel = "Completar perfil de corretor"
- **Edit mode** (broker_profile ja existe): triggerLabel = "Editar perfil"; pre-preenche os campos com valores atuais via prop `existing?: BrokerProfileData`

Props:
```typescript
interface BrokerProfileData {
  id: string                                   // = profiles.id (1:1)
  susep_number: string | null
  monthly_goal: number
  commission_rate_default: number
  commission_rate_overrides: Record<string, number | undefined> | null
}

interface Props {
  slug: string
  profileId: string                             // ID do profile (corretor) que recebera o broker_profile
  brokerName: string                            // para o DialogTitle "Perfil de corretor — {nome}"
  existing?: BrokerProfileData                  // se passado, opera em edit mode; senao, create
  triggerVariant?: 'default' | 'outline' | 'ghost'
  triggerSize?: 'default' | 'sm'
  triggerLabel?: string                         // default: existing ? 'Editar perfil' : 'Completar perfil de corretor'
}
```

Estrutura do form (copy literal do UI-SPEC secao "Broker Profile Dialog"):
- DialogTitle: `Perfil de corretor — {brokerName}`
- Hidden input `profile_id` = `profileId`
- Label "Numero SUSEP" + Input name="susep_number" placeholder="Opcional — ex.: 100001234"
- Label "Meta mensal (R$)" + Input name="monthly_goal" type="number" step="0.01" min="0" placeholder="Ex.: 10000,00"
- Label "Taxa de comissao padrao (%)" + Input name="commission_rate_default" type="number" step="0.0001" min="0" max="1" — helper text "Taxa aplicada quando nao ha override por tipo de produto"
- Section h3: "Taxas por tipo de produto (opcional)"
- Helper p: "Deixe em branco para usar a taxa padrao"
- Grid `grid grid-cols-3 gap-4` com 9 inputs name="override_{key}" para `auto, vida, residencial, empresarial, saude, outros, consorcio_auto, consorcio_imovel, consorcio_servico`. Cada input tem Label visivel com nome legivel (Auto, Vida, Residencial, Empresarial, Saude, Outros, Consorcio Auto, Consorcio Imovel, Consorcio Servico). type="number" step="0.0001" min="0" max="1".
- Botoes: "Fechar sem salvar" (variant=outline, type=button) + "Salvar perfil" (type=submit, loading state "Salvando...")

handleSubmit:
```typescript
async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
  e.preventDefault()
  setIsSubmitting(true)
  try {
    const fd = new FormData(e.currentTarget)
    const result = await upsertBrokerProfileAction(slug, fd)
    if (result && 'error' in result) {
      const errs = result.error
      const firstMsg = typeof errs === 'object' && errs && '_form' in errs
        ? errs._form?.[0]
        : Object.values(errs ?? {})[0]?.[0]
      toast.error(firstMsg ?? 'Erro ao salvar o perfil. Tente novamente.')
      return
    }
    toast.success('Perfil de corretor atualizado.')
    setOpen(false)
  } finally {
    setIsSubmitting(false)
  }
}
```

Pre-fill em edit mode: usar `defaultValue` em cada Input quando `existing` esta presente (NAO `value` — defaultValue evita re-render controlado em form longo).

**Sub-step 2.2 — Criar `src/components/corretores/broker-list-table.tsx`** (Client Component):

```typescript
'use client'
import Link from 'next/link'
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@/components/ui/table'
import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem } from '@/components/ui/dropdown-menu'
import { Button } from '@/components/ui/button'
import { MoreHorizontal } from 'lucide-react'
import { BrokerProfileDialog } from './broker-profile-dialog'

export interface BrokerRow {
  profile_id: string
  full_name: string
  susep_number: string | null
  monthly_goal: number | null         // null quando nao tem broker_profile ainda
  production_count: number            // count de policies do mes corrente
  has_broker_profile: boolean
  broker_profile?: {                   // existe somente quando has_broker_profile=true
    susep_number: string | null
    monthly_goal: number
    commission_rate_default: number
    commission_rate_overrides: Record<string, number | undefined> | null
  }
}

interface Props {
  slug: string
  rows: BrokerRow[]
}

function formatBRL(v: number | null | undefined): string {
  if (v === null || v === undefined) return '—'
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}

export function BrokerListTable({ slug, rows }: Props) { /* render Table */ }
```

Colunas (copy exata UI-SPEC "Broker List Table"):
- Nome (`row.full_name`)
- SUSEP (`row.susep_number ?? '—'`)
- Meta mensal (`formatBRL(row.monthly_goal)`)
- Producao do mes (`{row.production_count} apolice(s)`)
- Acoes — DropdownMenu com:
  - "Ver dashboard" → `<Link href="/${slug}/corretores/${row.profile_id}">`
  - "Editar perfil" OU "Completar perfil de corretor" — abre `<BrokerProfileDialog ... existing={row.broker_profile} />` ou cria novo

Nota: O Dialog precisa estar montado no DOM para abrir programaticamente. Renderizar `<BrokerProfileDialog />` dentro da celula Acoes com `triggerLabel` calculado a partir de `row.has_broker_profile`. Isso evita state hoisting.

**Sub-step 2.3 — Criar `src/app/(app)/[slug]/corretores/page.tsx`** (Server Component):

```typescript
import { notFound, redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { BrokerListTable, type BrokerRow } from '@/components/corretores/broker-list-table'
import { Button } from '@/components/ui/button'
import Link from 'next/link'
import { startOfMonth, endOfMonth, format } from 'date-fns'

const PAGE_SIZE = 25

interface Props {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ page?: string }>
}

export default async function CorretoresPage({ params, searchParams }: Props) {
  const { slug } = await params
  const sp = await searchParams
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = (await createClient()) as any
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  // Redirect: se usuario e corretor, manda direto para o proprio dashboard
  const role = (user.app_metadata as { role?: string })?.role
  if (role === 'corretor') {
    redirect(`/${slug}/corretores/${user.id}`)
  }

  // 1. Buscar profiles com role='corretor' do tenant (RLS aplica tenant_id)
  const pageNum = Math.max(1, parseInt(sp.page ?? '1', 10))
  const from = (pageNum - 1) * PAGE_SIZE

  const { data: profiles, count } = await supabase
    .from('profiles')
    .select('id, full_name', { count: 'exact' })
    .eq('role', 'corretor')
    .order('full_name', { ascending: true })
    .range(from, from + PAGE_SIZE - 1)

  const profileIds = (profiles ?? []).map((p: { id: string }) => p.id)

  // 2. Buscar broker_profiles correspondentes
  const { data: brokerProfiles } = profileIds.length > 0
    ? await supabase
        .from('broker_profiles')
        .select('id, susep_number, monthly_goal, commission_rate_default, commission_rate_overrides')
        .is('deleted_at', null)
        .in('id', profileIds)
    : { data: [] as any[] }

  const brokerProfileMap = new Map(
    (brokerProfiles ?? []).map((b: any) => [b.id, b]),
  )

  // 3. Producao do mes corrente: count de policies por assigned_to no mes corrente
  const monthStart = format(startOfMonth(new Date()), 'yyyy-MM-dd')
  const monthEnd = format(endOfMonth(new Date()), 'yyyy-MM-dd')

  const productionMap = new Map<string, number>()
  if (profileIds.length > 0) {
    const { data: prodRows } = await supabase
      .from('policies')
      .select('assigned_to')
      .in('assigned_to', profileIds)
      .gte('created_at', monthStart)
      .lte('created_at', monthEnd + 'T23:59:59')
      .is('deleted_at', null)
    for (const r of (prodRows ?? []) as { assigned_to: string }[]) {
      productionMap.set(r.assigned_to, (productionMap.get(r.assigned_to) ?? 0) + 1)
    }
  }

  // 4. Montar BrokerRow[]
  const rows: BrokerRow[] = (profiles ?? []).map((p: { id: string; full_name: string }) => {
    const bp = brokerProfileMap.get(p.id) as { susep_number: string | null; monthly_goal: number; commission_rate_default: number; commission_rate_overrides: any } | undefined
    return {
      profile_id: p.id,
      full_name: p.full_name,
      susep_number: bp?.susep_number ?? null,
      monthly_goal: bp?.monthly_goal ?? null,
      production_count: productionMap.get(p.id) ?? 0,
      has_broker_profile: !!bp,
      broker_profile: bp ? {
        susep_number: bp.susep_number,
        monthly_goal: bp.monthly_goal,
        commission_rate_default: bp.commission_rate_default,
        commission_rate_overrides: bp.commission_rate_overrides,
      } : undefined,
    }
  })

  const totalPages = Math.max(1, Math.ceil((count ?? 0) / PAGE_SIZE))

  return (
    <div className="mx-auto max-w-7xl space-y-4 p-6">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-semibold">Corretores</h1>
          <p className="text-sm text-muted-foreground">{count ?? 0} corretor(es) cadastrado(s)</p>
        </div>
      </div>

      {rows.length === 0 ? (
        <div className="rounded-lg border border-dashed p-8 text-center space-y-3">
          <p className="font-medium">Nenhum corretor cadastrado ainda</p>
          <p className="text-sm text-muted-foreground">
            Convide um corretor na area de Usuarios e complete o perfil com numero SUSEP, meta e taxa de comissao.
          </p>
          <Button asChild variant="outline">
            <Link href={`/${slug}/configuracoes/usuarios`}>Ir para Usuarios</Link>
          </Button>
        </div>
      ) : (
        <BrokerListTable slug={slug} rows={rows} />
      )}

      {/* Paginacao igual ao padrao seguros/page.tsx */}
      {totalPages > 1 && (
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">Pagina {pageNum} de {totalPages}</span>
          <div className="flex gap-2">
            {pageNum > 1 && (
              <Button asChild variant="outline" size="sm">
                <Link href={`?page=${pageNum - 1}`}>Anterior</Link>
              </Button>
            )}
            {pageNum < totalPages && (
              <Button asChild variant="outline" size="sm">
                <Link href={`?page=${pageNum + 1}`}>Proxima</Link>
              </Button>
            )}
          </div>
        </div>
      )}
    </div>
  )
}
```

Notas criticas:
- Filter row ausente (UI-SPEC: "No filter row on corretores list — small data set")
- A query de `broker_profiles` usa `.is('deleted_at', null)` para excluir soft-deletados.
- A query de policies do mes usa `created_at` entre start/end of month — alinhado com `policies.created_at` (timestamp).
- Use `as any` cast no client (padrao Phase 3 STATE.md).
- A redirect quando `role === 'corretor'` esta DUPLICADO no Plan 04 (Server Component de [id]). Aqui no /corretores tambem redireciona para evitar que corretor veja a lista (D-11 implicitamente).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/components/corretores/broker-profile-dialog.tsx` existe e contem literal `import { upsertBrokerProfileAction } from '@/lib/actions/broker-profiles'`
    - Arquivo `src/components/corretores/broker-profile-dialog.tsx` contem 9 inputs com name=`override_{auto,vida,residencial,empresarial,saude,outros,consorcio_auto,consorcio_imovel,consorcio_servico}` (grep `name="override_` retorna 9)
    - Arquivo `src/components/corretores/broker-profile-dialog.tsx` contem literal `Salvar perfil` e literal `Fechar sem salvar`
    - Arquivo `src/components/corretores/broker-profile-dialog.tsx` contem literal `grid-cols-3 gap-4` (UI-SPEC nota: gap-4 nao gap-3)
    - Arquivo `src/components/corretores/broker-list-table.tsx` existe e exporta `BrokerListTable` e `BrokerRow`
    - Arquivo `src/components/corretores/broker-list-table.tsx` contem literal `Ver dashboard` e literal `Editar perfil`
    - Arquivo `src/app/(app)/[slug]/corretores/page.tsx` existe e contem `export default async function CorretoresPage`
    - Arquivo `src/app/(app)/[slug]/corretores/page.tsx` contem literal `if (role === 'corretor')` E `redirect(\`/\${slug}/corretores/\${user.id}\`)`
    - Arquivo `src/app/(app)/[slug]/corretores/page.tsx` contem literal `.eq('role', 'corretor')`
    - Arquivo `src/app/(app)/[slug]/corretores/page.tsx` contem literal `corretor(es) cadastrado(s)` (UI-SPEC subcount)
    - Arquivo `src/app/(app)/[slug]/corretores/page.tsx` contem literal `Nenhum corretor cadastrado ainda` E `Ir para Usuarios`
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>Rota /corretores funcional com tabela paginada, dialog de perfil cobrindo 9 chaves de override e redirect de corretor para o proprio dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Criar Dialog dual-mode de parceiro + tabela de parceiros + AlertDialog de exclusao + rota /[slug]/parceiros</name>
  <files>
    - src/components/parceiros/partner-dialog.tsx
    - src/components/parceiros/partner-table.tsx
    - src/components/parceiros/partner-delete-confirm.tsx
    - src/app/(app)/[slug]/parceiros/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-corretores-comissoes/04-CONTEXT.md (D-04, D-05, D-07)
    - .planning/phases/04-corretores-comissoes/04-UI-SPEC.md (secao Parceiros List Page, New Partner Dialog, Partner List Table, Destructive Actions)
    - .planning/phases/04-corretores-comissoes/04-02-PLAN.md (interfaces — assinaturas createPartnerAction, updatePartnerAction, softDeletePartnerAction)
    - src/components/consorcio/group-edit-dialog.tsx (padrao Dialog dual-mode com pre-fill via defaultValue)
    - src/components/seguros/policy-table.tsx (padrao Table + DropdownMenu acoes)
    - src/app/(app)/[slug]/seguros/page.tsx (padrao Server Component listagem com paginacao)
  </read_first>
  <action>
**Sub-step 3.1 — `src/components/parceiros/partner-dialog.tsx`** (Client, dual-mode):

```typescript
'use client'
import { useState } from 'react'
import { toast } from 'sonner'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Button } from '@/components/ui/button'
import { createPartnerAction, updatePartnerAction } from '@/lib/actions/partners'

interface PartnerData {
  id: string
  name: string
  cnpj: string | null
  contact_email: string | null
  contact_phone: string | null
  commission_rate_default: number
  commission_rate_overrides: Record<string, number | undefined> | null
}

interface Props {
  slug: string
  existing?: PartnerData                          // edit mode quando passado
  triggerLabel?: string                            // default depende do mode
  triggerVariant?: 'default' | 'outline' | 'ghost'
  triggerSize?: 'default' | 'sm'
}
```

Form fields (copy literal UI-SPEC "New Partner Dialog"):
- Hidden input `partner_id` = `existing?.id` (somente em edit mode)
- Label "Nome do parceiro" + Input name="name" required minLength=2
- Label "CNPJ (opcional)" + Input name="cnpj"
- Label "E-mail de contato" + Input name="contact_email" type="email"
- Label "Telefone de contato" + Input name="contact_phone"
- Label "Taxa de repasse padrao (%)" + Input name="commission_rate_default" type="number" step="0.0001" min="0" max="1"
- Section h3: "Taxas por tipo de produto (opcional)"
- Grid `grid grid-cols-3 gap-4` com 9 inputs `override_{key}` (mesmas chaves do broker dialog)
- Botoes: "Descartar" (outline, type=button, onClick=fecha) + "Cadastrar parceiro" / "Salvar parceiro" (type=submit)

handleSubmit:
```typescript
const fd = new FormData(e.currentTarget)
const result = existing
  ? await updatePartnerAction(slug, existing.id, fd)
  : await createPartnerAction(slug, fd)

if (result && 'error' in result) {
  toast.error(extractFirstMsg(result.error) ?? 'Erro ao cadastrar parceiro. Tente novamente.')
  return
}
toast.success(existing ? 'Parceiro atualizado com sucesso.' : 'Parceiro cadastrado com sucesso.')
setOpen(false)
```

Loading label: `existing ? 'Salvando...' : 'Cadastrando...'`

**Sub-step 3.2 — `src/components/parceiros/partner-delete-confirm.tsx`** (Client, AlertDialog):

```typescript
'use client'
import { useState, useTransition } from 'react'
import { toast } from 'sonner'
import {
  AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent,
  AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger,
} from '@/components/ui/alert-dialog'
import { Button } from '@/components/ui/button'
import { softDeletePartnerAction } from '@/lib/actions/partners'

interface Props {
  slug: string
  partnerId: string
  partnerName: string
}

export function PartnerDeleteConfirm({ slug, partnerId, partnerName }: Props) {
  const [open, setOpen] = useState(false)
  const [isPending, startTransition] = useTransition()

  function handleConfirm() {
    startTransition(async () => {
      const result = await softDeletePartnerAction(slug, partnerId)
      if (result && 'error' in result) {
        toast.error(typeof result.error === 'string' ? result.error : 'Erro ao excluir parceiro.')
        return
      }
      toast.info('Parceiro removido.')
      setOpen(false)
    })
  }

  return (
    <AlertDialog open={open} onOpenChange={setOpen}>
      <AlertDialogTrigger asChild>
        <button className="text-destructive">Excluir</button>
      </AlertDialogTrigger>
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Excluir parceiro?</AlertDialogTitle>
          <AlertDialogDescription>
            O parceiro <strong>{partnerName}</strong> sera removido (soft delete).
            Lancamentos de comissao ja registrados sao mantidos no ledger.
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogCancel>Manter parceiro</AlertDialogCancel>
          <AlertDialogAction onClick={handleConfirm} disabled={isPending}>
            {isPending ? 'Excluindo...' : 'Sim, excluir'}
          </AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  )
}
```

Copy literal do UI-SPEC "Destructive Actions": title "Excluir parceiro?", body "O parceiro sera removido (soft delete). Lancamentos de comissao ja registrados sao mantidos no ledger.", confirm "Sim, excluir", cancel "Manter parceiro".

**Sub-step 3.3 — `src/components/parceiros/partner-table.tsx`** (Client):

```typescript
'use client'
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@/components/ui/table'
import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem } from '@/components/ui/dropdown-menu'
import { Button } from '@/components/ui/button'
import { MoreHorizontal } from 'lucide-react'
import { PartnerDialog } from './partner-dialog'
import { PartnerDeleteConfirm } from './partner-delete-confirm'

export interface PartnerRow {
  id: string
  name: string
  cnpj: string | null
  contact_email: string | null
  contact_phone: string | null
  commission_rate_default: number
  commission_rate_overrides: Record<string, number | undefined> | null
}

interface Props {
  slug: string
  rows: PartnerRow[]
}
```

Colunas (UI-SPEC "Partner List Table"):
- Nome (`row.name`)
- CNPJ (`row.cnpj ?? '—'`)
- E-mail de contato (`row.contact_email ?? '—'`)
- Taxa padrao — formatada como `Intl.NumberFormat('pt-BR', { style: 'percent', minimumFractionDigits: 2 }).format(row.commission_rate_default)`
- Acoes — DropdownMenu com:
  - "Editar" → renderiza `<PartnerDialog slug={slug} existing={row} triggerVariant="ghost" />` dentro do menu
  - "Excluir" → renderiza `<PartnerDeleteConfirm slug={slug} partnerId={row.id} partnerName={row.name} />`

**Sub-step 3.4 — `src/app/(app)/[slug]/parceiros/page.tsx`** (Server Component):

```typescript
import { notFound } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { Button } from '@/components/ui/button'
import { PartnerTable, type PartnerRow } from '@/components/parceiros/partner-table'
import { PartnerDialog } from '@/components/parceiros/partner-dialog'

const PAGE_SIZE = 25

interface Props {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ page?: string }>
}

export default async function ParceirosPage({ params, searchParams }: Props) {
  const { slug } = await params
  const sp = await searchParams
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = (await createClient()) as any
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) notFound()

  const pageNum = Math.max(1, parseInt(sp.page ?? '1', 10))
  const from = (pageNum - 1) * PAGE_SIZE

  const { data: partners, count } = await supabase
    .from('partners')
    .select('id, name, cnpj, contact_email, contact_phone, commission_rate_default, commission_rate_overrides', { count: 'exact' })
    .is('deleted_at', null)
    .order('name', { ascending: true })
    .range(from, from + PAGE_SIZE - 1)

  const rows = (partners ?? []) as unknown as PartnerRow[]
  const totalPages = Math.max(1, Math.ceil((count ?? 0) / PAGE_SIZE))

  return (
    <div className="mx-auto max-w-7xl space-y-4 p-6">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-semibold">Parceiros externos</h1>
          <p className="text-sm text-muted-foreground">{count ?? 0} parceiro(s) cadastrado(s)</p>
        </div>
        <PartnerDialog slug={slug} triggerLabel="Novo parceiro" />
      </div>

      {rows.length === 0 ? (
        <div className="rounded-lg border border-dashed p-8 text-center space-y-3">
          <p className="font-medium">Nenhum parceiro externo cadastrado</p>
          <p className="text-sm text-muted-foreground">
            Parceiros sao indicadores externos sem acesso ao sistema. Cadastre aqui para registrar repasses automaticos.
          </p>
          <PartnerDialog slug={slug} triggerLabel="Cadastrar primeiro parceiro" triggerVariant="outline" />
        </div>
      ) : (
        <PartnerTable slug={slug} rows={rows} />
      )}

      {totalPages > 1 && (
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">Pagina {pageNum} de {totalPages}</span>
          <div className="flex gap-2">
            {pageNum > 1 && (
              <Button asChild variant="outline" size="sm">
                <a href={`?page=${pageNum - 1}`}>Anterior</a>
              </Button>
            )}
            {pageNum < totalPages && (
              <Button asChild variant="outline" size="sm">
                <a href={`?page=${pageNum + 1}`}>Proxima</a>
              </Button>
            )}
          </div>
        </div>
      )}
    </div>
  )
}
```

Nota: PartnerDialog renderizado tanto no header (CTA "Novo parceiro") quanto no empty state (CTA "Cadastrar primeiro parceiro") — ambos abrem o mesmo Dialog em create mode (sem `existing`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/components/parceiros/partner-dialog.tsx` existe e contem literal `import { createPartnerAction, updatePartnerAction } from '@/lib/actions/partners'`
    - Arquivo `src/components/parceiros/partner-dialog.tsx` contem 9 inputs `name="override_*"` (mesmas chaves do broker)
    - Arquivo `src/components/parceiros/partner-dialog.tsx` contem literal `Cadastrar parceiro` E `Descartar`
    - Arquivo `src/components/parceiros/partner-dialog.tsx` contem literal `grid-cols-3 gap-4`
    - Arquivo `src/components/parceiros/partner-table.tsx` existe e exporta `PartnerTable` + `PartnerRow`
    - Arquivo `src/components/parceiros/partner-delete-confirm.tsx` existe e contem literal `import { softDeletePartnerAction }`
    - Arquivo `src/components/parceiros/partner-delete-confirm.tsx` contem literal `Excluir parceiro?` E `Sim, excluir` E `Manter parceiro`
    - Arquivo `src/components/parceiros/partner-delete-confirm.tsx` contem literal `Lancamentos de comissao ja registrados sao mantidos no ledger.`
    - Arquivo `src/app/(app)/[slug]/parceiros/page.tsx` existe e contem `export default async function ParceirosPage`
    - Arquivo `src/app/(app)/[slug]/parceiros/page.tsx` contem literal `Parceiros externos` (page heading) E `parceiro(s) cadastrado(s)` (subcount)
    - Arquivo `src/app/(app)/[slug]/parceiros/page.tsx` contem literal `Novo parceiro` E `Cadastrar primeiro parceiro`
    - Arquivo `src/app/(app)/[slug]/parceiros/page.tsx` contem `.from('partners')` E `.is('deleted_at', null)`
    - `npm run build` retorna exit code 0
  </acceptance_criteria>
  <done>Rota /parceiros funcional com tabela, dialog dual-mode (create/edit) e AlertDialog de exclusao seguindo copywriting exato do UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (FormData) → Server Action | FormData submetido via Dialog/Form atravessa boundary; Server Action (Plan 02) faz Zod safeParse antes de qualquer DB call |
| Authenticated user (JWT) → RLS | tenant_id e role do app_metadata; RLS de broker_profiles/partners filtra por tenant |
| Sidebar render → autenticacao | Sidebar nao expoe rotas a usuarios sem sessao (layout.tsx redireciona em /login) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | broker-profile-dialog hidden input `profile_id` | mitigate | Server Action upsertBrokerProfileAction (Plan 02) valida profile_id via Zod uuid + RLS WITH CHECK admin-only impede insert para outro perfil |
| T-04-09 | Information Disclosure | /corretores Server Component lista profiles role='corretor' | mitigate | RLS aplica tenant_id no SELECT — admin de tenant A nunca ve corretores de tenant B; redirect quando role='corretor' impede listagem cross-broker |
| T-04-10 | Information Disclosure | /parceiros Server Component lista partners | mitigate | RLS partners_select restringe por tenant_id (Plan 01); deleted_at IS NULL exclui soft-deletados |
| T-04-11 | Tampering | partner-delete-confirm chama softDelete via Client Action | mitigate | softDeletePartnerAction (Plan 02) requer role='admin' no JWT; AlertDialog so previne UX accidental, RLS e a barreira real |
| T-04-12 | Repudiation | DropdownMenu de "Editar perfil" sem audit log | accept | Phase 4 nao tem audit_log table (deferred ao Phase 7); broker_profiles.updated_at registra timestamp da ultima edicao |
| T-04-13 | Information Disclosure | Empty state expoe link `/configuracoes/usuarios` | accept | Configuracoes/usuarios e protegida por sua propria RLS (Phase 1); link em si nao revela dados |
</threat_model>

<verification>
- `/[slug]/corretores` lista corretores com paginacao e producao do mes
- `/[slug]/parceiros` lista parceiros e permite criar/editar/excluir via Dialogs
- Sidebar exibe links para ambas as rotas entre Consorcio e Configuracoes
- Build sem erros TS/ESLint: `npm run build` exit 0
- Testes do Plan 02 continuam verdes (este plan nao mexe em util/actions)
</verification>

<success_criteria>
- COM-01: admin pode cadastrar corretor interno preenchendo SUSEP, meta, taxa default e 9 overrides via Dialog
- COM-02: admin pode cadastrar parceiro externo com mesmas 9 chaves de override e dados de contato
- COM-06 (porta de entrada): cada linha de corretor expoe botao "Ver dashboard" — destino implementado no Plan 04
- Copywriting pt-BR identico ao UI-SPEC (toasts, CTAs, empty states, AlertDialog)
- Tenant isolation preservado por RLS — Server Actions e Server Components ja extraem tenant_id do JWT
</success_criteria>

<output>
After completion, create `.planning/phases/04-corretores-comissoes/04-03-SUMMARY.md` with:
- Sidebar atualizada (label, href, icon)
- Rotas criadas: /corretores e /parceiros
- 5 componentes criados (paths absolutos)
- Verificacao de build (`npm run build`)
- Proximo passo: Plan 04 (dashboard individual + integracao com /seguros/[id] e /consorcio/[id])
</output>
</content>
</invoke>