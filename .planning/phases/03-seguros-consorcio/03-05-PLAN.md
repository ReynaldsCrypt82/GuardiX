---
phase: 03-seguros-consorcio
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/policies.ts
  - src/app/(app)/[slug]/seguros/page.tsx
  - src/app/(app)/[slug]/layout.tsx
  - supabase/migrations/20260420_0015_fix_claims_rls.sql
  - tests/actions/policies.test.ts
autonomous: true
gap_closure: true
requirements:
  - SEG-01
  - SEG-02
  - SEG-03
  - SEG-04
  - SEG-05
  - SEG-06
  - SEG-07
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05
  - CON-06

must_haves:
  truths:
    - "updatePolicyAction valida FormData com updatePolicySchema.safeParse antes de qualquer DB call — nunca faz spread de raw em .update()"
    - "updatePolicyAction não permite que campos não-whitelist (tenant_id, client_id, deleted_at, type, type_data) sejam sobrescritos via FormData"
    - "claims_update WITH CHECK espelha exatamente o USING clause: tenant_id + deleted_at IS NULL + role guard + EXISTS(policy assigned_to AND p.deleted_at IS NULL)"
    - "Cálculos de boundary date em seguros/page.tsx usam addDays(startOfToday(), N) de date-fns — nunca Date.now() + N*24*60*60*1000"
    - "Cálculos de boundary date em layout.tsx usam addDays(startOfToday(), N) de date-fns — same source-of-truth do getVigenciaStatus()"
    - "npm test continua verde após as correções (123/123 testes passando, 1 fail intencional pré-existente em rls-isolation)"
    - "Novo teste de updatePolicyAction confirma que campos não-whitelist do FormData são rejeitados/ignorados"
  artifacts:
    - path: "src/lib/actions/policies.ts"
      provides: "updatePolicyAction com validação Zod via updatePolicySchema.safeParse — usando apenas parsed.data no .update()"
      contains: "updatePolicySchema.safeParse"
    - path: "supabase/migrations/20260420_0015_fix_claims_rls.sql"
      provides: "DROP+CREATE da policy claims_update com WITH CHECK simétrico ao USING clause"
      contains: "DROP POLICY IF EXISTS \"claims_update\""
    - path: "src/app/(app)/[slug]/seguros/page.tsx"
      provides: "Date arithmetic via addDays(startOfToday(), N) — alinhado com getVigenciaStatus() (vigencia.ts)"
      contains: "addDays(startOfToday()"
    - path: "src/app/(app)/[slug]/layout.tsx"
      provides: "Date arithmetic via addDays(startOfToday(), N) — alinhado com getVigenciaStatus() (vigencia.ts)"
      contains: "addDays(startOfToday()"
    - path: "tests/actions/policies.test.ts"
      provides: "Testes adicionais para updatePolicyAction garantindo rejeição de FormData inválido e whitelist de campos"
      contains: "describe('updatePolicyAction"
  key_links:
    - from: "src/lib/actions/policies.ts updatePolicyAction"
      to: "src/lib/validations/policy-schemas.ts updatePolicySchema"
      via: "import + safeParse"
      pattern: "import.*updatePolicySchema.*from '@/lib/validations/policy-schemas'"
    - from: "supabase/migrations/20260420_0015_fix_claims_rls.sql"
      to: "public.claims claims_update policy"
      via: "DROP + CREATE POLICY"
      pattern: "CREATE POLICY \"claims_update\""
    - from: "seguros/page.tsx + layout.tsx"
      to: "src/lib/utils/vigencia.ts startOfToday usage"
      via: "import { addDays, startOfToday, format } from 'date-fns'"
      pattern: "addDays\\(startOfToday\\(\\)"
---

<objective>
Fechar os 3 gaps reportados em `.planning/phases/03-seguros-consorcio/03-VERIFICATION.md`:

1. **Gap 1 (Blocker — Security):** `updatePolicyAction` em `src/lib/actions/policies.ts` linha 114 espalha `raw` FormData não-validado direto no `.update()`. Adicionar `updatePolicySchema.safeParse({ id: policyId, ...raw })` antes do DB call e usar apenas `parsed.data` (sem `id`) no update.

2. **Gap 2 (Blocker — RLS):** A policy `claims_update` em `supabase/migrations/20260420_0012_seguros_rls.sql` tem WITH CHECK mais fraco que o USING clause. Criar nova migration `20260420_0015_fix_claims_rls.sql` que DROP+CREATE a policy com WITH CHECK simétrico ao USING.

3. **Gap 3 (Warning — Logic):** Date arithmetic em `seguros/page.tsx` (linhas 40-44) e `layout.tsx` (linhas 40-43) usa `Date.now() + N*24*60*60*1000` em vez de `startOfToday()` (usado em `getVigenciaStatus`). Substituir por `addDays(startOfToday(), N)` formatado como `yyyy-MM-dd` em ambos os arquivos.

Purpose: Eliminar vulnerabilidade de segurança (write de campos arbitrários), corrigir assimetria RLS (UPDATE pode aceitar policies em estado inconsistente), e remover drift de DST entre badge visual e filtros de listagem/contadores de alerta.

Output: Phase 03 com VERIFICATION zerado de gaps blocker/warning e pronto para `/gsd-verify-phase` re-run que deverá retornar `status: passed`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seguros-consorcio/03-CONTEXT.md
@.planning/phases/03-seguros-consorcio/03-VERIFICATION.md
@src/lib/actions/policies.ts
@src/lib/validations/policy-schemas.ts
@src/lib/utils/vigencia.ts
@src/app/(app)/[slug]/seguros/page.tsx
@src/app/(app)/[slug]/layout.tsx
@supabase/migrations/20260420_0012_seguros_rls.sql
@tests/actions/policies.test.ts

<interfaces>
<!-- Schemas e utilitários já existentes que esta plan usa -->

<!-- src/lib/validations/policy-schemas.ts (linhas 64-78) -->
```typescript
export const updatePolicySchema = z.object({
  id: z.string().uuid(),
  policy_number: z.string().min(1).optional(),
  insurer: z.string().min(2).optional(),
  vigencia_inicio: z.string().date().optional(),
  vigencia_fim: z.string().date().optional(),
  premio_total: z.coerce.number().min(0).optional(),
  client_id: z.string().uuid().optional(),
  assigned_to: z.string().uuid().optional(),
  observacoes: z.string().optional(),
  type_data: z.record(z.unknown()).optional(),
})
export type UpdatePolicyInput = z.infer<typeof updatePolicySchema>
```

<!-- src/lib/utils/vigencia.ts -->
```typescript
import { differenceInDays, parseISO, startOfToday } from 'date-fns'
export function getVigenciaStatus(vigencia_fim: string): 'verde' | 'amarelo' | 'vermelho' {
  const today = startOfToday()
  const end = parseISO(vigencia_fim)
  const daysLeft = differenceInDays(end, today)
  if (daysLeft > 60) return 'verde'
  if (daysLeft > 30) return 'amarelo'
  return 'vermelho'
}
```

<!-- date-fns já presente no package.json (versão 4.1.0) — addDays, startOfToday, format já exportados -->

<!-- supabase/migrations/20260420_0012_seguros_rls.sql linhas 80-99: claims_update USING clause é o gold standard -->
<!-- USING clause checa: tenant_id + deleted_at IS NULL + (admin OR (corretor AND EXISTS policy assigned_to AND p.deleted_at IS NULL)) -->
<!-- WITH CHECK atual checa apenas: tenant_id (assimétrico) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar validação Zod ao updatePolicyAction (Gap 1 — Blocker Security)</name>
  <read_first>
    - src/lib/actions/policies.ts (estado atual — confirmar linhas 85-122 do updatePolicyAction)
    - src/lib/validations/policy-schemas.ts (confirmar updatePolicySchema export linhas 66-78 — z.object com id obrigatório + campos opcionais)
    - tests/actions/policies.test.ts (padrão existente de mock — usar mockSupabase + makeFormData)
    - .planning/phases/03-seguros-consorcio/03-VERIFICATION.md §Gap 1 (concrete fix sample)
  </read_first>
  <files>
    src/lib/actions/policies.ts,
    tests/actions/policies.test.ts
  </files>
  <behavior>
    Antes de implementar, escrever testes que descrevem o comportamento esperado:

    - Test 1: updatePolicyAction com FormData válido (apenas insurer + premio_total) chama .update() apenas com esses campos + updated_at — sem outros campos
    - Test 2: updatePolicyAction com FormData contendo tenant_id malicioso ignora esse campo no .update() (whitelist via Zod)
    - Test 3: updatePolicyAction com FormData contendo deleted_at malicioso ignora esse campo no .update()
    - Test 4: updatePolicyAction com FormData contendo client_id em formato inválido (não-UUID) retorna { error: 'Dados inválidos.' } sem chamar .update()
    - Test 5: updatePolicyAction com FormData contendo premio_total negativo retorna { error: 'Dados inválidos.' } sem chamar .update()
    - Test 6: updatePolicyAction com FormData vazio (apenas updated_at gerado pela ação) chama .update() com apenas updated_at — comportamento idempotente
    - Test 7: updatePolicyAction continua respeitando guard de role (corretor não pode editar apólice de outro)
  </behavior>
  <action>
    **Passo 1 — Adicionar testes em `tests/actions/policies.test.ts`:**

    Após o bloco `describe('softDeletePolicyAction', ...)` existente (linha 225+), adicionar um novo bloco `describe('updatePolicyAction — validação Zod e whitelist de campos', ...)`. Use os mesmos helpers (`makeFormData`, `adminUser`, `corretorUser`, `CLIENT_UUID`) e o mesmo padrão de mock já estabelecido no arquivo.

    **CRITICAL — extender o mockPoliciesChain para suportar a query `.eq().is().single()` que updatePolicyAction faz para fetchar a policy antes do update.** O mock atual em linhas 6-22 só tem `insert` e `update`. Adicionar:
    ```typescript
    const mockSelectChain = {
      eq: vi.fn(() => mockSelectChain),
      is: vi.fn(() => mockSelectChain),
      single: vi.fn(),
    }
    // Em mockPoliciesChain adicionar: select: vi.fn(() => mockSelectChain),
    ```

    Para capturar o argumento do `.update()`, o mock atual já permite via `mockPoliciesChain.update.mock.calls`.

    Adicionar exatamente estes 7 testes (use o padrão `import { updatePolicyAction } from '@/lib/actions/policies'`):

    ```typescript
    // Add at top of file alongside other imports:
    import { updatePolicyAction } from '@/lib/actions/policies'

    describe('updatePolicyAction — validação Zod e whitelist de campos', () => {
      const POLICY_ID = '11111111-1111-1111-1111-111111111111'

      beforeEach(() => {
        vi.clearAllMocks()
        mockSupabase.auth.getUser.mockResolvedValue({ data: { user: adminUser() } })
        // Mock .select(...).eq(...).is(...).single() to return existing policy assigned_to admin
        mockSelectChain.single.mockResolvedValue({
          data: { assigned_to: ADMIN_UUID },
          error: null,
        })
        // Mock .update(...).eq(...) to resolve OK
        mockPoliciesChain.update.mockReturnValue({
          eq: vi.fn(() => Promise.resolve({ error: null })),
        })
      })

      it('updatePolicyAction com FormData válido chama .update() apenas com campos whitelist + updated_at', async () => {
        const fd = makeFormData({ insurer: 'Bradesco Seguros', premio_total: 2000 })
        const result = await updatePolicyAction('slug-test', POLICY_ID, fd)
        expect(result).toEqual({ success: true })
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const updateArg = (mockPoliciesChain.update.mock.calls as any)[0][0] as Record<string, unknown>
        expect(updateArg.insurer).toBe('Bradesco Seguros')
        expect(updateArg.premio_total).toBe(2000)
        expect(updateArg.updated_at).toEqual(expect.any(String))
        // id deve ser excluído do update
        expect(updateArg).not.toHaveProperty('id')
      })

      it('updatePolicyAction ignora tenant_id malicioso no FormData', async () => {
        const fd = makeFormData({ insurer: 'Bradesco', tenant_id: 'attacker-tenant-uuid' })
        await updatePolicyAction('slug-test', POLICY_ID, fd)
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const updateArg = (mockPoliciesChain.update.mock.calls as any)[0][0] as Record<string, unknown>
        expect(updateArg).not.toHaveProperty('tenant_id')
      })

      it('updatePolicyAction ignora deleted_at malicioso no FormData', async () => {
        const fd = makeFormData({ insurer: 'Bradesco', deleted_at: '2026-01-01' })
        await updatePolicyAction('slug-test', POLICY_ID, fd)
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const updateArg = (mockPoliciesChain.update.mock.calls as any)[0][0] as Record<string, unknown>
        expect(updateArg).not.toHaveProperty('deleted_at')
      })

      it('updatePolicyAction rejeita client_id em formato não-UUID — retorna error sem chamar .update()', async () => {
        const fd = makeFormData({ client_id: 'not-a-uuid' })
        const result = await updatePolicyAction('slug-test', POLICY_ID, fd)
        expect(result).toHaveProperty('error')
        expect(mockPoliciesChain.update).not.toHaveBeenCalled()
      })

      it('updatePolicyAction rejeita premio_total negativo — retorna error sem chamar .update()', async () => {
        const fd = makeFormData({ premio_total: -100 })
        const result = await updatePolicyAction('slug-test', POLICY_ID, fd)
        expect(result).toHaveProperty('error')
        expect(mockPoliciesChain.update).not.toHaveBeenCalled()
      })

      it('updatePolicyAction com FormData vazio chama .update() apenas com updated_at', async () => {
        const fd = new FormData()
        const result = await updatePolicyAction('slug-test', POLICY_ID, fd)
        expect(result).toEqual({ success: true })
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const updateArg = (mockPoliciesChain.update.mock.calls as any)[0][0] as Record<string, unknown>
        expect(Object.keys(updateArg)).toEqual(['updated_at'])
      })

      it('updatePolicyAction bloqueia corretor editando apólice de outro corretor', async () => {
        mockSupabase.auth.getUser.mockResolvedValue({ data: { user: corretorUser() } })
        // Policy assigned_to é diferente do CORRETOR_UUID
        mockSelectChain.single.mockResolvedValue({
          data: { assigned_to: ADMIN_UUID },
          error: null,
        })
        const fd = makeFormData({ insurer: 'Bradesco' })
        const result = await updatePolicyAction('slug-test', POLICY_ID, fd)
        expect(result).toEqual({ error: 'Sem permissão para editar esta apólice.' })
        expect(mockPoliciesChain.update).not.toHaveBeenCalled()
      })
    })
    ```

    Rodar `npm test -- tests/actions/policies.test.ts` — os 7 novos testes DEVEM falhar (comportamento atual não valida com Zod, então tenant_id/deleted_at/etc. passam direto). Confirmar que falham por motivo correto (campo presente no update arg).

    **Passo 2 — Modificar `src/lib/actions/policies.ts`:**

    Mudança 1: Adicionar import de `updatePolicySchema`. Substituir linha 4:
    ```typescript
    import { createPolicySchema, updatePolicySchema } from '@/lib/validations/policy-schemas'
    ```

    Mudança 2: Substituir o corpo da função `updatePolicyAction` (linhas 85-122). Manter a assinatura `(slug: string, policyId: string, formData: FormData)` e a estrutura geral (auth check, fetch da policy para ownership guard), mas adicionar Zod safeParse antes do `.update()`.

    Substituir EXATAMENTE o bloco a seguir:
    ```typescript
    export async function updatePolicyAction(slug: string, policyId: string, formData: FormData) {
      const raw = Object.fromEntries(formData) as Record<string, unknown>
      if (raw.premio_total !== undefined) raw.premio_total = Number(raw.premio_total)

      const supabase = (await createClient()) as AnySupabase
      const {
        data: { user },
      } = await supabase.auth.getUser()
      if (!user) return { error: 'Sessão expirada.' }

      const role = (user.app_metadata as { role?: string })?.role

      // Buscar apólice para verificar propriedade
      const { data: policy, error: fetchError } = await supabase
        .from('policies')
        .select('assigned_to')
        .eq('id', policyId)
        .is('deleted_at', null)
        .single()

      if (fetchError || !policy) return { error: 'Apólice não encontrada.' }

      // Corretor só pode editar apólice própria
      if (role === 'corretor' && (policy as { assigned_to: string }).assigned_to !== user.id) {
        return { error: 'Sem permissão para editar esta apólice.' }
      }

      const { error } = await supabase
        .from('policies')
        .update({ ...raw, updated_at: new Date().toISOString() })
        .eq('id', policyId)

      if (error) return { error: 'Erro ao atualizar apólice.' }

      revalidatePath(`/${slug}/seguros`)
      revalidatePath(`/${slug}/seguros/${policyId}`)
      return { success: true }
    }
    ```

    Pelo NOVO bloco abaixo:
    ```typescript
    export async function updatePolicyAction(slug: string, policyId: string, formData: FormData) {
      const raw = Object.fromEntries(formData) as Record<string, unknown>
      // Coerce numeric strings before Zod validate (FormData entries are always strings)
      if (raw.premio_total !== undefined) raw.premio_total = Number(raw.premio_total)

      // CR-01 fix: Zod whitelist — only validated fields can reach .update()
      // updatePolicySchema is z.object with optional fields + required id
      const parsed = updatePolicySchema.safeParse({ id: policyId, ...raw })
      if (!parsed.success) {
        return { error: 'Dados inválidos.' }
      }
      // Strip id from update payload — id is the WHERE filter, not a column to set
      const { id: _id, ...updateData } = parsed.data

      const supabase = (await createClient()) as AnySupabase
      const {
        data: { user },
      } = await supabase.auth.getUser()
      if (!user) return { error: 'Sessão expirada.' }

      const role = (user.app_metadata as { role?: string })?.role

      // Buscar apólice para verificar propriedade
      const { data: policy, error: fetchError } = await supabase
        .from('policies')
        .select('assigned_to')
        .eq('id', policyId)
        .is('deleted_at', null)
        .single()

      if (fetchError || !policy) return { error: 'Apólice não encontrada.' }

      // Corretor só pode editar apólice própria
      if (role === 'corretor' && (policy as { assigned_to: string }).assigned_to !== user.id) {
        return { error: 'Sem permissão para editar esta apólice.' }
      }

      const { error } = await supabase
        .from('policies')
        .update({ ...updateData, updated_at: new Date().toISOString() })
        .eq('id', policyId)

      if (error) return { error: 'Erro ao atualizar apólice.' }

      revalidatePath(`/${slug}/seguros`)
      revalidatePath(`/${slug}/seguros/${policyId}`)
      return { success: true }
    }
    ```

    Rodar `npm test -- tests/actions/policies.test.ts` — todos os 7 novos testes DEVEM passar agora. Os testes existentes do `createPolicyAction` e `softDeletePolicyAction` devem continuar verdes.
  </action>
  <verify>
    <automated>npm test -- tests/actions/policies.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "updatePolicySchema" src/lib/actions/policies.ts` retorna exatamente 2 linhas: 1 import + 1 chamada `updatePolicySchema.safeParse`
    - `grep -n "{ ...raw, updated_at" src/lib/actions/policies.ts` retorna 0 matches (o spread inseguro foi removido)
    - `grep -n "{ ...updateData, updated_at" src/lib/actions/policies.ts` retorna exatamente 1 match
    - `grep -n "describe('updatePolicyAction" tests/actions/policies.test.ts` retorna pelo menos 1 match
    - `npm test -- tests/actions/policies.test.ts` finaliza com 0 testes failing (todos os 7+7=14 testes passando)
    - `npm run typecheck` retorna 0 novos erros TypeScript em src/lib/actions/policies.ts ou tests/actions/policies.test.ts
  </acceptance_criteria>
  <done>
    - updatePolicyAction valida FormData via updatePolicySchema.safeParse antes do .update()
    - Apenas campos whitelist (parsed.data — sem id) atingem o .update()
    - 7 novos testes em tests/actions/policies.test.ts cobrem: tenant_id ignorado, deleted_at ignorado, client_id inválido rejeitado, premio_total negativo rejeitado, FormData vazio idempotente, role guard preservado
    - npm test verde (124+ testes passando, 1 fail intencional pré-existente em rls-isolation)
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir Date.now() por addDays(startOfToday()) em seguros/page.tsx e layout.tsx (Gap 3 — Warning Logic)</name>
  <read_first>
    - src/app/(app)/[slug]/seguros/page.tsx (estado atual — linhas 39-45 são o alvo)
    - src/app/(app)/[slug]/layout.tsx (estado atual — linhas 39-45 são o alvo)
    - src/lib/utils/vigencia.ts (referência — startOfToday + parseISO + differenceInDays já em uso)
    - .planning/phases/03-seguros-consorcio/03-VERIFICATION.md §Gap 3 (concrete fix sample para ambos arquivos)
    - package.json (confirmar date-fns ^4.1.0 já é dependência)
  </read_first>
  <files>
    src/app/(app)/[slug]/seguros/page.tsx,
    src/app/(app)/[slug]/layout.tsx
  </files>
  <action>
    **Mudança em `src/app/(app)/[slug]/seguros/page.tsx`:**

    Adicionar import no topo do arquivo (após o `import { PolicyTable, type PolicyRow }` na linha 5):
    ```typescript
    import { addDays, startOfToday, format } from 'date-fns'
    ```

    Substituir EXATAMENTE as linhas 39-45 (o bloco com comentário "Dates for vigencia status filtering"):
    ```typescript
      // Dates for vigencia status filtering
      const thirtyDaysLater = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000)
        .toISOString()
        .split('T')[0]
      const sixtyDaysLater = new Date(Date.now() + 60 * 24 * 60 * 60 * 1000)
        .toISOString()
        .split('T')[0]
    ```

    Pelo bloco abaixo (CR-03 fix — usa o mesmo `startOfToday()` que `getVigenciaStatus`):
    ```typescript
      // Dates for vigencia status filtering — use startOfToday() to match getVigenciaStatus() (vigencia.ts)
      // CR-03 fix: addDays + startOfToday avoids DST drift versus Date.now() arithmetic
      const today = startOfToday()
      const thirtyDaysLater = format(addDays(today, 30), 'yyyy-MM-dd')
      const sixtyDaysLater = format(addDays(today, 60), 'yyyy-MM-dd')
    ```

    Não modificar o restante do arquivo. As variáveis `thirtyDaysLater` e `sixtyDaysLater` mantêm o mesmo nome e tipo (string `yyyy-MM-dd`), portanto as queries existentes em linhas 67-74 (`.lte('vigencia_fim', thirtyDaysLater)`, `.gt(...).lte(...)`, `.gt(...)`) continuam funcionando inalteradas.

    **Mudança em `src/app/(app)/[slug]/layout.tsx`:**

    Adicionar import no topo do arquivo (após o `import { AlertToastTrigger }` na linha 6):
    ```typescript
    import { addDays, startOfToday, format } from 'date-fns'
    ```

    Substituir EXATAMENTE as linhas 39-45 (o bloco com comentário "Alertas in-app (D-06)..."):
    ```typescript
      // Alertas in-app (D-06): apólices com vigencia_fim ≤ 30 dias (SEG-03)
      // Fallback gracioso: se tabelas não existirem (antes do db push), usa count=0 (T-03-19)
      const today = new Date().toISOString().split('T')[0]
      const thirtyDaysLater = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000)
        .toISOString()
        .split('T')[0]
      const threeDaysLater = new Date(Date.now() + 3 * 24 * 60 * 60 * 1000)
        .toISOString()
        .split('T')[0]
    ```

    Pelo bloco abaixo (CR-03 fix — alinha com getVigenciaStatus()):
    ```typescript
      // Alertas in-app (D-06): apólices com vigencia_fim ≤ 30 dias (SEG-03)
      // Fallback gracioso: se tabelas não existirem (antes do db push), usa count=0 (T-03-19)
      // CR-03 fix: addDays + startOfToday matches getVigenciaStatus() — avoids DST drift on boundary days
      const todayDate = startOfToday()
      const today = format(todayDate, 'yyyy-MM-dd')
      const thirtyDaysLater = format(addDays(todayDate, 30), 'yyyy-MM-dd')
      const threeDaysLater = format(addDays(todayDate, 3), 'yyyy-MM-dd')
    ```

    Não modificar o restante do arquivo. As variáveis `today`, `thirtyDaysLater`, `threeDaysLater` mantêm o mesmo nome e tipo (string `yyyy-MM-dd`), portanto as queries existentes em linhas 53-72 continuam funcionando inalteradas.

    **Note (anti-clobber):** Não renomear `today` no layout.tsx para `todayString` ou similar — a variável `today` é usada em `.gte('next_assembly_date', today)` (linha 70 do arquivo original) e renomear ela quebraria essa query. O `todayDate` (Date object) só existe localmente para feed `addDays` e `format`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep "error TS" | grep -E "(seguros/page|layout)\.tsx" || echo "TypeScript OK for changed files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "addDays(startOfToday()" src/app/\(app\)/\[slug\]/seguros/page.tsx` retorna pelo menos 1 match (anteriormente 0)
    - `grep -n "Date.now() + .* \* 24 \* 60 \* 60 \* 1000" src/app/\(app\)/\[slug\]/seguros/page.tsx` retorna 0 matches (a aritmética legacy foi removida)
    - `grep -n "addDays(todayDate" src/app/\(app\)/\[slug\]/layout.tsx` retorna pelo menos 2 matches (thirtyDaysLater + threeDaysLater)
    - `grep -n "Date.now() + .* \* 24 \* 60 \* 60 \* 1000" src/app/\(app\)/\[slug\]/layout.tsx` retorna 0 matches
    - `grep -n "from 'date-fns'" src/app/\(app\)/\[slug\]/seguros/page.tsx` retorna 1 match contendo addDays/startOfToday/format
    - `grep -n "from 'date-fns'" src/app/\(app\)/\[slug\]/layout.tsx` retorna 1 match contendo addDays/startOfToday/format
    - `npm run typecheck` retorna 0 novos erros TypeScript em seguros/page.tsx ou layout.tsx
    - `grep -n "\\.gte('next_assembly_date', today)" src/app/\(app\)/\[slug\]/layout.tsx` continua retornando 1 match (a query não foi quebrada)
  </acceptance_criteria>
  <done>
    - seguros/page.tsx: thirtyDaysLater + sixtyDaysLater calculados via addDays(startOfToday(), N) formatado como yyyy-MM-dd
    - layout.tsx: today + thirtyDaysLater + threeDaysLater calculados via startOfToday + addDays + format
    - Ambos os arquivos importam { addDays, startOfToday, format } from 'date-fns'
    - Boundary cases de DST agora alinhados com getVigenciaStatus() — badge e filtro de status concordam
    - npm run typecheck verde
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar migration 0015 para corrigir claims_update WITH CHECK (Gap 2 — Blocker RLS)</name>
  <read_first>
    - supabase/migrations/20260420_0012_seguros_rls.sql (linhas 80-99 — claims_update USING + WITH CHECK atual)
    - supabase/migrations/20260420_0002_rls_helpers.sql (confirmar que public.jwt_tenant_id() e public.jwt_tenant_role() existem como funções)
    - supabase/migrations/ — listar diretório para confirmar que 0015 é o próximo número sequencial livre
    - .planning/phases/03-seguros-consorcio/03-VERIFICATION.md §Gap 2 (concrete fix SQL completo)
    - .planning/STATE.md (decisão "supabase db push contornado via supabase db query --linked -f por versioning collision" — ciente que CLI usa apenas data como version key)
  </read_first>
  <files>
    supabase/migrations/20260420_0015_fix_claims_rls.sql
  </files>
  <action>
    Criar a nova migration `supabase/migrations/20260420_0015_fix_claims_rls.sql` com o conteúdo EXATO abaixo. A migration faz `DROP POLICY IF EXISTS` + `CREATE POLICY` para `claims_update` com WITH CHECK simétrico ao USING clause (linhas 82-96 da migration 0012).

    **Conteúdo do arquivo:**
    ```sql
    -- Phase 03 Gap Closure (Plan 05) — CR-02 fix
    -- Corrige assimetria de segurança em claims_update: WITH CHECK era mais fraco que USING
    -- USING checava: tenant_id + deleted_at IS NULL + (admin OR (corretor AND EXISTS policy assigned_to AND p.deleted_at IS NULL))
    -- WITH CHECK antigo só checava: tenant_id (permitia UPDATEs em claims soft-deleted ou em policies não-próprias)
    -- Reference: .planning/phases/03-seguros-consorcio/03-VERIFICATION.md Gap 2

    DROP POLICY IF EXISTS "claims_update" ON public.claims;

    CREATE POLICY "claims_update" ON public.claims
      FOR UPDATE TO authenticated
      USING (
        tenant_id = (SELECT public.jwt_tenant_id())
        AND deleted_at IS NULL
        AND (
          (SELECT public.jwt_tenant_role()) = 'admin'
          OR (
            (SELECT public.jwt_tenant_role()) = 'corretor'
            AND EXISTS (
              SELECT 1 FROM public.policies p
              WHERE p.id = claims.policy_id
                AND p.assigned_to = (SELECT auth.uid())
                AND p.deleted_at IS NULL
            )
          )
        )
      )
      WITH CHECK (
        tenant_id = (SELECT public.jwt_tenant_id())
        AND deleted_at IS NULL
        AND (
          (SELECT public.jwt_tenant_role()) = 'admin'
          OR (
            (SELECT public.jwt_tenant_role()) = 'corretor'
            AND EXISTS (
              SELECT 1 FROM public.policies p
              WHERE p.id = claims.policy_id
                AND p.assigned_to = (SELECT auth.uid())
                AND p.deleted_at IS NULL
            )
          )
        )
      );
    ```

    Diferenças críticas entre USING e WITH CHECK no novo policy:
    - USING e WITH CHECK agora são **idênticos** — ambos checam tenant_id, deleted_at IS NULL, role guard (admin OR corretor com EXISTS policy assigned_to AND p.deleted_at IS NULL)
    - O `EXISTS` agora é parte do WITH CHECK também — bloqueando UPDATE para claim cujo policy assigned_to mudou para outro user mid-transaction
    - O `deleted_at IS NULL` no WITH CHECK previne que UPDATE consiga "ressuscitar" um claim que foi soft-deleted simultaneamente

    **Aplicar a migration:**
    Devido ao "versioning collision" decision em STATE.md (CLI usa apenas data como version key), a maneira segura de aplicar é via `supabase db query --linked -f`:
    ```
    supabase db query --linked -f supabase/migrations/20260420_0015_fix_claims_rls.sql
    ```

    **NOTA importante:** Não tentar `supabase db push` se houver versioning conflict — usar `supabase db query --linked -f` segue o padrão estabelecido na Phase 03 (ver STATE.md Decisions). Se o ambiente local não tiver acesso ao Supabase linked, deixar a migration commitada para aplicação manual posterior.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260420_0015_fix_claims_rls.sql && grep -c "EXISTS" supabase/migrations/20260420_0015_fix_claims_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260420_0015_fix_claims_rls.sql` existe
    - `grep -c "EXISTS" supabase/migrations/20260420_0015_fix_claims_rls.sql` retorna `2` (um EXISTS no USING, um EXISTS no WITH CHECK — confirmando simetria)
    - `grep -c "deleted_at IS NULL" supabase/migrations/20260420_0015_fix_claims_rls.sql` retorna pelo menos `4` (claims.deleted_at em USING e WITH CHECK + p.deleted_at em USING e WITH CHECK)
    - `grep -c "p.assigned_to = (SELECT auth.uid())" supabase/migrations/20260420_0015_fix_claims_rls.sql` retorna `2` (USING e WITH CHECK)
    - `grep -n "DROP POLICY IF EXISTS \"claims_update\"" supabase/migrations/20260420_0015_fix_claims_rls.sql` retorna 1 match
    - `grep -n "CREATE POLICY \"claims_update\"" supabase/migrations/20260420_0015_fix_claims_rls.sql` retorna 1 match
    - O conteúdo do USING e WITH CHECK é idêntico (verificável visualmente — ambos têm os mesmos 4 sub-checks)
  </acceptance_criteria>
  <done>
    - Migration 0015 criada com DROP+CREATE de claims_update
    - WITH CHECK do novo policy espelha exatamente o USING clause
    - Asymmetry de segurança eliminada — UPDATEs em claims agora são bloqueados se claim foi soft-deleted ou policy não pertence ao corretor
    - Migration commitada (aplicação no banco fica para deploy / `supabase db query --linked -f` quando ambiente disponível)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action (updatePolicyAction) | FormData crossed boundary; Zod whitelist agora bloqueia campos não esperados |
| Authenticated user → claims table (UPDATE) | RLS WITH CHECK agora simétrico ao USING — sem assimetria de privilégio |
| Server Component (layout.tsx, seguros/page.tsx) → DB date arithmetic | Cálculos de boundary date agora alinhados com `startOfToday()` da utility — sem drift de DST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | updatePolicyAction — FormData spread sem validação | mitigate | Task 1: Zod safeParse via updatePolicySchema antes de .update(). Apenas campos whitelist atingem o DB. Testes de regressão (tenant_id, deleted_at, client_id inválido) garantem que ataque é bloqueado |
| T-03-22 | Elevation of Privilege | claims_update WITH CHECK fraco — UPDATE em claim de policy não-própria | mitigate | Task 3: nova migration replica EXISTS(policy assigned_to + p.deleted_at IS NULL) no WITH CHECK. UPDATE bloqueado se claim ou policy associado for soft-deleted, ou se policy mudou de owner |
| T-03-23 | Tampering | claims_update WITH CHECK não verifica deleted_at — UPDATE pode "ressuscitar" claim soft-deleted | mitigate | Task 3: WITH CHECK agora inclui `deleted_at IS NULL` — UPDATE bloqueado se claim já foi soft-deleted |
| T-03-24 | Information Disclosure | DST drift entre badge visual e filtro de status | mitigate | Task 2: addDays(startOfToday(), N) em ambos arquivos — mesma source-of-truth do getVigenciaStatus(). Badge e filtro concordam em 100% dos boundary cases |
| T-03-25 | Denial of Service | Migration 0015 com versioning collision pode ser ignorada pelo `supabase db push` | accept | STATE.md já documenta workaround via `supabase db query --linked -f`. Operacional, não bloqueia entrega |
</threat_model>

<verification>
```bash
# 1. Gap 1 — updatePolicyAction validado com Zod
grep -n "updatePolicySchema.safeParse" src/lib/actions/policies.ts
# Esperado: 1 match na função updatePolicyAction

grep -n "{ ...raw, updated_at" src/lib/actions/policies.ts
# Esperado: 0 matches (spread inseguro removido)

grep -c "describe('updatePolicyAction" tests/actions/policies.test.ts
# Esperado: pelo menos 1

npm test -- tests/actions/policies.test.ts
# Esperado: 0 testes failing — todos os 7 novos + 7 existentes passando

# 2. Gap 3 — Date arithmetic substituída em ambos arquivos
grep -n "addDays(startOfToday()" "src/app/(app)/[slug]/seguros/page.tsx"
# Esperado: pelo menos 1 match

grep -n "Date.now() + " "src/app/(app)/[slug]/seguros/page.tsx"
# Esperado: 0 matches

grep -n "addDays(todayDate" "src/app/(app)/[slug]/layout.tsx"
# Esperado: pelo menos 2 matches (thirtyDaysLater + threeDaysLater)

grep -n "Date.now() + " "src/app/(app)/[slug]/layout.tsx"
# Esperado: 0 matches

grep -n "from 'date-fns'" "src/app/(app)/[slug]/seguros/page.tsx" "src/app/(app)/[slug]/layout.tsx"
# Esperado: 1 match em cada arquivo

# 3. Gap 2 — Migration 0015 corrige claims_update
test -f supabase/migrations/20260420_0015_fix_claims_rls.sql && echo "OK"
# Esperado: OK

grep -c "EXISTS" supabase/migrations/20260420_0015_fix_claims_rls.sql
# Esperado: 2 (USING + WITH CHECK simétricos)

grep -c "p.assigned_to = (SELECT auth.uid())" supabase/migrations/20260420_0015_fix_claims_rls.sql
# Esperado: 2

grep -c "deleted_at IS NULL" supabase/migrations/20260420_0015_fix_claims_rls.sql
# Esperado: pelo menos 4 (claims + p.policies, em USING + WITH CHECK)

# 4. Suíte completa final
npm test
# Esperado: 130+/131+ passed (8+ novos + 123 existentes; 1 fail intencional pré-existente em rls-isolation)

# 5. TypeScript limpo
npm run typecheck 2>&1 | grep "error TS" || echo "OK"
# Esperado: OK
```
</verification>

<success_criteria>
- **Gap 1 fechado:** updatePolicyAction usa updatePolicySchema.safeParse — 7 novos testes verde, 0 spread inseguro
- **Gap 2 fechado:** migration 0015 criada com claims_update WITH CHECK simétrico ao USING (ambos com tenant_id + deleted_at IS NULL + role guard + EXISTS policy)
- **Gap 3 fechado:** seguros/page.tsx e layout.tsx usam addDays(startOfToday(), N) — alinhado com getVigenciaStatus()
- npm test: suite completa verde (124+/125+ testes; 1 fail pré-existente em rls-isolation aceito)
- npm run typecheck: 0 novos erros
- Pronto para `/gsd-verify-phase 3` re-run que deverá retornar `status: passed` com 16/16 must-haves verified (assumindo que os 5 human-verify spot-checks também passam)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-seguros-consorcio/03-05-SUMMARY.md` seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md. O summary deve documentar:
- Quais 3 gaps foram fechados (referenciar CR-01, CR-02, CR-03 do VERIFICATION)
- Que a migration 0015 não foi aplicada no banco (apenas commitada) se o ambiente local não tinha link Supabase — anotar para deploy
- Resultado final do `npm test` (contagem total)
- Pronto para re-verificação via `/gsd-verify-phase 3`
</output>
