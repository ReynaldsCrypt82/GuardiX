---
phase: 03-seguros-consorcio
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/app/(app)/[slug]/clientes/[id]/page.tsx
  - src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx
  - src/app/(app)/[slug]/clientes/[id]/consortium-tab.tsx
  - src/app/(app)/[slug]/layout.tsx
autonomous: true
requirements:
  - SEG-03
  - SEG-07
  - CON-02
  - CON-05

must_haves:
  truths:
    - "Tela do cliente /[slug]/clientes/[id] exibe 4 abas: Dados | Timeline | Tarefas | Apólices"
    - "Aba 'Apólices' exibe apólices do cliente com VigenciaBadge — sem mais o placeholder 'Em breve'"
    - "Aba 'Consórcio' exibe cotas do cliente (status, grupo, parcela, data contemplação)"
    - "Sidebar badge contador mostra N apólices vencendo/vencidas (vigencia_fim ≤ 30 dias) ao lado de 'Seguros'"
    - "Sidebar badge contador mostra N assembleias dentro de 3 dias ao lado de 'Consórcio'"
    - "Toast Sonner é exibido ao abrir o sistema quando há alertas pendentes (apólices ou assembleias)"
    - "Query de alerta de assembleia filtra next_assembly_date IS NOT NULL antes da comparação com data (Pitfall 5)"
  artifacts:
    - path: "src/app/(app)/[slug]/clientes/[id]/page.tsx"
      provides: "Tela de detalhes do cliente com 4 abas (Dados/Timeline/Tarefas/Apólices) e aba Consórcio"
    - path: "src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx"
      provides: "Client Component — lista apólices do cliente com VigenciaBadge e link para detalhes"
    - path: "src/app/(app)/[slug]/clientes/[id]/consortium-tab.tsx"
      provides: "Client Component — lista cotas do cliente com status e link para grupo"
    - path: "src/app/(app)/[slug]/layout.tsx"
      provides: "Queries de alerta (policies + assembleias) passadas para SidebarShell como props"
  key_links:
    - from: "layout.tsx"
      to: "supabase.from('policies').select('id', { count: 'exact', head: true }).lte('vigencia_fim', thirtyDaysLater).is('deleted_at', null)"
      via: "Server Component count query"
      pattern: "policiesAlert.*count|vigencia_fim.*lte"
    - from: "layout.tsx"
      to: "supabase.from('consortium_groups').select('id', { count: 'exact', head: true }).not('next_assembly_date', 'is', null)"
      via: "Server Component count query com NOT NULL guard (Pitfall 5)"
      pattern: "next_assembly_date.*not.*null|assemblyAlert.*count"
    - from: "SidebarShell props"
      to: "badge counter + Toast Sonner"
      via: "alertCounts prop passado do layout para sidebar"
      pattern: "alertCounts|policiesAlert|assemblyAlert"
---

<objective>
Implementar a tela de detalhes do cliente (a rota /[slug]/clientes/[id] que não foi criada na Phase 2) com as abas Dados, Timeline, Tarefas, Apólices e Consórcio, e adicionar os alertas in-app no layout (badge contadores na sidebar e toast Sonner).

Purpose: Fecha os D-02 e D-06. Integra os dois módulos novos (Seguros e Consórcio) na tela central do cliente. Entrega os alertas visuais que permitem ao corretor reagir antes das vigências expirarem e das assembleias acontecerem.
Output: Tela de detalhes do cliente completa (5 abas), badge contadores na sidebar, toast de alerta ao abrir o sistema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seguros-consorcio/03-CONTEXT.md
@.planning/phases/03-seguros-consorcio/03-RESEARCH.md
@.planning/phases/02-crm-clientes/02-CONTEXT.md
@.planning/phases/03-seguros-consorcio/03-02-SUMMARY.md
@.planning/phases/03-seguros-consorcio/03-03-SUMMARY.md
@src/app/(app)/[slug]/layout.tsx
@src/components/auth/sidebar-shell.tsx

<interfaces>
<!-- Componentes criados nos Plans 03-02 e 03-03 — usar estes imports -->

<!-- src/components/seguros/vigencia-badge.tsx -->
```typescript
export function VigenciaBadge({ vigencia_fim }: { vigencia_fim: string }): JSX.Element
// Exibe badge verde/amarelo/vermelho baseado em getVigenciaStatus(vigencia_fim)
```

<!-- src/lib/utils/vigencia.ts -->
```typescript
export function getVigenciaStatus(vigencia_fim: string): 'verde' | 'amarelo' | 'vermelho'
```

<!-- Tabelas disponíveis no banco (Plans 03-01 a 03-03 completados) -->
<!-- policies: id, policy_number, type, insurer, vigencia_fim, premio_total, client_id, assigned_to, type_data -->
<!-- claims: id, policy_id, claim_date, type, status, description -->
<!-- endorsements: id, policy_id, endorsement_date, type, description, premium_impact -->
<!-- consortium_quotas: id, group_id, client_id, quota_number, monthly_payment, status, contemplation_date, post_contemplation_stage -->
<!-- consortium_groups: id, administrator, type, credit_value, term_months, next_assembly_date -->

<!-- SidebarShell interface atual (src/components/auth/sidebar-shell.tsx — criado/modificado Plan 03-02) -->
```typescript
interface SidebarShellProps {
  slug: string
  // Novo: alertCounts para badges
}
```
<!-- NOTA: O Plan 03-02 pode não ter adicionado alertCounts. Este plan deve estender a interface. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tela de detalhes do cliente — /[slug]/clientes/[id] com 5 abas</name>
  <read_first>
    - src/app/(app)/[slug]/clientes/page.tsx (padrão Server Component com supabase query — ver como passa params)
    - src/app/(app)/[slug]/clientes/clients-table.tsx (ver tipo ClientRow para usar nome/tipo/documento)
    - src/components/ui/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent — reutilizar exatamente este padrão)
    - src/components/ui/badge.tsx
    - src/components/ui/card.tsx
    - src/components/seguros/vigencia-badge.tsx (criado Plan 03-02 — usar no policy-tab)
    - .planning/phases/02-crm-clientes/02-CONTEXT.md (D-10 — layout abas: Dados|Timeline|Tarefas|Apólices)
    - .planning/phases/03-seguros-consorcio/03-RESEARCH.md (Exemplo 3 — PolicyTab com query de apólices por client_id)
  </read_first>
  <files>
    src/app/(app)/[slug]/clientes/[id]/page.tsx,
    src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx,
    src/app/(app)/[slug]/clientes/[id]/consortium-tab.tsx
  </files>
  <action>
Criar a rota de detalhes do cliente que foi deixada como placeholder na Phase 2.

**src/app/(app)/[slug]/clientes/[id]/page.tsx** — Server Component:
```typescript
import { notFound } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { PolicyTab } from './policy-tab'
import { ConsortiumTab } from './consortium-tab'

export default async function ClientDetailPage({
  params,
}: {
  params: Promise<{ slug: string; id: string }>
}) {
  const { slug, id: clientId } = await params
  const supabase = await createClient()

  // Busca dados completos do cliente
  const { data: client } = await supabase
    .from('clients')
    .select('*, stage:pipeline_stages(name, color), profile:profiles!assigned_to(full_name)')
    .eq('id', clientId)
    .is('deleted_at', null)
    .single()

  if (!client) notFound()

  // Busca apólices do cliente (para PolicyTab)
  const { data: policies } = await supabase
    .from('policies')
    .select('id, policy_number, type, insurer, vigencia_fim, premio_total')
    .eq('client_id', clientId)
    .is('deleted_at', null)
    .order('vigencia_fim', { ascending: true })

  // Busca cotas do cliente (para ConsortiumTab)
  const { data: quotas } = await supabase
    .from('consortium_quotas')
    .select('id, quota_number, monthly_payment, status, contemplation_date, post_contemplation_stage, group:consortium_groups(id, administrator, type, credit_value)')
    .eq('client_id', clientId)
    .is('deleted_at', null)
    .order('created_at', { ascending: false })

  // Busca interações (para Timeline tab) — já existia antes, reutilizar padrão
  const { data: interactions } = await supabase
    .from('client_interactions')
    .select('*')
    .eq('client_id', clientId)
    .is('deleted_at', null)
    .order('occurred_at', { ascending: false })

  // Busca tarefas (para Tasks tab) — já existia antes
  const { data: tasks } = await supabase
    .from('client_tasks')
    .select('*')
    .eq('client_id', clientId)
    .is('deleted_at', null)
    .order('due_date', { ascending: true })

  return (
    <div className="flex flex-col gap-6 p-6">
      {/* Header card com dados cadastrais */}
      <Card>
        <CardHeader>
          <div className="flex items-start justify-between">
            <div>
              <CardTitle className="text-xl">{client.name}</CardTitle>
              <p className="text-sm text-muted-foreground mt-1">
                {/* documento formatado: CPF com máscara para PF, CNPJ para PJ */}
                {client.type === 'pf'
                  ? client.document.replace(/(\d{3})(\d{3})(\d{3})(\d{2})/, '$1.$2.$3-$4')
                  : client.document.replace(/(\d{2})(\d{3})(\d{3})(\d{4})(\d{2})/, '$1.$2.$3/$4-$5')}
              </p>
            </div>
            <div className="flex gap-2">
              <Badge variant="outline">{client.type === 'pf' ? 'Pessoa Física' : 'Pessoa Jurídica'}</Badge>
              {client.stage && (
                <Badge
                  style={{ backgroundColor: client.stage.color + '20', color: client.stage.color, borderColor: client.stage.color + '50' }}
                  variant="outline"
                >
                  {client.stage.name}
                </Badge>
              )}
            </div>
          </div>
          <div className="text-sm text-muted-foreground">
            Corretor: {(client as { profile?: { full_name?: string } }).profile?.full_name ?? '—'}
            {client.email && <span className="ml-4">{client.email}</span>}
            {client.phone && <span className="ml-4">{client.phone}</span>}
          </div>
        </CardHeader>
      </Card>

      {/* Abas: Dados | Timeline | Tarefas | Apólices | Consórcio */}
      <Tabs defaultValue="dados">
        <TabsList>
          <TabsTrigger value="dados">Dados</TabsTrigger>
          <TabsTrigger value="timeline">
            Timeline {interactions && interactions.length > 0 && `(${interactions.length})`}
          </TabsTrigger>
          <TabsTrigger value="tarefas">
            Tarefas {tasks && tasks.length > 0 && `(${tasks.length})`}
          </TabsTrigger>
          <TabsTrigger value="apolices">
            Apólices {policies && policies.length > 0 && `(${policies.length})`}
          </TabsTrigger>
          <TabsTrigger value="consorcio">
            Consórcio {quotas && quotas.length > 0 && `(${quotas.length})`}
          </TabsTrigger>
        </TabsList>

        <TabsContent value="dados">
          {/* Dados completos do cliente: endereço, responsável PJ, observações */}
          <Card>
            <CardContent className="pt-4">
              {client.type === 'pj' && client.responsible && (
                <p className="text-sm"><span className="font-medium">Responsável:</span> {client.responsible}</p>
              )}
              {client.address && (
                <p className="text-sm mt-2">
                  <span className="font-medium">Endereço:</span>{' '}
                  {typeof client.address === 'object' && (client.address as Record<string, string>).logradouro
                    ? `${(client.address as Record<string, string>).logradouro}, ${(client.address as Record<string, string>).cidade} — ${(client.address as Record<string, string>).uf}`
                    : JSON.stringify(client.address)}
                </p>
              )}
              {!client.responsible && !client.address && (
                <p className="text-sm text-muted-foreground">Nenhum dado adicional cadastrado.</p>
              )}
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="timeline">
          {/* Listagem de interações existentes da Phase 2 — replicar o componente InteractionFeed se existir, ou exibir lista simples */}
          <Card>
            <CardContent className="pt-4">
              {(!interactions || interactions.length === 0) ? (
                <p className="text-sm text-muted-foreground">Nenhuma interação registrada.</p>
              ) : (
                <ul className="space-y-3">
                  {interactions.map((i) => (
                    <li key={i.id} className="text-sm border-l-2 pl-3 py-1">
                      <span className="font-medium">{i.type}</span>{' '}
                      <span className="text-muted-foreground">
                        {new Date(i.occurred_at).toLocaleDateString('pt-BR')}
                      </span>
                      <p className="mt-0.5 text-muted-foreground">{i.description}</p>
                    </li>
                  ))}
                </ul>
              )}
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="tarefas">
          <Card>
            <CardContent className="pt-4">
              {(!tasks || tasks.length === 0) ? (
                <p className="text-sm text-muted-foreground">Nenhuma tarefa cadastrada.</p>
              ) : (
                <ul className="space-y-3">
                  {tasks.map((t) => (
                    <li key={t.id} className="text-sm border-l-2 pl-3 py-1">
                      <span className={t.completed_at ? 'line-through text-muted-foreground' : 'font-medium'}>
                        {t.description}
                      </span>
                      <span className="text-muted-foreground ml-2">
                        Prazo: {new Date(t.due_date).toLocaleDateString('pt-BR')}
                      </span>
                    </li>
                  ))}
                </ul>
              )}
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="apolices">
          <PolicyTab policies={policies ?? []} slug={slug} />
        </TabsContent>

        <TabsContent value="consorcio">
          <ConsortiumTab quotas={quotas ?? []} slug={slug} />
        </TabsContent>
      </Tabs>
    </div>
  )
}
```

IMPORTANTE: Se `client_interactions` ou `client_tasks` não existirem como tabelas (verificar no schema Phase 2 — migrations 0008 e 0009), adaptar as queries. Ler as migrations existentes antes de escrever as queries de Timeline e Tarefas.

**src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx** — Client Component:
```typescript
'use client'
import Link from 'next/link'
import { VigenciaBadge } from '@/components/seguros/vigencia-badge'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Car, Heart, Home, Building2, HeartPulse, Shield } from 'lucide-react'
import { Intl } from 'next/dist/server/app-render/entry-base' // usar Intl nativo

const typeIcons: Record<string, React.ReactNode> = {
  auto: <Car size={14} />,
  vida: <Heart size={14} />,
  residencial: <Home size={14} />,
  empresarial: <Building2 size={14} />,
  saude: <HeartPulse size={14} />,
  outros: <Shield size={14} />,
}

const typeLabels: Record<string, string> = {
  auto: 'Auto', vida: 'Vida', residencial: 'Residencial',
  empresarial: 'Empresarial', saude: 'Saúde', outros: 'Outros',
}

interface PolicyRow {
  id: string
  policy_number: string
  type: string
  insurer: string
  vigencia_fim: string
  premio_total: number
}

export function PolicyTab({ policies, slug }: { policies: PolicyRow[]; slug: string }) {
  if (policies.length === 0) {
    return (
      <div className="text-center py-8 text-sm text-muted-foreground">
        Nenhuma apólice vinculada a este cliente.{' '}
        <Link href={`/${slug}/seguros/nova`} className="text-primary underline">Cadastrar apólice</Link>
      </div>
    )
  }

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Número</TableHead>
          <TableHead>Tipo</TableHead>
          <TableHead>Seguradora</TableHead>
          <TableHead>Vigência</TableHead>
          <TableHead>Status</TableHead>
          <TableHead className="text-right">Prêmio</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {policies.map((p) => (
          <TableRow key={p.id} className="cursor-pointer hover:bg-muted/50">
            <TableCell>
              <Link href={`/${slug}/seguros/${p.id}`} className="font-medium hover:underline">
                {p.policy_number}
              </Link>
            </TableCell>
            <TableCell>
              <span className="flex items-center gap-1.5 text-sm">
                {typeIcons[p.type]}
                {typeLabels[p.type] ?? p.type}
              </span>
            </TableCell>
            <TableCell className="text-sm">{p.insurer}</TableCell>
            <TableCell className="text-sm">
              {new Date(p.vigencia_fim).toLocaleDateString('pt-BR')}
            </TableCell>
            <TableCell>
              <VigenciaBadge vigencia_fim={p.vigencia_fim} />
            </TableCell>
            <TableCell className="text-right text-sm">
              {new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(p.premio_total)}
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```

**src/app/(app)/[slug]/clientes/[id]/consortium-tab.tsx** — Client Component:
```typescript
'use client'
import Link from 'next/link'
import { Badge } from '@/components/ui/badge'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'

const statusConfig: Record<string, { label: string; className: string }> = {
  ativo:       { label: 'Ativo',       className: 'bg-blue-100 text-blue-800 border-blue-200' },
  contemplado: { label: 'Contemplado', className: 'bg-green-100 text-green-800 border-green-200' },
  cancelado:   { label: 'Cancelado',   className: 'bg-gray-100 text-gray-600 border-gray-200' },
}

const stageLabels: Record<string, string> = {
  aguardando_docs:  'Aguardando docs',
  em_analise:       'Em análise',
  credito_liberado: 'Crédito liberado',
}

interface QuotaRow {
  id: string
  quota_number: string
  monthly_payment: number
  status: string
  contemplation_date: string | null
  post_contemplation_stage: string | null
  group: { id: string; administrator: string; type: string; credit_value: number } | null
}

export function ConsortiumTab({ quotas, slug }: { quotas: QuotaRow[]; slug: string }) {
  if (quotas.length === 0) {
    return (
      <div className="text-center py-8 text-sm text-muted-foreground">
        Nenhuma cota de consórcio vinculada a este cliente.
      </div>
    )
  }

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Cota</TableHead>
          <TableHead>Grupo / Administradora</TableHead>
          <TableHead>Parcela</TableHead>
          <TableHead>Status</TableHead>
          <TableHead>Pós-contemplação</TableHead>
          <TableHead>Contemplação</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {quotas.map((q) => (
          <TableRow key={q.id}>
            <TableCell className="font-medium">{q.quota_number}</TableCell>
            <TableCell>
              {q.group ? (
                <Link href={`/${slug}/consorcio/${q.group.id}`} className="text-sm hover:underline">
                  {q.group.administrator}
                  <span className="text-muted-foreground ml-1 text-xs">({q.group.type})</span>
                </Link>
              ) : '—'}
            </TableCell>
            <TableCell className="text-sm">
              {new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(q.monthly_payment)}
            </TableCell>
            <TableCell>
              <Badge
                variant="outline"
                className={statusConfig[q.status]?.className ?? ''}
              >
                {statusConfig[q.status]?.label ?? q.status}
              </Badge>
            </TableCell>
            <TableCell>
              {q.status === 'contemplado' && q.post_contemplation_stage ? (
                <span className="text-xs text-muted-foreground">
                  {stageLabels[q.post_contemplation_stage] ?? q.post_contemplation_stage}
                </span>
              ) : '—'}
            </TableCell>
            <TableCell className="text-sm">
              {q.contemplation_date
                ? new Date(q.contemplation_date).toLocaleDateString('pt-BR')
                : '—'}
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep "error TS" | head -20 || echo "TypeScript OK"</automated>
  </verify>
  <done>
    - src/app/(app)/[slug]/clientes/[id]/page.tsx criado com 5 abas: Dados, Timeline, Tarefas, Apólices, Consórcio
    - Aba "Apólices" usa PolicyTab com VigenciaBadge — sem placeholder "Em breve"
    - Aba "Consórcio" usa ConsortiumTab com badge de status e link para o grupo
    - Se not found → notFound() chamado corretamente
    - npm run typecheck sem novos erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Alertas in-app — badge contadores na sidebar + toast Sonner no layout</name>
  <read_first>
    - src/app/(app)/[slug]/layout.tsx (EXATO estado atual — ler antes de modificar)
    - src/components/auth/sidebar-shell.tsx (EXATO estado atual após Plan 03-02 — ler props atuais)
    - .planning/phases/03-seguros-consorcio/03-RESEARCH.md (Pattern 6 — código EXATO das queries de alerta)
    - .planning/phases/02-crm-clientes/02-CONTEXT.md (D-15 — padrão de alerta in-app da Phase 2 com badge + toast)
    - .planning/phases/03-seguros-consorcio/03-CONTEXT.md (D-06 — alertas in-app: apólices ≤30d + assembleias ≤3d)
  </read_first>
  <files>
    src/app/(app)/[slug]/layout.tsx,
    src/components/auth/sidebar-shell.tsx
  </files>
  <action>
Estender o layout e a sidebar para exibir alertas de apólices vencendo e assembleias próximas.

**src/app/(app)/[slug]/layout.tsx** — adicionar queries de alerta ao Server Component existente:

Após as queries existentes (profile, etc.), adicionar:
```typescript
// Alertas in-app (D-06): apólices com vigencia_fim ≤ 30 dias (semáforo vermelho — SEG-03)
const today = new Date().toISOString().split('T')[0]
const thirtyDaysLater = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString().split('T')[0]

const { count: policiesAlertCount } = await supabase
  .from('policies')
  .select('id', { count: 'exact', head: true })
  .lte('vigencia_fim', thirtyDaysLater)
  .is('deleted_at', null)

// Assembleias em ≤ 3 dias (CON-05) — CRITICAL: filtrar next_assembly_date IS NOT NULL (Pitfall 5)
const threeDaysLater = new Date(Date.now() + 3 * 24 * 60 * 60 * 1000).toISOString().split('T')[0]

const { count: assemblyAlertCount } = await supabase
  .from('consortium_groups')
  .select('id', { count: 'exact', head: true })
  .not('next_assembly_date', 'is', null)
  .gte('next_assembly_date', today)
  .lte('next_assembly_date', threeDaysLater)
  .is('deleted_at', null)

const alertCounts = {
  policies: policiesAlertCount ?? 0,
  assemblies: assemblyAlertCount ?? 0,
}
```

Passar `alertCounts` para `<SidebarShell slug={slug} alertCounts={alertCounts} />`.

O toast deve ser gerado no layout com um client component wrapper:
```typescript
// Criar src/components/auth/alert-toast-trigger.tsx (Client Component)
// Props: policiesCount: number, assembliesCount: number
// useEffect(() => { if (policiesCount > 0 || assembliesCount > 0) { toast.info(...) } }, [])
```

**Fallback gracioso:** Se as tabelas `policies` ou `consortium_groups` não existirem ainda (primeira execução antes do db push), envolver as queries em try/catch e usar count=0 como fallback — evitar crash do layout.

**src/components/auth/sidebar-shell.tsx** — estender com alertCounts:

Adicionar à interface SidebarShellProps:
```typescript
interface AlertCounts {
  policies: number
  assemblies: number
}
interface SidebarShellProps {
  slug: string
  alertCounts?: AlertCounts  // opcional — defaults a { policies: 0, assemblies: 0 }
}
```

No render dos itens de navegação, adicionar badge de contador ao lado dos links Seguros e Consórcio:
```tsx
// No item "Seguros" — exibir badge se alertCounts.policies > 0
{item.label === 'Seguros' && (alertCounts?.policies ?? 0) > 0 && (
  <span className="ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-red-500 text-[10px] font-semibold text-white px-1">
    {(alertCounts?.policies ?? 0) > 99 ? '99+' : alertCounts?.policies}
  </span>
)}

// No item "Consórcio" — exibir badge se alertCounts.assemblies > 0
{item.label === 'Consórcio' && (alertCounts?.assemblies ?? 0) > 0 && (
  <span className="ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-orange-500 text-[10px] font-semibold text-white px-1">
    {(alertCounts?.assemblies ?? 0) > 99 ? '99+' : alertCounts?.assemblies}
  </span>
)}
```

**src/components/auth/alert-toast-trigger.tsx** — Client Component:
```typescript
'use client'
import { useEffect } from 'react'
import { toast } from 'sonner'

interface AlertToastTriggerProps {
  policiesCount: number
  assembliesCount: number
}

export function AlertToastTrigger({ policiesCount, assembliesCount }: AlertToastTriggerProps) {
  useEffect(() => {
    if (policiesCount > 0) {
      toast.warning(
        `${policiesCount} apólice${policiesCount > 1 ? 's' : ''} vencendo ou vencida${policiesCount > 1 ? 's' : ''}`,
        {
          description: 'Acesse Seguros para revisar.',
          duration: 6000,
          id: 'policies-alert', // evita duplicate toasts no mesmo render
        }
      )
    }
    if (assembliesCount > 0) {
      toast.info(
        `${assembliesCount} assembleia${assembliesCount > 1 ? 's' : ''} nos próximos 3 dias`,
        {
          description: 'Acesse Consórcio para verificar.',
          duration: 6000,
          id: 'assembly-alert',
        }
      )
    }
  }, []) // eslint-disable-line react-hooks/exhaustive-deps — dispara apenas no mount (ao abrir o sistema)

  return null // sem UI — apenas side effect de toast
}
```

Adicionar `<AlertToastTrigger policiesCount={alertCounts.policies} assembliesCount={alertCounts.assemblies} />` ao JSX do layout, após o `<Toaster>`. Manter o `<Toaster position="top-right" />` já existente.

CRÍTICO — garantir que as queries de alerta NÃO causem erro quando tabelas não existem: usar try/catch ou verificar se as queries retornam `error` antes de acessar `count`.
  </action>
  <verify>
    <automated>grep -n "policiesAlertCount\|assemblyAlertCount\|alertCounts\|AlertToastTrigger" src/app/\(app\)/\[slug\]/layout.tsx | head -20</automated>
  </verify>
  <done>
    - src/app/(app)/[slug]/layout.tsx: contém policiesAlertCount query com .lte('vigencia_fim', thirtyDaysLater)
    - src/app/(app)/[slug]/layout.tsx: contém assemblyAlertCount query com .not('next_assembly_date', 'is', null) — Pitfall 5 mitigado
    - src/app/(app)/[slug]/layout.tsx: passa alertCounts para SidebarShell e AlertToastTrigger
    - src/components/auth/sidebar-shell.tsx: SidebarShellProps inclui alertCounts?: AlertCounts
    - src/components/auth/alert-toast-trigger.tsx: Client Component com useEffect que dispara toast.warning/info
    - grep "not.*next_assembly_date.*null" src/app/(app)/[slug]/layout.tsx retorna linha
    - npm run typecheck sem novos erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout Server Component → Client Components | alertCounts são calculados server-side, passados para client como props — sem exposição de queries ao cliente |
| client_id URL param | ID do cliente vem da URL — poderia ser UUID de outro tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | clientes/[id] — acesso a cliente de outro tenant | mitigate | Query inclui `.is('deleted_at', null)` e o RLS `clients_select` filtra por `tenant_id = jwt_tenant_id()` — se clientId não pertence ao tenant, supabase retorna null → notFound() |
| T-03-18 | Information Disclosure | policy-tab e consortium-tab — dados de outro tenant | mitigate | Queries usam `.eq('client_id', clientId)` — RLS policies_select e consortium_quotas_select filtram tenant_id adicionalmente (dupla camada) |
| T-03-19 | Denial of Service | layout queries de alerta — tabelas inexistentes antes do db push | mitigate | try/catch ou verificação de error antes de acessar count — layout nunca crasha por tabela ausente |
| T-03-20 | Information Disclosure | alertCounts passados como props — expose count para client | accept | Counts são números agregados (N apólices, N assembleias) sem dados pessoais — risco mínimo |
</threat_model>

<verification>
```bash
# 1. Rota de detalhes do cliente criada
ls "src/app/(app)/[slug]/clientes/[id]/page.tsx"

# 2. PolicyTab e ConsortiumTab criados
ls "src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx"
ls "src/app/(app)/[slug]/clientes/[id]/consortium-tab.tsx"

# 3. VigenciaBadge usado no PolicyTab
grep "VigenciaBadge" "src/app/(app)/[slug]/clientes/[id]/policy-tab.tsx"

# 4. notFound() presente no page.tsx de detalhes
grep "notFound" "src/app/(app)/[slug]/clientes/[id]/page.tsx"

# 5. Queries de alerta no layout
grep "policiesAlertCount\|thirtyDaysLater\|assemblyAlertCount\|threeDaysLater" src/app/\(app\)/\[slug\]/layout.tsx

# 6. Pitfall 5 mitigado: IS NOT NULL no filtro de assembleia
grep "next_assembly_date.*is.*null\|not.*next_assembly" src/app/\(app\)/\[slug\]/layout.tsx

# 7. AlertToastTrigger criado
ls src/components/auth/alert-toast-trigger.tsx

# 8. SidebarShell com alertCounts
grep "alertCounts\|AlertCounts" src/components/auth/sidebar-shell.tsx

# 9. TypeScript limpo
npm run typecheck 2>&1 | grep "error TS" || echo "OK"

# 10. Suíte completa final
npm test
```
</verification>

<success_criteria>
- /[slug]/clientes/[id] criado com 5 abas (Dados, Timeline, Tarefas, Apólices, Consórcio) — aba Apólices com VigenciaBadge real, sem placeholder
- Layout executa queries de alerta server-side: apólices ≤30d + assembleias ≤3d com .not('next_assembly_date', 'is', null)
- SidebarShell exibe badge vermelho em "Seguros" e laranja em "Consórcio" quando há alertas
- AlertToastTrigger dispara toast.warning/info ao montar — apenas uma vez (id única no toast)
- npm run typecheck: 0 erros TypeScript novos
- npm test: suíte completa verde (todos os plans 03-01 a 03-04 acumulados)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-seguros-consorcio/03-04-SUMMARY.md` seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
