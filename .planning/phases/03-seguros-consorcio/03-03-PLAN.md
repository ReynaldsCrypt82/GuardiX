---
phase: 03-seguros-consorcio
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/lib/actions/consortium-groups.ts
  - src/lib/actions/consortium-quotas.ts
  - src/app/(app)/[slug]/consorcio/page.tsx
  - src/app/(app)/[slug]/consorcio/grupos/novo/page.tsx
  - src/app/(app)/[slug]/consorcio/grupos/novo/group-form.tsx
  - src/app/(app)/[slug]/consorcio/[id]/page.tsx
  - src/components/consorcio/quota-table.tsx
  - src/components/consorcio/quota-form.tsx
  - src/components/consorcio/contemplation-dialog.tsx
  - tests/actions/consortium.test.ts
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-06

must_haves:
  truths:
    - "Usuário pode acessar /[slug]/consorcio e ver todos os grupos com suas cotas"
    - "Usuário pode criar novo grupo em /[slug]/consorcio/grupos/novo com administradora, tipo, crédito, prazo, cotas"
    - "Usuário pode criar cota vinculada a cliente em /[slug]/consorcio/[id] (grupo existente)"
    - "Usuário pode registrar contemplação de uma cota — tipo sorteio ou lance, lance_value obrigatório se lance"
    - "Ao registrar contemplação, status da cota muda para 'contemplado' e post_contemplation_stage = 'aguardando_docs'"
    - "Usuário pode mover cota entre stages pós-contemplação: aguardando_docs → em_analise → credito_liberado"
    - "Filtros de status (ativo/contemplado/cancelado) e administradora funcionam via URL searchParams"
    - "Server Action cria grupo com tenant_id do JWT — corretor só pode criar cota com assigned_to = próprio user.id"
  artifacts:
    - path: "src/lib/actions/consortium-groups.ts"
      provides: "createGroupAction, updateGroupAction (next_assembly_date)"
      exports: ["createGroupAction", "updateGroupAction"]
    - path: "src/lib/actions/consortium-quotas.ts"
      provides: "createQuotaAction, updateQuotaContemplationAction, updateQuotaStageAction"
      exports: ["createQuotaAction", "updateQuotaContemplationAction", "updateQuotaStageAction"]
    - path: "src/app/(app)/[slug]/consorcio/page.tsx"
      provides: "Listagem global de grupos e cotas com filtros"
    - path: "src/app/(app)/[slug]/consorcio/grupos/novo/group-form.tsx"
      provides: "Formulário de criação de grupo — Client Component"
    - path: "src/app/(app)/[slug]/consorcio/[id]/page.tsx"
      provides: "Detalhes do grupo + lista de cotas + ações por cota"
    - path: "src/components/consorcio/quota-table.tsx"
      provides: "Tabela de cotas com badges de status e ações inline"
    - path: "src/components/consorcio/quota-form.tsx"
      provides: "Formulário de nova cota inline ou dialog"
    - path: "src/components/consorcio/contemplation-dialog.tsx"
      provides: "Dialog para registrar contemplação e pipeline pós-contemplação"
  key_links:
    - from: "updateQuotaContemplationAction"
      to: "consortium_quotas.status = 'contemplado' + post_contemplation_stage = 'aguardando_docs'"
      via: "supabase UPDATE com discrimination_type"
      pattern: "contemplado.*aguardando_docs|post_contemplation_stage"
    - from: "consorcio/page.tsx"
      to: "status + administrator filter"
      via: "URL searchParams → .eq('status') + .ilike('administrator')"
      pattern: "searchParams.*status|searchParams.*administrator"
    - from: "updateQuotaContemplationSchema discriminatedUnion"
      to: "lance_value obrigatório para tipo='lance'"
      via: "z.discriminatedUnion('contemplation_type', [literal('sorteio'), literal('lance')])"
      pattern: "discriminatedUnion.*contemplation_type|sorteio.*lance"
---

<objective>
Implementar o módulo completo de Consórcio: Server Actions para CRUD de grupos e cotas, registro de contemplação com pipeline pós-contemplação, filtros por status e administradora, UI de listagem e detalhes do grupo.

Purpose: Entrega CON-01 a CON-04 e CON-06. Permite à corretora substituir planilhas de controle de cotas e acompanhar o pipeline pós-contemplação (D-04).
Output: 2 Server Actions, 5 rotas Next.js, 3 componentes, testes unitários reais para todas as Server Actions do consórcio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seguros-consorcio/03-CONTEXT.md
@.planning/phases/03-seguros-consorcio/03-RESEARCH.md
@.planning/phases/03-seguros-consorcio/03-01-SUMMARY.md
@src/lib/actions/clients.ts
@src/app/(app)/[slug]/clientes/page.tsx
@tests/actions/clients.test.ts

<interfaces>
<!-- Contratos criados pelo Plan 03-01 — usar exatamente estas assinaturas -->

<!-- src/lib/validations/consortium-schemas.ts -->
```typescript
export const createGroupSchema: z.ZodObject<{
  administrator: z.ZodString          // min 2
  type: z.ZodEnum<['auto','imovel','servico']>
  credit_value: z.ZodNumber           // > 0
  term_months: z.ZodNumber            // int >= 1
  start_date: z.ZodString             // ISO date
  total_quotas: z.ZodNumber           // int >= 1
  next_assembly_date: z.ZodOptional<z.ZodNullable<z.ZodString>>  // ISO date ou null
}>
export type CreateGroupInput = z.infer<typeof createGroupSchema>

export const createQuotaSchema: z.ZodObject<{
  group_id: z.ZodString       // UUID
  client_id: z.ZodString      // UUID
  assigned_to: z.ZodString    // UUID
  quota_number: z.ZodString   // min 1
  monthly_payment: z.ZodNumber // >= 0
}>
export type CreateQuotaInput = z.infer<typeof createQuotaSchema>

export const updateQuotaContemplationSchema: z.ZodDiscriminatedUnion<'contemplation_type', [...]>
// contemplation_type: 'sorteio' → lance_value: undefined opcional
// contemplation_type: 'lance'  → lance_value: number >= 0.01 OBRIGATÓRIO
// Ambos: quota_id UUID, contemplation_date ISO date
//        post_contemplation_stage enum ('aguardando_docs'|'em_analise'|'credito_liberado') default 'aguardando_docs'
//        post_contemplation_notes: string opcional
export type UpdateQuotaContemplationInput = z.infer<typeof updateQuotaContemplationSchema>
```

<!-- Database tables (existem após Plan 03-01 supabase db push) -->
<!-- consortium_groups: id, tenant_id, administrator, type, credit_value, term_months, start_date, total_quotas, next_assembly_date, deleted_at, created_at, updated_at -->
<!-- consortium_quotas: id, tenant_id, group_id, client_id, quota_number, monthly_payment, status, contemplation_date, contemplation_type, lance_value, post_contemplation_stage, post_contemplation_notes, assigned_to, deleted_at, created_at, updated_at -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions — consortium-groups e consortium-quotas</name>
  <read_first>
    - src/lib/actions/clients.ts (padrão EXATO a replicar)
    - src/lib/validations/consortium-schemas.ts (schemas criados no Plan 03-01)
    - tests/actions/consortium.test.ts (stubs it.todo criados no Plan 03-01 — substituir por testes reais)
    - tests/actions/clients.test.ts (padrão de mock vi.mock + helper makeFormData)
    - .planning/phases/03-seguros-consorcio/03-CONTEXT.md (D-04 — modelo de dados, D-11 RBAC)
  </read_first>
  <behavior>
    consortium.test.ts (RED first, então GREEN):

    createGroupAction:
    - rejeita administrator vazio (min 2) — retorna error.administrator
    - rejeita type inválido (ex: 'barco') — retorna error.type
    - rejeita credit_value = 0 — retorna error.credit_value
    - rejeita term_months = 0 — retorna error.term_months
    - aceita next_assembly_date null/undefined (campo opcional)
    - insere com tenant_id do JWT (nunca do FormData)
    - corretor não pode criar grupo para outro tenant (tenantId guard)
    - revalida path /{slug}/consorcio após sucesso

    createQuotaAction:
    - rejeita group_id inválido (não UUID) — retorna error.group_id
    - rejeita client_id inválido (não UUID) — retorna error.client_id
    - bloqueia corretor atribuindo cota a outro corretor (assigned_to !== user.id)
    - insere com tenant_id do JWT, status default 'ativo'
    - revalida path /{slug}/consorcio/{group_id} após sucesso

    updateQuotaContemplationAction:
    - rejeita contemplation_type inválido (ex: 'ganhou')
    - para tipo 'lance': rejeita lance_value = 0 ou ausente
    - para tipo 'sorteio': aceita sem lance_value
    - após sucesso: status='contemplado', post_contemplation_stage='aguardando_docs' por default
    - revalida path /{slug}/consorcio/{group_id}

    updateQuotaStageAction:
    - rejeita stage inválido (ex: 'assinado')
    - aceita transição aguardando_docs → em_analise → credito_liberado
    - rejeita não-admin/corretor atualizando cota de outro corretor
  </behavior>
  <files>
    src/lib/actions/consortium-groups.ts,
    src/lib/actions/consortium-quotas.ts,
    tests/actions/consortium.test.ts
  </files>
  <action>
Criar os 2 arquivos de Server Actions. Replicar exatamente o padrão de clients.ts.

**src/lib/actions/consortium-groups.ts:**
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createGroupSchema } from '@/lib/validations/consortium-schemas'

export async function createGroupAction(slug: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  // Normalizar next_assembly_date: string vazia → null
  if (raw.next_assembly_date === '') raw.next_assembly_date = null

  const parsed = createGroupSchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: { _form: ['Sem permissão para criar grupos de consórcio.'] } }
  }

  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return { error: { _form: ['Tenant não identificado.'] } }

  const { data, error } = await supabase
    .from('consortium_groups')
    .insert({
      tenant_id: tenantId,
      administrator: parsed.data.administrator,
      type: parsed.data.type,
      credit_value: parsed.data.credit_value,
      term_months: parsed.data.term_months,
      start_date: parsed.data.start_date,
      total_quotas: parsed.data.total_quotas,
      next_assembly_date: parsed.data.next_assembly_date ?? null,
    })
    .select('id')
    .single()

  if (error) return { error: { _form: ['Erro ao salvar grupo de consórcio.'] } }

  revalidatePath(`/${slug}/consorcio`)
  return { id: data.id }
}

export async function updateGroupAction(slug: string, groupId: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  if (raw.next_assembly_date === '') raw.next_assembly_date = null

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Sessão expirada.' }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: 'Sem permissão para editar grupos.' }
  }

  const { error } = await supabase
    .from('consortium_groups')
    .update({
      next_assembly_date: raw.next_assembly_date as string | null,
      updated_at: new Date().toISOString(),
    })
    .eq('id', groupId)

  if (error) return { error: 'Erro ao atualizar grupo.' }

  revalidatePath(`/${slug}/consorcio`)
  revalidatePath(`/${slug}/consorcio/${groupId}`)
  return { success: true }
}
```

**src/lib/actions/consortium-quotas.ts:**
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createQuotaSchema, updateQuotaContemplationSchema } from '@/lib/validations/consortium-schemas'
import { z } from 'zod'

export async function createQuotaAction(slug: string, groupId: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = createQuotaSchema.safeParse({ ...raw, group_id: groupId })
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (role === 'corretor' && parsed.data.assigned_to !== user.id) {
    return { error: { assigned_to: ['Corretor só pode registrar cota em seu próprio nome.'] } }
  }

  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return { error: { _form: ['Tenant não identificado.'] } }

  const { data, error } = await supabase
    .from('consortium_quotas')
    .insert({
      tenant_id: tenantId,
      group_id: parsed.data.group_id,
      client_id: parsed.data.client_id,
      assigned_to: parsed.data.assigned_to,
      quota_number: parsed.data.quota_number,
      monthly_payment: parsed.data.monthly_payment,
      status: 'ativo',
    })
    .select('id')
    .single()

  if (error) {
    if ((error as { code?: string }).code === '23505') {
      return { error: { quota_number: ['Número de cota já cadastrado neste grupo.'] } }
    }
    return { error: { _form: ['Erro ao salvar cota.'] } }
  }

  revalidatePath(`/${slug}/consorcio/${groupId}`)
  return { id: data.id }
}

export async function updateQuotaContemplationAction(
  slug: string,
  groupId: string,
  formData: FormData
) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = updateQuotaContemplationSchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: { _form: ['Sem permissão para registrar contemplação.'] } }
  }

  const { contemplation_type, lance_value, contemplation_date,
          post_contemplation_stage, post_contemplation_notes, quota_id } = parsed.data

  const { error } = await supabase
    .from('consortium_quotas')
    .update({
      status: 'contemplado',
      contemplation_date,
      contemplation_type,
      lance_value: contemplation_type === 'lance' ? lance_value : null,
      post_contemplation_stage: post_contemplation_stage ?? 'aguardando_docs',
      post_contemplation_notes: post_contemplation_notes || null,
      updated_at: new Date().toISOString(),
    })
    .eq('id', quota_id)

  if (error) return { error: { _form: ['Erro ao registrar contemplação.'] } }

  revalidatePath(`/${slug}/consorcio/${groupId}`)
  return { success: true }
}

const updateStageSchema = z.object({
  quota_id: z.string().uuid(),
  stage: z.enum(['aguardando_docs', 'em_analise', 'credito_liberado']),
  notes: z.string().optional(),
})

export async function updateQuotaStageAction(
  slug: string,
  groupId: string,
  formData: FormData
) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = updateStageSchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: { _form: ['Sem permissão para atualizar estágio pós-contemplação.'] } }
  }

  // Se corretor, verificar que a cota é dele
  if (role === 'corretor') {
    const { data: quota } = await supabase
      .from('consortium_quotas')
      .select('assigned_to')
      .eq('id', parsed.data.quota_id)
      .maybeSingle()
    if (!quota || quota.assigned_to !== user.id) {
      return { error: { _form: ['Corretor só pode atualizar cotas próprias.'] } }
    }
  }

  const { error } = await supabase
    .from('consortium_quotas')
    .update({
      post_contemplation_stage: parsed.data.stage,
      post_contemplation_notes: parsed.data.notes || null,
      updated_at: new Date().toISOString(),
    })
    .eq('id', parsed.data.quota_id)

  if (error) return { error: { _form: ['Erro ao atualizar estágio.'] } }

  revalidatePath(`/${slug}/consorcio/${groupId}`)
  return { success: true }
}
```

Para tests/actions/consortium.test.ts: substituir todos os it.todo pelos testes reais listados em `<behavior>`. Usar o mesmo padrão de mock vi.mock + makeFormData + adminUser/corretorUser helpers de clients.test.ts. Criar helpers separados mockGroupChain e mockQuotaChain para cada tabela.
  </action>
  <verify>
    <automated>npx vitest run tests/actions/consortium.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/lib/actions/consortium-groups.ts: 'use server', createGroupAction + updateGroupAction
    - src/lib/actions/consortium-quotas.ts: 'use server', createQuotaAction + updateQuotaContemplationAction + updateQuotaStageAction
    - Todos os 3 Server Actions usam tenant_id de user.app_metadata.tenant_id
    - updateQuotaContemplationAction: status='contemplado' + post_contemplation_stage='aguardando_docs' por default
    - updateQuotaContemplationAction: lance_value preservado para tipo='lance', null para tipo='sorteio'
    - npx vitest run tests/actions/consortium.test.ts: todos os testes passando
  </done>
</task>

<task type="auto">
  <name>Task 2: UI do módulo Consórcio — listagem, formulários, detalhes do grupo</name>
  <read_first>
    - src/app/(app)/[slug]/seguros/page.tsx (padrão Server Component com filtros — criado no Plan 03-02, ler o resultado)
    - src/app/(app)/[slug]/clientes/page.tsx (padrão de paginação e URL state)
    - src/components/seguros/vigencia-badge.tsx (criado no Plan 03-02 — ler para usar mesmo padrão de Badge)
    - src/components/seguros/claim-dialog.tsx (padrão Dialog — ler para replicar no contemplation-dialog)
    - src/components/ui/badge.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/table.tsx
    - .planning/phases/03-seguros-consorcio/03-CONTEXT.md (D-04 — modelo de dados, D-06 alertas)
    - .planning/phases/03-seguros-consorcio/03-RESEARCH.md (Pitfall 5 next_assembly_date nulo)
  </read_first>
  <files>
    src/app/(app)/[slug]/consorcio/page.tsx,
    src/app/(app)/[slug]/consorcio/grupos/novo/page.tsx,
    src/app/(app)/[slug]/consorcio/grupos/novo/group-form.tsx,
    src/app/(app)/[slug]/consorcio/[id]/page.tsx,
    src/components/consorcio/quota-table.tsx,
    src/components/consorcio/quota-form.tsx,
    src/components/consorcio/contemplation-dialog.tsx
  </files>
  <action>
Criar os arquivos de UI do módulo Consórcio.

**src/app/(app)/[slug]/consorcio/page.tsx** — Server Component:
- Busca grupos com contagem de cotas:
```typescript
const supabase = await createClient()
const type = searchParams.type as string | undefined
const administrator = searchParams.administrator as string | undefined
const quotaStatus = searchParams.status as string | undefined

let query = supabase
  .from('consortium_groups')
  .select('id, administrator, type, credit_value, term_months, next_assembly_date, start_date, total_quotas, consortium_quotas(id, status)')
  .is('deleted_at', null)
  .order('created_at', { ascending: false })

if (type) query = query.eq('type', type)
if (administrator) query = query.ilike('administrator', `%${administrator}%`)
```
- Filtros de quotaStatus ('ativo'/'contemplado'/'cancelado') aplicados client-side no componente filho ou via subquery separada
- Layout: tabela de grupos com colunas: administradora, tipo (badge), crédito (Intl BRL), prazo (N meses), cotas (X ativas / Y total), próxima assembleia (data ou "—"), ações (ver grupo)
- Link "Novo grupo" → /consorcio/grupos/novo
- Estado vazio: "Nenhum grupo de consórcio cadastrado" + botão "Criar grupo"
- Filtros inline acima da tabela: select de tipo (auto/imóvel/serviço), input de administradora, select de status de cota

**src/app/(app)/[slug]/consorcio/grupos/novo/page.tsx** — Server Component wrapper (passa slug, clientes, corretores para GroupForm):
```typescript
// Busca clientes e profiles para selects no formulário
const { data: clients } = await supabase.from('clients').select('id, name, type').is('deleted_at', null).order('name')
const { data: corretores } = await supabase.from('profiles').select('id, full_name').eq('role', 'corretor').order('full_name')
// Renderiza <GroupForm slug={slug} />
```

**src/app/(app)/[slug]/consorcio/grupos/novo/group-form.tsx** — Client Component:
- React Hook Form com createGroupSchema-aligned fields
- Campos: administrator (input), type (select: auto/imóvel/serviço), credit_value (number), term_months (number), start_date (date), total_quotas (number), next_assembly_date (date, opcional — label "Próxima assembleia (opcional)")
- Submit via `createGroupAction(slug, formData)` com FormData
- Redirect para `/[slug]/consorcio/${data.id}` após sucesso

**src/app/(app)/[slug]/consorcio/[id]/page.tsx** — Server Component:
```typescript
// Busca grupo
const { data: group } = await supabase
  .from('consortium_groups')
  .select('*')
  .eq('id', groupId)
  .is('deleted_at', null)
  .single()
if (!group) notFound()

// Busca cotas do grupo com cliente e corretor
const statusFilter = searchParams.status as string | undefined
let quotaQuery = supabase
  .from('consortium_quotas')
  .select('*, client:clients(name, type), profile:profiles!assigned_to(full_name)')
  .eq('group_id', groupId)
  .is('deleted_at', null)
  .order('quota_number', { ascending: true })

if (statusFilter) quotaQuery = quotaQuery.eq('status', statusFilter)
const { data: quotas } = await quotaQuery
```
- Header: card com dados do grupo (tipo, crédito, prazo, total cotas, administradora)
- Campo "Próxima assembleia" com data ou "Não agendada" + botão admin para atualizar
- Seção cotas: `<QuotaTable quotas={quotas} slug={slug} groupId={group.id} />`
- Botão "Adicionar cota" → `<QuotaForm slug={slug} groupId={group.id} />` inline ou dialog

**src/components/consorcio/quota-table.tsx** — Client Component:
- Props: `quotas: QuotaRow[]`, `slug: string`, `groupId: string`
- Colunas: número da cota, cliente (nome), parcela (BRL), status (badge), data contemplação (ou "—"), stage pós-contemplação (badge apenas se status='contemplado'), corretor, ações
- Badge status: ativo=azul (`bg-blue-100 text-blue-800`), contemplado=verde (`bg-green-100 text-green-800`), cancelado=gray (`bg-gray-100 text-gray-600`)
- Badge post_contemplation_stage (apenas quando status='contemplado'): aguardando_docs=amarelo, em_analise=laranja, credito_liberado=verde
- Botão "Contemplar" visível apenas para cotas com status='ativo' → abre `<ContemplationDialog>`
- Botão "Avançar stage" (apenas para status='contemplado') → select inline ou dialog mínimo para mudar post_contemplation_stage
- Tipo `QuotaRow`: `{ id: string; quota_number: string; status: string; monthly_payment: number; contemplation_date: string | null; contemplation_type: string | null; lance_value: number | null; post_contemplation_stage: string | null; post_contemplation_notes: string | null; client?: { name: string; type: string }; profile?: { full_name: string } }`

**src/components/consorcio/quota-form.tsx** — Client Component (dialog ou inline):
- Props: `slug: string`, `groupId: string`, lista de `clients`, lista de `profiles`
- Campos: client_id (select), assigned_to (select), quota_number (input), monthly_payment (number)
- Submit via `createQuotaAction(slug, groupId, formData)`
- Toast "Cota adicionada" após sucesso

**src/components/consorcio/contemplation-dialog.tsx** — Client Component:
- Props: `slug: string`, `groupId: string`, `quotaId: string`, `quotaNumber: string`
- Dialog com campos:
  - contemplation_date (date input, obrigatório)
  - contemplation_type (select: sorteio / lance)
  - lance_value (number input — exibido somente quando contemplation_type='lance')
  - post_contemplation_stage (select: aguardando_docs / em_analise / credito_liberado, default aguardando_docs)
  - post_contemplation_notes (textarea, opcional)
- Campos: hidden input quota_id com valor quotaId
- Submit via `updateQuotaContemplationAction(slug, groupId, formData)`
- Toast "Contemplação registrada" após sucesso

Pitfall crítico: `next_assembly_date IS NULL` — nunca comparar data com NULL. Na tela do grupo, exibir "Não agendada" quando null e NOT renderizar badge de alerta se null.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep "error TS" | head -20 || echo "TypeScript OK"</automated>
  </verify>
  <done>
    - src/app/(app)/[slug]/consorcio/page.tsx: Server Component com filtros por type + administrator via searchParams
    - src/app/(app)/[slug]/consorcio/grupos/novo/group-form.tsx: 'use client', campos obrigatórios do grupo
    - src/app/(app)/[slug]/consorcio/[id]/page.tsx: Server Component, busca grupo + cotas, passa para QuotaTable
    - src/components/consorcio/quota-table.tsx: badges de status, botão "Contemplar" → ContemplationDialog
    - src/components/consorcio/contemplation-dialog.tsx: campo lance_value condicional (visível quando tipo='lance')
    - hidden input quota_id presente no form do ContemplationDialog
    - npm run typecheck sem novos erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData → createGroupAction | Campos do grupo não-confiáveis do browser |
| contemplation_type field | Usuário poderia enviar tipo inválido para manipular lance_value |
| quota_id em formData | Usuário poderia referenciar quota de outro tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege | createQuotaAction — assigned_to | mitigate | Guard explícito `role === 'corretor' && assigned_to !== user.id` — mesmo padrão de clients e policies |
| T-03-13 | Tampering | updateQuotaContemplationAction — contemplation_type injection | mitigate | Zod discriminatedUnion valida estritamente os dois literais 'sorteio'/'lance' — tipo inválido retorna erro antes de qualquer DB call |
| T-03-14 | Spoofing | consortium_groups/quotas — tenant_id | mitigate | tenant_id vem exclusivamente de user.app_metadata.tenant_id em todos os Server Actions — RLS é defense-in-depth adicional |
| T-03-15 | Denial of Service | next_assembly_date NULL na query de alertas (Pitfall 5) | mitigate | Query de alertas inclui `.not('next_assembly_date', 'is', null)` antes do filtro de data — nunca compara NULL com date |
| T-03-16 | Information Disclosure | quota de outro tenant via quota_id em FormData | mitigate | RLS consortium_quotas_select filtra por tenant_id — RLS updateQuota bloqueia UPDATE cross-tenant; updateQuotaStageAction guard de role corretor inclui query de verificação assigned_to |
</threat_model>

<verification>
```bash
# 1. Server Actions com 'use server'
head -1 src/lib/actions/consortium-groups.ts
head -1 src/lib/actions/consortium-quotas.ts

# 2. tenant_id do JWT
grep "app_metadata.*tenant_id" src/lib/actions/consortium-groups.ts
grep "app_metadata.*tenant_id" src/lib/actions/consortium-quotas.ts

# 3. status='contemplado' + post_contemplation_stage na contemplation action
grep "contemplado\|post_contemplation_stage.*aguardando" src/lib/actions/consortium-quotas.ts

# 4. Rotas criadas
ls src/app/\(app\)/\[slug\]/consorcio/page.tsx
ls src/app/\(app\)/\[slug\]/consorcio/grupos/novo/group-form.tsx
ls src/app/\(app\)/\[slug\]/consorcio/\[id\]/page.tsx

# 5. Componentes criados
ls src/components/consorcio/quota-table.tsx
ls src/components/consorcio/contemplation-dialog.tsx

# 6. Testes passando
npx vitest run tests/actions/consortium.test.ts --reporter=verbose

# 7. TypeScript limpo
npm run typecheck 2>&1 | grep "error TS" || echo "OK"

# 8. Suíte completa pós-Wave 1
npm test
```
</verification>

<success_criteria>
- createGroupAction: valida tipo, crédito > 0, term_months >= 1, insere com tenant_id do JWT
- createQuotaAction: guard de corretor, 23505 tratado (quota_number duplicado no grupo)
- updateQuotaContemplationAction: discriminatedUnion, lance_value obrigatório para 'lance', post_contemplation_stage default 'aguardando_docs'
- updateQuotaStageAction: rejeita stage inválido, guard de corretor via DB check
- UI: badge de status ativo/contemplado/cancelado, campo lance_value condicional no ContemplationDialog
- npx vitest run tests/actions/consortium.test.ts: todos passando
- npm test: suíte completa verde (plans 03-01 + 03-02 + 03-03 acumulados)
- npm run typecheck sem novos erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-seguros-consorcio/03-03-SUMMARY.md` seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
