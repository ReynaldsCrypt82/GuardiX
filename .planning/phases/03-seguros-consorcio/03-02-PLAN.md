---
phase: 03-seguros-consorcio
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/lib/actions/policies.ts
  - src/lib/actions/claims.ts
  - src/lib/actions/endorsements.ts
  - src/app/(app)/[slug]/seguros/page.tsx
  - src/app/(app)/[slug]/seguros/nova/page.tsx
  - src/app/(app)/[slug]/seguros/nova/policy-form.tsx
  - src/app/(app)/[slug]/seguros/[id]/page.tsx
  - src/components/seguros/policy-table.tsx
  - src/components/seguros/vigencia-badge.tsx
  - src/components/seguros/claim-dialog.tsx
  - src/components/seguros/endorsement-dialog.tsx
  - src/components/auth/sidebar-shell.tsx
  - tests/actions/policies.test.ts
  - tests/actions/claims.test.ts
  - tests/actions/endorsements.test.ts
autonomous: true
requirements:
  - SEG-01
  - SEG-02
  - SEG-03
  - SEG-04
  - SEG-05
  - SEG-06
  - SEG-07

must_haves:
  truths:
    - "Usuário pode acessar /[slug]/seguros e ver tabela de apólices do tenant com semáforo verde/amarelo/vermelho"
    - "Usuário pode cadastrar nova apólice em /[slug]/seguros/nova com formulário dinâmico — campos mudam conforme tipo selecionado"
    - "Formulário auto exibe campos placa/chassi/marca_modelo/ano/valor_fipe/cobertura; vida exibe valor_assegurado/beneficiarios"
    - "Server Action createPolicyAction extrai campos core + type_data JSONB separados, nunca misturados"
    - "Corretor só pode criar apólice com assigned_to = próprio user.id"
    - "Usuário pode registrar sinistro (data/tipo/protocolo/descrição) na tela de detalhes da apólice"
    - "Usuário pode registrar endosso (data/tipo/descrição/impacto) na tela de detalhes da apólice"
    - "Filtros tipo, seguradora, corretor e status vigência funcionam via URL searchParams"
    - "Sidebar exibe seção 'Produtos' com links 'Seguros' e 'Consórcio' entre Clientes e Configurações"
  artifacts:
    - path: "src/lib/actions/policies.ts"
      provides: "createPolicyAction, updatePolicyAction, softDeletePolicyAction"
      exports: ["createPolicyAction", "updatePolicyAction", "softDeletePolicyAction"]
    - path: "src/lib/actions/claims.ts"
      provides: "createClaimAction"
      exports: ["createClaimAction"]
    - path: "src/lib/actions/endorsements.ts"
      provides: "createEndorsementAction"
      exports: ["createEndorsementAction"]
    - path: "src/app/(app)/[slug]/seguros/page.tsx"
      provides: "Listagem global de apólices com filtros por URL state"
    - path: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      provides: "Formulário dinâmico com campos por tipo — Client Component"
    - path: "src/app/(app)/[slug]/seguros/[id]/page.tsx"
      provides: "Detalhes da apólice + listas de sinistros e endossos"
    - path: "src/components/seguros/vigencia-badge.tsx"
      provides: "Badge colorido verde/amarelo/vermelho baseado em getVigenciaStatus"
    - path: "src/components/seguros/policy-table.tsx"
      provides: "Tabela de apólices com VigenciaBadge e filtros"
    - path: "src/components/seguros/claim-dialog.tsx"
      provides: "Dialog para registrar sinistro"
    - path: "src/components/seguros/endorsement-dialog.tsx"
      provides: "Dialog para registrar endosso"
    - path: "src/components/auth/sidebar-shell.tsx"
      provides: "Label 'Produtos' + links Seguros e Consórcio adicionados"
  key_links:
    - from: "policy-form.tsx watch('type')"
      to: "seção condicional de campos por tipo"
      via: "React Hook Form watch + useEffect reset"
      pattern: "watch.*type|reset.*type"
    - from: "createPolicyAction"
      to: "type_data JSONB"
      via: "desestruturação explícita de campos core + spread ...typeSpecific"
      pattern: "typeSpecific|type_data"
    - from: "seguros/page.tsx"
      to: "vigencia_fim filter"
      via: "URL searchParams + .lte/.gt queries"
      pattern: "vigencia_fim.*lte|lte.*vigencia_fim"
---

<objective>
Implementar o módulo completo de Seguros: Server Actions para CRUD de apólices, sinistros e endossos; páginas de listagem global com filtros; formulário dinâmico por tipo de seguro; tela de detalhes com tabs; componente VigenciaBadge; extensão da sidebar.

Purpose: Entrega SEG-01 a SEG-07. É o módulo core de seguros que permite à corretora substituir planilhas de controle de apólices.
Output: 3 Server Actions, 5 rotas Next.js, 5 componentes, sidebar estendida, testes unitários reais para todas as Server Actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seguros-consorcio/03-CONTEXT.md
@.planning/phases/03-seguros-consorcio/03-RESEARCH.md
@.planning/phases/03-seguros-consorcio/03-01-SUMMARY.md
@src/lib/actions/clients.ts
@src/components/auth/sidebar-shell.tsx
@src/app/(app)/[slug]/clientes/page.tsx
@tests/actions/clients.test.ts

<interfaces>
<!-- Contratos criados pelo Plan 03-01 — usar exatamente estas assinaturas -->

<!-- src/lib/utils/vigencia.ts -->
```typescript
export type VigenciaStatus = 'verde' | 'amarelo' | 'vermelho'
export function getVigenciaStatus(vigencia_fim: string): VigenciaStatus
```

<!-- src/lib/validations/policy-schemas.ts -->
```typescript
export const createPolicySchema: z.ZodDiscriminatedUnion<'type', [...]>
export type CreatePolicyInput = z.infer<typeof createPolicySchema>
// Tipos discriminados: 'auto' | 'vida' | 'residencial' | 'empresarial' | 'saude' | 'outros'
// Campos core: policy_number, insurer, vigencia_inicio, vigencia_fim, premio_total, client_id, assigned_to, observacoes
// Campos auto: placa, chassi, marca_modelo, ano, valor_fipe, cobertura ('basica'|'compreensiva')
// Campos vida: valor_assegurado, beneficiarios (string opcional)
// Campos residencial: endereco_imovel, valor_imovel, tipo_imovel ('proprio'|'alugado'), cobertura
// Campos empresarial: cnpj_risco, endereco_empresa, tipo_atividade, valor_patrimonial
// Campos saude: operadora, numero_carteirinha, acomodacao ('enfermaria'|'apartamento'), dependentes
// Campos outros: apenas core fields
```

<!-- src/lib/validations/claim-schemas.ts -->
```typescript
export const createClaimSchema: z.ZodObject<{
  policy_id: z.ZodString  // UUID
  claim_date: z.ZodString // ISO date
  type: z.ZodString       // qualquer string não-vazia
  protocol_number: z.ZodOptional<z.ZodString>
  status: z.ZodDefault<z.ZodEnum<['aberto', 'em_analise', 'encerrado']>>
  description: z.ZodString
}>
export type CreateClaimInput = z.infer<typeof createClaimSchema>
```

<!-- src/lib/validations/endorsement-schemas.ts -->
```typescript
export const createEndorsementSchema: z.ZodObject<{
  policy_id: z.ZodString  // UUID
  endorsement_date: z.ZodString // ISO date
  type: z.ZodEnum<['inclusao', 'exclusao', 'alteracao']>
  description: z.ZodString
  premium_impact: z.ZodOptional<z.ZodNullable<z.ZodNumber>>
}>
export type CreateEndorsementInput = z.infer<typeof createEndorsementSchema>
```

<!-- Database tables (existem após Plan 03-01 supabase db push) -->
<!-- policies: id, tenant_id, policy_number, insurer, type, vigencia_inicio, vigencia_fim, premio_total, client_id, assigned_to, type_data JSONB, observacoes, deleted_at, created_at, updated_at -->
<!-- claims: id, tenant_id, policy_id, claim_date, type, protocol_number, status, description, created_by, deleted_at, created_at, updated_at -->
<!-- endorsements: id, tenant_id, policy_id, endorsement_date, type, description, premium_impact, created_by, deleted_at, created_at, updated_at -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions — policies, claims, endorsements</name>
  <read_first>
    - src/lib/actions/clients.ts (padrão EXATO a replicar: 'use server', safeParse, getUser, role guard, tenantId, insert, revalidatePath)
    - src/lib/validations/policy-schemas.ts (schema criado no Plan 03-01)
    - src/lib/validations/claim-schemas.ts (schema criado no Plan 03-01)
    - src/lib/validations/endorsement-schemas.ts (schema criado no Plan 03-01)
    - tests/actions/clients.test.ts (padrão de mock vi.mock + makeFormData a replicar para policies/claims/endorsements)
    - tests/actions/policies.test.ts (stubs it.todo criados no Plan 03-01 — substituir por testes reais)
    - tests/actions/claims.test.ts (stubs it.todo — substituir por testes reais)
    - tests/actions/endorsements.test.ts (stubs it.todo — substituir por testes reais)
    - .planning/phases/03-seguros-consorcio/03-RESEARCH.md (Pattern 5 — createPolicyAction completo)
  </read_first>
  <behavior>
    policies.test.ts (RED first, então GREEN):
    - createPolicyAction rejeita type inválido (ex: 'xx') — retorna error.type
    - createPolicyAction rejeita tipo 'auto' sem placa — retorna error.placa
    - createPolicyAction tipo 'outros' aceita sem campos extras — insere com type_data: {}
    - createPolicyAction tipo 'auto' insere type_data com {placa, marca_modelo, ano, valor_fipe, cobertura} mas SEM policy_number/insurer/vigencia_inicio/vigencia_fim/premio_total/client_id/assigned_to (desestruturação correta)
    - createPolicyAction bloqueia corretor atribuindo apólice a outro (assigned_to !== user.id)
    - createPolicyAction rejeita quando tenantId ausente — retorna error._form
    - createPolicyAction rejeita número de apólice duplicado (error code '23505') — retorna error.policy_number
    - updatePolicyAction rejeita não-admin atualizando apólice de outro corretor
    - softDeletePolicyAction faz UPDATE deleted_at = NOW() não DELETE físico

    claims.test.ts (RED first):
    - createClaimAction valida policy_id como UUID obrigatório
    - createClaimAction valida claim_date como ISO date
    - createClaimAction valida description não-vazia
    - createClaimAction insere com tenant_id do JWT, não do FormData (tenant_id hardcoded no Server Action)
    - createClaimAction rejeita status inválido (ex: 'xyz')
    - createClaimAction revalida path /{slug}/seguros/{policy_id} após sucesso

    endorsements.test.ts (RED first):
    - createEndorsementAction rejeita type inválido (ex: 'mudança')
    - createEndorsementAction aceita premium_impact nulo/undefined
    - createEndorsementAction insere com tenant_id do JWT
  </behavior>
  <files>
    src/lib/actions/policies.ts,
    src/lib/actions/claims.ts,
    src/lib/actions/endorsements.ts,
    tests/actions/policies.test.ts,
    tests/actions/claims.test.ts,
    tests/actions/endorsements.test.ts
  </files>
  <action>
Criar os 3 arquivos de Server Actions. Replicar EXATAMENTE o padrão de clients.ts.

**src/lib/actions/policies.ts** — seguir Pattern 5 do RESEARCH.md:
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createPolicySchema } from '@/lib/validations/policy-schemas'

export async function createPolicyAction(slug: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = createPolicySchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (role === 'corretor' && parsed.data.assigned_to !== user.id) {
    return { error: { assigned_to: ['Corretor só pode registrar apólice em seu próprio nome.'] } }
  }

  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return { error: { _form: ['Tenant não identificado.'] } }

  // CRITICAL (Pitfall 1): desestruturar campos core explicitamente, spread restante para type_data
  const { type, policy_number, insurer, vigencia_inicio, vigencia_fim,
          premio_total, client_id, assigned_to, observacoes, ...typeSpecific } = parsed.data

  const { data, error } = await supabase
    .from('policies')
    .insert({
      tenant_id: tenantId,
      type, policy_number, insurer, vigencia_inicio, vigencia_fim,
      premio_total, client_id, assigned_to,
      observacoes: observacoes || null,
      type_data: Object.keys(typeSpecific).length > 0 ? typeSpecific : {},
    })
    .select('id')
    .single()

  if (error) {
    if ((error as { code?: string }).code === '23505') {
      return { error: { policy_number: ['Número de apólice já cadastrado nesta corretora.'] } }
    }
    return { error: { _form: ['Erro ao salvar apólice.'] } }
  }

  revalidatePath(`/${slug}/seguros`)
  revalidatePath(`/${slug}/clientes/${client_id}`)
  return { id: data.id }
}

export async function softDeletePolicyAction(slug: string, policyId: string) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: 'Sessão expirada.' }

  const role = (user.app_metadata as { role?: string })?.role
  if (role !== 'admin') return { error: 'Apenas administradores podem arquivar apólices.' }

  const { error } = await supabase
    .from('policies')
    .update({ deleted_at: new Date().toISOString() })
    .eq('id', policyId)

  if (error) return { error: 'Erro ao arquivar apólice.' }
  revalidatePath(`/${slug}/seguros`)
  return { success: true }
}
```

**src/lib/actions/claims.ts:**
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createClaimSchema } from '@/lib/validations/claim-schemas'

export async function createClaimAction(slug: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = createClaimSchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: { _form: ['Sem permissão para registrar sinistros.'] } }
  }

  // CRITICAL (Pitfall 4): tenant_id SEMPRE do JWT, nunca do FormData
  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return { error: { _form: ['Tenant não identificado.'] } }

  const { data, error } = await supabase
    .from('claims')
    .insert({
      tenant_id: tenantId,
      policy_id: parsed.data.policy_id,
      claim_date: parsed.data.claim_date,
      type: parsed.data.type,
      protocol_number: parsed.data.protocol_number || null,
      status: parsed.data.status,
      description: parsed.data.description,
      created_by: user.id,
    })
    .select('id')
    .single()

  if (error) return { error: { _form: ['Erro ao registrar sinistro.'] } }

  revalidatePath(`/${slug}/seguros/${parsed.data.policy_id}`)
  return { id: data.id }
}
```

**src/lib/actions/endorsements.ts:**
```typescript
'use server'
import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { createEndorsementSchema } from '@/lib/validations/endorsement-schemas'

export async function createEndorsementAction(slug: string, formData: FormData) {
  const raw = Object.fromEntries(formData) as Record<string, unknown>
  const parsed = createEndorsementSchema.safeParse(raw)
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: { _form: ['Sessão expirada.'] } }

  const role = (user.app_metadata as { role?: string })?.role
  if (!['admin', 'corretor'].includes(role ?? '')) {
    return { error: { _form: ['Sem permissão para registrar endossos.'] } }
  }

  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return { error: { _form: ['Tenant não identificado.'] } }

  const { data, error } = await supabase
    .from('endorsements')
    .insert({
      tenant_id: tenantId,
      policy_id: parsed.data.policy_id,
      endorsement_date: parsed.data.endorsement_date,
      type: parsed.data.type,
      description: parsed.data.description,
      premium_impact: parsed.data.premium_impact ?? null,
      created_by: user.id,
    })
    .select('id')
    .single()

  if (error) return { error: { _form: ['Erro ao registrar endosso.'] } }

  revalidatePath(`/${slug}/seguros/${parsed.data.policy_id}`)
  return { id: data.id }
}
```

Para os testes, substituir os it.todo do Plan 03-01 por testes reais usando o mesmo padrão mock de clients.test.ts. Usar os casos listados em `<behavior>` acima. O mock chain do Supabase para policies precisa incluir:
```typescript
const mockPoliciesChain = {
  insert: vi.fn(() => ({ select: vi.fn(() => ({ single: mockSingle })) }))
}
// mockSupabase.from.mockImplementation((table: string) => {
//   if (table === 'policies') return mockPoliciesChain
//   ...
// })
```
  </action>
  <verify>
    <automated>npx vitest run tests/actions/policies.test.ts tests/actions/claims.test.ts tests/actions/endorsements.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - 3 Server Actions criados com 'use server' no topo
    - createPolicyAction desestrutura campos core e usa ...typeSpecific para type_data (grep 'typeSpecific' policies.ts)
    - tenant_id vem exclusivamente de user.app_metadata.tenant_id em todos os 3 Server Actions
    - npx vitest run tests/actions/policies.test.ts tests/actions/claims.test.ts tests/actions/endorsements.test.ts: todos passando
    - revalidatePath chamado com /{slug}/seguros após createPolicyAction e /{slug}/seguros/{policy_id} após claims/endorsements
  </done>
</task>

<task type="auto">
  <name>Task 2: UI do módulo Seguros — listagem, formulário dinâmico, detalhes, componentes</name>
  <read_first>
    - src/app/(app)/[slug]/clientes/page.tsx (padrão Server Component com searchParams + supabase query + filtros)
    - src/app/(app)/[slug]/clientes/novo/new-client-form.tsx (padrão React Hook Form + Server Action no client)
    - src/components/ui/badge.tsx (Badge component a reutilizar no VigenciaBadge)
    - src/components/ui/dialog.tsx (Dialog a reutilizar em claim-dialog e endorsement-dialog)
    - src/components/ui/tabs.tsx (Tabs a reutilizar na tela de detalhes da apólice)
    - src/components/ui/table.tsx (Table a reutilizar em policy-table)
    - src/components/auth/sidebar-shell.tsx (estrutura atual a estender com seção Produtos)
    - .planning/phases/03-seguros-consorcio/03-RESEARCH.md (Exemplo 1 VigenciaBadge, Exemplo 2 sidebar, Exemplo 4 filtros)
  </read_first>
  <files>
    src/app/(app)/[slug]/seguros/page.tsx,
    src/app/(app)/[slug]/seguros/nova/page.tsx,
    src/app/(app)/[slug]/seguros/nova/policy-form.tsx,
    src/app/(app)/[slug]/seguros/[id]/page.tsx,
    src/components/seguros/policy-table.tsx,
    src/components/seguros/vigencia-badge.tsx,
    src/components/seguros/claim-dialog.tsx,
    src/components/seguros/endorsement-dialog.tsx,
    src/components/auth/sidebar-shell.tsx
  </files>
  <action>
Criar todos os arquivos de UI do módulo Seguros.

**src/components/seguros/vigencia-badge.tsx** — componente puro:
```tsx
import { Badge } from '@/components/ui/badge'
import { getVigenciaStatus } from '@/lib/utils/vigencia'
import { cn } from '@/lib/utils'

const statusConfig = {
  verde:    { label: 'Vigente',   className: 'bg-green-100 text-green-800 border-green-200 hover:bg-green-100' },
  amarelo:  { label: 'A vencer',  className: 'bg-yellow-100 text-yellow-800 border-yellow-200 hover:bg-yellow-100' },
  vermelho: { label: 'Vencida',   className: 'bg-red-100 text-red-800 border-red-200 hover:bg-red-100' },
}

export function VigenciaBadge({ vigencia_fim }: { vigencia_fim: string }) {
  const status = getVigenciaStatus(vigencia_fim)
  const { label, className } = statusConfig[status]
  return (
    <Badge variant="outline" className={cn('text-xs font-medium', className)}>
      {label}
    </Badge>
  )
}
```

**src/components/seguros/policy-table.tsx** — Client Component com tabela shadcn:
- Props: `policies: PolicyRow[]`, `slug: string`
- Colunas: número da apólice, tipo (badge com ícone Lucide), seguradora, cliente (nome), corretor (nome), vigência fim (formatada), semáforo (VigenciaBadge), prêmio (Intl BRL)
- Ícones por tipo (lucide-react): `auto`→Car, `vida`→Heart, `residencial`→Home, `empresarial`→Building2, `saude`→HeartPulse, `outros`→Shield
- Ordenação default: vigencia_fim ASC (apólices mais próximas de vencer no topo)
- Linha clicável: `<Link href={/${slug}/seguros/${policy.id}}>` envolvendo a row (ou onClick com router.push)
- Tipo `PolicyRow`: `{ id: string; policy_number: string; type: string; insurer: string; vigencia_fim: string; premio_total: number; client?: { name: string }; profile?: { full_name: string } }`

**src/app/(app)/[slug]/seguros/page.tsx** — Server Component:
```tsx
// Recebe searchParams: type?, insurer?, assigned_to?, status? (verde/amarelo/vermelho), page?
// Query filtrando por searchParams:
const today = new Date().toISOString().split('T')[0]
const thirtyDaysLater = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString().split('T')[0]
const sixtyDaysLater = new Date(Date.now() + 60 * 24 * 60 * 60 * 1000).toISOString().split('T')[0]

let query = supabase
  .from('policies')
  .select('id, policy_number, type, insurer, vigencia_fim, premio_total, client:clients(name), profile:profiles!assigned_to(full_name)')
  .is('deleted_at', null)
  .order('vigencia_fim', { ascending: true })

// Aplicar filtros de searchParams
if (type) query = query.eq('type', type)
if (insurer) query = query.ilike('insurer', `%${insurer}%`)
if (assigned_to) query = query.eq('assigned_to', assigned_to)
if (status === 'vermelho') query = query.lte('vigencia_fim', thirtyDaysLater)
else if (status === 'amarelo') query = query.gt('vigencia_fim', thirtyDaysLater).lte('vigencia_fim', sixtyDaysLater)
else if (status === 'verde') query = query.gt('vigencia_fim', sixtyDaysLater)

// Paginação: página 25 items
const pageNum = Math.max(1, Number(searchParams.page) || 1)
const from = (pageNum - 1) * 25
query = query.range(from, from + 24)
```
Passar policies para `<PolicyTable>` Client Component. Incluir filtros inline (selects para tipo, status vigência; input para seguradora). Estado vazio: mensagem "Nenhuma apólice cadastrada" + botão "Cadastrar apólice" → /nova.

**src/app/(app)/[slug]/seguros/nova/policy-form.tsx** — Client Component React Hook Form:
- `useForm` com `resolver: zodResolver(createPolicySchema)` — mas criar schema parcial sem discriminação para uso direto no form client-side (o Server Action valida com o schema completo)
- `watch('type')` para controle do tipo selecionado
- `useEffect(() => { reset({ type: newType }) }, [type])` ao trocar tipo — CRÍTICO para limpar campos (Pitfall 6)
- Select de tipo: auto, vida, residencial, empresarial, saúde, outros
- Seção condicional: renderiza campos específicos do tipo apenas quando `type === 'auto'`, `type === 'vida'`, etc.
- Campos obrigatórios presentes sempre: policy_number, insurer, vigencia_inicio (calendar), vigencia_fim (calendar), premio_total (number input), client_id (select com clientes do tenant), assigned_to (select com corretores/admin do tenant)
- Submit via `createPolicyAction(slug, formData)` — usando `new FormData(event.target)` ou `handleSubmit` do RHF convertendo para FormData
- Erro 23505 retorna `error.policy_number` → exibir inline sob o campo

**src/app/(app)/[slug]/seguros/[id]/page.tsx** — Server Component:
- Busca apólice completa: `supabase.from('policies').select('*, client:clients(name, type), profile:profiles!assigned_to(full_name)').eq('id', policyId).is('deleted_at', null).single()`
- Se not found ou deleted → `notFound()`
- Busca claims: `supabase.from('claims').select('*').eq('policy_id', policyId).is('deleted_at', null).order('claim_date', { ascending: false })`
- Busca endorsements: `supabase.from('endorsements').select('*').eq('policy_id', policyId).is('deleted_at', null).order('endorsement_date', { ascending: false })`
- Layout: card com dados core da apólice no topo (type_data em seção expansível), Tabs com "Sinistros" e "Endossos"
- Botão "Registrar sinistro" → `<ClaimDialog>`, botão "Registrar endosso" → `<EndorsementDialog>`

**src/components/seguros/claim-dialog.tsx** — Client Component:
- Props: `slug: string`, `policyId: string`
- Dialog com form: claim_date (date input), type (text input — "colisão", "roubo", "incêndio", etc.), protocol_number (opcional), description (textarea), status (select: aberto/em_análise/encerrado, default: aberto)
- Submit via `createClaimAction(slug, fd)` com `new FormData`
- Fechar dialog após sucesso, toast sonner "Sinistro registrado"

**src/components/seguros/endorsement-dialog.tsx** — Client Component:
- Props: `slug: string`, `policyId: string`
- Dialog com form: endorsement_date, type (select: inclusão/exclusão/alteração), description (textarea), premium_impact (number, opcional)
- Submit via `createEndorsementAction(slug, fd)`
- Toast sonner "Endosso registrado" após sucesso

**src/components/auth/sidebar-shell.tsx** — estender navItems (per D-02 / Claude's Discretion):
Adicionar após o item 'Clientes' e antes de 'Configurações', um label de seção estático + 2 novos links:
```typescript
import { Shield, CircleDollarSign } from 'lucide-react'
// Adicionar em navItems após Clientes:
// Seção label (não clicável, implementar como div estática no render):
{
  label: 'Produtos',
  sectionLabel: true, // flag para renderizar como label de seção
  icon: null,
  href: '',
},
{
  label: 'Seguros',
  href: `/${slug}/seguros`,
  icon: <Shield size={16} />,
},
{
  label: 'Consórcio',
  href: `/${slug}/consorcio`,
  icon: <CircleDollarSign size={16} />,
},
```
Renderizar o `sectionLabel: true` como `<div className="px-3 py-1 mt-2 text-xs font-semibold text-muted-foreground uppercase tracking-wider">Produtos</div>` — não clicável, não active. Implementar a interface NavItem com `sectionLabel?: boolean` opcional.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep "error TS" | head -20 || echo "TypeScript OK"</automated>
  </verify>
  <done>
    - src/components/seguros/vigencia-badge.tsx: importa getVigenciaStatus + Badge, exporta VigenciaBadge
    - src/app/(app)/[slug]/seguros/page.tsx: Server Component com filtros via searchParams, query com .is('deleted_at', null), passa dados para PolicyTable
    - src/app/(app)/[slug]/seguros/nova/policy-form.tsx: 'use client', watch('type'), useEffect com reset ao trocar tipo
    - src/app/(app)/[slug]/seguros/[id]/page.tsx: Server Component busca apólice + claims + endorsements
    - src/components/seguros/claim-dialog.tsx e endorsement-dialog.tsx: 'use client', Dialog shadcn
    - src/components/auth/sidebar-shell.tsx: contém Shield e CircleDollarSign imports, links /seguros e /consorcio
    - npm run typecheck sem novos erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormData → createPolicyAction | Todos os campos do formulário dinâmico chegam não-confiáveis |
| assigned_to field | Corretor poderia tentar atribuir apólice a outro usuário via FormData |
| policy_id em claims/endorsements | Usuário poderia referenciar policy_id de outro tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | createPolicyAction — assigned_to | mitigate | Guard explícito `role === 'corretor' && assigned_to !== user.id` antes de qualquer DB call (Linha ~15 de policies.ts) |
| T-03-08 | Tampering | type_data JSONB injection | mitigate | Zod discriminatedUnion valida e enumera explicitamente cada campo por tipo — campos desconhecidos descartados pelo spread typeSpecific (apenas campos validados entram) |
| T-03-09 | Information Disclosure | policies SELECT — corretor vê apólice de outro | mitigate | RLS policies_select: `assigned_to = (SELECT auth.uid())` para role=corretor — defense-in-depth + Server Component não passa dados além do permitido pelo RLS |
| T-03-10 | Spoofing | claims/endorsements — policy_id de outro tenant | mitigate | RLS claims_insert exige `tenant_id = jwt_tenant_id()` + policy_id referencia policy que o RLS policies_select já filtra pelo tenant |
| T-03-11 | Tampering | Formulário resetar campos ao trocar tipo (Pitfall 6) | mitigate | useEffect chama reset({type: newType}) — campos do tipo anterior não chegam ao Server Action |
</threat_model>

<verification>
```bash
# 1. Server Actions com 'use server' no topo
head -1 src/lib/actions/policies.ts    # deve ser "'use server'"
head -1 src/lib/actions/claims.ts      # deve ser "'use server'"
head -1 src/lib/actions/endorsements.ts # deve ser "'use server'"

# 2. Desestruturação crítica no createPolicyAction
grep "typeSpecific\|type_data" src/lib/actions/policies.ts

# 3. tenant_id do JWT (nunca do FormData)
grep "app_metadata.*tenant_id" src/lib/actions/policies.ts
grep "app_metadata.*tenant_id" src/lib/actions/claims.ts

# 4. Rotas criadas
ls src/app/\(app\)/\[slug\]/seguros/page.tsx
ls src/app/\(app\)/\[slug\]/seguros/nova/policy-form.tsx
ls src/app/\(app\)/\[slug\]/seguros/\[id\]/page.tsx

# 5. Componentes criados
ls src/components/seguros/vigencia-badge.tsx
ls src/components/seguros/claim-dialog.tsx
ls src/components/seguros/endorsement-dialog.tsx

# 6. Sidebar estendida com Seguros e Consórcio
grep "seguros\|consorcio\|Produtos\|Shield\|CircleDollarSign" src/components/auth/sidebar-shell.tsx

# 7. Testes passando
npx vitest run tests/actions/policies.test.ts tests/actions/claims.test.ts tests/actions/endorsements.test.ts

# 8. TypeScript limpo
npm run typecheck 2>&1 | grep "error TS" || echo "OK"
```
</verification>

<success_criteria>
- createPolicyAction: desestruturação core/type_data correta, guard de corretor, 23505 tratado
- createClaimAction e createEndorsementAction: tenant_id do JWT, guards de role, revalidatePath correto
- VigenciaBadge renderiza verde/amarelo/vermelho via getVigenciaStatus
- Formulário dinâmico: seção condicional por tipo + reset ao trocar tipo (Pitfall 6 mitigado)
- Sidebar contém links /seguros e /consorcio com ícones Shield/CircleDollarSign e label "Produtos"
- npx vitest run tests/actions/policies.test.ts tests/actions/claims.test.ts tests/actions/endorsements.test.ts: todos passando
- npm run typecheck sem novos erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-seguros-consorcio/03-02-SUMMARY.md` seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
