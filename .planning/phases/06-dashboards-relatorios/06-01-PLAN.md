---
phase: 06-dashboards-relatorios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/utils/dashboard-queries.ts
  - tests/utils/dashboard-queries.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
tags: [dashboard, helpers, tests, wave0, exceljs]

must_haves:
  truths:
    - "exceljs 4.4.0 esta instalado e disponivel para imports server-side"
    - "Helpers puros (sem Supabase) para agregar broker ranking, dedup carteira ativa e calcular ranges de data existem em src/lib/utils/dashboard-queries.ts"
    - "Suite Vitest tests/utils/dashboard-queries.test.ts existe, contem stubs nomeados para DASH-01, DASH-02, DASH-03, DASH-09, e roda em <15s"
    - "RBAC helper isExecutiveRole(role) retorna true apenas para 'admin' e 'financeiro'"
  artifacts:
    - path: "package.json"
      provides: "Declaracao de dependencia exceljs ^4.4.0"
      contains: "\"exceljs\""
    - path: "src/lib/utils/dashboard-queries.ts"
      provides: "aggregateBrokerRanking, dedupeClientIds, parseSelectedMonth, isExecutiveRole, ALLOWED_EXPORT_TYPES"
      exports: ["aggregateBrokerRanking", "dedupeClientIds", "parseSelectedMonth", "isExecutiveRole", "ALLOWED_EXPORT_TYPES"]
      min_lines: 40
    - path: "tests/utils/dashboard-queries.test.ts"
      provides: "Wave 0 stubs cobrindo agrupamento, dedup, parse de mes e RBAC"
      contains: "describe('aggregateBrokerRanking'"
  key_links:
    - from: "src/lib/utils/dashboard-queries.ts"
      to: "tests/utils/dashboard-queries.test.ts"
      via: "import { aggregateBrokerRanking, dedupeClientIds, parseSelectedMonth, isExecutiveRole, ALLOWED_EXPORT_TYPES }"
      pattern: "from '@/lib/utils/dashboard-queries'"
    - from: "package.json"
      to: "node_modules/exceljs"
      via: "npm install exceljs"
      pattern: "\"exceljs\""
---

<objective>
Estabelecer a fundacao da Phase 6: instalar `exceljs@^4.4.0` (escolhido em RESEARCH.md vs xlsx@0.18.5 com CVE-2023-30533), criar helpers puros para o dashboard executivo (agrupamento de ranking, dedup de carteira, parse de mes, whitelist RBAC) e a suite Wave 0 de testes Vitest cobrindo DASH-01/02/03/09.

**Purpose:** Tudo que Plan 02 (dashboard) e Plan 03 (export) consomem em paralelo na Wave 2 deve estar pronto e testado em Wave 1 — exceljs no node_modules, helpers como fonte unica de verdade para agrupamento e RBAC, e stubs de teste falhando que servem de contrato para a implementacao.

**Output:**
- `package.json` com `exceljs` declarado
- `src/lib/utils/dashboard-queries.ts` com 5 funcoes puras exportadas
- `tests/utils/dashboard-queries.test.ts` com >=15 stubs/casos cobrindo cada funcao
</objective>

<execution_context>
@C:/Users/Reinaldo - Local/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Reinaldo - Local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dashboards-relatorios/06-CONTEXT.md
@.planning/phases/06-dashboards-relatorios/06-RESEARCH.md
@.planning/phases/06-dashboards-relatorios/06-VALIDATION.md
@tests/utils/commission-rate.test.ts
@tests/setup.ts
@package.json

<interfaces>
<!-- Test framework already in place — Vitest 2.1.9 com setup.ts em tests/setup.ts -->
<!-- Padrao de teste puro existente (ver commission-rate.test.ts): -->
```typescript
// tests/utils/commission-rate.test.ts (referencia)
import { describe, it, expect } from 'vitest'
import { resolveCommissionRate } from '@/lib/utils/commission-rate'

describe('resolveCommissionRate', () => {
  it('...', () => { expect(...).toBe(...) })
})
```

<!-- Tipos exportados pelo helper que sera criado: -->
```typescript
// src/lib/utils/dashboard-queries.ts (a CRIAR neste plano)
export type BrokerRankingRow = {
  brokerId: string
  fullName: string
  productionCount: number
  commissionTotal: number
}

export type ProfileRow = { id: string; full_name: string }
export type CommissionRow = { broker_id: string; amount: number | string }
export type ProductionRow = { assigned_to: string }

export function aggregateBrokerRanking(
  profiles: ProfileRow[],
  commissions: CommissionRow[],
  productions: ProductionRow[],
): BrokerRankingRow[]

export function dedupeClientIds(
  rows: Array<{ client_id: string | null }>[]
): Set<string>

export function parseSelectedMonth(
  monthParam: string | undefined,
  today?: Date,
): { monthStartStr: string; monthEndStr: string; monthValue: string; monthLabel: string }

export function isExecutiveRole(role: string | null | undefined): boolean

export const ALLOWED_EXPORT_TYPES: readonly ['apolices', 'clientes', 'comissoes']
```

<!-- Padrao package.json existente — confirmar formato semver: -->
```json
"exceljs": "^4.4.0"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar exceljs 4.4.0 e criar helpers puros do dashboard</name>
  <files>package.json, package-lock.json, src/lib/utils/dashboard-queries.ts</files>
  <read_first>
    - package.json (estado atual de deps)
    - .planning/phases/06-dashboards-relatorios/06-RESEARCH.md (decisao exceljs vs xlsx — Padrao 5 / Anti-Patterns)
    - src/lib/utils/commission-rate.ts (padrao de helper puro com testes)
    - src/app/(app)/[slug]/corretores/page.tsx (referencia de productionMap — agrupamento atual)
    - src/app/(app)/[slug]/corretores/[id]/page.tsx (referencia de Set dedup linhas 89-104)
    - src/app/(app)/[slug]/financeiro/page.tsx (referencia parseSelectedMonth — linhas 45-54)
  </read_first>
  <behavior>
    - **aggregateBrokerRanking(profiles, commissions, productions):** retorna `BrokerRankingRow[]` com 1 linha por profile, somando `amount` (Number coerce) por `broker_id` e contando ocorrencias por `assigned_to`. Ordena DESC por `commissionTotal`, tiebreak por `productionCount` DESC, tiebreak por `fullName` ASC. Profiles sem comissao/producao aparecem com 0/0. Brokers em commissions/productions sem profile correspondente sao ignorados (nao geram linha).
    - **dedupeClientIds(arrays):** recebe array de arrays de `{client_id}`. Retorna `Set<string>` com client_ids nao nulos de todas as fontes. Garante dedup quando mesmo client aparece em policies + quotas.
    - **parseSelectedMonth(monthParam, today=new Date()):** se monthParam = `"2026-04"` retorna `monthStartStr="2026-04-01"`, `monthEndStr="2026-04-30"`, `monthValue="2026-04"`, `monthLabel="abril 2026"` (ptBR). Se monthParam invalido ou undefined, faz fallback para `startOfMonth(today)`. Lida com Invalid Date sem throw.
    - **isExecutiveRole(role):** retorna `true` apenas para `'admin'` e `'financeiro'`. Qualquer outro valor (incluindo null/undefined/'corretor'/'visualizador') retorna `false`.
    - **ALLOWED_EXPORT_TYPES:** const `['apolices', 'clientes', 'comissoes'] as const` — usado por Plan 03 como whitelist do query param `?type=`.
  </behavior>
  <action>
Passo 1 — Instalar exceljs:
```bash
npm install exceljs@^4.4.0
```
Verificar que `package.json` agora contem `"exceljs": "^4.4.0"` em `dependencies` (nao em devDependencies). Verificar que `node_modules/exceljs/package.json` existe e reporta `"version": "4.4.0"` (ou patch >=).

Passo 2 — Criar `src/lib/utils/dashboard-queries.ts` exportando exatamente:

```typescript
import { startOfMonth, endOfMonth, format, parse, isValid } from 'date-fns'
import { ptBR } from 'date-fns/locale'

export type BrokerRankingRow = {
  brokerId: string
  fullName: string
  productionCount: number
  commissionTotal: number
}

export type ProfileRow = { id: string; full_name: string }
export type CommissionRow = { broker_id: string; amount: number | string }
export type ProductionRow = { assigned_to: string }

export const ALLOWED_EXPORT_TYPES = ['apolices', 'clientes', 'comissoes'] as const
export type AllowedExportType = (typeof ALLOWED_EXPORT_TYPES)[number]

export function isExecutiveRole(role: string | null | undefined): boolean {
  return role === 'admin' || role === 'financeiro'
}

export function aggregateBrokerRanking(
  profiles: ProfileRow[],
  commissions: CommissionRow[],
  productions: ProductionRow[],
): BrokerRankingRow[] {
  const commissionMap = new Map<string, number>()
  for (const c of commissions) {
    const v = Number(c.amount) || 0
    commissionMap.set(c.broker_id, (commissionMap.get(c.broker_id) ?? 0) + v)
  }
  const productionMap = new Map<string, number>()
  for (const p of productions) {
    productionMap.set(p.assigned_to, (productionMap.get(p.assigned_to) ?? 0) + 1)
  }
  const rows: BrokerRankingRow[] = profiles.map((p) => ({
    brokerId: p.id,
    fullName: p.full_name,
    productionCount: productionMap.get(p.id) ?? 0,
    commissionTotal: commissionMap.get(p.id) ?? 0,
  }))
  rows.sort((a, b) => {
    if (b.commissionTotal !== a.commissionTotal) return b.commissionTotal - a.commissionTotal
    if (b.productionCount !== a.productionCount) return b.productionCount - a.productionCount
    return a.fullName.localeCompare(b.fullName, 'pt-BR')
  })
  return rows
}

export function dedupeClientIds(
  arrays: Array<Array<{ client_id: string | null }>>,
): Set<string> {
  const set = new Set<string>()
  for (const arr of arrays) {
    for (const r of arr) {
      if (r.client_id) set.add(r.client_id)
    }
  }
  return set
}

export function parseSelectedMonth(
  monthParam: string | undefined,
  today: Date = new Date(),
): { monthStartStr: string; monthEndStr: string; monthValue: string; monthLabel: string } {
  let baseDate = startOfMonth(today)
  if (monthParam) {
    const parsed = parse(monthParam + '-01', 'yyyy-MM-dd', today)
    if (isValid(parsed)) baseDate = startOfMonth(parsed)
  }
  return {
    monthStartStr: format(baseDate, 'yyyy-MM-dd'),
    monthEndStr: format(endOfMonth(baseDate), 'yyyy-MM-dd'),
    monthValue: format(baseDate, 'yyyy-MM'),
    monthLabel: format(baseDate, 'MMMM yyyy', { locale: ptBR }),
  }
}
```

NOTAS:
- `Number(c.amount) || 0` lida com `amount` vindo como string do Postgres NUMERIC.
- `parseSelectedMonth` aceita `today` injetavel para testes deterministicos.
- `localeCompare(... 'pt-BR')` ordena nomes com acentuacao corretamente.
- Reutilizar `date-fns` que ja esta em deps (v4.1.0).

Passo 3 — Verificar imports limpos com `npx tsc --noEmit` (deve nao adicionar novos erros alem dos 44 pre-existentes — ver 05-03-SUMMARY).
  </action>
  <verify>
    <automated>npm install exceljs@^4.4.0 && npx tsc --noEmit 2>&1 | grep -E "src/lib/utils/dashboard-queries" || echo "no new errors"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contem string literal `"exceljs": "^4.4.0"` em `dependencies` (verificavel com `grep '"exceljs"' package.json`)
    - `node_modules/exceljs/package.json` existe (verificavel com `test -f node_modules/exceljs/package.json`)
    - `src/lib/utils/dashboard-queries.ts` existe e exporta os 5 simbolos: `aggregateBrokerRanking`, `dedupeClientIds`, `parseSelectedMonth`, `isExecutiveRole`, `ALLOWED_EXPORT_TYPES` (verificavel com `grep -E "^export (function|const|type) (aggregateBrokerRanking|dedupeClientIds|parseSelectedMonth|isExecutiveRole|ALLOWED_EXPORT_TYPES)" src/lib/utils/dashboard-queries.ts | wc -l` >= 5)
    - `npx tsc --noEmit src/lib/utils/dashboard-queries.ts` nao retorna erro novo
    - Imports usam alias `@/...` ou `date-fns` (sem `../../../` relativos)
  </acceptance_criteria>
  <done>
    - exceljs no package.json e node_modules
    - 5 helpers exportados em src/lib/utils/dashboard-queries.ts
    - Tipos publicos `BrokerRankingRow`, `AllowedExportType` exportados
    - Sem novos erros TypeScript
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite Wave 0 — testes Vitest cobrindo helpers e RBAC</name>
  <files>tests/utils/dashboard-queries.test.ts</files>
  <read_first>
    - tests/utils/commission-rate.test.ts (referencia de estilo describe/it/expect)
    - tests/utils/vigencia.test.ts (referencia de teste puro com datas)
    - src/lib/utils/dashboard-queries.ts (do Task 1 — origem dos imports)
    - .planning/phases/06-dashboards-relatorios/06-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <behavior>
    Cobertura por requisito da phase:

    **DASH-01 (KPI cards corretos por tenant)** — testa que `parseSelectedMonth` retorna ranges corretos para mes corrente, mes anterior e mes invalido.

    **DASH-02 (ranking agrega corretamente)** — testa `aggregateBrokerRanking` com:
    - 3 corretores, comissoes e producao distintas → ordem correta (DESC commission)
    - Tiebreak por productionCount quando comissoes iguais
    - Tiebreak alfabetico quando comissao e producao iguais
    - Profile sem entries (commission e production vazios) → linha com 0/0
    - Commission `amount` como string (Postgres NUMERIC) → coerce para numero
    - Multiplos commissions para mesmo broker → soma
    - broker_id em commissions sem profile correspondente → ignorado

    **DASH-03 (alertas — dedup carteira)** — testa `dedupeClientIds`:
    - Arrays vazios → Set vazio
    - client_id null → ignorado
    - Mesmo client em 2 fontes (policies + quotas) → 1 entrada no Set

    **DASH-09 (RBAC)** — testa `isExecutiveRole`:
    - 'admin' → true
    - 'financeiro' → true
    - 'corretor' → false
    - 'visualizador' → false
    - null → false
    - undefined → false
    - '' → false

    **Whitelist export** — testa `ALLOWED_EXPORT_TYPES`:
    - inclui 'apolices', 'clientes', 'comissoes'
    - tem comprimento 3 (nenhum tipo extra)
    - eh readonly (typeof check)
  </behavior>
  <action>
Criar `tests/utils/dashboard-queries.test.ts` com este esqueleto exato (todos os testes devem PASSAR ja na implementacao do Task 1 — Wave 0 = stubs falham apenas se o helper estiver incompleto):

```typescript
import { describe, it, expect } from 'vitest'
import {
  aggregateBrokerRanking,
  dedupeClientIds,
  parseSelectedMonth,
  isExecutiveRole,
  ALLOWED_EXPORT_TYPES,
  type ProfileRow,
  type CommissionRow,
  type ProductionRow,
} from '@/lib/utils/dashboard-queries'

describe('aggregateBrokerRanking (DASH-02)', () => {
  const profiles: ProfileRow[] = [
    { id: 'p1', full_name: 'Alice' },
    { id: 'p2', full_name: 'Bruno' },
    { id: 'p3', full_name: 'Carlos' },
  ]

  it('ordena DESC por comissao quando valores diferem', () => {
    const commissions: CommissionRow[] = [
      { broker_id: 'p1', amount: 100 },
      { broker_id: 'p2', amount: 500 },
      { broker_id: 'p3', amount: 250 },
    ]
    const rows = aggregateBrokerRanking(profiles, commissions, [])
    expect(rows.map((r) => r.brokerId)).toEqual(['p2', 'p3', 'p1'])
  })

  it('tiebreak por producao quando comissoes iguais', () => {
    const commissions: CommissionRow[] = [
      { broker_id: 'p1', amount: 100 },
      { broker_id: 'p2', amount: 100 },
    ]
    const productions: ProductionRow[] = [
      { assigned_to: 'p1' },
      { assigned_to: 'p1' },
      { assigned_to: 'p2' },
    ]
    const rows = aggregateBrokerRanking(profiles, commissions, productions)
    // p1 deve vir antes de p2 (mais producao com mesma comissao)
    const p1 = rows.findIndex((r) => r.brokerId === 'p1')
    const p2 = rows.findIndex((r) => r.brokerId === 'p2')
    expect(p1).toBeLessThan(p2)
  })

  it('tiebreak alfabetico quando comissao e producao iguais', () => {
    const rows = aggregateBrokerRanking(profiles, [], [])
    expect(rows.map((r) => r.fullName)).toEqual(['Alice', 'Bruno', 'Carlos'])
  })

  it('profile sem entries -> linha com 0/0', () => {
    const rows = aggregateBrokerRanking(profiles, [], [])
    for (const r of rows) {
      expect(r.commissionTotal).toBe(0)
      expect(r.productionCount).toBe(0)
    }
  })

  it('amount como string (Postgres NUMERIC) -> coerce para numero', () => {
    const commissions: CommissionRow[] = [
      { broker_id: 'p1', amount: '123.45' as unknown as number },
    ]
    const rows = aggregateBrokerRanking(profiles, commissions, [])
    expect(rows.find((r) => r.brokerId === 'p1')?.commissionTotal).toBeCloseTo(123.45)
  })

  it('multiplos commissions para mesmo broker -> soma', () => {
    const commissions: CommissionRow[] = [
      { broker_id: 'p1', amount: 100 },
      { broker_id: 'p1', amount: 50 },
      { broker_id: 'p1', amount: 25 },
    ]
    const rows = aggregateBrokerRanking(profiles, commissions, [])
    expect(rows.find((r) => r.brokerId === 'p1')?.commissionTotal).toBe(175)
  })

  it('broker_id em commissions sem profile correspondente -> ignorado', () => {
    const commissions: CommissionRow[] = [
      { broker_id: 'p1', amount: 100 },
      { broker_id: 'ghost', amount: 999 },
    ]
    const rows = aggregateBrokerRanking(profiles, commissions, [])
    expect(rows).toHaveLength(3)
    expect(rows.find((r) => r.brokerId === 'ghost')).toBeUndefined()
  })
})

describe('dedupeClientIds (DASH-03)', () => {
  it('arrays vazios -> Set vazio', () => {
    expect(dedupeClientIds([]).size).toBe(0)
    expect(dedupeClientIds([[], []]).size).toBe(0)
  })

  it('client_id null e ignorado', () => {
    const set = dedupeClientIds([[{ client_id: 'c1' }, { client_id: null }]])
    expect(set.size).toBe(1)
    expect(set.has('c1')).toBe(true)
  })

  it('mesmo client em 2 fontes (policies + quotas) -> 1 entrada', () => {
    const set = dedupeClientIds([
      [{ client_id: 'c1' }, { client_id: 'c2' }],
      [{ client_id: 'c1' }, { client_id: 'c3' }],
    ])
    expect(set.size).toBe(3)
    expect(Array.from(set).sort()).toEqual(['c1', 'c2', 'c3'])
  })
})

describe('parseSelectedMonth (DASH-01)', () => {
  const today = new Date('2026-04-15T12:00:00Z')

  it('monthParam undefined -> fallback para mes corrente', () => {
    const r = parseSelectedMonth(undefined, today)
    expect(r.monthStartStr).toBe('2026-04-01')
    expect(r.monthEndStr).toBe('2026-04-30')
    expect(r.monthValue).toBe('2026-04')
  })

  it('monthParam valido -> usa o mes informado', () => {
    const r = parseSelectedMonth('2026-02', today)
    expect(r.monthStartStr).toBe('2026-02-01')
    expect(r.monthEndStr).toBe('2026-02-28')
    expect(r.monthValue).toBe('2026-02')
  })

  it('monthParam invalido -> fallback graceful', () => {
    const r = parseSelectedMonth('garbage', today)
    expect(r.monthStartStr).toBe('2026-04-01')
    expect(r.monthValue).toBe('2026-04')
  })

  it('monthLabel em pt-BR', () => {
    const r = parseSelectedMonth('2026-04', today)
    expect(r.monthLabel.toLowerCase()).toContain('abril')
  })
})

describe('isExecutiveRole (DASH-09)', () => {
  it('admin -> true', () => expect(isExecutiveRole('admin')).toBe(true))
  it('financeiro -> true', () => expect(isExecutiveRole('financeiro')).toBe(true))
  it('corretor -> false', () => expect(isExecutiveRole('corretor')).toBe(false))
  it('visualizador -> false', () => expect(isExecutiveRole('visualizador')).toBe(false))
  it('null -> false', () => expect(isExecutiveRole(null)).toBe(false))
  it('undefined -> false', () => expect(isExecutiveRole(undefined)).toBe(false))
  it('string vazia -> false', () => expect(isExecutiveRole('')).toBe(false))
})

describe('ALLOWED_EXPORT_TYPES (whitelist)', () => {
  it('contem exatamente 3 tipos', () => {
    expect(ALLOWED_EXPORT_TYPES).toHaveLength(3)
  })
  it('inclui apolices, clientes, comissoes', () => {
    expect(ALLOWED_EXPORT_TYPES).toContain('apolices')
    expect(ALLOWED_EXPORT_TYPES).toContain('clientes')
    expect(ALLOWED_EXPORT_TYPES).toContain('comissoes')
  })
})
```

NOTAS:
- O cast `as unknown as number` para testar string amount evita strict-mode TS errors mantendo o teste honesto.
- Datas `'2026-04-15T12:00:00Z'` evita drift de timezone.
- `toBeCloseTo` para floats (NUMERIC do Postgres pode ter precisao).
  </action>
  <verify>
    <automated>npm test -- tests/utils/dashboard-queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/utils/dashboard-queries.test.ts` existe (verificavel com `test -f`)
    - `npm test -- tests/utils/dashboard-queries.test.ts` reporta 0 falhas e >=15 testes (verificavel via stdout do Vitest: `Test Files 1 passed`)
    - Runtime total da suite <15s
    - Todos os 5 simbolos importados estao em uso (sem `unused import`)
    - Cobertura inclui pelo menos 1 `describe` por requisito (DASH-01, DASH-02, DASH-03, DASH-09 + whitelist)
  </acceptance_criteria>
  <done>
    - Vitest reporta `passed` em todos os blocos describe
    - Suite cobre as 4 requisitos identificados em 06-VALIDATION.md (DASH-01/02/03/09) + whitelist do export
    - Helpers do Task 1 sao exercitados ponta-a-ponta
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → npm registry | exceljs e baixado do registry npm publico — supply-chain |
| code → tests | helpers de RBAC sao codigo critico de seguranca; testes sao o gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00 | Supply chain (Tampering) | exceljs npm package | accept | Versao pinada `^4.4.0` (4.4.0 publicado em 2024-12-20). Sem vulnerabilidades criticas server-side write-only. Auditavel via `npm audit` em CI futuro. |
| T-06-01a | Information Disclosure | isExecutiveRole helper | mitigate | Whitelist explicita `role === 'admin' || role === 'financeiro'` — qualquer outro valor (incluindo strings derivadas de user input) retorna false. Testado em DASH-09. |
| T-06-01b | Tampering | ALLOWED_EXPORT_TYPES const | mitigate | `as const` readonly tuple — Plan 03 importa exatamente esta whitelist; nao pode ser estendida em runtime. Testado para garantir comprimento 3. |
| T-06-01c | Tampering | parseSelectedMonth | mitigate | Input invalido (string arbitraria) cai em `isValid` check e fallback para mes corrente — sem throw, sem injecao SQL (helper puro nao toca banco). Testado em DASH-01. |
</threat_model>

<verification>
Verificacao da phase:
- `npm test -- tests/utils/dashboard-queries.test.ts` passa com 0 falhas
- `grep '"exceljs"' package.json` retorna `"exceljs": "^4.4.0"`
- `npx tsc --noEmit` nao adiciona erros novos (tolerancia: 44 pre-existentes ja conhecidos do 05-03)
- Plans 02 e 03 podem importar de `@/lib/utils/dashboard-queries` sem fallback
</verification>

<success_criteria>
- exceljs 4.4.0 instalado e disponivel para Plan 03
- 5 helpers puros exportados de `src/lib/utils/dashboard-queries.ts`
- Suite Vitest verde com >=15 testes cobrindo DASH-01/02/03/09 + whitelist export
- Sem novos erros TypeScript
- Plan 02 e Plan 03 desbloqueados para Wave 2
</success_criteria>

<output>
Apos completion, criar `.planning/phases/06-dashboards-relatorios/06-01-SUMMARY.md` com:
- Lista exata de simbolos exportados de dashboard-queries.ts
- Comando vitest com count de testes passando
- Versao exata de exceljs instalada (npm ls exceljs)
- Confirmar que a suite roda em <15s (validation gate)
</output>
