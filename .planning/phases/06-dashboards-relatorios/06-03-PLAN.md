---
phase: 06-dashboards-relatorios
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/export/export-button.tsx
  - src/app/api/[slug]/export/route.ts
  - src/app/(app)/[slug]/seguros/page.tsx
  - src/app/(app)/[slug]/clientes/page.tsx
  - src/app/(app)/[slug]/corretores/page.tsx
autonomous: true
requirements:
  - DASH-04
tags: [export, excel, route-handler, exceljs, rbac, listings]

must_haves:
  truths:
    - "Admin/financeiro logado clica em 'Exportar Excel' em /[slug]/seguros e baixa apolices.xlsx com todas as apolices que casam com os filtros ativos"
    - "Mesmo botao em /[slug]/clientes baixa clientes.xlsx respeitando filtros"
    - "Mesmo botao em /[slug]/corretores baixa comissoes.xlsx (1 linha por corretor com producao+comissao do mes corrente)"
    - "Usuario com role corretor ou visualizador NAO ve o botao Exportar Excel"
    - "Tentativa direta a /api/{slug}/export?type=apolices por corretor retorna 403"
    - "Tentativa de tampering: usuario do tenant A tentando /api/tenantB/export retorna 403"
    - "Tentativa de injetar ?type=evil retorna 400"
    - "Arquivo .xlsx baixado abre corretamente no Excel/Numbers/LibreOffice com cabecalho em negrito"
  artifacts:
    - path: "src/app/api/[slug]/export/route.ts"
      provides: "Route Handler GET com whitelist type, RBAC, slug check, geracao via exceljs"
      contains: "ALLOWED_EXPORT_TYPES"
      min_lines: 100
    - path: "src/components/export/export-button.tsx"
      provides: "Client Component renderizando <a href download> sobre Button shadcn"
      contains: "ExportButton"
    - path: "src/app/(app)/[slug]/seguros/page.tsx"
      provides: "Botao Exportar Excel no header com searchParams forwarded — visivel apenas para admin/financeiro"
      contains: "ExportButton"
    - path: "src/app/(app)/[slug]/clientes/page.tsx"
      provides: "Botao Exportar Excel no header — visivel apenas para admin/financeiro"
      contains: "ExportButton"
    - path: "src/app/(app)/[slug]/corretores/page.tsx"
      provides: "Botao Exportar Excel no header — visivel apenas para admin/financeiro"
      contains: "ExportButton"
  key_links:
    - from: "src/components/export/export-button.tsx"
      to: "src/app/api/[slug]/export/route.ts"
      via: "<a href={`/api/${slug}/export?type=...`} download>"
      pattern: "/api/\\$\\{slug\\}/export"
    - from: "src/app/api/[slug]/export/route.ts"
      to: "src/lib/utils/dashboard-queries.ts"
      via: "import { ALLOWED_EXPORT_TYPES, isExecutiveRole }"
      pattern: "from '@/lib/utils/dashboard-queries'"
    - from: "src/app/api/[slug]/export/route.ts"
      to: "exceljs node_module"
      via: "import ExcelJS from 'exceljs'"
      pattern: "from 'exceljs'"
---

<objective>
Implementar a capacidade de export Excel — DASH-04. Tres botoes "Exportar Excel" contextuais (D-07) nas listagens de Seguros, Clientes e Corretores; um Route Handler unificado em `/api/[slug]/export?type=...&...` com RBAC, whitelist de tipo (T-06-22), validacao de slug (T-06-21), filtros forwarded a partir dos searchParams (D-08), gerando .xlsx com exceljs (instalado em Plan 01).

**Purpose:** Cumpre DASH-04. Admin e financeiro podem exportar dados filtrados visiveis na tela como arquivo .xlsx pronto para abrir no Excel — sem pagina de relatorios separada (deferred), sem PDF (deferred), respeitando exatamente o que o usuario ve.

**Output:**
- Route Handler unico em `src/app/api/[slug]/export/route.ts`
- Client Component `ExportButton` reutilizavel
- 3 listagens existentes recebem o botao com searchParams forwarded
</objective>

<execution_context>
@C:/Users/Reinaldo - Local/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Reinaldo - Local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboards-relatorios/06-CONTEXT.md
@.planning/phases/06-dashboards-relatorios/06-RESEARCH.md
@.planning/phases/06-dashboards-relatorios/06-UI-SPEC.md
@.planning/phases/06-dashboards-relatorios/06-01-PLAN.md
@src/lib/supabase/server.ts
@src/app/(app)/[slug]/seguros/page.tsx
@src/app/(app)/[slug]/clientes/page.tsx
@src/app/(app)/[slug]/corretores/page.tsx
@src/components/ui/button.tsx

<interfaces>
<!-- Interfaces consumidas (Plan 01 + supabase server) -->

```typescript
// src/lib/utils/dashboard-queries.ts (Plan 01)
export const ALLOWED_EXPORT_TYPES: readonly ['apolices', 'clientes', 'comissoes']
export type AllowedExportType = (typeof ALLOWED_EXPORT_TYPES)[number]
export function isExecutiveRole(role: string | null | undefined): boolean

// src/lib/supabase/server.ts
export async function createClient(): Promise<SupabaseClient>
// Cookies do request sao acessados internamente — funciona em Route Handler do App Router

// exceljs (instalado Plan 01)
// import ExcelJS from 'exceljs'
// new ExcelJS.Workbook()
// workbook.addWorksheet(name)
// sheet.columns = [{ header, key, width }]
// sheet.addRow(rowObject)
// workbook.xlsx.writeBuffer() => Promise<ArrayBuffer>

// src/components/ui/button.tsx (shadcn)
// <Button variant="outline" size="sm" asChild><a>...</a></Button>
```

<!-- Whitelists existentes nas listagens (re-usar para export query): -->
- seguros/page.tsx: `ALLOWED_TYPES = ['auto','vida','residencial','empresarial','saude','outros']`, `ALLOWED_STATUSES = ['verde','amarelo','vermelho']`, `insurer` slice 100 chars, `assigned_to`
- clientes/page.tsx: `q` slice 100, `corretor`, `stage`, `type` (apenas 'pf' ou 'pj')
- corretores/page.tsx: nao tem filtros — export e do mes corrente

<!-- App-Router Route Handler signature (Next.js 15): -->
```typescript
export async function GET(
  request: Request,
  { params }: { params: Promise<{ slug: string }> }
): Promise<Response>
```

<!-- Schema das tabelas para colunas do .xlsx: -->
- policies: id, policy_number, type, insurer, vigencia_fim, premio_total, client(name), profile(full_name)
- clients: id, name, type (pf/pj), document, created_at, assigned_to(full_name), stage(name)
- corretores: profiles + commission_entries (sum), policies (count), do mes corrente
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportButton (Client Component) + Route Handler /api/[slug]/export — apolices</name>
  <files>src/components/export/export-button.tsx, src/app/api/[slug]/export/route.ts</files>
  <read_first>
    - src/components/ui/button.tsx (shadcn Button — variants, asChild)
    - src/lib/supabase/server.ts (createClient pattern para Route Handler)
    - src/lib/utils/dashboard-queries.ts (Plan 01 — ALLOWED_EXPORT_TYPES, isExecutiveRole)
    - src/app/(app)/[slug]/seguros/page.tsx (campos e ALLOWED_TYPES de policies)
    - src/app/(app)/[slug]/layout.tsx (padrao de slug check via app_metadata)
    - .planning/phases/06-dashboards-relatorios/06-RESEARCH.md (Padrao 5 + Pitfall 4 + Pitfall 5 + Pitfall 6 — Code Examples)
    - package.json (confirmar exceljs ja instalado por Plan 01)
  </read_first>
  <behavior>
    **ExportButton** (Client Component):
    - Recebe `slug: string`, `type: 'apolices'|'clientes'|'comissoes'`, `params?: Record<string, string>`.
    - Renderiza `<a href={url} download className="..."><Button variant="outline" size="sm">Exportar Excel</Button></a>` (Pitfall 5 do RESEARCH: usar `<a download>`, NAO `<Link>`).
    - URL construida com `URLSearchParams`: `/api/{slug}/export?type={type}&{...params}`.
    - Sem estado de loading em v1 (UI-SPEC explicito — browser trata download).

    **Route Handler `GET /api/[slug]/export`** — apenas tipo 'apolices' nesta task:
    - Validar `type` contra `ALLOWED_EXPORT_TYPES` → 400 se invalido (T-06-22).
    - Autenticar via `createClient()` + `supabase.auth.getUser()` → 401 se nao logado.
    - Validar `user.app_metadata.slug === slug` → 403 se mismatch (T-06-21).
    - Validar `isExecutiveRole(role)` → 403 se nao admin/financeiro (T-06-20).
    - Forward filtros whitelisted: `sp.type` (whitelist ALLOWED_TYPES de policies), `sp.insurer` (slice 100), `sp.assigned_to` (UUID), `sp.status` (whitelist ALLOWED_STATUSES) — outros searchParams sao IGNORADOS (T-06-23).
    - Quando `type === 'apolices'`: query `policies` com mesmas colunas do listing + `client(name)` + `profile(full_name)`, sem paginacao (limite 10000), `.is('deleted_at', null)`, `.order('vigencia_fim', { ascending: true })`.
    - Gerar workbook com sheet "Apolices" e colunas: Numero, Tipo, Seguradora, Vigencia Fim, Premio Total, Cliente, Corretor.
    - Cabecalho em negrito (`sheet.getRow(1).font = { bold: true }`).
    - Retornar `new Response(buffer, { headers: { 'Content-Disposition': 'attachment; filename="apolices.xlsx"', 'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', 'Cache-Control': 'no-store' } })`.
    - Para `type === 'clientes' | 'comissoes'`: retornar 501 `'Not implemented yet'` — sera completado na Task 2.
  </behavior>
  <action>
Passo 1 — Criar `src/components/export/export-button.tsx`:

```typescript
import Link from 'next/link'
import { Button } from '@/components/ui/button'

interface Props {
  slug: string
  type: 'apolices' | 'clientes' | 'comissoes'
  params?: Record<string, string>
  label?: string
}

export function ExportButton({ slug, type, params, label = 'Exportar Excel' }: Props) {
  const search = new URLSearchParams({ type })
  if (params) {
    for (const [k, v] of Object.entries(params)) {
      if (v) search.set(k, v)
    }
  }
  const href = `/api/${slug}/export?${search.toString()}`
  return (
    // <a download> em vez de <Link> — Pitfall 5: <Link> faria client-side routing, nao download.
    // eslint-disable-next-line @next/next/no-html-link-for-pages
    <Button asChild variant="outline" size="sm">
      <a href={href} download>{label}</a>
    </Button>
  )
}
```

NOTA: ExportButton e Server Component-friendly (sem `'use client'`) porque renderiza apenas tags HTML estaticas com `asChild`. Pode ser usado tanto em Server Components (listings) quanto em Client. O `eslint-disable` e necessario para evitar o lint do Next.js que sugere `<Link>`.

Passo 2 — Criar `src/app/api/[slug]/export/route.ts`:

```typescript
import ExcelJS from 'exceljs'
import { createClient } from '@/lib/supabase/server'
import {
  ALLOWED_EXPORT_TYPES,
  isExecutiveRole,
  type AllowedExportType,
} from '@/lib/utils/dashboard-queries'

const ALLOWED_POLICY_TYPES = ['auto', 'vida', 'residencial', 'empresarial', 'saude', 'outros']
const ALLOWED_STATUSES = ['verde', 'amarelo', 'vermelho']

export async function GET(
  request: Request,
  { params }: { params: Promise<{ slug: string }> },
) {
  const { slug } = await params
  const { searchParams } = new URL(request.url)
  const typeRaw = searchParams.get('type') ?? ''

  // T-06-22: whitelist obrigatoria
  if (!ALLOWED_EXPORT_TYPES.includes(typeRaw as AllowedExportType)) {
    return new Response('Invalid type', { status: 400 })
  }
  const type = typeRaw as AllowedExportType

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = (await createClient()) as any
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return new Response('Unauthorized', { status: 401 })

  const meta = (user.app_metadata as { slug?: string; role?: string }) ?? {}
  // T-06-21: cross-tenant — usuario do tenant X nao pode acessar /api/Y/export
  if (meta.slug && meta.slug !== slug) {
    return new Response('Forbidden', { status: 403 })
  }
  // T-06-20: RBAC — apenas admin e financeiro
  if (!isExecutiveRole(meta.role)) {
    return new Response('Forbidden', { status: 403 })
  }

  if (type === 'apolices') {
    return generateApolicesXlsx(supabase, searchParams)
  }
  // type === 'clientes' | 'comissoes' — Task 2
  return new Response('Not implemented yet', { status: 501 })
}

async function generateApolicesXlsx(
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  supabase: any,
  sp: URLSearchParams,
): Promise<Response> {
  // Forward filtros whitelisted (T-06-23: nao confiar em qualquer searchParam — copiar logica de seguros/page.tsx)
  let query = supabase
    .from('policies')
    .select(
      'id, policy_number, type, insurer, vigencia_fim, premio_total, client:clients(name), profile:profiles!assigned_to(full_name)',
    )
    .is('deleted_at', null)
    .order('vigencia_fim', { ascending: true })
    .limit(10000)

  const policyType = sp.get('type_filter')
  if (policyType && ALLOWED_POLICY_TYPES.includes(policyType)) {
    query = query.eq('type', policyType)
  }
  const insurer = sp.get('insurer')
  if (insurer) query = query.ilike('insurer', `%${insurer.slice(0, 100)}%`)
  const assignedTo = sp.get('assigned_to')
  if (assignedTo) query = query.eq('assigned_to', assignedTo)
  // status (semaforo): mesmo dispatch que seguros/page.tsx — usa vigencia_fim ranges
  const statusParam = sp.get('status')
  if (statusParam && ALLOWED_STATUSES.includes(statusParam)) {
    const today = new Date()
    const todayStr = today.toISOString().slice(0, 10)
    const plus30 = new Date(today.getTime() + 30 * 86400000).toISOString().slice(0, 10)
    const plus60 = new Date(today.getTime() + 60 * 86400000).toISOString().slice(0, 10)
    if (statusParam === 'vermelho') query = query.lte('vigencia_fim', plus30)
    else if (statusParam === 'amarelo') query = query.gt('vigencia_fim', plus30).lte('vigencia_fim', plus60)
    else if (statusParam === 'verde') query = query.gt('vigencia_fim', plus60)
    if (statusParam !== 'vermelho') query = query.gte('vigencia_fim', todayStr)
  }

  const { data, error } = await query
  if (error) {
    return new Response('Failed to query policies', { status: 500 })
  }

  type Row = {
    policy_number: string
    type: string
    insurer: string
    vigencia_fim: string
    premio_total: number | string
    client: { name: string } | null
    profile: { full_name: string } | null
  }

  const workbook = new ExcelJS.Workbook()
  workbook.creator = 'NEXUS AGENT'
  workbook.created = new Date()
  const sheet = workbook.addWorksheet('Apolices')
  sheet.columns = [
    { header: 'Numero', key: 'policy_number', width: 20 },
    { header: 'Tipo', key: 'type', width: 15 },
    { header: 'Seguradora', key: 'insurer', width: 25 },
    { header: 'Vigencia Fim', key: 'vigencia_fim', width: 15 },
    { header: 'Premio Total (BRL)', key: 'premio_total', width: 18 },
    { header: 'Cliente', key: 'client_name', width: 30 },
    { header: 'Corretor', key: 'corretor_name', width: 25 },
  ]
  sheet.getRow(1).font = { bold: true }
  for (const r of (data ?? []) as Row[]) {
    sheet.addRow({
      policy_number: r.policy_number,
      type: r.type,
      insurer: r.insurer,
      vigencia_fim: r.vigencia_fim,
      premio_total: Number(r.premio_total) || 0,
      client_name: r.client?.name ?? '',
      corretor_name: r.profile?.full_name ?? '',
    })
  }

  const buffer = await workbook.xlsx.writeBuffer()
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'Content-Disposition': 'attachment; filename="apolices.xlsx"',
      'Cache-Control': 'no-store',
    },
  })
}
```

NOTAS CRITICAS:
- **Pitfall 4 (Route Handler auth):** `createClient()` consome cookies automaticamente em Route Handlers Next.js 15.
- **Pitfall 6 (slug check):** `meta.slug !== slug` antes de qualquer query.
- **Filtro `type` da listagem vs query param `type` do export:** o handler usa `?type=apolices|clientes|comissoes` para roteamento — o filtro de tipo da apolice e enviado como `?type_filter=auto|vida|...` para evitar colisao. ExportButton de seguros vai mapear `sp.type` → `type_filter` no params.
- **Sem paginacao:** `.limit(10000)` cobre o limite de tenant do roadmap (5k clientes / 10k apolices). Para tenants extremos, export seria lento (open question documentada em RESEARCH.md).
- **Erro de query → 500 com mensagem generica.** Nunca propagar dados internos.
- **`Cache-Control: no-store`:** export sempre fresco; CDN nao cacheia dados sensiveis.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/api/[slug]/export/route.ts src/components/export/export-button.tsx 2>&1 | grep -E "src/(app/api|components/export)" || echo "no new errors in export files"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/[slug]/export/route.ts` existe e exporta `GET` async function (verificavel com `grep -E "^export async function GET" src/app/api/[slug]/export/route.ts`)
    - Contem `import ExcelJS from 'exceljs'`
    - Contem `import { ALLOWED_EXPORT_TYPES, isExecutiveRole } from '@/lib/utils/dashboard-queries'`
    - Contem `if (!ALLOWED_EXPORT_TYPES.includes(typeRaw as AllowedExportType))` retornando 400
    - Contem `if (meta.slug && meta.slug !== slug)` retornando 403
    - Contem `if (!isExecutiveRole(meta.role))` retornando 403
    - Contem `Content-Disposition` com `attachment; filename="apolices.xlsx"`
    - Contem `Cache-Control` com `no-store`
    - Contem `await workbook.xlsx.writeBuffer()`
    - `src/components/export/export-button.tsx` exporta `ExportButton`
    - ExportButton renderiza `<a href={...} download>` (NAO `<Link>`)
    - `npx tsc --noEmit` nao reporta novo erro nesses arquivos
  </acceptance_criteria>
  <done>
    - Route Handler funcional para type=apolices retorna .xlsx valido
    - Whitelist + RBAC + slug check implementados
    - ExportButton reutilizavel disponivel para Task 3
    - Type=clientes e type=comissoes retornam 501 (sera completado em Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Estender Route Handler com type=clientes e type=comissoes</name>
  <files>src/app/api/[slug]/export/route.ts</files>
  <read_first>
    - src/app/api/[slug]/export/route.ts (Task 1 — base do handler com switch type)
    - src/app/(app)/[slug]/clientes/page.tsx (filtros e select de clientes — q, corretor, stage, type pf/pj)
    - src/app/(app)/[slug]/corretores/page.tsx (productionMap pattern e brokerProfiles join)
    - src/app/(app)/[slug]/corretores/[id]/page.tsx (commission_entries query com reference_month — Pitfall 2)
    - src/lib/utils/clients-query.ts (buildSearchClause)
    - src/lib/utils/dashboard-queries.ts (aggregateBrokerRanking se util)
    - .planning/phases/06-dashboards-relatorios/06-RESEARCH.md (Pitfall 2: reference_month DATE)
  </read_first>
  <behavior>
    Adicionar duas helper functions ao route handler:

    **generateClientesXlsx(supabase, sp):**
    - Filtros forwarded: `q` (slice 100, busca name OR document), `corretor` (UUID), `stage` (UUID), `type_filter` (whitelist 'pf','pj').
    - Query: `clients` com select id/name/type/document/created_at/assigned_to(full_name)/stage(name), `.is('deleted_at', null)`, `.order('created_at', desc)`, `.limit(10000)`.
    - Sheet "Clientes" colunas: Nome, Tipo (PF/PJ), Documento, Corretor responsavel, Estagio, Cadastrado em.

    **generateComissoesXlsx(supabase, sp):**
    - Sem filtros — exporta resumo do MES SELECIONADO (default mes corrente, override com `?month=YYYY-MM`).
    - Reutilizar `parseSelectedMonth` do helper compartilhado.
    - Query: profiles com `role=corretor` + commission_entries com `reference_month = monthStartStr` + policies count com created_at no mes.
    - Reutilizar `aggregateBrokerRanking` (Plan 01) para gerar as linhas.
    - Sheet "Comissoes" colunas: Corretor, Producao do mes (#), Comissao (R$), Mes de referencia.
    - Filename: `comissoes-{monthValue}.xlsx`.
  </behavior>
  <action>
Editar `src/app/api/[slug]/export/route.ts` substituindo a linha:

```typescript
  return new Response('Not implemented yet', { status: 501 })
```

por:

```typescript
  if (type === 'clientes') {
    return generateClientesXlsx(supabase, searchParams)
  }
  if (type === 'comissoes') {
    return generateComissoesXlsx(supabase, searchParams)
  }
  // unreachable (whitelist garante)
  return new Response('Invalid type', { status: 400 })
```

E adicionar ao final do arquivo (apos `generateApolicesXlsx`):

```typescript
async function generateClientesXlsx(
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  supabase: any,
  sp: URLSearchParams,
): Promise<Response> {
  // Forward filtros whitelisted (mesma logica de clientes/page.tsx)
  let query = supabase
    .from('clients')
    .select(
      'id, name, type, document, created_at, assigned_to:profiles!clients_assigned_to_fkey(full_name), stage:pipeline_stages(name)',
    )
    .is('deleted_at', null)
    .order('created_at', { ascending: false })
    .limit(10000)

  const q = sp.get('q')
  if (q) {
    const safeQ = q.slice(0, 100).replace(/[%_]/g, '') // remove wildcards
    query = query.or(`name.ilike.%${safeQ}%,document.ilike.%${safeQ}%`)
  }
  const corretor = sp.get('corretor')
  if (corretor) query = query.eq('assigned_to', corretor)
  const stage = sp.get('stage')
  if (stage) query = query.eq('stage_id', stage)
  const typeFilter = sp.get('type_filter')
  if (typeFilter === 'pf' || typeFilter === 'pj') query = query.eq('type', typeFilter)

  const { data, error } = await query
  if (error) return new Response('Failed to query clients', { status: 500 })

  type Row = {
    name: string
    type: string
    document: string
    created_at: string
    assigned_to: { full_name: string } | null
    stage: { name: string } | null
  }

  const workbook = new ExcelJS.Workbook()
  workbook.creator = 'NEXUS AGENT'
  workbook.created = new Date()
  const sheet = workbook.addWorksheet('Clientes')
  sheet.columns = [
    { header: 'Nome', key: 'name', width: 30 },
    { header: 'Tipo', key: 'type', width: 8 },
    { header: 'Documento', key: 'document', width: 20 },
    { header: 'Corretor responsavel', key: 'corretor', width: 25 },
    { header: 'Estagio', key: 'stage', width: 18 },
    { header: 'Cadastrado em', key: 'created_at', width: 18 },
  ]
  sheet.getRow(1).font = { bold: true }
  for (const r of (data ?? []) as Row[]) {
    sheet.addRow({
      name: r.name,
      type: r.type?.toUpperCase() ?? '',
      document: r.document,
      corretor: r.assigned_to?.full_name ?? '',
      stage: r.stage?.name ?? '',
      created_at: r.created_at?.slice(0, 10) ?? '',
    })
  }

  const buffer = await workbook.xlsx.writeBuffer()
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'Content-Disposition': 'attachment; filename="clientes.xlsx"',
      'Cache-Control': 'no-store',
    },
  })
}

async function generateComissoesXlsx(
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  supabase: any,
  sp: URLSearchParams,
): Promise<Response> {
  // Reutilizar parseSelectedMonth e aggregateBrokerRanking do helper Plan 01
  const { parseSelectedMonth, aggregateBrokerRanking } = await import(
    '@/lib/utils/dashboard-queries'
  )
  const month = parseSelectedMonth(sp.get('month') ?? undefined)

  const { data: profiles } = await supabase
    .from('profiles')
    .select('id, full_name')
    .eq('role', 'corretor')
    .order('full_name')
  const profilesArr = (profiles ?? []) as Array<{ id: string; full_name: string }>
  const profileIds = profilesArr.map((p) => p.id)

  if (profileIds.length === 0) {
    return emptyComissoesXlsx(month.monthValue)
  }

  const [commRes, prodRes] = await Promise.all([
    supabase
      .from('commission_entries')
      .select('broker_id, amount')
      .in('broker_id', profileIds)
      .eq('reference_month', month.monthStartStr), // Pitfall 2: DATE, primeiro dia do mes
    supabase
      .from('policies')
      .select('assigned_to')
      .in('assigned_to', profileIds)
      .gte('created_at', month.monthStartStr)
      .lte('created_at', month.monthEndStr + 'T23:59:59')
      .is('deleted_at', null),
  ])
  const commissions = (commRes.data ?? []) as Array<{ broker_id: string; amount: number | string }>
  const productions = (prodRes.data ?? []) as Array<{ assigned_to: string }>
  const rows = aggregateBrokerRanking(profilesArr, commissions, productions)

  const workbook = new ExcelJS.Workbook()
  workbook.creator = 'NEXUS AGENT'
  workbook.created = new Date()
  const sheet = workbook.addWorksheet('Comissoes')
  sheet.columns = [
    { header: 'Corretor', key: 'fullName', width: 30 },
    { header: 'Producao do mes (#)', key: 'productionCount', width: 18 },
    { header: 'Comissao (BRL)', key: 'commissionTotal', width: 18 },
    { header: 'Mes de referencia', key: 'referenceMonth', width: 16 },
  ]
  sheet.getRow(1).font = { bold: true }
  for (const r of rows) {
    sheet.addRow({
      fullName: r.fullName,
      productionCount: r.productionCount,
      commissionTotal: r.commissionTotal,
      referenceMonth: month.monthValue,
    })
  }

  const buffer = await workbook.xlsx.writeBuffer()
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'Content-Disposition': `attachment; filename="comissoes-${month.monthValue}.xlsx"`,
      'Cache-Control': 'no-store',
    },
  })
}

async function emptyComissoesXlsx(monthValue: string): Promise<Response> {
  const workbook = new ExcelJS.Workbook()
  const sheet = workbook.addWorksheet('Comissoes')
  sheet.columns = [
    { header: 'Corretor', key: 'fullName', width: 30 },
    { header: 'Producao do mes (#)', key: 'productionCount', width: 18 },
    { header: 'Comissao (BRL)', key: 'commissionTotal', width: 18 },
  ]
  sheet.getRow(1).font = { bold: true }
  const buffer = await workbook.xlsx.writeBuffer()
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'Content-Disposition': `attachment; filename="comissoes-${monthValue}.xlsx"`,
      'Cache-Control': 'no-store',
    },
  })
}
```

NOTAS:
- **Whitelist `q.replace(/[%_]/g, '')`:** remove wildcards SQL LIKE para impedir injection patterns no `ilike` (defesa em profundidade alem do RLS).
- **Reutilizar `aggregateBrokerRanking`:** mesma logica do dashboard — single source of truth para ordenacao.
- **`await import(...)`:** evita ciclo top-level se o handler for chamado fora do contexto (e tambem reduz cold-start surface). Alternativa: import normal no topo — escolher import top-level se preferir.
- **emptyComissoesXlsx:** edge case quando tenant nao tem nenhum corretor cadastrado — retorna xlsx vazio em vez de 404.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/api/[slug]/export/route.ts 2>&1 | grep -E "src/app/api/\[slug\]/export" || echo "no new errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/[slug]/export/route.ts` agora contem `generateClientesXlsx` e `generateComissoesXlsx` (verificavel com `grep -E "function (generateClientesXlsx|generateComissoesXlsx)" src/app/api/[slug]/export/route.ts`)
    - Switch no GET roteia para os 3 tipos sem cair em 501
    - generateComissoesXlsx usa `.eq('reference_month', month.monthStartStr)` (Pitfall 2)
    - generateClientesXlsx aplica `.replace(/[%_]/g, '')` em `q` antes de `.or(... ilike ...)` (defesa contra wildcards)
    - Filename de comissoes inclui `${monthValue}` (verificavel via grep `comissoes-\${month.monthValue}`)
    - Sem `'use client'` no arquivo (e Route Handler server)
    - tsc nao reporta novo erro
  </acceptance_criteria>
  <done>
    - Route Handler completo para os 3 tipos
    - 501 nao retorna mais
    - Whitelists de filtros aplicadas em cada tipo
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar ExportButton em /seguros, /clientes, /corretores com searchParams forwarded e gating por role</name>
  <files>src/app/(app)/[slug]/seguros/page.tsx, src/app/(app)/[slug]/clientes/page.tsx, src/app/(app)/[slug]/corretores/page.tsx</files>
  <read_first>
    - src/app/(app)/[slug]/seguros/page.tsx (estado atual — header e ALLOWED_TYPES)
    - src/app/(app)/[slug]/clientes/page.tsx (estado atual — header e filtros sp.q, sp.corretor, sp.stage, sp.type)
    - src/app/(app)/[slug]/corretores/page.tsx (estado atual — header)
    - src/components/export/export-button.tsx (Task 1 — props da ExportButton)
    - src/lib/utils/dashboard-queries.ts (isExecutiveRole)
    - .planning/phases/06-dashboards-relatorios/06-UI-SPEC.md (Layout de Exportacao — Botao a ESQUERDA do CTA primario)
    - .planning/phases/06-dashboards-relatorios/06-CONTEXT.md (D-08: respeitar filtros)
  </read_first>
  <action>
**Edicao 1 — `src/app/(app)/[slug]/seguros/page.tsx`:**

Adicionar import logo abaixo da `import { Button }`:
```typescript
import { ExportButton } from '@/components/export/export-button'
import { isExecutiveRole } from '@/lib/utils/dashboard-queries'
```

Logo apos a linha `if (!user) notFound()` (apos getUser), adicionar:
```typescript
  const role = (user.app_metadata as { role?: string })?.role ?? ''
  const canExport = isExecutiveRole(role)
```

Construir os params do export logo antes do `return`:
```typescript
  // D-08: passar searchParams ativos como params para o export — mapeando type → type_filter
  const exportParams: Record<string, string> = {}
  if (sp.type) exportParams.type_filter = sp.type
  if (sp.insurer) exportParams.insurer = sp.insurer
  if (sp.assigned_to) exportParams.assigned_to = sp.assigned_to
  if (sp.status) exportParams.status = sp.status
```

Substituir a div do header (linha do `flex items-center justify-between`):
```tsx
      <div className="flex items-center justify-between gap-3">
        <div>
          <h1 className="text-2xl font-semibold">Apólices de Seguro</h1>
          <p className="text-sm text-muted-foreground">{count ?? 0} apólice(s) encontrada(s)</p>
        </div>
        <div className="flex items-center gap-2">
          {canExport && (
            <ExportButton slug={slug} type="apolices" params={exportParams} />
          )}
          <Button asChild>
            <Link href={`/${slug}/seguros/nova`}>+ Nova apólice</Link>
          </Button>
        </div>
      </div>
```

Posicionamento: ExportButton fica a ESQUERDA do CTA "+ Nova apolice" (UI-SPEC: "ExportButton sempre a esquerda do botao primario").

**Edicao 2 — `src/app/(app)/[slug]/clientes/page.tsx`:**

Adicionar import (apos `import { ClientsPagination }`):
```typescript
import { ExportButton } from '@/components/export/export-button'
import { isExecutiveRole } from '@/lib/utils/dashboard-queries'
```

Apos `const userId = user.id`, adicionar:
```typescript
  const canExport = isExecutiveRole(userRole)
```

Antes do `return`, montar params:
```typescript
  const exportParams: Record<string, string> = {}
  if (sp.q) exportParams.q = sp.q
  if (sp.corretor) exportParams.corretor = sp.corretor
  if (sp.stage) exportParams.stage = sp.stage
  if (sp.type) exportParams.type_filter = sp.type
```

Substituir a div do header `flex items-center justify-between`:
```tsx
      <div className="flex items-center justify-between gap-3">
        <div>
          <h1 className="text-2xl font-semibold">Clientes</h1>
          <p className="text-sm text-muted-foreground">{count ?? 0} cliente(s) encontrado(s)</p>
        </div>
        <div className="flex items-center gap-2">
          {canExport && (
            <ExportButton slug={slug} type="clientes" params={exportParams} />
          )}
          <Button asChild>
            <Link href={`/${slug}/clientes/novo`}>+ Novo cliente</Link>
          </Button>
        </div>
      </div>
```

**Edicao 3 — `src/app/(app)/[slug]/corretores/page.tsx`:**

Adicionar import:
```typescript
import { ExportButton } from '@/components/export/export-button'
import { isExecutiveRole } from '@/lib/utils/dashboard-queries'
import { format, startOfMonth } from 'date-fns'  // se ainda nao importado: format ja existe; conferir startOfMonth
```

Apos `if (role === 'corretor') redirect(...)`, adicionar:
```typescript
  const canExport = isExecutiveRole(role)
  const currentMonthValue = format(startOfMonth(new Date()), 'yyyy-MM')
```

Substituir a div do header:
```tsx
      <div className="flex items-center justify-between gap-3">
        <div>
          <h1 className="text-2xl font-semibold">Corretores</h1>
          <p className="text-sm text-muted-foreground">
            {count ?? 0} corretor(es) cadastrado(s)
          </p>
        </div>
        {canExport && (
          <ExportButton
            slug={slug}
            type="comissoes"
            params={{ month: currentMonthValue }}
          />
        )}
      </div>
```

NOTAS:
- **D-08 (filtros respeitados):** os params espelham exatamente os filtros aceitos por cada listagem. Filtros nao-listados sao IGNORADOS (T-06-23 — defesa contra injecao).
- **`type` da apolice → `type_filter`:** colisao com query param `?type=apolices|clientes|comissoes` do handler. Mapeamos para `type_filter` consistentemente.
- **Gating por role:** ExportButton so renderiza quando `canExport === true` (admin/financeiro). Corretor/visualizador nao veem o botao (UI-SPEC).
- **Layout:** Header agora envolve children em `<div className="flex items-center gap-2">` para alinhar ExportButton + CTA primario.
- **Corretores nao tem filtros** — apenas o mes corrente. Plano nao adiciona filtros nesta phase (deferred).
- **Para listagem de clientes**, nao alterar o resto do arquivo (logica de overdueClientIds, query, etc).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(seguros/page|clientes/page|corretores/page)\.tsx" | grep -v "node_modules" || echo "no new errors in listing pages"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/[slug]/seguros/page.tsx` contem `import { ExportButton } from '@/components/export/export-button'`
    - `src/app/(app)/[slug]/seguros/page.tsx` contem `<ExportButton slug={slug} type="apolices" params={exportParams} />`
    - `src/app/(app)/[slug]/seguros/page.tsx` contem `if (sp.type) exportParams.type_filter = sp.type` (mapeamento type → type_filter)
    - `src/app/(app)/[slug]/seguros/page.tsx` contem `canExport && (` (gating por role)
    - `src/app/(app)/[slug]/clientes/page.tsx` contem `<ExportButton slug={slug} type="clientes"` 
    - `src/app/(app)/[slug]/clientes/page.tsx` mapeia `q`, `corretor`, `stage`, `type` (como type_filter) em exportParams
    - `src/app/(app)/[slug]/corretores/page.tsx` contem `<ExportButton slug={slug} type="comissoes"`
    - Cada listagem tem o botao Exportar a ESQUERDA do CTA primario (verificavel pela ordem no JSX — ExportButton antes do `<Button asChild>`)
    - tsc nao reporta novos erros nestes 3 arquivos
    - `npm test` continua verde (sem regressao)
  </acceptance_criteria>
  <done>
    - 3 listagens tem botao Exportar Excel visivel apenas para admin/financeiro
    - searchParams ativos sao forwarded para o handler exatamente como na listagem
    - Filtros desconhecidos sao descartados pelo handler (whitelist da Task 1/2)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /api/[slug]/export | Route Handler recebe slug pelo path + filtros pelo query string (ambos sob controle do usuario) |
| Route Handler → Supabase | Request com cookies do usuario; RLS aplica isolamento |
| browser → .xlsx download | Buffer binario retornado com Content-Disposition; nenhum dado executavel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Elevation of Privilege | RBAC do export | mitigate | `if (!isExecutiveRole(meta.role)) return 403` antes de qualquer query. ExportButton tambem nao renderiza se canExport=false (defesa em profundidade UI). Helper isExecutiveRole testado em Plan 01. |
| T-06-21 | Tampering / Cross-tenant | slug do path | mitigate | `if (meta.slug && meta.slug !== slug) return 403`. RLS e segunda camada. Padrao identico a [slug]/layout.tsx. |
| T-06-22 | Tampering | query param `type` invalido | mitigate | `if (!ALLOWED_EXPORT_TYPES.includes(typeRaw))` retorna 400. Whitelist `as const` importada de Plan 01. Testada em Plan 01 (comprimento exato 3). |
| T-06-23 | Injection | filtros via searchParams | mitigate | Cada filtro forwarded contra whitelist correspondente: `type_filter` (ALLOWED_POLICY_TYPES, type pf/pj), `status` (ALLOWED_STATUSES), `q` (slice 100 + remove wildcards SQL LIKE), UUIDs (assigned_to, corretor, stage) passados sem ilike. Searchparams nao-listados sao ignorados silenciosamente. |
| T-06-24 | Information Disclosure | error response com detalhes | mitigate | Erros de query retornam mensagem generica ('Failed to query policies') sem stack trace, codigos PG ou nome de tabela. |
| T-06-25 | Denial of Service | export ilimitado | accept | `.limit(10000)` em policies/clients (limite operacional do roadmap: 5k clientes / 10k apolices). Comissoes limitado pelo numero de corretores do tenant. exceljs server-side gera buffer em memoria — aceitavel para tenants v1. |
| T-06-26 | Information Disclosure | cache de export | mitigate | `Cache-Control: no-store` em todas as respostas — CDN (Vercel) e browser nao cacheiam .xlsx com dados sensiveis. |
| T-06-27 | Tampering / XSS via xlsx | dados em celulas | accept | exceljs escapa conteudo XML automaticamente. Nao ha injecao de formulas perigosas (CSV injection: `=cmd|...`) porque os campos exportados nao sao texto livre arbitrario do usuario que possa comecar com `=` — se necessario em phase futura, prefixar com `'` (apostrofo). Aceitavel em v1: dados sao internos e o export e para o proprio admin. |
</threat_model>

<verification>
Verificacao de phase:
- `npm test` continua verde (Plan 01 + Plan 02 + sem regressao)
- `npx tsc --noEmit` nao reporta novos erros
- Verificacao manual de end-to-end (executa em ambiente Supabase real):
  - Admin clica Exportar Excel em /seguros sem filtros → baixa apolices.xlsx com todas as apolices nao deletadas (max 10k)
  - Admin filtra por seguradora "Bradesco" e clica Exportar → baixa apolices.xlsx so com Bradesco
  - Financeiro clica Exportar em /clientes → baixa clientes.xlsx
  - Admin clica Exportar em /corretores → baixa comissoes-2026-04.xlsx
  - Corretor logado nao ve o botao em nenhuma das 3 paginas
  - Visualizador logado nao ve o botao
  - `curl /api/{slug}/export?type=apolices` autenticado como corretor → 403
  - `curl /api/{slug}/export?type=garbage` autenticado como admin → 400
  - `curl /api/{outro-slug}/export?type=apolices` → 403
  - Arquivo .xlsx abre no LibreOffice/Excel/Numbers sem warning de corrupcao; cabecalho em negrito
</verification>

<success_criteria>
- DASH-04 entregue: 3 botoes Exportar Excel funcionais nas listagens correspondentes
- Filtros ativos refletidos no .xlsx baixado (D-08)
- RBAC respeitado: corretor/visualizador nao acessam (UI nem API)
- Cross-tenant access bloqueado por slug check
- Whitelist em todos os filtros (defesa contra injecao)
- Sem `'use client'` no Route Handler
- exceljs gera arquivo abrivel sem corrupcao
</success_criteria>

<output>
Apos completion, criar `.planning/phases/06-dashboards-relatorios/06-03-SUMMARY.md` com:
- Comando curl para cada cenario de teste manual de seguranca (com/sem auth, role mismatch, slug mismatch, type invalido)
- Filename gerado por cada tipo (apolices.xlsx, clientes.xlsx, comissoes-{YYYY-MM}.xlsx)
- Limite efetivo de linhas por tipo (10k apolices/clientes, todos os corretores)
- Confirmacao de que .xlsx abre em pelo menos 1 leitor (Excel ou LibreOffice)
- Threats T-06-20 a T-06-27 mapeados para a linha de codigo que os mitiga
</output>
