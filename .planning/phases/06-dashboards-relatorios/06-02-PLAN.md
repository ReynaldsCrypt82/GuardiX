---
phase: 06-dashboards-relatorios
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/dashboard/alert-section.tsx
  - src/components/dashboard/broker-ranking-table.tsx
  - src/app/(app)/[slug]/dashboard/page.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
tags: [dashboard, kpis, ranking, alerts, rbac, server-component]

must_haves:
  truths:
    - "Admin que abre /[slug]/dashboard ve 4 stat cards reais (Apolices ativas, Receita do mes, Inadimplencia, Vencendo 30d)"
    - "Financeiro que abre /[slug]/dashboard ve o mesmo conteudo do admin (D-10)"
    - "Corretor que abre /[slug]/dashboard e redirecionado para /[slug]/corretores/{user.id}"
    - "Visualizador que abre /[slug]/dashboard recebe 404"
    - "Pagina exibe 3 listas de alertas (apolices vencendo, cobrancas em atraso, assembleias proximas) com max 5 itens cada e link 'Ver todos'"
    - "MonthSelector troca o mes via ?month=YYYY-MM e os cards Receita/Inadimplencia se atualizam"
    - "Tabela de ranking mostra 1 linha por corretor do tenant ordenada por comissao DESC"
  artifacts:
    - path: "src/app/(app)/[slug]/dashboard/page.tsx"
      provides: "Painel executivo Server Component com RBAC + 4 cards + alertas + ranking"
      contains: "isExecutiveRole"
      min_lines: 150
    - path: "src/components/dashboard/alert-section.tsx"
      provides: "Server Component que renderiza 3 cards de alerta lado a lado"
      contains: "Apolices vencendo"
    - path: "src/components/dashboard/broker-ranking-table.tsx"
      provides: "Server Component que renderiza tabela de ranking shadcn"
      contains: "Ranking de Corretores"
  key_links:
    - from: "src/app/(app)/[slug]/dashboard/page.tsx"
      to: "src/lib/utils/dashboard-queries.ts"
      via: "import { isExecutiveRole, parseSelectedMonth, aggregateBrokerRanking }"
      pattern: "from '@/lib/utils/dashboard-queries'"
    - from: "src/app/(app)/[slug]/dashboard/page.tsx"
      to: "src/components/corretores/month-selector.tsx"
      via: "import { MonthSelector }"
      pattern: "from '@/components/corretores/month-selector'"
    - from: "src/app/(app)/[slug]/dashboard/page.tsx"
      to: "src/components/corretores/stat-card.tsx"
      via: "import { StatCard }"
      pattern: "from '@/components/corretores/stat-card'"
    - from: "src/app/(app)/[slug]/dashboard/page.tsx"
      to: "policies + financial_entries + commission_entries + consortium_groups"
      via: "supabase.from(...)"
      pattern: "supabase\\n?\\s*\\.from\\('(policies|financial_entries|commission_entries|consortium_groups)'\\)"
---

<objective>
Substituir o placeholder atual de `/[slug]/dashboard` por um painel executivo real para roles `admin` e `financeiro` (D-10): 4 KPI cards (D-02), 3 listas de alertas (D-05), tabela de ranking de corretores (D-04), MonthSelector URL-driven (D-03), com RBAC diferenciado por role (D-09).

**Purpose:** Cumpre DASH-01, DASH-02 e DASH-03 — o admin tem visao consolidada do tenant em uma unica tela, sem dependencia de gráficos ou Realtime (deferred). Reutiliza StatCard/MonthSelector existentes; cria apenas 2 componentes novos para alertas e ranking.

**Output:**
- `src/app/(app)/[slug]/dashboard/page.tsx` reescrito como Server Component com RBAC + queries paralelas
- 2 novos componentes em `src/components/dashboard/`
</objective>

<execution_context>
@C:/Users/Reinaldo - Local/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Reinaldo - Local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dashboards-relatorios/06-CONTEXT.md
@.planning/phases/06-dashboards-relatorios/06-RESEARCH.md
@.planning/phases/06-dashboards-relatorios/06-UI-SPEC.md
@.planning/phases/06-dashboards-relatorios/06-01-PLAN.md
@src/app/(app)/[slug]/dashboard/page.tsx
@src/app/(app)/[slug]/financeiro/page.tsx
@src/app/(app)/[slug]/corretores/[id]/page.tsx
@src/app/(app)/[slug]/corretores/page.tsx
@src/components/corretores/stat-card.tsx
@src/components/corretores/month-selector.tsx
@src/components/ui/card.tsx
@src/components/ui/table.tsx

<interfaces>
<!-- Interfaces consumidas (provenientes do Plan 01 + componentes existentes) -->

```typescript
// src/lib/utils/dashboard-queries.ts (Plan 01)
export function isExecutiveRole(role: string | null | undefined): boolean
export function parseSelectedMonth(monthParam, today?): {
  monthStartStr, monthEndStr, monthValue, monthLabel
}
export function aggregateBrokerRanking(
  profiles: ProfileRow[],
  commissions: CommissionRow[],
  productions: ProductionRow[],
): BrokerRankingRow[]
export function dedupeClientIds(arrays): Set<string>

// src/components/corretores/stat-card.tsx
export function StatCard(props: {
  title: string
  value: string
  subtext?: string
  progress?: number
  progressOverflow?: boolean
}): JSX.Element

// src/components/corretores/month-selector.tsx
export function MonthSelector(props: { selected: string }): JSX.Element

// src/components/ui/card.tsx (shadcn)
export { Card, CardHeader, CardContent, CardTitle }

// src/components/ui/table.tsx (shadcn)
export { Table, TableHeader, TableBody, TableRow, TableHead, TableCell }
```

<!-- Schema das tabelas consultadas (verificadas em RESEARCH.md): -->
- `policies(id, policy_number, type, vigencia_fim, premio_total, deleted_at, client:clients(name))` — sem campo `status` (Pitfall 1: usar `vigencia_fim >= hoje`)
- `financial_entries(id, description, amount, due_date, status, paid_at, entry_type, client_id, deleted_at)`
- `commission_entries(broker_id, amount, reference_month)` — reference_month e DATE primeiro dia do mes (Pitfall 2)
- `consortium_groups(id, administrator, next_assembly_date, deleted_at)`
- `profiles(id, full_name, role)`

<!-- Padrao Server Component existente (financeiro/page.tsx — copiar shape): -->
```typescript
const supabase = (await createClient()) as any
const { data: { user } } = await supabase.auth.getUser()
if (!user) notFound()
const role = (user.app_metadata as { role?: string })?.role ?? ''
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componentes AlertSection e BrokerRankingTable (Server Components)</name>
  <files>src/components/dashboard/alert-section.tsx, src/components/dashboard/broker-ranking-table.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (Card, CardHeader, CardContent, CardTitle API)
    - src/components/ui/table.tsx (Table, TableHeader, TableBody, TableRow, TableHead, TableCell API)
    - src/components/ui/badge.tsx (Badge API e variants)
    - src/components/corretores/commission-table.tsx (padrao de Server Component table)
    - .planning/phases/06-dashboards-relatorios/06-UI-SPEC.md (Layout do Dashboard Executivo + Color tokens)
  </read_first>
  <behavior>
    **AlertSection** (Server Component):
    - Recebe 3 props (`vencendo`, `cobrancas`, `assembleias`) — cada uma com shape `{ items: Array<{...}>, totalCount: number }`.
    - Renderiza 3 Cards lado a lado (`grid grid-cols-1 md:grid-cols-3 gap-4`).
    - Cada card tem CardTitle no formato `"{Cabecalho} ({totalCount})"`.
    - Cada card mostra max 5 items como `<li>` com link interno apropriado.
    - Cada item exibe Badge colorido conforme UI-SPEC: vencendo = `outline + text-amber-600`, cobrancas = `destructive`, assembleias = `secondary`.
    - Empty state inline: `"Nenhum item."` (sem icone).
    - Footer com link `"Ver todos"` apontando para a rota completa correspondente — usa `text-primary hover:underline`.

    **BrokerRankingTable** (Server Component):
    - Recebe `rows: BrokerRankingRow[]` (do helper `aggregateBrokerRanking`) + `slug: string` + `monthLabel: string`.
    - Renderiza Card com title `"Ranking de Corretores — {monthLabel}"`.
    - Tabela shadcn com 3 colunas: Corretor (link para `/corretores/{brokerId}`), Producao do mes (numero), Comissao (R$ — formatado com `Intl.NumberFormat('pt-BR', {style:'currency', currency:'BRL'})` — alinhamento DIREITA).
    - Empty state: linha unica com mensagem `"Nenhum corretor com producao neste periodo."` (UI-SPEC).
    - Hover de linha: `hover:bg-muted/50` (padrao shadcn).
  </behavior>
  <action>
Passo 1 — Criar `src/components/dashboard/alert-section.tsx`:

```typescript
import Link from 'next/link'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { format } from 'date-fns'
import { ptBR } from 'date-fns/locale'

export interface VencendoItem {
  id: string
  policy_number: string
  vigencia_fim: string
  client_name: string | null
}
export interface CobrancaItem {
  id: string
  description: string
  amount: number | string
  due_date: string
}
export interface AssembleiaItem {
  id: string
  administrator: string
  next_assembly_date: string
}

interface Props {
  slug: string
  vencendo: { items: VencendoItem[]; totalCount: number }
  cobrancas: { items: CobrancaItem[]; totalCount: number }
  assembleias: { items: AssembleiaItem[]; totalCount: number }
}

function formatBRL(v: number | string) {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(Number(v) || 0)
}
function formatDateBR(iso: string) {
  return format(new Date(iso + 'T00:00:00'), 'dd/MM/yyyy', { locale: ptBR })
}

export function AlertSection({ slug, vencendo, cobrancas, assembleias }: Props) {
  return (
    <section className="space-y-4">
      <h2 className="text-base font-semibold">Alertas</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {/* Apolices vencendo */}
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-base">
              Apolices vencendo ({vencendo.totalCount})
            </CardTitle>
          </CardHeader>
          <CardContent className="space-y-2">
            {vencendo.items.length === 0 ? (
              <p className="text-sm text-muted-foreground">Nenhum item.</p>
            ) : (
              <ul className="space-y-2">
                {vencendo.items.map((it) => (
                  <li key={it.id} className="flex items-center justify-between text-sm">
                    <Link
                      href={`/${slug}/seguros/${it.id}`}
                      className="hover:underline truncate mr-2"
                    >
                      {it.policy_number}
                      {it.client_name && ` — ${it.client_name}`}
                    </Link>
                    <Badge variant="outline" className="text-amber-600 border-amber-600 shrink-0">
                      {formatDateBR(it.vigencia_fim)}
                    </Badge>
                  </li>
                ))}
              </ul>
            )}
            <Link href={`/${slug}/seguros`} className="text-sm text-primary hover:underline block pt-2">
              Ver todas as apolices →
            </Link>
          </CardContent>
        </Card>

        {/* Cobrancas em atraso */}
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-base">
              Cobrancas em atraso ({cobrancas.totalCount})
            </CardTitle>
          </CardHeader>
          <CardContent className="space-y-2">
            {cobrancas.items.length === 0 ? (
              <p className="text-sm text-muted-foreground">Nenhum item.</p>
            ) : (
              <ul className="space-y-2">
                {cobrancas.items.map((it) => (
                  <li key={it.id} className="flex items-center justify-between text-sm">
                    <span className="truncate mr-2">{it.description}</span>
                    <Badge variant="destructive" className="shrink-0">
                      {formatBRL(it.amount)}
                    </Badge>
                  </li>
                ))}
              </ul>
            )}
            <Link href={`/${slug}/financeiro?tab=overdue`} className="text-sm text-primary hover:underline block pt-2">
              Ver lancamentos →
            </Link>
          </CardContent>
        </Card>

        {/* Assembleias proximas */}
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-base">
              Assembleias proximas ({assembleias.totalCount})
            </CardTitle>
          </CardHeader>
          <CardContent className="space-y-2">
            {assembleias.items.length === 0 ? (
              <p className="text-sm text-muted-foreground">Nenhum item.</p>
            ) : (
              <ul className="space-y-2">
                {assembleias.items.map((it) => (
                  <li key={it.id} className="flex items-center justify-between text-sm">
                    <Link
                      href={`/${slug}/consorcio/grupos/${it.id}`}
                      className="hover:underline truncate mr-2"
                    >
                      {it.administrator}
                    </Link>
                    <Badge variant="secondary" className="shrink-0">
                      {formatDateBR(it.next_assembly_date)}
                    </Badge>
                  </li>
                ))}
              </ul>
            )}
            <Link href={`/${slug}/consorcio`} className="text-sm text-primary hover:underline block pt-2">
              Ver grupos de consorcio →
            </Link>
          </CardContent>
        </Card>
      </div>
    </section>
  )
}
```

Passo 2 — Criar `src/components/dashboard/broker-ranking-table.tsx`:

```typescript
import Link from 'next/link'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'
import type { BrokerRankingRow } from '@/lib/utils/dashboard-queries'

interface Props {
  slug: string
  monthLabel: string
  rows: BrokerRankingRow[]
}

function formatBRL(v: number) {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}

export function BrokerRankingTable({ slug, monthLabel, rows }: Props) {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-base">Ranking de Corretores — {monthLabel}</CardTitle>
      </CardHeader>
      <CardContent>
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Corretor</TableHead>
              <TableHead className="text-right">Producao do mes</TableHead>
              <TableHead className="text-right">Comissao (R$)</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {rows.length === 0 ? (
              <TableRow>
                <TableCell colSpan={3} className="text-center text-sm text-muted-foreground py-8">
                  Nenhum corretor com producao neste periodo.
                </TableCell>
              </TableRow>
            ) : (
              rows.map((r) => (
                <TableRow key={r.brokerId}>
                  <TableCell>
                    <Link
                      href={`/${slug}/corretores/${r.brokerId}`}
                      className="hover:underline"
                    >
                      {r.fullName}
                    </Link>
                  </TableCell>
                  <TableCell className="text-right">{r.productionCount}</TableCell>
                  <TableCell className="text-right">{formatBRL(r.commissionTotal)}</TableCell>
                </TableRow>
              ))
            )}
          </TableBody>
        </Table>
      </CardContent>
    </Card>
  )
}
```

NOTAS:
- Sem `'use client'` — ambos sao Server Components puros (so usam Link do Next).
- Badge variants seguem UI-SPEC: outline+amber, destructive, secondary.
- formatBRL e formatDateBR sao utilitarios locais (nao vale criar helper compartilhado para 2 usos).
- Item de cobranca nao tem link individual (financial_entries nao tem rota /financeiro/[id] em v1) — link unico no footer da seção.
- Item de assembleia aponta para `/consorcio/grupos/{id}` que pode existir ou cair em 404 (rota generica `/consorcio` cobre o "Ver todos"). Aceitavel em v1.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/dashboard/alert-section.tsx src/components/dashboard/broker-ranking-table.tsx 2>&1 | grep -E "src/components/dashboard" || echo "no errors in dashboard components"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/dashboard/alert-section.tsx` existe e exporta `AlertSection` (verificavel com `grep -E "^export function AlertSection" src/components/dashboard/alert-section.tsx`)
    - `src/components/dashboard/broker-ranking-table.tsx` existe e exporta `BrokerRankingTable` (verificavel com `grep -E "^export function BrokerRankingTable" src/components/dashboard/broker-ranking-table.tsx`)
    - AlertSection contem strings literais: `"Apolices vencendo"`, `"Cobrancas em atraso"`, `"Assembleias proximas"`, `"Nenhum item."`, `"Ver todas as apolices"`, `"Ver lancamentos"`, `"Ver grupos de consorcio"`
    - BrokerRankingTable contem `"Ranking de Corretores"` + `"Producao do mes"` + `"Comissao"`
    - AlertSection usa `grid-cols-1 md:grid-cols-3` (UI-SPEC)
    - Sem `'use client'` em nenhum dos dois arquivos
    - tsc nao reporta novo erro nesses arquivos
  </acceptance_criteria>
  <done>
    - 2 Server Components renderizam dados via props (sem fetch interno)
    - Strings copy seguem UI-SPEC literalmente
    - Imports usam `@/components/ui/*` e `@/lib/utils/dashboard-queries`
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescrever /[slug]/dashboard/page.tsx — Server Component executivo com RBAC + queries paralelas</name>
  <files>src/app/(app)/[slug]/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/[slug]/dashboard/page.tsx (placeholder atual a substituir)
    - src/app/(app)/[slug]/financeiro/page.tsx (padrao RBAC + MonthSelector + StatCards + Promise.all)
    - src/app/(app)/[slug]/corretores/[id]/page.tsx (padrao queries de commission_entries + reference_month)
    - src/app/(app)/[slug]/corretores/page.tsx (padrao agregar producao por corretor — productionMap)
    - src/lib/utils/dashboard-queries.ts (Plan 01 — fonte das funcoes puras)
    - src/components/corretores/stat-card.tsx (props da StatCard)
    - src/components/corretores/month-selector.tsx (prop `selected: string`)
    - src/components/dashboard/alert-section.tsx (Task 1)
    - src/components/dashboard/broker-ranking-table.tsx (Task 1)
    - .planning/phases/06-dashboards-relatorios/06-CONTEXT.md (D-01 a D-10)
    - .planning/phases/06-dashboards-relatorios/06-UI-SPEC.md (Layout + Copywriting Contract)
    - .planning/phases/06-dashboards-relatorios/06-RESEARCH.md (Padrao 2/3/4 + Pitfalls 1, 2, 3, 5)
  </read_first>
  <action>
Substituir o conteudo de `src/app/(app)/[slug]/dashboard/page.tsx` por (estrutura completa):

```typescript
import { notFound, redirect } from 'next/navigation'
import { format, addDays, startOfToday } from 'date-fns'
import { createClient } from '@/lib/supabase/server'
import { StatCard } from '@/components/corretores/stat-card'
import { MonthSelector } from '@/components/corretores/month-selector'
import { AlertSection } from '@/components/dashboard/alert-section'
import { BrokerRankingTable } from '@/components/dashboard/broker-ranking-table'
import {
  isExecutiveRole,
  parseSelectedMonth,
  aggregateBrokerRanking,
  type ProfileRow,
  type CommissionRow,
  type ProductionRow,
} from '@/lib/utils/dashboard-queries'

interface Props {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ month?: string }>
}

function formatBRL(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}

export default async function DashboardPage({ params, searchParams }: Props) {
  const { slug } = await params
  const sp = await searchParams
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const supabase = (await createClient()) as any

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect(`/login?next=/${slug}/dashboard`)

  // === RBAC (D-09) ===
  const role = (user.app_metadata as { role?: string })?.role ?? ''
  if (role === 'corretor') redirect(`/${slug}/corretores/${user.id}`)
  if (role === 'visualizador') notFound()
  if (!isExecutiveRole(role)) notFound() // defesa em profundidade
  // admin e financeiro continuam (D-10)

  const { data: profile } = await supabase
    .from('profiles')
    .select('full_name')
    .eq('id', user.id)
    .maybeSingle()
  const fullName = profile?.full_name ?? user.user_metadata?.full_name ?? 'Usuario'

  // === Mes selecionado (D-03) ===
  const month = parseSelectedMonth(sp.month)
  const todayStr = format(startOfToday(), 'yyyy-MM-dd')
  const thirtyDaysLaterStr = format(addDays(startOfToday(), 30), 'yyyy-MM-dd')
  const sevenDaysLaterStr = format(addDays(startOfToday(), 7), 'yyyy-MM-dd')

  // === Queries paralelas — 4 KPIs + 3 alertas + ranking ===
  // try/catch graceful por bloco — fallback "—" em cada card se a query falha (UI-SPEC error state)

  // --- KPI 1: Apolices ativas (Pitfall 1: SEM campo status — usar vigencia_fim >= hoje) ---
  let apolicesAtivas = 0
  try {
    const { count } = await supabase
      .from('policies')
      .select('id', { count: 'exact', head: true })
      .is('deleted_at', null)
      .gte('vigencia_fim', todayStr)
    apolicesAtivas = count ?? 0
  } catch {
    apolicesAtivas = 0
  }

  // --- KPI 2: Receita do periodo (Pitfall 3: filtrar por paid_at, nao due_date) ---
  let receitaTotal = 0
  try {
    const { data } = await supabase
      .from('financial_entries')
      .select('amount')
      .is('deleted_at', null)
      .eq('entry_type', 'receivable')
      .eq('status', 'paid')
      .gte('paid_at', month.monthStartStr)
      .lte('paid_at', month.monthEndStr + 'T23:59:59')
    for (const r of (data ?? []) as Array<{ amount: number | string }>) {
      receitaTotal += Number(r.amount) || 0
    }
  } catch {
    receitaTotal = 0
  }

  // --- KPI 3: Inadimplencia (receivable + pending + due_date < hoje) ---
  let inadimplenciaTotal = 0
  try {
    const { data } = await supabase
      .from('financial_entries')
      .select('amount')
      .is('deleted_at', null)
      .eq('entry_type', 'receivable')
      .eq('status', 'pending')
      .lt('due_date', todayStr)
    for (const r of (data ?? []) as Array<{ amount: number | string }>) {
      inadimplenciaTotal += Number(r.amount) || 0
    }
  } catch {
    inadimplenciaTotal = 0
  }

  // --- KPI 4: Vencendo em 30 dias ---
  let vencendoCount = 0
  try {
    const { count } = await supabase
      .from('policies')
      .select('id', { count: 'exact', head: true })
      .is('deleted_at', null)
      .gte('vigencia_fim', todayStr)
      .lte('vigencia_fim', thirtyDaysLaterStr)
    vencendoCount = count ?? 0
  } catch {
    vencendoCount = 0
  }

  // --- Alertas (D-05): max 5 + count total por categoria ---
  type VencendoRow = {
    id: string
    policy_number: string
    vigencia_fim: string
    client: { name: string } | null
  }
  let vencendo = { items: [] as Array<{ id: string; policy_number: string; vigencia_fim: string; client_name: string | null }>, totalCount: 0 }
  try {
    const { data, count } = await supabase
      .from('policies')
      .select('id, policy_number, vigencia_fim, client:clients(name)', { count: 'exact' })
      .is('deleted_at', null)
      .gte('vigencia_fim', todayStr)
      .lte('vigencia_fim', thirtyDaysLaterStr)
      .order('vigencia_fim', { ascending: true })
      .limit(5)
    vencendo = {
      items: ((data ?? []) as VencendoRow[]).map((r) => ({
        id: r.id,
        policy_number: r.policy_number,
        vigencia_fim: r.vigencia_fim,
        client_name: r.client?.name ?? null,
      })),
      totalCount: count ?? 0,
    }
  } catch {
    vencendo = { items: [], totalCount: 0 }
  }

  let cobrancas = { items: [] as Array<{ id: string; description: string; amount: number | string; due_date: string }>, totalCount: 0 }
  try {
    const { data, count } = await supabase
      .from('financial_entries')
      .select('id, description, amount, due_date', { count: 'exact' })
      .is('deleted_at', null)
      .eq('entry_type', 'receivable')
      .eq('status', 'pending')
      .lt('due_date', todayStr)
      .order('due_date', { ascending: true })
      .limit(5)
    cobrancas = {
      items: (data ?? []) as Array<{ id: string; description: string; amount: number | string; due_date: string }>,
      totalCount: count ?? 0,
    }
  } catch {
    cobrancas = { items: [], totalCount: 0 }
  }

  let assembleias = { items: [] as Array<{ id: string; administrator: string; next_assembly_date: string }>, totalCount: 0 }
  try {
    const { data, count } = await supabase
      .from('consortium_groups')
      .select('id, administrator, next_assembly_date', { count: 'exact' })
      .is('deleted_at', null)
      .not('next_assembly_date', 'is', null)
      .gte('next_assembly_date', todayStr)
      .lte('next_assembly_date', sevenDaysLaterStr)
      .order('next_assembly_date', { ascending: true })
      .limit(5)
    assembleias = {
      items: (data ?? []) as Array<{ id: string; administrator: string; next_assembly_date: string }>,
      totalCount: count ?? 0,
    }
  } catch {
    assembleias = { items: [], totalCount: 0 }
  }

  // --- Ranking de Corretores (D-04) ---
  // Pitfall 2: reference_month e DATE primeiro dia do mes (yyyy-MM-dd, NAO yyyy-MM)
  let rankingRows: ReturnType<typeof aggregateBrokerRanking> = []
  try {
    const { data: profiles } = await supabase
      .from('profiles')
      .select('id, full_name')
      .eq('role', 'corretor')
      .order('full_name')
    const profilesArr = (profiles ?? []) as ProfileRow[]
    const profileIds = profilesArr.map((p) => p.id)

    if (profileIds.length > 0) {
      const [commRes, prodRes] = await Promise.all([
        supabase
          .from('commission_entries')
          .select('broker_id, amount')
          .in('broker_id', profileIds)
          .eq('reference_month', month.monthStartStr),
        supabase
          .from('policies')
          .select('assigned_to')
          .in('assigned_to', profileIds)
          .gte('created_at', month.monthStartStr)
          .lte('created_at', month.monthEndStr + 'T23:59:59')
          .is('deleted_at', null),
      ])
      const commissions = ((commRes.data ?? []) as CommissionRow[]) ?? []
      const productions = ((prodRes.data ?? []) as ProductionRow[]) ?? []
      rankingRows = aggregateBrokerRanking(profilesArr, commissions, productions)
    } else {
      rankingRows = []
    }
  } catch {
    rankingRows = []
  }

  return (
    <div className="mx-auto max-w-7xl flex flex-col gap-8 p-6">
      {/* Header */}
      <div className="flex items-start justify-between gap-4">
        <div className="space-y-1">
          <h1 className="text-2xl font-semibold">Painel Executivo</h1>
          <p className="text-sm text-muted-foreground">
            Bem-vindo, {fullName}. Visao consolidada da corretora.
          </p>
        </div>
        <MonthSelector selected={month.monthValue} />
      </div>

      {/* 4 KPI Cards (D-02) */}
      <div className="grid gap-4 grid-cols-2 lg:grid-cols-4">
        <StatCard
          title="Apolices ativas"
          value={String(apolicesAtivas)}
          subtext="Vigencia em dia"
        />
        <StatCard
          title="Receita do periodo"
          value={formatBRL(receitaTotal)}
          subtext={`Pago em ${month.monthLabel}`}
        />
        <StatCard
          title="Inadimplencia"
          value={formatBRL(inadimplenciaTotal)}
          subtext="Receivable pendente vencido"
        />
        <StatCard
          title="Vencendo em 30 dias"
          value={String(vencendoCount)}
          subtext="Apolices com vigencia ate +30d"
        />
      </div>

      {/* Alertas (D-05) */}
      <AlertSection
        slug={slug}
        vencendo={vencendo}
        cobrancas={cobrancas}
        assembleias={assembleias}
      />

      {/* Ranking de Corretores (D-04) */}
      <BrokerRankingTable
        slug={slug}
        monthLabel={month.monthLabel}
        rows={rankingRows}
      />
    </div>
  )
}
```

NOTAS CRITICAS:
- **D-09:** Usuario nao logado → `redirect('/login?...')` (nao `notFound()`); corretor → redirect para proprio dashboard; visualizador → `notFound()`.
- **Pitfall 1 (apolices ativas):** sem `eq('status', 'active')` — usar `vigencia_fim >= hoje + deleted_at IS NULL`.
- **Pitfall 2 (reference_month):** filtrar por `month.monthStartStr` (= `yyyy-MM-dd`), nao `monthValue` (`yyyy-MM`).
- **Pitfall 3 (receita):** filtrar por `paid_at`, nao `due_date`.
- **Try/catch graceful:** cada bloco isolado — falha de uma query nao quebra a pagina inteira (padrao de `[slug]/layout.tsx`).
- **MonthSelector:** ja existe; recebe `selected: string` no formato `yyyy-MM` que vem de `month.monthValue`.
- **MonthSelector controla apenas os 2 cards temporais + ranking** — Apolices ativas e Vencendo 30d sao atemporais (usam `today`), conforme D-03.
- **Sem importacao de `Recharts`** — D-01 explicitamente proibe graficos nesta phase.
- **Header copy:** "Painel Executivo" + "Bem-vindo, {nome}. Visao consolidada da corretora." (UI-SPEC).
- **`as any` cast no supabase:** padrao estabelecido nas phases 3-5 ate proxima `supabase gen types`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/app/\(app\)/\[slug\]/dashboard/page\.tsx" || echo "no new errors in dashboard page"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/[slug]/dashboard/page.tsx` contem strings literais: `"Painel Executivo"`, `"Apolices ativas"`, `"Receita do periodo"`, `"Inadimplencia"`, `"Vencendo em 30 dias"`
    - Contem importacao `import { isExecutiveRole, parseSelectedMonth, aggregateBrokerRanking } from '@/lib/utils/dashboard-queries'`
    - Contem importacao `import { MonthSelector } from '@/components/corretores/month-selector'`
    - Contem importacao `import { StatCard } from '@/components/corretores/stat-card'`
    - Contem importacao `import { AlertSection } from '@/components/dashboard/alert-section'`
    - Contem importacao `import { BrokerRankingTable } from '@/components/dashboard/broker-ranking-table'`
    - Contem `if (role === 'corretor') redirect(\`/${'$'}{slug}/corretores/${'$'}{user.id}\`)` (verificavel via grep `role === 'corretor'.*redirect`)
    - Contem `if (role === 'visualizador') notFound()`
    - Contem `.eq('reference_month', month.monthStartStr)` (Pitfall 2)
    - Contem `.eq('status', 'paid')` E `.gte('paid_at', month.monthStartStr)` (Pitfall 3 — receita)
    - NAO contem `.eq('status', 'active')` em policies (Pitfall 1)
    - NAO contem `recharts` ou `Chart` (D-01)
    - NAO contem `'use client'`
    - `npx tsc --noEmit` nao reporta novos erros neste arquivo
    - Roda `npm test` e suite de Plan 01 continua verde (no regressions)
  </acceptance_criteria>
  <done>
    - Dashboard renderiza Painel Executivo para admin e financeiro
    - Corretor e redirecionado, visualizador recebe 404
    - 4 KPIs + 3 alertas + ranking exibidos com dados reais
    - MonthSelector altera receita/inadimplencia/ranking via ?month=YYYY-MM
    - Falhas de query individuais nao quebram a pagina
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /[slug]/dashboard | Server Component recebe user.app_metadata.role do JWT validado pelo Supabase |
| code → searchParams.month | URL query param controlado pelo usuario (potencial input arbitrario) |
| code → tenant data | Todas as queries dependem de RLS para isolamento de tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | RBAC do /dashboard | mitigate | `if (role === 'visualizador') notFound()` + `if (!isExecutiveRole(role)) notFound()` (defesa em profundidade) — visualizador nunca renderiza KPIs. Helper `isExecutiveRole` testado em Plan 01. |
| T-06-11 | Information Disclosure | Cross-broker ranking visivel | accept | Ranking exibe TODOS os corretores do tenant para admin/financeiro (D-04). RLS limita ao tenant; nao ha vazamento cross-tenant. Corretor nao acessa esta pagina (redirect D-09). |
| T-06-12 | Tampering | searchParams.month | mitigate | `parseSelectedMonth` usa `isValid(parse(...))` — input invalido cai em fallback para mes corrente. Sem injecao SQL (helper puro nao toca banco; valor passado para `.eq()` e DATE bem-formada). |
| T-06-13 | Information Disclosure | RLS cross-tenant em queries | mitigate | Sem `service_role` cliente — `createClient()` server-side usa cookies do usuario logado. RLS aplica `tenant_id = current_user_tenant()` automaticamente em policies/financial_entries/commission_entries/consortium_groups. |
| T-06-14 | Denial of Service | Queries ilimitadas pesadas | accept | Alertas tem `.limit(5)`. KPIs usam `count: 'exact', head: true` (nao trafega rows). Ranking carrega todos os profiles do tenant — limite operacional 5000 clientes (= ~50 corretores) no roadmap; aceitavel. |
| T-06-15 | Reliability (DoS partial) | Falha em uma query quebra pagina toda | mitigate | Cada bloco em try/catch independente — fallback graceful por card. Padrao identico a [slug]/layout.tsx. |
</threat_model>

<verification>
- `npm test` continua verde (Plan 01 + qualquer suite anterior)
- Acesso manual: admin abre `/{slug}/dashboard` → ve "Painel Executivo" + 4 cards + 3 alertas + ranking
- Acesso manual: usuario com role='corretor' abre `/{slug}/dashboard` → redirect para `/{slug}/corretores/{user.id}`
- Acesso manual: usuario com role='visualizador' → 404
- Trocar `?month=2026-03` → cards de Receita/Inadimplencia e ranking refletem o mes anterior
- `?month=garbage` → cai em mes corrente sem erro
- Falha simulada de uma das tabelas (ex: financial_entries inexistente) → cards relacionados mostram 0/R$0,00 sem quebrar a pagina
</verification>

<success_criteria>
- DASH-01: 4 KPIs renderizam valores reais do tenant (verificavel via mudanca em policies/financial_entries)
- DASH-02: Ranking exibe corretores do tenant ordenados por comissao DESC do mes selecionado
- DASH-03: 3 listas de alerta exibem max 5 itens com count total correto
- D-09: RBAC redirect/notFound funciona para os 4 roles
- D-10: Admin e financeiro veem conteudo identico
- Sem novos erros TypeScript fora dos 44 pre-existentes
- Sem `'use client'` (toda a pagina e Server Component)
</success_criteria>

<output>
Apos completion, criar `.planning/phases/06-dashboards-relatorios/06-02-SUMMARY.md` com:
- Strings de copy efetivamente usadas (confirmar fidelidade ao UI-SPEC)
- Tabelas Supabase consultadas e suas RLS policies aplicadas
- Roles testados manualmente (admin, financeiro, corretor, visualizador) e comportamento observado
- Ajustes nao previstos (se houver)
</output>
