---
phase: 07-automacoes-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - supabase/migrations/20260420_0021_automacoes_schema.sql
  - supabase/migrations/20260420_0022_automacoes_rls.sql
  - supabase/migrations/20260420_0023_automacoes_cron.sql
  - supabase/config.toml
  - src/lib/validations/automation-schemas.ts
  - src/lib/utils/webhook-url.ts
  - src/lib/utils/email-template.ts
  - src/lib/utils/ai-escalation.ts
  - src/emails/policy-expiring.tsx
  - src/emails/financial-overdue.tsx
  - src/emails/consortium-contemplated.tsx
  - src/app/api/internal/send-automation-email/route.ts
  - tests/utils/webhook-url.test.ts
  - tests/utils/email-template.test.ts
  - tests/utils/ai-escalation.test.ts
  - tests/utils/dispatch-webhook.test.ts
  - tests/actions/webhook-configs.test.ts
  - tests/actions/email-templates.test.ts
  - tests/actions/whatsapp-endpoint.test.ts
  - tests/actions/chat-isolation.test.ts
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04
  - AUTO-05
  - AUTO-06
tags: [schema, rls, supabase, migrations, ai-sdk, validators, wave-0, tests, react-email]

must_haves:
  truths:
    - "Migration 0021 cria webhook_configs, webhook_logs, email_templates com tenant_id NOT NULL e CHECK em event_type"
    - "Migration 0022 habilita RLS nas 3 tabelas com role='admin' guard via jwt_tenant_role()"
    - "Migration 0023 agenda cron 'automation-cron-daily' em 11:00 UTC (08:00 BRT) que faz POST a /functions/v1/automation-cron"
    - "supabase db push aplica as 3 migrations sem erro"
    - "config.toml inclui [functions.automation-cron] com verify_jwt = false"
    - "ai, @ai-sdk/openai, @ai-sdk/react instalados em package.json"
    - "react-email, @react-email/components, @react-email/render instalados em package.json"
    - "isUrlSafe() rejeita IPs privados RFC1918, loopback, link-local e protocolos nao-http/https"
    - "renderEmailTemplate() substitui as 6 variaveis E escapa HTML em valores"
    - "isLowConfidenceResponse() retorna true quando finishReason='length' OU text.length<20"
    - "3 React Email templates existem em src/emails/ e compilam sem erro TypeScript"
    - "POST /api/internal/send-automation-email retorna 403 quando x-internal-secret invalido"
    - "Wave 0 cria 8 arquivos de teste com stubs verde (Vitest passa) cobrindo todos os requirements"
  artifacts:
    - path: "supabase/migrations/20260420_0021_automacoes_schema.sql"
      provides: "Schema das 3 tabelas com FKs, indices, UNIQUE composto"
      contains: "CREATE TABLE public.webhook_configs"
      min_lines: 60
    - path: "supabase/migrations/20260420_0022_automacoes_rls.sql"
      provides: "RLS policies admin-only para webhook_configs e email_templates; SELECT-only para webhook_logs"
      contains: "jwt_tenant_role()"
      min_lines: 40
    - path: "supabase/migrations/20260420_0023_automacoes_cron.sql"
      provides: "pg_cron schedule + pg_net invocando Edge Function"
      contains: "cron.schedule"
    - path: "src/lib/utils/webhook-url.ts"
      provides: "isUrlSafe() puro — SSRF guard contra T-07-SSRF"
      exports: ["isUrlSafe"]
      min_lines: 20
    - path: "src/lib/utils/email-template.ts"
      provides: "renderEmailTemplate() com escape HTML e default templates por evento"
      exports: ["renderEmailTemplate", "DEFAULT_TEMPLATES", "TEMPLATE_VARIABLES"]
      min_lines: 50
    - path: "src/lib/utils/ai-escalation.ts"
      provides: "isLowConfidenceResponse() puro — heuristicas para AUTO-05"
      exports: ["isLowConfidenceResponse", "ESCALATION_MESSAGE"]
    - path: "src/lib/validations/automation-schemas.ts"
      provides: "Zod schemas de webhook_configs e email_templates"
      exports: ["webhookConfigSchema", "emailTemplateSchema"]
    - path: "src/emails/policy-expiring.tsx"
      provides: "React Email template para vencimento de apolice (D-07)"
      contains: "PolicyExpiringEmail"
    - path: "src/emails/financial-overdue.tsx"
      provides: "React Email template para inadimplencia financeira (D-07)"
      contains: "FinancialOverdueEmail"
    - path: "src/emails/consortium-contemplated.tsx"
      provides: "React Email template para contemplacao de consorcio (D-07)"
      contains: "ConsortiumContemplatedEmail"
    - path: "src/app/api/internal/send-automation-email/route.ts"
      provides: "Route Handler Node.js que renderiza React Email e chama Resend — consumido pela Edge Function (D-07)"
      contains: "INTERNAL_EMAIL_SECRET"
      min_lines: 50
    - path: "package.json"
      provides: "AI SDK v6 + React Email dependencies"
      contains: "\"ai\":"
    - path: "tests/utils/webhook-url.test.ts"
      provides: "Stubs cobrindo SSRF — IPs privados, loopback, esquemas invalidos"
    - path: "tests/utils/email-template.test.ts"
      provides: "Stubs cobrindo substituicao de 6 vars + HTML escaping"
    - path: "tests/utils/ai-escalation.test.ts"
      provides: "Stubs cobrindo finishReason e text.length heuristicas"
    - path: "tests/utils/dispatch-webhook.test.ts"
      provides: "Stub do dispatch (mock fetch) — sera populado em Plan 02"
    - path: "tests/actions/webhook-configs.test.ts"
      provides: "Stubs RBAC admin-only para Server Actions de Plan 02"
    - path: "tests/actions/email-templates.test.ts"
      provides: "Stubs RBAC admin-only para Server Actions de Plan 02"
    - path: "tests/actions/whatsapp-endpoint.test.ts"
      provides: "Stubs auth header check para Plan 04 Route Handler"
    - path: "tests/actions/chat-isolation.test.ts"
      provides: "Stubs cross-tenant guard para Plan 04 Route Handler"
  key_links:
    - from: "supabase/migrations/20260420_0023_automacoes_cron.sql"
      to: "supabase/functions/automation-cron/index.ts (criado em Plan 02)"
      via: "net.http_post() para /functions/v1/automation-cron"
      pattern: "automation-cron"
    - from: "supabase/migrations/20260420_0022_automacoes_rls.sql"
      to: "supabase/migrations/20260420_0002_rls_helpers.sql"
      via: "jwt_tenant_id() + jwt_tenant_role()"
      pattern: "jwt_tenant_role"
    - from: "supabase/functions/automation-cron/index.ts (Plan 02)"
      to: "src/app/api/internal/send-automation-email/route.ts"
      via: "POST com x-internal-secret header"
      pattern: "send-automation-email"
    - from: "src/app/api/internal/send-automation-email/route.ts"
      to: "src/emails/policy-expiring.tsx + financial-overdue.tsx + consortium-contemplated.tsx"
      via: "render() from @react-email/render"
      pattern: "render\\("
---

<objective>
Wave 1 fundacao da Phase 7: schema SQL para 3 tabelas (webhook_configs, webhook_logs, email_templates) + RLS admin-guard, agendamento cron via pg_cron+pg_net, instalacao do AI SDK v6 + React Email packages, helpers puros (SSRF guard, template renderer, AI escalation heuristics), 3 React Email templates (D-07), Route Handler interno para envio de email via React Email + Resend, e Wave 0 — todos os 8 arquivos de teste exigidos por VALIDATION.md criados como stubs verdes.

**Purpose:** Estabelece o substrato de dados (D-01, D-02, D-03), a configuracao de cron (D-01 horario), a fronteira RBAC (D-09), os utilitarios puros que Plan 02/03/04 consomem, e o contrato de email (D-07) via Route Handler Node.js com React Email. Sem este Plan, nada da Phase 7 funciona — webhook nao tem onde ser configurado, email nao tem template, IA nao tem dependencia instalada.

**D-07 compliance:** O template padrao e fixo via React Email (`src/emails/*.tsx`). A Edge Function (Deno, Plan 02) NAO renderiza HTML inline — chama `POST /api/internal/send-automation-email` (Node.js Route Handler) que renderiza via `render()` do `@react-email/render`.

**Output:**
- 3 migrations SQL aplicadas via `supabase db push`
- 6 dependencies npm adicionadas (ai, @ai-sdk/openai, @ai-sdk/react, react-email, @react-email/components, @react-email/render)
- 4 utilitarios puros em src/lib/utils/ e src/lib/validations/
- 3 React Email template components em src/emails/
- 1 Route Handler interno (POST /api/internal/send-automation-email)
- 8 arquivos de teste populados com stubs verdes
- config.toml estendido com `[functions.automation-cron]`
</objective>

<execution_context>
@C:/Users/Reinaldo - Local/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Reinaldo - Local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-automacoes-ia/07-CONTEXT.md
@.planning/phases/07-automacoes-ia/07-RESEARCH.md
@.planning/phases/07-automacoes-ia/07-VALIDATION.md
@supabase/migrations/20260420_0002_rls_helpers.sql
@supabase/migrations/20260420_0019_financeiro_schema.sql
@supabase/migrations/20260420_0020_financeiro_rls.sql
@supabase/config.toml
@vitest.config.ts

<interfaces>
<!-- Funcoes RLS ja existentes, usadas pelas policies das 3 novas tabelas -->

```sql
-- supabase/migrations/20260420_0002_rls_helpers.sql
public.jwt_tenant_id() RETURNS UUID  -- le app_metadata.tenant_id do JWT
public.jwt_tenant_role() RETURNS TEXT  -- le app_metadata.role do JWT
public.jwt_tenant_slug() RETURNS TEXT
```

<!-- Triggers de soft delete ja existentes -->
```sql
-- supabase/migrations/20260420_0001_foundation_schema.sql
public.set_updated_at()        -- trigger function
public.prevent_hard_delete()    -- trigger function
```

<!-- FK destinos -->
```sql
public.tenants(id) UUID
public.profiles(id) UUID
```

<!-- Vitest config -->
- Test paths: tests/**/*.test.ts | tests/**/*.test.tsx
- Aliases: @ -> src, server-only -> tests/__mocks__/server-only.ts
- Setup: tests/setup.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — instalar AI SDK + React Email + criar 8 arquivos de teste com stubs verdes</name>
  <files>
    package.json,
    tests/utils/webhook-url.test.ts,
    tests/utils/email-template.test.ts,
    tests/utils/ai-escalation.test.ts,
    tests/utils/dispatch-webhook.test.ts,
    tests/actions/webhook-configs.test.ts,
    tests/actions/email-templates.test.ts,
    tests/actions/whatsapp-endpoint.test.ts,
    tests/actions/chat-isolation.test.ts
  </files>
  <read_first>
    .planning/phases/07-automacoes-ia/07-VALIDATION.md (toda a tabela "Per-Task Verification Map" e "Wave 0 Requirements"),
    .planning/phases/07-automacoes-ia/07-RESEARCH.md (sections "Code Examples", "Common Pitfalls", "New Tables Required"),
    tests/utils/dashboard-queries.test.ts (padrao de teste de utilitario puro com Vitest),
    tests/actions/financial-entries.test.ts (padrao de mock vi.mock('@/lib/supabase/server') + vi.mock('next/cache')),
    vitest.config.ts (paths e aliases)
  </read_first>
  <behavior>
    Cada arquivo de teste DEVE conter pelo menos 1 caso it() que pode ser populado em Plans 02/04, mas que ja passa hoje. Use placeholders verdes (`it.todo()` NAO permitido — use `it('placeholder', () => { expect(true).toBe(true) })` para arquivos de Plan 02/04 onde a logica nao existe ainda; ja tem testes reais para os 3 utilitarios criados nesta task).

    Cobertura de stubs por arquivo:
    - tests/utils/webhook-url.test.ts: 5 testes reais sobre `isUrlSafe()` (https publica OK, http://localhost rejeitada, http://10.0.0.1 rejeitada, http://192.168.1.1 rejeitada, ftp://x.com rejeitada)
    - tests/utils/email-template.test.ts: 4 testes reais sobre `renderEmailTemplate()` (substitui {{nome_cliente}}, escapa < > & ", retorna fallback DEFAULT_TEMPLATES['policy_expiring'].subject quando subject vazio, suporta as 6 variaveis)
    - tests/utils/ai-escalation.test.ts: 4 testes reais sobre `isLowConfidenceResponse()` (length finishReason -> true, text < 20 chars -> true, normal completion + text >= 20 -> false, [INCERTO] prefix -> true)
    - tests/utils/dispatch-webhook.test.ts: 1 teste placeholder `expect(true).toBe(true)` (logica criada em Plan 02)
    - tests/actions/webhook-configs.test.ts: 1 teste placeholder (Server Action criada em Plan 02)
    - tests/actions/email-templates.test.ts: 1 teste placeholder
    - tests/actions/whatsapp-endpoint.test.ts: 1 teste placeholder
    - tests/actions/chat-isolation.test.ts: 1 teste placeholder
  </behavior>
  <action>
    1. Instalar AI SDK v6 dependencies. Executar:
       ```bash
       npm install ai@^6.0.0 @ai-sdk/openai@^3.0.0 @ai-sdk/react@^1.0.0
       ```
       Verificar que package.json contem todas as 3 entries em "dependencies".

    2. Instalar React Email dependencies (D-07 — templates fixos via React Email):
       ```bash
       npm install react-email @react-email/components @react-email/render
       ```
       Verificar que package.json contem as 3 entries.

    3. Criar tests/utils/webhook-url.test.ts importando `{ isUrlSafe }` de `@/lib/utils/webhook-url`. Casos exatos:
       ```typescript
       import { describe, it, expect } from 'vitest'
       import { isUrlSafe } from '@/lib/utils/webhook-url'

       describe('isUrlSafe', () => {
         it('aceita https publico', () => { expect(isUrlSafe('https://api.example.com/webhook')).toBe(true) })
         it('rejeita http://localhost', () => { expect(isUrlSafe('http://localhost/x')).toBe(false) })
         it('rejeita IP RFC1918 10.x', () => { expect(isUrlSafe('http://10.0.0.1/x')).toBe(false) })
         it('rejeita IP RFC1918 192.168.x', () => { expect(isUrlSafe('https://192.168.1.1/y')).toBe(false) })
         it('rejeita protocolo ftp', () => { expect(isUrlSafe('ftp://x.com')).toBe(false) })
         it('rejeita 169.254.x (link-local)', () => { expect(isUrlSafe('http://169.254.169.254/')).toBe(false) })
         it('rejeita string nao-URL', () => { expect(isUrlSafe('not a url')).toBe(false) })
       })
       ```

    4. Criar tests/utils/email-template.test.ts importando `{ renderEmailTemplate, DEFAULT_TEMPLATES }` de `@/lib/utils/email-template`. Casos:
       - Substitui `{{nome_cliente}}` por valor passado
       - Escapa `<script>` para `&lt;script&gt;` no valor
       - Substitui as 6 variaveis (`nome_cliente`, `cpf_cnpj`, `vencimento`, `valor`, `nome_apolice`, `corretor`)
       - DEFAULT_TEMPLATES tem chaves `policy_expiring`, `financial_overdue`, `consortium_contemplated`

    5. Criar tests/utils/ai-escalation.test.ts importando `{ isLowConfidenceResponse, ESCALATION_MESSAGE }` de `@/lib/utils/ai-escalation`. Casos:
       - `isLowConfidenceResponse({ finishReason: 'length', text: 'long text long text long text' })` -> true
       - `isLowConfidenceResponse({ finishReason: 'stop', text: 'curto' })` -> true (text < 20)
       - `isLowConfidenceResponse({ finishReason: 'stop', text: 'A apolice X esta vigente ate 30/05/2026' })` -> false
       - `isLowConfidenceResponse({ finishReason: 'stop', text: '[INCERTO] nao tenho dados suficientes' })` -> true
       - ESCALATION_MESSAGE eh string nao-vazia

    6. Criar 5 placeholder test files (dispatch-webhook, webhook-configs, email-templates, whatsapp-endpoint, chat-isolation) com:
       ```typescript
       import { describe, it, expect } from 'vitest'

       describe('placeholder — populado em Plan 0X', () => {
         it('compila sem erros', () => { expect(true).toBe(true) })
       })
       ```

    7. Criar diretorios `tests/utils/` e `tests/actions/` se nao existirem (eles ja existem). Nao criar arquivos .gitkeep.

    8. Executar `npm test` — todos os 8 novos arquivos de teste devem passar (alguns como placeholder, outros com asserts reais sobre os 3 utilitarios — que serao criados nas Tasks 2/3/4).
       NOTA: o npm test SO passara apos os utilitarios das Tasks 3/4 existirem. Esta Task 1 faz commit dos testes; se quiser commits atomicos, executar Task 1 ANTES de testar — ou aceitar que testes ficam vermelhos ate Task 4 completar.

    DECISAO: criar testes vermelhos primeiro (TDD-style) — Task 4 fecha o ciclo verde. Aceitar `npm test` falhando entre Task 1 e Task 4 deste plano.
  </action>
  <verify>
    <automated>grep -E '"ai"|"@ai-sdk/openai"|"@ai-sdk/react"' package.json && grep -E '"react-email"|"@react-email/components"|"@react-email/render"' package.json && ls tests/utils/webhook-url.test.ts tests/utils/email-template.test.ts tests/utils/ai-escalation.test.ts tests/utils/dispatch-webhook.test.ts tests/actions/webhook-configs.test.ts tests/actions/email-templates.test.ts tests/actions/whatsapp-endpoint.test.ts tests/actions/chat-isolation.test.ts && npx vitest run tests/utils/dispatch-webhook.test.ts tests/actions/webhook-configs.test.ts tests/actions/email-templates.test.ts tests/actions/whatsapp-endpoint.test.ts tests/actions/chat-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"ai"|"@ai-sdk/openai"|"@ai-sdk/react"' package.json` retorna 3+ matches
    - `grep -E '"react-email"|"@react-email/components"|"@react-email/render"' package.json` retorna 3 matches
    - 8 arquivos `tests/utils/*.test.ts` e `tests/actions/*.test.ts` existem e cada um tem pelo menos 1 `it(`
    - `tests/utils/webhook-url.test.ts` contem `import { isUrlSafe }` e tem >=5 chamadas a `expect(isUrlSafe(...)).toBe(...)`
    - `tests/utils/email-template.test.ts` contem `import { renderEmailTemplate, DEFAULT_TEMPLATES }`
    - `tests/utils/ai-escalation.test.ts` contem `import { isLowConfidenceResponse, ESCALATION_MESSAGE }`
    - 5 arquivos placeholder contem string `populado em Plan` no describe
  </acceptance_criteria>
  <done>Wave 0 completa: 8 test stubs no diretorio correto + AI SDK v6 + React Email instalados em package.json. Tasks subsequentes criam os utilitarios que tornam os testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations SQL — webhook_configs, webhook_logs, email_templates + RLS + cron schedule + config.toml</name>
  <files>
    supabase/migrations/20260420_0021_automacoes_schema.sql,
    supabase/migrations/20260420_0022_automacoes_rls.sql,
    supabase/migrations/20260420_0023_automacoes_cron.sql,
    supabase/config.toml
  </files>
  <read_first>
    supabase/migrations/20260420_0019_financeiro_schema.sql (padrao de tabela: tenant_id NOT NULL REFERENCES tenants(id), trigger updated_at, trigger prevent_hard_delete, indices padroes),
    supabase/migrations/20260420_0020_financeiro_rls.sql (padrao de policy SELECT/INSERT/UPDATE com jwt_tenant_role e jwt_tenant_id),
    supabase/migrations/20260420_0001_foundation_schema.sql (existencia de set_updated_at e prevent_hard_delete trigger functions),
    supabase/migrations/20260420_0002_rls_helpers.sql (jwt_tenant_id, jwt_tenant_role),
    supabase/config.toml (linhas 371-373 — formato existente de [functions.custom-access-token]),
    .planning/phases/07-automacoes-ia/07-RESEARCH.md (sections "New Tables Required" linhas 452-535 e "Architecture Patterns -> Pattern 1" linhas 146-189)
  </read_first>
  <behavior>
    Migration 0021 cria 3 tabelas com EXATAMENTE os campos especificados em RESEARCH.md "New Tables Required" + indices. Migration 0022 cria RLS policies role='admin' guard. Migration 0023 cria pg_cron schedule. config.toml ganha 1 nova secao.

    Constraints estruturais (verificaveis):
    - webhook_configs: PK uuid, FK tenant_id NOT NULL REFERENCES tenants(id), CHECK event_type IN ('policy_expiring','financial_overdue','consortium_contemplated'), UNIQUE (tenant_id, event_type) WHERE deleted_at IS NULL, deleted_at TIMESTAMPTZ, created_at NOT NULL, updated_at NOT NULL.
    - webhook_logs: PK uuid, FK tenant_id NOT NULL, FK config_id REFERENCES webhook_configs(id), JSONB payload NOT NULL DEFAULT '{}', http_status INTEGER nullable, error_message TEXT nullable, triggered_at NOT NULL DEFAULT NOW(), index composto (tenant_id, triggered_at DESC), SEM updated_at, SEM deleted_at.
    - email_templates: PK uuid, FK tenant_id NOT NULL, CHECK event_type, UNIQUE (tenant_id, event_type) WHERE deleted_at IS NULL, body_html TEXT NOT NULL, deleted_at, updated_at.
  </behavior>
  <action>
    1. Criar `supabase/migrations/20260420_0021_automacoes_schema.sql` com EXATAMENTE este conteudo (header em comentario igual ao padrao das phases 5/6):
       ```sql
       -- Phase 07 Plan 01 — Automacoes & IA Schema (D-01, D-02, D-03)
       -- References: 07-CONTEXT.md D-02 (3 eventos core), D-03 (webhook_logs append-only), D-06 (templates por tenant)
       -- 07-RESEARCH.md New Tables Required

       -- ========================================================================
       -- webhook_configs — config de webhook por tenant + evento (D-01, D-02)
       -- ========================================================================
       CREATE TABLE public.webhook_configs (
         id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id     UUID NOT NULL REFERENCES public.tenants(id),
         event_type    TEXT NOT NULL CHECK (event_type IN ('policy_expiring','financial_overdue','consortium_contemplated')),
         url           TEXT NOT NULL,
         days_before   INTEGER CHECK (days_before > 0),
         active        BOOLEAN NOT NULL DEFAULT true,
         created_by    UUID NOT NULL REFERENCES public.profiles(id),
         deleted_at    TIMESTAMPTZ,
         created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
       );
       CREATE UNIQUE INDEX webhook_configs_tenant_event_unique ON public.webhook_configs (tenant_id, event_type) WHERE deleted_at IS NULL;
       CREATE INDEX idx_webhook_configs_tenant_id ON public.webhook_configs(tenant_id);
       CREATE INDEX idx_webhook_configs_event_type ON public.webhook_configs(event_type);
       CREATE INDEX idx_webhook_configs_active ON public.webhook_configs(active) WHERE active = true AND deleted_at IS NULL;

       CREATE TRIGGER trg_webhook_configs_updated_at BEFORE UPDATE ON public.webhook_configs
         FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();
       CREATE TRIGGER trg_webhook_configs_no_hard_delete BEFORE DELETE ON public.webhook_configs
         FOR EACH ROW EXECUTE FUNCTION public.prevent_hard_delete();

       -- ========================================================================
       -- webhook_logs — historico append-only de disparos (D-03 logar e seguir)
       -- ========================================================================
       CREATE TABLE public.webhook_logs (
         id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id     UUID NOT NULL REFERENCES public.tenants(id),
         event_type    TEXT NOT NULL,
         config_id     UUID REFERENCES public.webhook_configs(id),
         url_destino   TEXT NOT NULL,
         payload       JSONB NOT NULL DEFAULT '{}',
         http_status   INTEGER,
         error_message TEXT,
         triggered_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
       );
       CREATE INDEX idx_webhook_logs_tenant_created ON public.webhook_logs(tenant_id, triggered_at DESC);
       CREATE INDEX idx_webhook_logs_config_id ON public.webhook_logs(config_id);

       -- ========================================================================
       -- email_templates — template por tenant + evento (D-06, D-07 fallback default)
       -- ========================================================================
       CREATE TABLE public.email_templates (
         id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id     UUID NOT NULL REFERENCES public.tenants(id),
         event_type    TEXT NOT NULL CHECK (event_type IN ('policy_expiring','financial_overdue','consortium_contemplated')),
         subject       TEXT NOT NULL,
         body_html     TEXT NOT NULL,
         active        BOOLEAN NOT NULL DEFAULT true,
         created_by    UUID NOT NULL REFERENCES public.profiles(id),
         deleted_at    TIMESTAMPTZ,
         created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
       );
       CREATE UNIQUE INDEX email_templates_tenant_event_unique ON public.email_templates (tenant_id, event_type) WHERE deleted_at IS NULL;
       CREATE INDEX idx_email_templates_tenant_id ON public.email_templates(tenant_id);
       CREATE INDEX idx_email_templates_event_type ON public.email_templates(event_type);

       CREATE TRIGGER trg_email_templates_updated_at BEFORE UPDATE ON public.email_templates
         FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();
       CREATE TRIGGER trg_email_templates_no_hard_delete BEFORE DELETE ON public.email_templates
         FOR EACH ROW EXECUTE FUNCTION public.prevent_hard_delete();
       ```

    2. Criar `supabase/migrations/20260420_0022_automacoes_rls.sql`:
       ```sql
       -- Phase 07 Plan 01 — RLS: webhook_configs, webhook_logs, email_templates
       -- D-09: apenas role='admin' do tenant pode ler/escrever
       -- T-07-01 mitigacao: tenant_id isolation via jwt_tenant_id()

       ALTER TABLE public.webhook_configs ENABLE ROW LEVEL SECURITY;
       CREATE POLICY "webhook_configs_admin_manage" ON public.webhook_configs
         FOR ALL TO authenticated
         USING (
           tenant_id = (SELECT public.jwt_tenant_id())
           AND deleted_at IS NULL
           AND (SELECT public.jwt_tenant_role()) = 'admin'
         )
         WITH CHECK (
           tenant_id = (SELECT public.jwt_tenant_id())
           AND (SELECT public.jwt_tenant_role()) = 'admin'
         );

       ALTER TABLE public.webhook_logs ENABLE ROW LEVEL SECURITY;
       CREATE POLICY "webhook_logs_admin_select" ON public.webhook_logs
         FOR SELECT TO authenticated
         USING (
           tenant_id = (SELECT public.jwt_tenant_id())
           AND (SELECT public.jwt_tenant_role()) = 'admin'
         );
       -- SEM INSERT/UPDATE/DELETE: escrita apenas via service_role na Edge Function

       ALTER TABLE public.email_templates ENABLE ROW LEVEL SECURITY;
       CREATE POLICY "email_templates_admin_manage" ON public.email_templates
         FOR ALL TO authenticated
         USING (
           tenant_id = (SELECT public.jwt_tenant_id())
           AND deleted_at IS NULL
           AND (SELECT public.jwt_tenant_role()) = 'admin'
         )
         WITH CHECK (
           tenant_id = (SELECT public.jwt_tenant_id())
           AND (SELECT public.jwt_tenant_role()) = 'admin'
         );
       ```

    3. Criar `supabase/migrations/20260420_0023_automacoes_cron.sql` com habilitacao de extensoes + agendamento. Use environment-substituted approach (publishable key via Vault):
       ```sql
       -- Phase 07 Plan 01 — Cron schedule para automation-cron Edge Function
       -- D-01: 08h BRT diariamente = 11:00 UTC (pg_cron usa UTC exclusivamente — Pitfall 1)
       -- Esta migration pode falhar em ambiente local (sem pg_cron); admin DEVE rodar manualmente em producao

       CREATE EXTENSION IF NOT EXISTS pg_net;
       CREATE EXTENSION IF NOT EXISTS pg_cron;

       -- Remover schedule existente (idempotente)
       SELECT cron.unschedule('automation-cron-daily') WHERE EXISTS (
         SELECT 1 FROM cron.job WHERE jobname = 'automation-cron-daily'
       );

       -- Agendamento: 11:00 UTC = 08:00 BRT
       -- Project URL e service role key sao injetados via Supabase secrets/vault.
       -- Se vault nao estiver configurado, admin substitui inline as 2 strings abaixo no SQL Editor antes de executar.
       SELECT cron.schedule(
         'automation-cron-daily',
         '0 11 * * *',
         $$
         SELECT net.http_post(
           url := COALESCE(
             (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'project_url'),
             current_setting('app.settings.project_url', true)
           ) || '/functions/v1/automation-cron',
           headers := jsonb_build_object(
             'Content-Type', 'application/json',
             'Authorization', 'Bearer ' || COALESCE(
               (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'cron_publishable_key'),
               current_setting('app.settings.cron_publishable_key', true)
             ),
             'x-cron-secret', COALESCE(
               (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'cron_shared_secret'),
               'CHANGE_ME_IN_PRODUCTION'
             )
           ),
           body := jsonb_build_object('triggered_at', NOW()),
           timeout_milliseconds := 30000
         ) AS request_id;
         $$
       );

       -- Documentacao em SQL: para popular vault em producao, executar:
       --   SELECT vault.create_secret('https://<project-ref>.supabase.co', 'project_url');
       --   SELECT vault.create_secret('<ANON_KEY>', 'cron_publishable_key');
       --   SELECT vault.create_secret(gen_random_uuid()::text, 'cron_shared_secret');
       ```

    4. Editar `supabase/config.toml` adicionando novo bloco APOS a linha existente `[functions.custom-access-token]\nverify_jwt = false` (linhas 371-372):
       ```toml
       [functions.automation-cron]
       verify_jwt = false
       ```
       Inserir antes do bloco `[edge_runtime]`. NAO remover o bloco existente.

    5. Verificar sintaxe SQL com `cat`/grep — todos os 3 .sql devem ser parseaveis (nenhum `;` faltando, nenhum bloco $$ aberto sem fechar).

    NOTA OPERACIONAL — D-01 horario: o RESEARCH.md confirma que `0 11 * * *` UTC = 08:00 BRT (UTC-3). Em horario de verao brasileiro nao existe mais (PBV abolido em 2019), portanto fixo em UTC-3 ano todo.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE public.webhook_configs" supabase/migrations/20260420_0021_automacoes_schema.sql && grep -q "CREATE TABLE public.webhook_logs" supabase/migrations/20260420_0021_automacoes_schema.sql && grep -q "CREATE TABLE public.email_templates" supabase/migrations/20260420_0021_automacoes_schema.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260420_0022_automacoes_rls.sql && grep -q "cron.schedule" supabase/migrations/20260420_0023_automacoes_cron.sql && grep -q "\[functions.automation-cron\]" supabase/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "tenant_id UUID NOT NULL REFERENCES public.tenants" supabase/migrations/20260420_0021_automacoes_schema.sql | wc -l` >= 3 (3 tabelas)
    - `grep -c "event_type IN ('policy_expiring','financial_overdue','consortium_contemplated')" supabase/migrations/20260420_0021_automacoes_schema.sql` >= 2 (em webhook_configs e email_templates)
    - `grep -c "jwt_tenant_role()) = 'admin'" supabase/migrations/20260420_0022_automacoes_rls.sql` >= 4 (USING + WITH CHECK em 2 tabelas + USING webhook_logs)
    - `grep -q "CREATE EXTENSION IF NOT EXISTS pg_cron" supabase/migrations/20260420_0023_automacoes_cron.sql`
    - `grep -q "0 11 \* \* \*" supabase/migrations/20260420_0023_automacoes_cron.sql` (cron expression UTC)
    - `grep -A 1 "\[functions.automation-cron\]" supabase/config.toml | grep -q "verify_jwt = false"`
  </acceptance_criteria>
  <done>3 migrations + config.toml prontos. Aguardando supabase db push (Task 4) para aplicar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Helpers puros — webhook-url.ts, email-template.ts, ai-escalation.ts, automation-schemas.ts</name>
  <files>
    src/lib/utils/webhook-url.ts,
    src/lib/utils/email-template.ts,
    src/lib/utils/ai-escalation.ts,
    src/lib/validations/automation-schemas.ts
  </files>
  <read_first>
    src/lib/utils/dashboard-queries.ts (padrao de modulo puro com tipos exportados),
    src/lib/utils/vigencia.ts (padrao de modulo de logica de domain),
    src/lib/validations/financial-schemas.ts (padrao de Zod schema com discriminator + transforms),
    .planning/phases/07-automacoes-ia/07-RESEARCH.md (linha 681-708 SSRF Validation, linhas 252-260 renderTemplate, linhas 575-580 AI escalation),
    tests/utils/webhook-url.test.ts (criado em Task 1 — usar como spec do que isUrlSafe deve fazer),
    tests/utils/email-template.test.ts (criado em Task 1),
    tests/utils/ai-escalation.test.ts (criado em Task 1)
  </read_first>
  <behavior>
    `isUrlSafe(rawUrl: string): boolean` — retorna true APENAS se URL for parseavel, protocolo http/https, hostname NAO esta em lista privada (RFC1918, loopback, link-local, IPv6 link-local).

    `renderEmailTemplate(template: { subject: string; body_html: string }, vars: Record<string, string>): { subject: string; body_html: string }` — substitui {{<key>}} por escapeHtml(value) tanto em subject quanto body_html.

    `escapeHtml(s: string): string` — escapa &, <, >, ", '.

    `DEFAULT_TEMPLATES: Record<EventType, { subject: string; body_html: string }>` — fallback APENAS para o campo `custom_body_html` enviado pela Edge Function ao Route Handler. O template padrao renderizado (D-07) vive nos componentes React Email em src/emails/.

    `TEMPLATE_VARIABLES: readonly ['nome_cliente','cpf_cnpj','vencimento','valor','nome_apolice','corretor']` — whitelist de chaves.

    `isLowConfidenceResponse({ finishReason, text }: { finishReason: string; text: string }): boolean` — retorna true se finishReason==='length' OU text.length<20 OU text.startsWith('[INCERTO]').

    `ESCALATION_MESSAGE: string` — mensagem padrao retornada na escalacao.

    `webhookConfigSchema: z.ZodObject` — campos: event_type enum, url string com .refine(isUrlSafe, 'URL invalida'), days_before z.coerce.number().int().min(1).optional(), active z.coerce.boolean().default(true).

    `emailTemplateSchema: z.ZodObject` — campos: event_type enum, subject min(3) max(200), body_html min(10) max(10000), active default true.
  </behavior>
  <action>
    1. Criar `src/lib/utils/webhook-url.ts`:
       ```typescript
       // src/lib/utils/webhook-url.ts
       // Phase 07 Plan 01 — SSRF guard contra T-07-SSRF (07-RESEARCH.md Pitfall 3)
       // Bloqueia URLs com IPs privados RFC1918, loopback, link-local e protocolos nao-http/https.

       const PRIVATE_HOSTNAME_PATTERNS: RegExp[] = [
         /^localhost$/i,
         /^127\./,
         /^0\.0\.0\.0$/,
         /^10\./,
         /^192\.168\./,
         /^172\.(1[6-9]|2\d|3[01])\./,
         /^169\.254\./,
         /^::1$/,
         /^fc00:/i,
         /^fe80:/i,
       ]

       export function isUrlSafe(rawUrl: string): boolean {
         let parsed: URL
         try {
           parsed = new URL(rawUrl)
         } catch {
           return false
         }
         if (!['http:', 'https:'].includes(parsed.protocol)) return false
         const hostname = parsed.hostname.toLowerCase()
         if (!hostname) return false
         return !PRIVATE_HOSTNAME_PATTERNS.some((p) => p.test(hostname))
       }
       ```

    2. Criar `src/lib/utils/email-template.ts`:
       ```typescript
       // src/lib/utils/email-template.ts
       // Phase 07 Plan 01 — Renderer de template com escape HTML
       // D-06: 6 variaveis fixas. D-07: DEFAULT_TEMPLATES usado quando tenant nao tem template customizado
       // (o template padrao RENDERIZADO usa React Email — ver src/emails/*.tsx e Route Handler interno).
       // Pitfall 4: escapar valores antes de inserir no body_html.

       export const TEMPLATE_VARIABLES = [
         'nome_cliente', 'cpf_cnpj', 'vencimento', 'valor', 'nome_apolice', 'corretor',
       ] as const
       export type TemplateVar = (typeof TEMPLATE_VARIABLES)[number]
       export type EventType = 'policy_expiring' | 'financial_overdue' | 'consortium_contemplated'

       export function escapeHtml(s: string): string {
         return s
           .replace(/&/g, '&amp;')
           .replace(/</g, '&lt;')
           .replace(/>/g, '&gt;')
           .replace(/"/g, '&quot;')
           .replace(/'/g, '&#39;')
       }

       export function renderEmailTemplate(
         template: { subject: string; body_html: string },
         vars: Partial<Record<TemplateVar, string>>,
       ): { subject: string; body_html: string } {
         const apply = (s: string) =>
           TEMPLATE_VARIABLES.reduce<string>(
             (acc, key) => acc.replaceAll(`{{${key}}}`, escapeHtml(vars[key] ?? '')),
             s,
           )
         return { subject: apply(template.subject), body_html: apply(template.body_html) }
       }

       // DEFAULT_TEMPLATES: usado pelo Route Handler como fallback de subject (D-06)
       // quando tenant nao configurou template. O body_html final vem dos componentes React Email.
       export const DEFAULT_TEMPLATES: Record<EventType, { subject: string; body_html: string }> = {
         policy_expiring: {
           subject: 'Apolice {{nome_apolice}} vence em {{vencimento}}',
           body_html: '<p>Ola {{nome_cliente}},</p><p>A apolice {{nome_apolice}} vence em {{vencimento}}. Valor: R$ {{valor}}. Corretor responsavel: {{corretor}}.</p>',
         },
         financial_overdue: {
           subject: 'Lancamento vencido — {{nome_cliente}}',
           body_html: '<p>Ola {{nome_cliente}},</p><p>Existe um lancamento financeiro vencido em {{vencimento}}, no valor de R$ {{valor}}. Procure o corretor {{corretor}} para regularizar.</p>',
         },
         consortium_contemplated: {
           subject: 'Parabens, {{nome_cliente}}! Voce foi contemplado',
           body_html: '<p>Ola {{nome_cliente}},</p><p>Sua cota foi contemplada. Entraremos em contato em breve para os proximos passos. Corretor: {{corretor}}.</p>',
         },
       }
       ```

    3. Criar `src/lib/utils/ai-escalation.ts`:
       ```typescript
       // src/lib/utils/ai-escalation.ts
       // Phase 07 Plan 01 — Heuristicas de escalacao para AUTO-05
       // Pitfall 7: AI SDK nao expoe confianca nativa — usar finishReason + heuristicas.

       export const ESCALATION_MESSAGE =
         'Nao consegui responder com seguranca. Um corretor ira te atender em breve.'

       export const MIN_RESPONSE_LENGTH = 20

       export function isLowConfidenceResponse(input: {
         finishReason: string | null | undefined
         text: string
       }): boolean {
         const { finishReason, text } = input
         if (!text) return true
         if (finishReason === 'length' || finishReason === 'max-steps') return true
         if (text.length < MIN_RESPONSE_LENGTH) return true
         if (text.trim().toUpperCase().startsWith('[INCERTO]')) return true
         return false
       }
       ```

    4. Criar `src/lib/validations/automation-schemas.ts`:
       ```typescript
       // src/lib/validations/automation-schemas.ts
       // Phase 07 Plan 01 — Zod schemas consumidos pelas Server Actions de Plan 02 e UI de Plan 03

       import { z } from 'zod'
       import { isUrlSafe } from '@/lib/utils/webhook-url'

       export const EVENT_TYPE_VALUES = ['policy_expiring', 'financial_overdue', 'consortium_contemplated'] as const
       export const eventTypeSchema = z.enum(EVENT_TYPE_VALUES)

       export const webhookConfigSchema = z.object({
         event_type: eventTypeSchema,
         url: z.string().url('URL invalida').refine(isUrlSafe, {
           message: 'URL aponta para IP privado, loopback ou protocolo nao permitido',
         }),
         days_before: z.coerce.number().int().min(1).max(365).optional(),
         active: z.coerce.boolean().default(true),
       })
       export type WebhookConfigInput = z.infer<typeof webhookConfigSchema>

       export const emailTemplateSchema = z.object({
         event_type: eventTypeSchema,
         subject: z.string().min(3).max(200),
         body_html: z.string().min(10).max(10000),
         active: z.coerce.boolean().default(true),
       })
       export type EmailTemplateInput = z.infer<typeof emailTemplateSchema>

       export const webhookTestSchema = z.object({
         url: z.string().url().refine(isUrlSafe, { message: 'URL nao permitida' }),
         event_type: eventTypeSchema,
       })
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/utils/webhook-url.test.ts tests/utils/email-template.test.ts tests/utils/ai-escalation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa (zero erros)
    - `npx vitest run tests/utils/webhook-url.test.ts` passa com >=5 testes verde
    - `npx vitest run tests/utils/email-template.test.ts` passa com >=4 testes verde
    - `npx vitest run tests/utils/ai-escalation.test.ts` passa com >=4 testes verde
    - `grep -q "export function isUrlSafe" src/lib/utils/webhook-url.ts`
    - `grep -q "export function renderEmailTemplate" src/lib/utils/email-template.ts`
    - `grep -q "export const DEFAULT_TEMPLATES" src/lib/utils/email-template.ts`
    - `grep -q "export function isLowConfidenceResponse" src/lib/utils/ai-escalation.ts`
    - `grep -q "export const webhookConfigSchema" src/lib/validations/automation-schemas.ts`
    - `grep -q "refine(isUrlSafe" src/lib/validations/automation-schemas.ts`
  </acceptance_criteria>
  <done>3 utilitarios puros + 1 schemas Zod prontos, todos verde no Vitest. Server Actions e Edge Function (Plan 02) e Route Handlers (Plan 04) consumirao estes modulos.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] supabase db push — aplicar migrations 0021/0022/0023</name>
  <files>(no file changes — DB only)</files>
  <read_first>
    supabase/migrations/20260420_0021_automacoes_schema.sql,
    supabase/migrations/20260420_0022_automacoes_rls.sql,
    supabase/migrations/20260420_0023_automacoes_cron.sql,
    .planning/STATE.md (Decisions section: "supabase db push contornado via supabase db query --linked -f por versioning collision (CLI usa apenas a data como version key)")
  </read_first>
  <action>
    1. Verificar que SUPABASE_ACCESS_TOKEN esta configurado:
       ```bash
       echo $SUPABASE_ACCESS_TOKEN  # se vazio, parar e pedir ao usuario
       ```

    2. Executar push:
       ```bash
       npx supabase db push
       ```

    3. Se push falhar com versioning collision (conhecido no projeto — STATE.md documenta padrao), aplicar migrations individualmente via:
       ```bash
       npx supabase db query --linked -f supabase/migrations/20260420_0021_automacoes_schema.sql
       npx supabase db query --linked -f supabase/migrations/20260420_0022_automacoes_rls.sql
       npx supabase db query --linked -f supabase/migrations/20260420_0023_automacoes_cron.sql
       ```
       NOTA: 0023 (cron) pode falhar em local sem pg_cron — aceitavel; em producao Supabase pg_cron eh disponivel.

    4. Verificar que as 3 tabelas existem:
       ```bash
       npx supabase db query --linked "SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('webhook_configs','webhook_logs','email_templates') ORDER BY table_name"
       ```
       Esperado: 3 linhas.

    5. Verificar RLS ativo:
       ```bash
       npx supabase db query --linked "SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('webhook_configs','webhook_logs','email_templates')"
       ```
       Esperado: rowsecurity = true em todas.

    6. Em caso de pg_cron indisponivel local, registrar em SUMMARY.md que a migration 0023 deve ser executada manualmente em producao via SQL Editor.
  </action>
  <verify>
    <automated>npx supabase db query --linked "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('webhook_configs','webhook_logs','email_templates')" 2>&1 | grep -q "3"</automated>
  </verify>
  <acceptance_criteria>
    - 3 tabelas existem em information_schema.tables
    - RLS ativo (rowsecurity=true) em todas as 3
    - Comando supabase db push (ou alternativo) retornou exit code 0 OU as queries individuais executaram com sucesso
    - Se 0023 falhou por pg_cron indisponivel local, motivo documentado no commit
  </acceptance_criteria>
  <done>Schema aplicado em DB linked. Plan 02/03/04 podem inserir/ler das 3 tabelas via Supabase client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: React Email templates — policy-expiring, financial-overdue, consortium-contemplated (D-07)</name>
  <files>
    src/emails/policy-expiring.tsx,
    src/emails/financial-overdue.tsx,
    src/emails/consortium-contemplated.tsx
  </files>
  <read_first>
    package.json (confirmar @react-email/components e @react-email/render instalados — Task 1),
    .planning/phases/07-automacoes-ia/07-CONTEXT.md (D-07: template padrao fixo React Email; D-06: 6 variaveis)
  </read_first>
  <behavior>
    Cada template eh um React component funcional com props tipadas. Usa componentes de `@react-email/components` (Html, Head, Body, Container, Text, Hr, Section). NAO usa CSS-in-JS externo — apenas inline styles simples. Compila sem erro TypeScript (`npx tsc --noEmit`).

    Props por template:
    - PolicyExpiringEmail: { nome_cliente, nome_apolice, vencimento, corretor, valor }
    - FinancialOverdueEmail: { nome_cliente, descricao, valor, vencimento, corretor }
    - ConsortiumContemplatedEmail: { nome_cliente, nome_grupo, valor, corretor }

    Todos os campos sao `string` — nenhum `optional` nesta versao (Edge Function passa string vazia se nao disponivel).
  </behavior>
  <action>
    1. Criar `src/emails/policy-expiring.tsx`:
       ```tsx
       // src/emails/policy-expiring.tsx
       // Phase 07 Plan 01 — React Email template para vencimento de apolice (D-07)
       import { Html, Head, Body, Container, Text, Section, Hr } from '@react-email/components'

       interface PolicyExpiringEmailProps {
         nome_cliente: string
         nome_apolice: string
         vencimento: string
         corretor: string
         valor: string
       }

       export function PolicyExpiringEmail({
         nome_cliente,
         nome_apolice,
         vencimento,
         corretor,
         valor,
       }: PolicyExpiringEmailProps) {
         return (
           <Html lang="pt-BR">
             <Head />
             <Body style={{ fontFamily: 'sans-serif', backgroundColor: '#f9f9f9' }}>
               <Container style={{ maxWidth: '600px', margin: '0 auto', backgroundColor: '#ffffff', padding: '24px', borderRadius: '8px' }}>
                 <Text style={{ fontSize: '20px', fontWeight: 'bold', color: '#1a1a1a' }}>
                   Apolice vencendo em breve
                 </Text>
                 <Hr />
                 <Section>
                   <Text>Ola, {nome_cliente}.</Text>
                   <Text>
                     Sua apolice <strong>{nome_apolice}</strong> vence em{' '}
                     <strong>{vencimento}</strong>.
                   </Text>
                   <Text>Valor: <strong>R$ {valor}</strong></Text>
                   <Text>Corretor responsavel: {corretor}</Text>
                 </Section>
                 <Hr />
                 <Text style={{ fontSize: '12px', color: '#666666' }}>
                   Entre em contato com sua corretora para renovacao.
                 </Text>
               </Container>
             </Body>
           </Html>
         )
       }
       ```

    2. Criar `src/emails/financial-overdue.tsx`:
       ```tsx
       // src/emails/financial-overdue.tsx
       // Phase 07 Plan 01 — React Email template para inadimplencia financeira (D-07)
       import { Html, Head, Body, Container, Text, Section, Hr } from '@react-email/components'

       interface FinancialOverdueEmailProps {
         nome_cliente: string
         descricao: string
         valor: string
         vencimento: string
         corretor: string
       }

       export function FinancialOverdueEmail({
         nome_cliente,
         descricao,
         valor,
         vencimento,
         corretor,
       }: FinancialOverdueEmailProps) {
         return (
           <Html lang="pt-BR">
             <Head />
             <Body style={{ fontFamily: 'sans-serif', backgroundColor: '#f9f9f9' }}>
               <Container style={{ maxWidth: '600px', margin: '0 auto', backgroundColor: '#ffffff', padding: '24px', borderRadius: '8px' }}>
                 <Text style={{ fontSize: '20px', fontWeight: 'bold', color: '#c0392b' }}>
                   Lancamento financeiro vencido
                 </Text>
                 <Hr />
                 <Section>
                   <Text>Ola, {nome_cliente}.</Text>
                   <Text>
                     Existe um lancamento <strong>{descricao}</strong> vencido em{' '}
                     <strong>{vencimento}</strong>, no valor de <strong>R$ {valor}</strong>.
                   </Text>
                   <Text>Corretor responsavel: {corretor}</Text>
                 </Section>
                 <Hr />
                 <Text style={{ fontSize: '12px', color: '#666666' }}>
                   Procure sua corretora para regularizar a situacao.
                 </Text>
               </Container>
             </Body>
           </Html>
         )
       }
       ```

    3. Criar `src/emails/consortium-contemplated.tsx`:
       ```tsx
       // src/emails/consortium-contemplated.tsx
       // Phase 07 Plan 01 — React Email template para contemplacao de consorcio (D-07)
       import { Html, Head, Body, Container, Text, Section, Hr } from '@react-email/components'

       interface ConsortiumContemplatedEmailProps {
         nome_cliente: string
         nome_grupo: string
         valor: string
         corretor: string
       }

       export function ConsortiumContemplatedEmail({
         nome_cliente,
         nome_grupo,
         valor,
         corretor,
       }: ConsortiumContemplatedEmailProps) {
         return (
           <Html lang="pt-BR">
             <Head />
             <Body style={{ fontFamily: 'sans-serif', backgroundColor: '#f9f9f9' }}>
               <Container style={{ maxWidth: '600px', margin: '0 auto', backgroundColor: '#ffffff', padding: '24px', borderRadius: '8px' }}>
                 <Text style={{ fontSize: '20px', fontWeight: 'bold', color: '#27ae60' }}>
                   Parabens! Voce foi contemplado.
                 </Text>
                 <Hr />
                 <Section>
                   <Text>Ola, {nome_cliente}!</Text>
                   <Text>
                     Sua cota do grupo <strong>{nome_grupo}</strong> foi contemplada.
                     Valor: <strong>R$ {valor}</strong>.
                   </Text>
                   <Text>
                     Seu corretor <strong>{corretor}</strong> entrara em contato em breve para os
                     proximos passos.
                   </Text>
                 </Section>
                 <Hr />
                 <Text style={{ fontSize: '12px', color: '#666666' }}>
                   NEXUS AGENT — Plataforma de gestao para corretoras.
                 </Text>
               </Container>
             </Body>
           </Html>
         )
       }
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "PolicyExpiringEmail" src/emails/policy-expiring.tsx && grep -q "FinancialOverdueEmail" src/emails/financial-overdue.tsx && grep -q "ConsortiumContemplatedEmail" src/emails/consortium-contemplated.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` zero erros apos criacao dos 3 templates
    - Cada arquivo exporta um componente funcional com interface tipada
    - Todos os 3 usam `@react-email/components` (Html, Head, Body, Container, Text, Hr)
    - `grep -q "import { Html" src/emails/policy-expiring.tsx`
    - `grep -q "import { Html" src/emails/financial-overdue.tsx`
    - `grep -q "import { Html" src/emails/consortium-contemplated.tsx`
  </acceptance_criteria>
  <done>3 React Email templates prontos. Route Handler (Task 6) importa estes componentes e os renderiza via render() para gerar HTML antes de chamar Resend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Route Handler interno — POST /api/internal/send-automation-email (D-07)</name>
  <files>
    src/app/api/internal/send-automation-email/route.ts
  </files>
  <read_first>
    src/emails/policy-expiring.tsx,
    src/emails/financial-overdue.tsx,
    src/emails/consortium-contemplated.tsx,
    src/app/api/[slug]/export/route.ts (padrao de Route Handler existente — error handling, Response.json),
    .planning/phases/07-automacoes-ia/07-CONTEXT.md (D-07: template padrao fixo React Email; D-05: destinatarios duplos)
  </read_first>
  <behavior>
    Route Handler Node.js (NAO Edge runtime) que:
    1. Valida `x-internal-secret` header contra `process.env.INTERNAL_EMAIL_SECRET` — mismatch -> 403.
    2. Parse do body JSON: { event_type, to, subject, vars, custom_body_html }.
       - `to`: string[] (corretor_email + cliente_email ja filtrados pela Edge Function)
       - `vars`: { nome_cliente, vencimento, valor, nome_apolice, corretor, ... }
       - `custom_body_html`: string | null — se tenant tem template custom (Edge Function ja aplicou substituicao de variaveis)
       - `subject`: string — ja renderizado pela Edge Function
    3. Se `custom_body_html` nao-null: usar como html diretamente (tenant template).
    4. Se `custom_body_html` null: renderizar React Email template via `render()`:
       - event_type === 'policy_expiring' -> render(<PolicyExpiringEmail {...vars} />)
       - event_type === 'financial_overdue' -> render(<FinancialOverdueEmail {...vars} />)
       - qualquer outro -> render(<ConsortiumContemplatedEmail {...vars} />)
    5. Chamar Resend via fetch:
       - from: process.env.RESEND_FROM_EMAIL ?? 'noreply@nexusagent.com.br'
       - to, subject, html
    6. Retornar { sent: true } em 200 ou { error: string } com status Resend.

    IMPORTANTE: este endpoint e INTERNO — nao tem autenticacao de usuario Supabase. A seguranca vem exclusivamente do `x-internal-secret` shared secret. NAO expor no cliente.

    Env vars requeridas (documentar em SUMMARY.md):
    - INTERNAL_EMAIL_SECRET — shared secret entre Edge Function e Next.js
    - NEXTJS_APP_URL — URL do app Next.js (usado pela Edge Function para montar a URL destino)
    - RESEND_API_KEY — ja existente no projeto
    - RESEND_FROM_EMAIL — remetente (ex: 'NEXUS AGENT <noreply@seudominio.com.br>')
  </behavior>
  <action>
    1. Criar `src/app/api/internal/send-automation-email/route.ts`:
       ```typescript
       // src/app/api/internal/send-automation-email/route.ts
       // Phase 07 Plan 01 — Route Handler INTERNO para envio de email via React Email + Resend (D-07)
       // Seguranca: x-internal-secret header validado contra INTERNAL_EMAIL_SECRET env var.
       // NAO usar Edge runtime — React Email requer Node.js para render().

       import { render } from '@react-email/render'
       import { PolicyExpiringEmail } from '@/emails/policy-expiring'
       import { FinancialOverdueEmail } from '@/emails/financial-overdue'
       import { ConsortiumContemplatedEmail } from '@/emails/consortium-contemplated'

       export const runtime = 'nodejs'

       export async function POST(req: Request) {
         // 1. Validar shared secret — T-07-INTERNAL-AUTH
         const secret = req.headers.get('x-internal-secret')
         if (!process.env.INTERNAL_EMAIL_SECRET || secret !== process.env.INTERNAL_EMAIL_SECRET) {
           return Response.json({ error: 'Forbidden' }, { status: 403 })
         }

         let body: {
           event_type: string
           to: string[]
           subject: string
           vars: Record<string, string>
           custom_body_html: string | null
         }
         try {
           body = await req.json()
         } catch {
           return Response.json({ error: 'Invalid JSON body' }, { status: 400 })
         }

         const { event_type, to, subject, vars, custom_body_html } = body

         if (!Array.isArray(to) || to.length === 0) {
           return Response.json({ error: 'No recipients' }, { status: 400 })
         }

         // 2. Renderizar HTML — tenant custom ou React Email default (D-07)
         let html: string
         if (custom_body_html) {
           // Tenant tem template customizado — Edge Function ja aplicou substituicao de variaveis
           html = custom_body_html
         } else {
           // Template padrao fixo via React Email (D-07)
           if (event_type === 'policy_expiring') {
             html = await render(
               <PolicyExpiringEmail
                 nome_cliente={vars.nome_cliente ?? ''}
                 nome_apolice={vars.nome_apolice ?? ''}
                 vencimento={vars.vencimento ?? ''}
                 corretor={vars.corretor ?? ''}
                 valor={vars.valor ?? ''}
               />
             )
           } else if (event_type === 'financial_overdue') {
             html = await render(
               <FinancialOverdueEmail
                 nome_cliente={vars.nome_cliente ?? ''}
                 descricao={vars.nome_apolice ?? ''}
                 valor={vars.valor ?? ''}
                 vencimento={vars.vencimento ?? ''}
                 corretor={vars.corretor ?? ''}
               />
             )
           } else {
             html = await render(
               <ConsortiumContemplatedEmail
                 nome_cliente={vars.nome_cliente ?? ''}
                 nome_grupo={vars.nome_apolice ?? ''}
                 valor={vars.valor ?? ''}
                 corretor={vars.corretor ?? ''}
               />
             )
           }
         }

         // 3. Enviar via Resend
         const fromAddress =
           process.env.RESEND_FROM_EMAIL ?? 'noreply@nexusagent.com.br'
         const resendRes = await fetch('https://api.resend.com/emails', {
           method: 'POST',
           headers: {
             Authorization: `Bearer ${process.env.RESEND_API_KEY}`,
             'Content-Type': 'application/json',
           },
           body: JSON.stringify({ from: fromAddress, to, subject, html }),
         })

         if (!resendRes.ok) {
           const err = await resendRes.text()
           return Response.json({ error: err }, { status: resendRes.status })
         }

         return Response.json({ sent: true })
       }
       ```

    2. Verificar que o arquivo NAO usa `export const runtime = 'edge'` (Route Handler deve rodar em Node.js para render() do React Email funcionar).

    3. Verificar que o arquivo importa de `@react-email/render` e NAO de `@react-email/render/client`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "x-internal-secret" src/app/api/internal/send-automation-email/route.ts && grep -q "INTERNAL_EMAIL_SECRET" src/app/api/internal/send-automation-email/route.ts && grep -q "render" src/app/api/internal/send-automation-email/route.ts && grep -q "PolicyExpiringEmail" src/app/api/internal/send-automation-email/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` zero erros
    - `grep -q "export const runtime = 'nodejs'" src/app/api/internal/send-automation-email/route.ts` (nao edge)
    - `grep -q "INTERNAL_EMAIL_SECRET" src/app/api/internal/send-automation-email/route.ts`
    - `grep -q "custom_body_html" src/app/api/internal/send-automation-email/route.ts` (logica de bifurcacao tenant vs default)
    - `grep -q "@react-email/render" src/app/api/internal/send-automation-email/route.ts`
    - `grep -q "api.resend.com/emails" src/app/api/internal/send-automation-email/route.ts`
    - Arquivo NAO contem `export const runtime = 'edge'`
  </acceptance_criteria>
  <done>Route Handler interno pronto. Edge Function (Plan 02 Task 2) chama este endpoint em vez de chamar Resend diretamente, permitindo que React Email components sejam usados para os templates padrao (D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Admin envia webhook URL via form POST — inputs untrusted |
| pg_cron → Edge Function | Cron HTTP POST cross-process; precisa de auth |
| Edge Function (service_role) → Tables | Bypass RLS — escrita em webhook_logs |
| Edge Function → Next.js Route Handler | POST interno com x-internal-secret; nao exposto ao cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SSRF | Tampering / EoP | url em webhookConfigSchema.refine | mitigate | `isUrlSafe()` em src/lib/utils/webhook-url.ts; rejeita RFC1918, loopback, link-local, esquemas nao-http |
| T-07-01 | Information Disclosure | RLS em webhook_configs/email_templates/webhook_logs | mitigate | Migration 0022 — policies USING tenant_id=jwt_tenant_id() AND role='admin'; webhook_logs SEM INSERT/UPDATE/DELETE policy (write only via service_role) |
| T-07-04 | Tampering | DEFAULT_TEMPLATES + escapeHtml em renderEmailTemplate | mitigate | escapeHtml() aplicado a TODOS os valores antes de substituir em body_html (Pitfall 4) |
| T-07-05 | Spoofing | pg_cron → Edge Function auth | mitigate | x-cron-secret header em migration 0023 + Authorization Bearer publishable_key; Edge Function (Plan 02) valida secret |
| T-07-06 | DoS | URL de webhook lenta trava cron | accept (parcial) | dispatchWebhook (Plan 02) usa AbortSignal.timeout(15000); webhook_logs registra erro; sem retry (D-03) |
| T-07-INTERNAL | Spoofing | POST /api/internal/send-automation-email chamado por terceiros | mitigate | x-internal-secret header validado contra INTERNAL_EMAIL_SECRET env var; retorna 403 sem revelar detalhes |
</threat_model>

<verification>
- `npm test` passa (todos os 8 test stubs)
- `npx tsc --noEmit` zero erros
- 3 migrations aplicadas (Task 4 verifica)
- `[functions.automation-cron]` em config.toml
- 4 utilitarios em src/lib/ exportam as funcoes documentadas
- 3 React Email templates em src/emails/ compilam sem erro
- Route Handler `/api/internal/send-automation-email` retorna 403 com secret invalido
</verification>

<success_criteria>
- 8 arquivos de teste em tests/utils/ e tests/actions/ existem (Wave 0 fechada)
- 3 utilitarios puros (`isUrlSafe`, `renderEmailTemplate`, `isLowConfidenceResponse`) testados e verde
- 1 modulo de schemas Zod (`webhookConfigSchema`, `emailTemplateSchema`)
- 3 tabelas no Postgres com RLS ativo
- pg_cron schedule criado (ou marcado para deploy manual em producao)
- AI SDK v6 (`ai`, `@ai-sdk/openai`, `@ai-sdk/react`) em package.json
- React Email (`react-email`, `@react-email/components`, `@react-email/render`) em package.json
- 3 React Email templates em src/emails/ com componentes tipados (D-07)
- Route Handler interno `POST /api/internal/send-automation-email` com secret auth + bifurcacao tenant/default
</success_criteria>

<output>
After completion, create `.planning/phases/07-automacoes-ia/07-01-SUMMARY.md` documenting:
- Migrations criadas e aplicadas (status local vs producao)
- Versoes exatas de ai/@ai-sdk/openai/@ai-sdk/react instaladas
- Versoes exatas de react-email/@react-email/components/@react-email/render instaladas
- Comportamento dos 3 utilitarios puros
- Status dos 8 test stubs (verde com asserts reais vs placeholder a popular em Plan 02/04)
- Notas operacionais para deploy: pg_cron, Vault secrets (project_url, cron_publishable_key, cron_shared_secret), RESEND_API_KEY, RESEND_FROM_EMAIL, OPENAI_API_KEY
- Env vars novas requeridas: INTERNAL_EMAIL_SECRET, NEXTJS_APP_URL
</output>
</output>
