---
phase: 01-auth-do-portal
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - src/app/(portal)/[slug]/portal/layout.tsx
  - src/app/(portal)/[slug]/portal/login/page.tsx
  - src/app/(portal)/[slug]/portal/login/portal-login-form.tsx
  - src/app/(portal)/[slug]/portal/cadastro/page.tsx
  - src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx
  - src/app/(portal)/[slug]/portal/home/page.tsx
autonomous: false
requirements:
  - PORTAL-AUTH-01
  - PORTAL-AUTH-02
  - PORTAL-AUTH-03
  - PORTAL-AUTH-04

must_haves:
  truths:
    - "Página /{slug}/portal/login renderiza formulário com email + senha"
    - "Página /{slug}/portal/cadastro renderiza formulário com CPF + email + senha"
    - "Submissão do cadastro chama registerPortalClient e exibe erros de campo"
    - "Submissão do login chama loginPortalClient e exibe erros de _form"
    - "Página /{slug}/portal/home (placeholder) é protegida — só portal_clients chegam (via middleware)"
    - "Layout (portal) é separado de (auth) e (app) — agrupamento próprio"
    - "Slug é extraído de route params e enviado como hidden field no FormData"
  artifacts:
    - path: "src/app/(portal)/[slug]/portal/layout.tsx"
      provides: "Layout próprio do portal (separado de (auth)/(app))"
      contains: "export default"
    - path: "src/app/(portal)/[slug]/portal/login/page.tsx"
      provides: "Página de login do portal (Server Component)"
      contains: "PortalLoginForm"
    - path: "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx"
      provides: "Form de login (Client Component)"
      contains: "use client"
    - path: "src/app/(portal)/[slug]/portal/cadastro/page.tsx"
      provides: "Página de cadastro do portal"
      contains: "PortalCadastroForm"
    - path: "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx"
      provides: "Form de cadastro com máscara CPF (Client Component)"
      contains: "use client"
    - path: "src/app/(portal)/[slug]/portal/home/page.tsx"
      provides: "Placeholder home (Phase 3 substituirá)"
      contains: "Bem-vindo"
  key_links:
    - from: "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx"
      to: "src/lib/actions/portal-auth.ts"
      via: "import { registerPortalClient }"
      pattern: "registerPortalClient"
    - from: "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx"
      to: "src/lib/actions/portal-auth.ts"
      via: "import { loginPortalClient }"
      pattern: "loginPortalClient"
    - from: "src/app/(portal)/[slug]/portal/cadastro/page.tsx"
      to: "params.slug from URL"
      via: "Next.js dynamic route segment passed to form"
      pattern: "params\\.slug|params:.*slug"
---

<objective>
Construir as páginas e formulários do portal para que o usuário final consiga ver e usar o fluxo de cadastro/login. Esta é a única plan que gera UI; toda a lógica server-side e middleware vive em Plans 01-01 e 01-02.

**Purpose:** Fechar a Phase 1 com um fluxo end-to-end funcional. Após esta plan, um usuário pode acessar `/{slug}/portal/cadastro`, criar conta com CPF válido e ser redirecionado para `/{slug}/portal/home` (placeholder). E pode fazer login depois.

**Output:**
- Route group `(portal)` paralelo a `(auth)` e `(app)`
- 3 páginas: login, cadastro, home (placeholder)
- 2 form components (Client) com react-hook-form + zod resolver
- 1 layout próprio do portal
- Checkpoint humano para verificação visual e fluxo
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-do-portal/01-CONTEXT.md
@.planning/phases/01-auth-do-portal/01-RESEARCH.md
@.planning/phases/01-auth-do-portal/01-02-SUMMARY.md

# Codebase patterns to replicate
@src/app/(auth)/layout.tsx
@src/app/(auth)/login/page.tsx
@src/app/(auth)/login/login-form.tsx
@src/lib/validations/cpf.ts

<interfaces>
<!-- Server Actions delivered by Plan 02 -->
```typescript
// src/lib/actions/portal-auth.ts
export async function registerPortalClient(
  formData: FormData,
): Promise<{ error?: PortalFormError } | void>

export async function loginPortalClient(
  formData: FormData,
): Promise<{ error?: PortalFormError } | void>
```

<!-- Schemas delivered by Plan 02 -->
```typescript
// src/lib/validations/portal-auth-schemas.ts
export const portalCadastroSchema: ZodSchema  // cpf, email, password, slug
export const portalLoginSchema: ZodSchema     // email, password, slug
export type PortalFormError = { _form?: string[]; [field: string]: string[] | undefined }
```

<!-- CPF formatter (existing — REUSE for input mask) -->
```typescript
// src/lib/validations/cpf.ts
export function formatCPF(cpf: string): string  // "12345678900" → "123.456.789-00"
export function stripCPF(cpf: string): string
```

<!-- shadcn/ui components available -->
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { Alert, AlertDescription } from '@/components/ui/alert'
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portal route group + layout + cadastro page/form</name>
  <files>
    - src/app/(portal)/[slug]/portal/layout.tsx
    - src/app/(portal)/[slug]/portal/cadastro/page.tsx
    - src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx
  </files>
  <read_first>
    - src/app/(auth)/layout.tsx (current auth layout — minimalist with SplitScreenLayout + Toaster)
    - src/app/(auth)/login/page.tsx (Server Component pattern — Suspense + searchParams)
    - src/app/(auth)/login/login-form.tsx (Client Component pattern — react-hook-form + Server Action)
    - src/lib/validations/cpf.ts (formatCPF for input mask)
    - src/lib/validations/portal-auth-schemas.ts (portalCadastroSchema from Plan 02)
    - src/lib/actions/portal-auth.ts (registerPortalClient signature)
    - .planning/phases/01-auth-do-portal/01-CONTEXT.md (D-12 — own layout, route structure)
  </read_first>
  <action>
    Per D-12 (CONTEXT.md): create route group `(portal)` parallel to existing `(auth)` and `(app)`. Layout is "minimal but distinct from internal" — use a centered card layout (do NOT reuse SplitScreenLayout from internal — keep portal visually separate so corretora's clients don't think they're in the broker's system).

    **Step 1 — Create `src/app/(portal)/[slug]/portal/layout.tsx`:**

    ```typescript
    import { Toaster } from '@/components/ui/sonner'

    export default function PortalLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="min-h-screen bg-muted/30 flex flex-col">
          <main className="flex-1 flex items-center justify-center p-4">
            <div className="w-full max-w-md">{children}</div>
          </main>
          <Toaster position="top-right" />
        </div>
      )
    }
    ```

    **Step 2 — Create `src/app/(portal)/[slug]/portal/cadastro/page.tsx`** (Server Component — extracts `slug` from params and passes to form):

    ```typescript
    import { PortalCadastroForm } from './portal-cadastro-form'
    import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

    interface PageProps {
      params: Promise<{ slug: string }>
    }

    export default async function PortalCadastroPage({ params }: PageProps) {
      const { slug } = await params
      return (
        <Card>
          <CardHeader>
            <CardTitle>Criar conta no portal</CardTitle>
            <CardDescription>
              Acesse suas apólices, consórcio e financeiro com seu CPF.
            </CardDescription>
          </CardHeader>
          <CardContent>
            <PortalCadastroForm slug={slug} />
          </CardContent>
        </Card>
      )
    }
    ```

    **Step 3 — Create `src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx`** (Client Component, follows `login-form.tsx` pattern):

    ```typescript
    'use client'
    import { useState, useTransition } from 'react'
    import Link from 'next/link'
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import { Eye, EyeOff, Loader2 } from 'lucide-react'
    import { z } from 'zod'
    import { portalCadastroSchema } from '@/lib/validations/portal-auth-schemas'
    import { formatCPF, stripCPF } from '@/lib/validations/cpf'
    import { registerPortalClient } from '@/lib/actions/portal-auth'
    import {
      Form,
      FormControl,
      FormField,
      FormItem,
      FormLabel,
      FormMessage,
    } from '@/components/ui/form'
    import { Input } from '@/components/ui/input'
    import { Button } from '@/components/ui/button'
    import { Alert, AlertDescription } from '@/components/ui/alert'

    type CadastroValues = z.infer<typeof portalCadastroSchema>

    interface PortalCadastroFormProps {
      slug: string
    }

    export function PortalCadastroForm({ slug }: PortalCadastroFormProps) {
      const [showPassword, setShowPassword] = useState(false)
      const [formError, setFormError] = useState<string | null>(null)
      const [isPending, startTransition] = useTransition()

      const form = useForm<CadastroValues>({
        resolver: zodResolver(portalCadastroSchema),
        defaultValues: { cpf: '', email: '', password: '', slug },
      })

      function onSubmit(values: CadastroValues) {
        setFormError(null)
        const fd = new FormData()
        fd.set('cpf', stripCPF(values.cpf))
        fd.set('email', values.email)
        fd.set('password', values.password)
        fd.set('slug', slug)

        startTransition(async () => {
          const result = await registerPortalClient(fd)
          if (result?.error) {
            // Field errors → set form errors; _form → setFormError banner
            const err = result.error
            if (err._form?.[0]) setFormError(err._form[0])
            if (err.cpf?.[0]) form.setError('cpf', { message: err.cpf[0] })
            if (err.email?.[0]) form.setError('email', { message: err.email[0] })
            if (err.password?.[0]) form.setError('password', { message: err.password[0] })
          }
        })
      }

      return (
        <div className="flex flex-col gap-6">
          {formError && (
            <Alert variant="destructive">
              <AlertDescription>{formError}</AlertDescription>
            </Alert>
          )}

          <Form {...form}>
            <form onSubmit={form.handleSubmit(onSubmit)} className="flex flex-col gap-4">
              <FormField
                control={form.control}
                name="cpf"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>CPF</FormLabel>
                    <FormControl>
                      <Input
                        placeholder="000.000.000-00"
                        inputMode="numeric"
                        autoComplete="off"
                        value={field.value}
                        onChange={(e) => {
                          const digits = stripCPF(e.target.value).slice(0, 11)
                          field.onChange(digits.length === 11 ? formatCPF(digits) : digits)
                        }}
                      />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <FormField
                control={form.control}
                name="email"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>E-mail</FormLabel>
                    <FormControl>
                      <Input
                        type="email"
                        placeholder="voce@email.com"
                        autoComplete="email"
                        {...field}
                      />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <FormField
                control={form.control}
                name="password"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Senha</FormLabel>
                    <FormControl>
                      <div className="relative">
                        <Input
                          type={showPassword ? 'text' : 'password'}
                          placeholder="Mínimo 8 caracteres"
                          autoComplete="new-password"
                          {...field}
                        />
                        <button
                          type="button"
                          onClick={() => setShowPassword((v) => !v)}
                          className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground"
                          tabIndex={-1}
                        >
                          {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
                        </button>
                      </div>
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <Button type="submit" className="h-11 w-full" disabled={isPending}>
                {isPending ? (
                  <>
                    <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                    Criando conta...
                  </>
                ) : (
                  'Criar conta'
                )}
              </Button>
            </form>
          </Form>

          <p className="text-center text-sm text-muted-foreground">
            Já tem conta?{' '}
            <Link
              href={`/${slug}/portal/login`}
              className="font-semibold text-primary hover:underline"
            >
              Faça login
            </Link>
          </p>
        </div>
      )
    }
    ```

    Run `npx tsc --noEmit` after creating these files.
  </action>
  <verify>
    <automated>
      test -f "src/app/(portal)/[slug]/portal/layout.tsx" && \
      test -f "src/app/(portal)/[slug]/portal/cadastro/page.tsx" && \
      test -f "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      grep -q "'use client'" "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      grep -q "registerPortalClient" "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      grep -q "portalCadastroSchema" "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      grep -q "formatCPF" "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      grep -q "params.*slug" "src/app/(portal)/[slug]/portal/cadastro/page.tsx" && \
      ! grep -q "createAdminClient" "src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx" && \
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - Three files exist at exact paths above
    - `portal-cadastro-form.tsx` starts with `'use client'`
    - `portal-cadastro-form.tsx` imports `registerPortalClient` from `@/lib/actions/portal-auth`
    - `portal-cadastro-form.tsx` imports `portalCadastroSchema` from `@/lib/validations/portal-auth-schemas`
    - `portal-cadastro-form.tsx` imports and uses `formatCPF` for input mask
    - `portal-cadastro-form.tsx` does NOT import `createAdminClient` (security — service_role never in client)
    - `cadastro/page.tsx` extracts `slug` from `params` and passes to form
    - `npx tsc --noEmit` exits 0
    - Layout uses Tailwind classes, NOT a copy of SplitScreenLayout (portal is visually distinct)
  </acceptance_criteria>
  <done>
    Cadastro page renders, form binds to registerPortalClient, slug threaded through props. Type check clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create login page/form + home placeholder</name>
  <files>
    - src/app/(portal)/[slug]/portal/login/page.tsx
    - src/app/(portal)/[slug]/portal/login/portal-login-form.tsx
    - src/app/(portal)/[slug]/portal/home/page.tsx
  </files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (existing pattern — copy structure, swap actions)
    - src/lib/validations/portal-auth-schemas.ts (portalLoginSchema from Plan 02)
    - src/lib/actions/portal-auth.ts (loginPortalClient signature)
    - src/app/(portal)/[slug]/portal/cadastro/portal-cadastro-form.tsx (Task 1 — replicate styling)
  </read_first>
  <action>
    **Step 1 — Create `src/app/(portal)/[slug]/portal/login/page.tsx`:**

    ```typescript
    import { PortalLoginForm } from './portal-login-form'
    import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

    interface PageProps {
      params: Promise<{ slug: string }>
    }

    export default async function PortalLoginPage({ params }: PageProps) {
      const { slug } = await params
      return (
        <Card>
          <CardHeader>
            <CardTitle>Entrar no portal</CardTitle>
            <CardDescription>Acesse com seu e-mail e senha.</CardDescription>
          </CardHeader>
          <CardContent>
            <PortalLoginForm slug={slug} />
          </CardContent>
        </Card>
      )
    }
    ```

    **Step 2 — Create `src/app/(portal)/[slug]/portal/login/portal-login-form.tsx`:**

    ```typescript
    'use client'
    import { useState, useTransition } from 'react'
    import Link from 'next/link'
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import { Eye, EyeOff, Loader2 } from 'lucide-react'
    import { z } from 'zod'
    import { portalLoginSchema } from '@/lib/validations/portal-auth-schemas'
    import { loginPortalClient } from '@/lib/actions/portal-auth'
    import {
      Form,
      FormControl,
      FormField,
      FormItem,
      FormLabel,
      FormMessage,
    } from '@/components/ui/form'
    import { Input } from '@/components/ui/input'
    import { Button } from '@/components/ui/button'
    import { Alert, AlertDescription } from '@/components/ui/alert'

    type LoginValues = z.infer<typeof portalLoginSchema>

    interface PortalLoginFormProps {
      slug: string
    }

    export function PortalLoginForm({ slug }: PortalLoginFormProps) {
      const [showPassword, setShowPassword] = useState(false)
      const [formError, setFormError] = useState<string | null>(null)
      const [isPending, startTransition] = useTransition()

      const form = useForm<LoginValues>({
        resolver: zodResolver(portalLoginSchema),
        defaultValues: { email: '', password: '', slug },
      })

      function onSubmit(values: LoginValues) {
        setFormError(null)
        const fd = new FormData()
        fd.set('email', values.email)
        fd.set('password', values.password)
        fd.set('slug', slug)

        startTransition(async () => {
          const result = await loginPortalClient(fd)
          if (result?.error?._form?.[0]) {
            setFormError(result.error._form[0])
          }
        })
      }

      return (
        <div className="flex flex-col gap-6">
          {formError && (
            <Alert variant="destructive">
              <AlertDescription>{formError}</AlertDescription>
            </Alert>
          )}

          <Form {...form}>
            <form onSubmit={form.handleSubmit(onSubmit)} className="flex flex-col gap-4">
              <FormField
                control={form.control}
                name="email"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>E-mail</FormLabel>
                    <FormControl>
                      <Input
                        type="email"
                        placeholder="voce@email.com"
                        autoComplete="email"
                        {...field}
                      />
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <FormField
                control={form.control}
                name="password"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Senha</FormLabel>
                    <FormControl>
                      <div className="relative">
                        <Input
                          type={showPassword ? 'text' : 'password'}
                          placeholder="••••••••"
                          autoComplete="current-password"
                          {...field}
                        />
                        <button
                          type="button"
                          onClick={() => setShowPassword((v) => !v)}
                          className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground"
                          tabIndex={-1}
                        >
                          {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
                        </button>
                      </div>
                    </FormControl>
                    <FormMessage />
                  </FormItem>
                )}
              />

              <Button type="submit" className="h-11 w-full" disabled={isPending}>
                {isPending ? (
                  <>
                    <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                    Entrando...
                  </>
                ) : (
                  'Entrar'
                )}
              </Button>
            </form>
          </Form>

          <p className="text-center text-sm text-muted-foreground">
            Ainda não tem conta?{' '}
            <Link
              href={`/${slug}/portal/cadastro`}
              className="font-semibold text-primary hover:underline"
            >
              Cadastre-se
            </Link>
          </p>
        </div>
      )
    }
    ```

    **Step 3 — Create `src/app/(portal)/[slug]/portal/home/page.tsx` (placeholder — Phase 3 replaces):**

    ```typescript
    import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

    export default function PortalHomePage() {
      return (
        <Card>
          <CardHeader>
            <CardTitle>Bem-vindo ao portal</CardTitle>
            <CardDescription>
              Sua área para acompanhar apólices, consórcio e financeiro.
            </CardDescription>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-muted-foreground">
              Em breve: visualização de apólices ativas, cotas de consórcio e parcelas em aberto.
            </p>
          </CardContent>
        </Card>
      )
    }
    ```

    Run `npx tsc --noEmit && npx next build`. Build MUST succeed. If build fails on type errors related to portal files, fix them; if fails on unrelated existing errors, document and proceed.
  </action>
  <verify>
    <automated>
      test -f "src/app/(portal)/[slug]/portal/login/page.tsx" && \
      test -f "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx" && \
      test -f "src/app/(portal)/[slug]/portal/home/page.tsx" && \
      grep -q "'use client'" "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx" && \
      grep -q "loginPortalClient" "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx" && \
      grep -q "portalLoginSchema" "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx" && \
      grep -q "Bem-vindo" "src/app/(portal)/[slug]/portal/home/page.tsx" && \
      ! grep -q "createAdminClient" "src/app/(portal)/[slug]/portal/login/portal-login-form.tsx" && \
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - Three files exist at exact paths above
    - `portal-login-form.tsx` starts with `'use client'`
    - `portal-login-form.tsx` imports `loginPortalClient` and `portalLoginSchema`
    - `portal-login-form.tsx` does NOT import `createAdminClient`
    - `login/page.tsx` extracts slug from params (Next.js 15 async params pattern: `params: Promise<{slug: string}>`)
    - `home/page.tsx` contains literal "Bem-vindo" (proves placeholder content is in place)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Login form, login page, and home placeholder all exist and pass type check. Wire-up to Plan 02 Server Actions complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke test of full portal auth flow</name>
  <what-built>
    Complete portal auth Phase 1:
    - Migration applied (portal_clients table + RLS + helpers)
    - Middleware extended (portal_client routing isolated from internal users)
    - Server Actions (registerPortalClient with CPF verification + rollback; loginPortalClient with role check)
    - UI: layout (portal), login page, cadastro page, home placeholder
  </what-built>
  <how-to-verify>
    Pre-requisites: a running dev server (`npm run dev`) and a tenant with a known slug (e.g. `acme`) plus a known PF client with valid CPF in that tenant's `clients` table.

    **1. Anon access to cadastro:**
       - Open browser to `http://localhost:3000/{slug}/portal/cadastro` (replace `{slug}` with a real tenant slug)
       - Expected: cadastro page renders with CPF + email + senha form
       - Expected: NO redirect to /login (anon allowed on portal auth routes)

    **2. CPF validation (client-side):**
       - Type `123.456.789-00` in CPF field, click submit
       - Expected: form rejects with "CPF inválido" error message

    **3. CPF not found in tenant (server-side):**
       - Type a valid CPF that does NOT exist in the tenant's clients table (e.g. `52998224725`)
       - Type valid email + password (8+ chars), submit
       - Expected: error message "CPF não encontrado para esta corretora" displayed
       - Expected: NO auth user created in Supabase Auth dashboard

    **4. Successful cadastro:**
       - Use a CPF that EXISTS in the tenant's clients table (look it up in DB or use a known seed)
       - Submit with valid email + password
       - Expected: auto-login + redirect to `/{slug}/portal/home`
       - Expected: home placeholder displays "Bem-vindo ao portal"
       - Verify in Supabase dashboard: new auth user with `app_metadata.role = 'portal_client'`, `tenant_id`, `portal_slug`
       - Verify in Supabase: `portal_clients` table has new row linking auth.users.id ↔ clients.id

    **5. Duplicate cadastro blocked:**
       - Logout (manually clear cookies or visit `/logout` if exists)
       - Try to cadastro again with the SAME CPF
       - Expected: error "Já existe uma conta para este CPF. Faça login."

    **6. Login flow:**
       - Visit `/{slug}/portal/login`
       - Enter the email + password from step 4
       - Expected: redirect to `/{slug}/portal/home`

    **7. Internal user blocked from portal (D-08):**
       - Open new browser/incognito, log in as internal user (admin/corretor) at `/login`
       - Try to navigate to `/{slug}/portal/home` (manually type URL)
       - Expected: redirect to `/{slug}/dashboard`

    **8. Portal client blocked from internal routes (D-07):**
       - Logged in as portal_client (from step 6)
       - Try to navigate to `/{slug}/dashboard` or `/{slug}/clientes`
       - Expected: redirect back to `/{slug}/portal/home`

    **9. Already-logged-in portal_client visits /portal/login:**
       - Visit `/{slug}/portal/login` while still logged in as portal_client
       - Expected: redirect to `/{slug}/portal/home`

    **10. Internal user logging in via portal login form (T-1-06):**
        - Log out
        - Visit `/{slug}/portal/login`
        - Try to log in with internal user credentials (admin email/password)
        - Expected: error "Acesso não autorizado ao portal" (loginPortalClient signs them out)

    Mark each numbered case as PASS or FAIL with notes if unexpected. Report any console errors.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 cases PASS. If any FAIL, paste the failing case number and description so the executor can debug.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → Server Action | User input crosses here (client validation is UX, server validation is authoritative) |
| URL params → Server Component | `params.slug` is attacker-controlled — Server Action validates with Zod |
| Client Component → Server Action | Client cannot import `createAdminClient` (server-only enforcement + ESLint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Information Disclosure | service_role exposure via client component | mitigate | Forms import only `registerPortalClient` / `loginPortalClient` (Server Actions). `createAdminClient` is gated by `server-only` package + ESLint. Acceptance criteria explicitly forbids import. |
| T-1-10 | Tampering | URL slug manipulation in form submission | mitigate | Slug is hidden field on FormData but server action re-validates by querying `tenants WHERE slug = ?` — invalid slug returns "Corretora não encontrada" |
| T-1-11 | Spoofing | XSS in form error messages | mitigate | React escapes by default. Error strings are constants, not user-supplied content rendered as HTML |
| T-1-12 | Information Disclosure | autocomplete leaking CPF/password | mitigate | CPF input uses `autoComplete="off"`; password uses `autoComplete="new-password"` (cadastro) and `autoComplete="current-password"` (login) — browser-managed best practice |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npx next build` succeeds (or fails only on pre-existing unrelated errors documented in summary)
3. Route group structure: `ls src/app/\(portal\)/\[slug\]/portal/` shows `cadastro`, `home`, `layout.tsx`, `login`
4. No client component imports admin client: `grep -rn createAdminClient src/app/\(portal\)/` returns ZERO matches
5. All 10 manual smoke tests in checkpoint task PASS

Phase gate: All above + checkpoint approval required to mark Phase 1 complete.
</verification>

<success_criteria>
- [ ] Route group `(portal)` exists with all 4 files (layout + 3 pages + 2 forms)
- [ ] Forms wire to Server Actions from Plan 02
- [ ] CPF input uses formatCPF mask
- [ ] Slug threaded from URL params to FormData hidden field
- [ ] No client component imports createAdminClient (security)
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx next build` succeeds
- [ ] Manual checkpoint approves all 10 smoke test cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-do-portal/01-03-SUMMARY.md` documenting:
- Files created (with paths)
- Routes available: /{slug}/portal/cadastro, /{slug}/portal/login, /{slug}/portal/home
- Smoke test results (which of the 10 cases passed)
- Any deferred issues to track in STATE.md
- Confirmation that Phase 1 deliverables are complete and Phase 2 (wildcard subdomain routing) can build on this without changing portal route structure
</output>
