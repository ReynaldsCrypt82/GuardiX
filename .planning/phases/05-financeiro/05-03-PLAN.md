---
phase: 05-financeiro
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/components/auth/sidebar-shell.tsx
  - src/app/(app)/[slug]/layout.tsx
  - src/app/(app)/[slug]/clientes/page.tsx
  - src/app/(app)/[slug]/clientes/clients-table.tsx
  - src/components/financeiro/suggest-entry-dialog.tsx
  - src/components/seguros/mark-commission-paid-dialog.tsx
  - tests/db/rls-financeiro.test.ts
  - tests/actions/financial-entries.test.ts
autonomous: true
requirements:
  - FIN-04
must_haves:
  truths:
    - "Item 'Financeiro' visível na sidebar APENAS para roles admin e financeiro (corretor e visualizador NÃO veem)"
    - "Listagem de clientes /[slug]/clientes exibe badge vermelho 'Inadimplente' apenas em linhas de clientes com lançamento pending+vencido"
    - "Badge de inadimplência respeita RBAC: visualizador NUNCA vê o badge; corretor vê apenas para clientes próprios; admin/financeiro veem para todos"
    - "Após marcar comissão como paga, dialog secundário SuggestEntryDialog é oferecido com campos pré-preenchidos (descrição, valor, vencimento=hoje, policy_id ou quota_id, client_id se disponível)"
    - "Usuario pode aceitar e criar o lançamento OU dispensar — em ambos casos sem efeito colateral indesejado"
  artifacts:
    - path: "src/components/financeiro/suggest-entry-dialog.tsx"
      provides: "Dialog secundário com campos pré-preenchidos pós-mark-commission-paid"
      exports: ["SuggestEntryDialog"]
    - path: "src/components/auth/sidebar-shell.tsx"
      provides: "Sidebar com item Financeiro condicionado a role"
      contains: "Financeiro"
    - path: "src/app/(app)/[slug]/clientes/page.tsx"
      provides: "Server Component lista clientes + query paralela de overdue client_ids"
      contains: "overdueClientIds"
    - path: "src/app/(app)/[slug]/clientes/clients-table.tsx"
      provides: "Tabela de clientes com badge de inadimplência condicional"
      contains: "Inadimplente"
    - path: "src/components/seguros/mark-commission-paid-dialog.tsx"
      provides: "Dialog de comissão estendido com integração SuggestEntryDialog após sucesso"
      contains: "SuggestEntryDialog"
  key_links:
    - from: "src/app/(app)/[slug]/clientes/page.tsx"
      to: "financial_entries table"
      via: "Promise.all com query separada de overdue client_ids"
      pattern: "financial_entries.*status.*pending.*due_date"
    - from: "src/app/(app)/[slug]/clientes/clients-table.tsx"
      to: "overdueClientIds prop"
      via: "Set<string> passada do Server Component"
      pattern: "overdueClientIds"
    - from: "src/components/seguros/mark-commission-paid-dialog.tsx"
      to: "SuggestEntryDialog"
      via: "useState abre SuggestEntryDialog após markCommissionPaidAction success"
      pattern: "SuggestEntryDialog"
    - from: "src/components/auth/sidebar-shell.tsx"
      to: "user role check"
      via: "prop userRole condiciona renderização do item Financeiro"
      pattern: "userRole.*===.*admin.*financeiro"
---

<objective>
Fechar o ciclo da Phase 5 com 3 integrações cirúrgicas:
1. **Sidebar** — adicionar item "Financeiro" entre "Parceiros" e "Configurações", visível apenas para admin e financeiro (D-05).
2. **Badge de inadimplência** — listagem de clientes ganha badge "Inadimplente" para clientes com lançamento vencido, respeitando RBAC (D-10).
3. **Sugestão pós-comissão** — após mark-commission-paid bem-sucedido, abrir dialog secundário oferecendo criar lançamento receivable com campos pré-preenchidos (D-04).

Purpose: Atender FIN-04 (badge de inadimplência) e fechar D-04 (sugestão opcional) e D-05 (RBAC sidebar). Sem este plano o módulo financeiro fica desconectado do resto do sistema.

Output: 1 componente novo (SuggestEntryDialog), 4 arquivos modificados (sidebar, clientes/page, clients-table, mark-commission-paid-dialog), 2 arquivos de teste expandidos com casos de borda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-financeiro/05-CONTEXT.md
@.planning/phases/05-financeiro/05-RESEARCH.md
@.planning/phases/05-financeiro/05-VALIDATION.md
@.planning/phases/05-financeiro/05-01-PLAN.md
@.planning/phases/05-financeiro/05-02-PLAN.md

<!-- Files to modify (read current state before editing) -->
@src/components/auth/sidebar-shell.tsx
@src/app/(app)/[slug]/clientes/page.tsx
@src/app/(app)/[slug]/clientes/clients-table.tsx
@src/components/seguros/mark-commission-paid-dialog.tsx

<!-- Reference implementations -->
@src/components/financeiro/new-entry-dialog.tsx
@src/lib/actions/financial-entries.ts
@src/lib/actions/commission-entries.ts

<interfaces>
<!-- From Plan 02 (Wave 2) -->
```typescript
import { createFinancialEntryAction } from '@/lib/actions/financial-entries'
// (slug: string, formData: FormData) => Promise<{success:true, entry_id?:string} | {error:...}>

import { type FinancialEntryRow } from '@/components/financeiro/financial-entries-table'
```

<!-- Existing sidebar-shell.tsx public API -->
```typescript
interface SidebarShellProps {
  slug: string
  alertCounts?: { policies: number; assemblies: number }
  // NEW field added in this plan: userRole?: string
}
export function SidebarShell(props: SidebarShellProps): JSX.Element
```

<!-- Existing clients-table.tsx public API (must extend) -->
```typescript
interface ClientsTableProps {
  slug: string
  clients: Client[]
  stages: Stage[]
  userRole: string
  userId: string
  // NEW field added in this plan: overdueClientIds: Set<string>
}
```

<!-- Existing mark-commission-paid-dialog.tsx — must extend with SuggestEntryDialog integration -->
```typescript
// Current props (DO NOT change shape, ADD optional props):
interface Props {
  slug: string
  sourceType: 'policy' | 'quota'
  sourceId: string
  amounts: CalculatedAmounts
  // NEW optional props for suggestion flow:
  policyId?: string         // for prefilling policy_id in SuggestEntryDialog
  quotaId?: string          // for prefilling quota_id
  clientId?: string         // for prefilling client_id (badge connection)
  baseDescription?: string  // e.g., "Premio Auto Bradesco - Joao Silva" for prefill
  baseAmount?: number       // typically the policy.premio_total or quota credit
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sidebar — adicionar item "Financeiro" condicionado a role</name>
  <files>src/components/auth/sidebar-shell.tsx, src/app/(app)/[slug]/layout.tsx</files>
  <read_first>
    - src/components/auth/sidebar-shell.tsx (estado atual — entender array `navItems`, props, Lucide icons usados)
    - src/app/(app)/[slug]/layout.tsx (verificar como SidebarShell é instanciado e qual user info já está disponível)
    - .planning/phases/05-financeiro/05-CONTEXT.md (D-05 RBAC, claude's discretion: "Wallet" do Lucide React)
    - .planning/phases/05-financeiro/05-RESEARCH.md (recomendação: `Wallet` mais semântico que `Banknote`; `DollarSign` em uso por Consórcio — evitar confusão)
  </read_first>
  <behavior>
    - SidebarShell aceita nova prop opcional `userRole?: string`
    - Se `userRole === 'admin'` OU `userRole === 'financeiro'` → renderizar item "Financeiro" entre "Parceiros" e "Configurações"
    - Se `userRole` é outro valor (corretor, visualizador, undefined) → NÃO renderizar item Financeiro
    - Item usa ícone `Wallet` do `lucide-react`
    - href: `/${slug}/financeiro`
    - layout.tsx (Server Component) lê role do user via `auth.getUser()` e passa para SidebarShell via prop `userRole`
    - layout.tsx NÃO quebra para roles existentes (corretor, visualizador) — apenas omite o item
  </behavior>
  <action>
**1a) Modificar `src/components/auth/sidebar-shell.tsx`:**

1. Adicionar `Wallet` ao import existente do lucide-react (linha 3 atual):
   ```typescript
   // ANTES:
   import { LayoutDashboard, Settings, Users, UserCog, Shield, CircleDollarSign } from 'lucide-react'
   // DEPOIS:
   import { LayoutDashboard, Settings, Users, UserCog, Shield, CircleDollarSign, Wallet } from 'lucide-react'
   ```

2. Estender `SidebarShellProps`:
   ```typescript
   // ANTES:
   interface SidebarShellProps {
     slug: string
     alertCounts?: AlertCounts
   }
   // DEPOIS:
   interface SidebarShellProps {
     slug: string
     alertCounts?: AlertCounts
     userRole?: string
   }
   ```

3. Aceitar `userRole` na assinatura da função:
   ```typescript
   // ANTES:
   export function SidebarShell({ slug, alertCounts }: SidebarShellProps) {
   // DEPOIS:
   export function SidebarShell({ slug, alertCounts, userRole }: SidebarShellProps) {
   ```

4. Adicionar item "Financeiro" no array `navItems` ENTRE o item "Parceiros" e o item "Configurações" — condicionado a role. Substituir o bloco que vai de `{ label: 'Parceiros', ... },` até antes de `{ label: 'Configurações',`:

   ```typescript
   {
     label: 'Parceiros',
     href: `/${slug}/parceiros`,
     icon: <Users size={16} />,
   },
   // D-05: Financeiro APENAS para admin e financeiro
   ...(userRole === 'admin' || userRole === 'financeiro'
     ? [{
         label: 'Financeiro',
         href: `/${slug}/financeiro`,
         icon: <Wallet size={16} />,
       }]
     : []),
   {
     label: 'Configurações',
     ...
   },
   ```

   Manter resto do array idêntico.

**1b) Modificar `src/app/(app)/[slug]/layout.tsx`:**

Ler o arquivo atual e adicionar a leitura do role:

1. Após `auth.getUser()` (já existe), extrair role:
   ```typescript
   const userRole = (user?.app_metadata as { role?: string })?.role ?? ''
   ```

2. Passar para `<SidebarShell ... userRole={userRole} />`. Se houver alertCounts já passado, manter; apenas adicionar o `userRole` prop.

(NOTA: se layout.tsx não chamar getUser hoje porque outras partes não precisam, adicionar a chamada. NÃO redirecionar nem fazer notFound — outras rotas (corretores, parceiros, etc.) já têm seus próprios guards.)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/auth/sidebar-shell.tsx','utf8');const must=['Wallet','userRole','Financeiro',\"label: 'Financeiro'\",\"/financeiro\",\"userRole === 'admin'\"];const missing=must.filter(s=>!c.includes(s));if(missing.length){console.error('MISSING:',missing);process.exit(1)}console.log('sidebar OK')" && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth/sidebar-shell.tsx` imports `Wallet` from `lucide-react`
    - `SidebarShellProps` interface contains optional `userRole?: string`
    - Function signature destructures `userRole` from props
    - File contains literal string `label: 'Financeiro'`
    - File contains literal string `/financeiro` in href
    - File contains conditional `userRole === 'admin'` AND `userRole === 'financeiro'` for sidebar visibility
    - `src/app/(app)/[slug]/layout.tsx` reads `user.app_metadata.role` and passes `userRole` to `<SidebarShell>`
    - `npm run typecheck` exits with code 0
  </acceptance_criteria>
  <done>
    Sidebar exibe "Financeiro" para admin/financeiro; oculto para corretor/visualizador. typecheck passa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badge de inadimplência na listagem de clientes (com RBAC)</name>
  <files>src/app/(app)/[slug]/clientes/page.tsx, src/app/(app)/[slug]/clientes/clients-table.tsx, tests/db/rls-financeiro.test.ts</files>
  <read_first>
    - src/app/(app)/[slug]/clientes/page.tsx (estado atual — entender query existente, paginação, prop drilling para ClientsTable)
    - src/app/(app)/[slug]/clientes/clients-table.tsx (estado atual — entender estrutura da tabela e onde adicionar badge na linha)
    - .planning/phases/05-financeiro/05-CONTEXT.md (D-09 critério, D-10 badge + RBAC)
    - .planning/phases/05-financeiro/05-RESEARCH.md (Pattern 4 — Promise.all + Set; Pitfall 2 — visualizador NUNCA vê badge; Pitfall 5 — todayStr via date-fns)
  </read_first>
  <behavior>
    - Server Component `clientes/page.tsx` faz query paralela adicional via `Promise.all`:
      - Se userRole ∈ ['admin', 'financeiro']: query `financial_entries` SELECT distinct client_id WHERE deleted_at IS NULL AND status='pending' AND due_date < todayStr AND client_id IS NOT NULL
      - Se userRole === 'corretor': mesma query (RLS já filtra por clients.assigned_to = auth.uid())
      - Se userRole === 'visualizador' OU outro: pula a query — `overdueClientIds = new Set()` (Pitfall 2)
    - Constrói `Set<string>` de client_ids vencidos
    - Passa `overdueClientIds` como prop para `<ClientsTable>` junto com props existentes
    - `ClientsTable` aceita nova prop `overdueClientIds: Set<string>`
    - Para cada cliente na tabela, se `overdueClientIds.has(c.id)`, renderizar badge `<Badge variant="destructive">Inadimplente</Badge>` ao lado do nome (próximo ao Link existente)
    - Tests: novos casos em `tests/db/rls-financeiro.test.ts` documentando o comportamento esperado da subquery (it.todo)
  </behavior>
  <action>
**2a) Modificar `src/app/(app)/[slug]/clientes/page.tsx`:**

Após o bloco `const [corretoresRes, stagesRes] = await Promise.all([...])` (atualmente linhas 41-54) e ANTES do bloco `let query = supabase.from('clients')...`, adicionar:

```typescript
  // === D-10: Query paralela para overdue client_ids (badge de inadimplência) ===
  // Pitfall 2: visualizador NUNCA vê badge; corretor vê apenas próprios (RLS já filtra)
  const { format } = await import('date-fns')
  const todayStr = format(new Date(), 'yyyy-MM-dd')
  let overdueClientIds = new Set<string>()
  if (userRole === 'admin' || userRole === 'financeiro' || userRole === 'corretor') {
    try {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const sb = supabase as any
      const { data: overdueRows } = await sb
        .from('financial_entries')
        .select('client_id')
        .is('deleted_at', null)
        .eq('status', 'pending')
        .lt('due_date', todayStr)
        .not('client_id', 'is', null)
      const ids = ((overdueRows ?? []) as Array<{ client_id: string | null }>)
        .map((r) => r.client_id)
        .filter((id): id is string => !!id)
      overdueClientIds = new Set(ids)
    } catch {
      // tabela financial_entries pode não existir antes da Phase 5 db push — fallback graceful
      overdueClientIds = new Set()
    }
  }
```

Mover o `import { format } from 'date-fns'` para o topo (preferir static import), removendo o `await import` dinâmico:

```typescript
// No topo do arquivo, junto aos outros imports:
import { format } from 'date-fns'
```

E remover a linha `const { format } = await import('date-fns')` do bloco acima.

Em seguida, modificar a chamada de `<ClientsTable>` para incluir `overdueClientIds`:

```typescript
<ClientsTable
  slug={slug}
  clients={clients ?? []}
  stages={stagesRes.data ?? []}
  userRole={userRole}
  userId={userId}
  overdueClientIds={overdueClientIds}  // NEW
/>
```

**2b) Modificar `src/app/(app)/[slug]/clientes/clients-table.tsx`:**

1. Estender props:
   ```typescript
   interface ClientsTableProps {
     slug: string
     clients: Client[]
     stages: Stage[]
     userRole: string
     userId: string
     overdueClientIds: Set<string>  // NEW
   }
   ```

2. Aceitar na assinatura: `export function ClientsTable({ slug, clients, stages, userRole, userId, overdueClientIds }: ClientsTableProps)`

3. Dentro do `clients.map((c) => { ... })`, na TableCell que renderiza o nome (atualmente envolve um `<Link>`), adicionar o badge condicional. Localizar o bloco que começa com `<TableCell>` para o nome do cliente (logo após a verificação de `canEdit`) e modificar para:

   ```tsx
   <TableCell>
     <div className="flex items-center gap-2">
       <Link
         href={`/${slug}/clientes/${c.id}`}
         className="font-medium hover:underline"
       >
         {c.name}
       </Link>
       {overdueClientIds.has(c.id) && (
         <Badge variant="destructive" className="text-[10px]">
           Inadimplente
         </Badge>
       )}
     </div>
   </TableCell>
   ```

   (Ajustar o JSX EXISTENTE — o Link e a estrutura atual já existem; apenas envelopar em `<div className="flex items-center gap-2">` e adicionar o Badge condicional.)

   Garantir que `import { Badge } from '@/components/ui/badge'` está no topo do arquivo (já está pelo código atual conforme leitura).

**2c) Atualizar `tests/db/rls-financeiro.test.ts`** — adicionar nova describe block ao final do arquivo existente:

```typescript
describe('financial_entries — overdue client_ids subquery (badge integration)', () => {
  it.todo('admin recebe Set com client_ids de TODOS os lançamentos pending+overdue do tenant')
  it.todo('financeiro recebe Set com client_ids de TODOS os lançamentos pending+overdue do tenant')
  it.todo('corretor recebe Set apenas de client_ids de clients onde assigned_to = auth.uid() (via RLS)')
  it.todo('visualizador recebe Set vazio (query é skipped no Server Component — Pitfall 2)')
  it.todo('lançamentos pagos (status=paid) NUNCA aparecem no Set, mesmo com due_date < hoje')
  it.todo('lançamentos sem client_id (avulsos) NUNCA aparecem no Set (filtro client_id NOT NULL)')
})
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c1=fs.readFileSync('src/app/(app)/[slug]/clientes/page.tsx','utf8');const c2=fs.readFileSync('src/app/(app)/[slug]/clientes/clients-table.tsx','utf8');const must1=['overdueClientIds','financial_entries','status','pending','due_date','todayStr'];const must2=['overdueClientIds','Inadimplente','destructive','overdueClientIds.has(c.id)'];const m1=must1.filter(s=>!c1.includes(s));const m2=must2.filter(s=>!c2.includes(s));if(m1.length){console.error('page MISSING:',m1);process.exit(1)}if(m2.length){console.error('table MISSING:',m2);process.exit(1)}console.log('OK')" && npm run typecheck 2>&1 | tail -10 && npx vitest run tests/db/rls-financeiro.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/[slug]/clientes/page.tsx` contains `overdueClientIds`, `financial_entries`, `status`, `pending`, `due_date`, `todayStr` literals
    - `src/app/(app)/[slug]/clientes/page.tsx` does NOT execute the financial_entries query when `userRole === 'visualizador'` (verified by code structure: condicional `if (userRole === 'admin' || userRole === 'financeiro' || userRole === 'corretor')`)
    - `src/app/(app)/[slug]/clientes/page.tsx` passes `overdueClientIds={overdueClientIds}` to `<ClientsTable>`
    - `src/app/(app)/[slug]/clientes/clients-table.tsx` declares `overdueClientIds: Set<string>` in props
    - `clients-table.tsx` contains literal `<Badge variant="destructive"` AND `Inadimplente`
    - `clients-table.tsx` checks `overdueClientIds.has(c.id)` before rendering badge
    - `tests/db/rls-financeiro.test.ts` contains new describe block "overdue client_ids subquery"
    - `npm run typecheck` exits with code 0
    - `npx vitest run tests/db/rls-financeiro.test.ts` exits with code 0
  </acceptance_criteria>
  <done>
    Listagem de clientes mostra badge "Inadimplente" condicional respeitando RBAC. typecheck e vitest passam.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sugestão pós-comissão — SuggestEntryDialog + integração no MarkCommissionPaidDialog</name>
  <files>src/components/financeiro/suggest-entry-dialog.tsx, src/components/seguros/mark-commission-paid-dialog.tsx, src/app/(app)/[slug]/seguros/[id]/page.tsx, src/app/(app)/[slug]/consorcio/[id]/page.tsx</files>
  <read_first>
    - src/components/seguros/mark-commission-paid-dialog.tsx (estado atual — entender estrutura: `Dialog`, `DialogTrigger`, `handleConfirm`, toast.success/error, `useState open`)
    - src/components/financeiro/new-entry-dialog.tsx (referência de form fields para createFinancialEntryAction — replicar estrutura simplificada)
    - src/app/(app)/[slug]/seguros/[id]/page.tsx (linhas 280-290 — entender como `MarkCommissionPaidDialog` é chamado; saber quais campos passar adicionalmente)
    - src/app/(app)/[slug]/consorcio/[id]/page.tsx (linha ~367 — mesmo dialog mas com `sourceType="quota"`)
    - src/lib/actions/financial-entries.ts (assinatura de `createFinancialEntryAction`)
    - .planning/phases/05-financeiro/05-CONTEXT.md (D-04 sugestão opcional pós-confirmação)
    - .planning/phases/05-financeiro/05-RESEARCH.md (Pitfall 4 — duplicata via doubleclick; UX é responsável)
  </read_first>
  <behavior>
    - `SuggestEntryDialog` (Client Component, modo controlado):
      - Props: `slug`, `open: boolean`, `onOpenChange: (v: boolean) => void`, `defaultDescription: string`, `defaultAmount: number`, `policyId?: string`, `quotaId?: string`, `clientId?: string`
      - Form com 4 campos editáveis: description (Input pre-fill), amount (Input number pre-fill), due_date (Input date default = hoje), notes (Textarea opcional)
      - Hidden inputs para policy_id, quota_id, client_id (vindas das props)
      - entry_type fixo = 'receivable' (sugestão do prêmio recebido)
      - onSubmit: chama `createFinancialEntryAction(slug, formData)` — toast.success / toast.error
      - 2 botões: "Criar lancamento" (submit) e "Agora nao" (close sem ação)
      - useState `submitting`; após sucesso, fecha automaticamente (Pitfall 4: previne duplicata via UX)

    - `MarkCommissionPaidDialog` (modificar):
      - Adicionar props OPCIONAIS: `policyId?`, `quotaId?`, `clientId?`, `baseDescription?`, `baseAmount?`
      - Adicionar useState `suggestOpen: boolean` (default false)
      - Após `markCommissionPaidAction` retornar `{ success: true }` E pelo menos um de `baseDescription`/`baseAmount` for fornecido, fazer `setSuggestOpen(true)` (em vez de só fechar)
      - O dialog primário fecha (`setOpen(false)`) e o secundário (SuggestEntryDialog) abre
      - Renderizar `<SuggestEntryDialog>` no JSX, passando props default

    - `seguros/[id]/page.tsx` (modificar):
      - Linha 284-289: estender props passadas ao MarkCommissionPaidDialog. Localizar onde está `policy.client_id`, `policy.policy_number`, `policy.premio_total`. Passar:
        - `policyId={policy.id}`
        - `clientId={policy.client?.id ?? undefined}` (verificar shape correto — `policy.client_id` se disponível direto)
        - `baseDescription={`Prêmio ${policy.policy_number} — ${policy.client?.name ?? ''}`}`
        - `baseAmount={Number(policy.premio_total)}`

    - `consorcio/[id]/page.tsx` (modificar):
      - Linha ~367-372: estender props. Para a row da quota:
        - `quotaId={cr.quotaId}`
        - `clientId={cr.clientId ?? undefined}` (se disponível na estrutura existente)
        - `baseDescription={`Cota ${cr.quotaNumber} — ${cr.clientName}`}`
        - `baseAmount={Number(group.credit_value)}`
  </behavior>
  <action>
**3a) Criar `src/components/financeiro/suggest-entry-dialog.tsx`:**

```typescript
'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
import { format } from 'date-fns'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { createFinancialEntryAction } from '@/lib/actions/financial-entries'

interface Props {
  slug: string
  open: boolean
  onOpenChange: (v: boolean) => void
  defaultDescription: string
  defaultAmount: number
  policyId?: string
  quotaId?: string
  clientId?: string
}

function formatBRL(v: number): string {
  return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v)
}

export function SuggestEntryDialog({
  slug, open, onOpenChange,
  defaultDescription, defaultAmount,
  policyId, quotaId, clientId,
}: Props) {
  const router = useRouter()
  const [submitting, setSubmitting] = useState(false)
  const todayStr = format(new Date(), 'yyyy-MM-dd')

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setSubmitting(true)
    try {
      const fd = new FormData(e.currentTarget)
      // entry_type fixo + IDs vindos das props (não confiar no FormData)
      fd.set('entry_type', 'receivable')
      if (policyId) fd.set('policy_id', policyId)
      if (quotaId) fd.set('quota_id', quotaId)
      if (clientId) fd.set('client_id', clientId)

      const r = await createFinancialEntryAction(slug, fd)
      if ('error' in r) {
        toast.error(r.error._form?.[0] ?? 'Erro ao criar lancamento.')
      } else {
        toast.success('Lancamento criado.')
        onOpenChange(false)
        router.refresh()
      }
    } finally {
      setSubmitting(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>Criar lancamento financeiro?</DialogTitle>
          <DialogDescription>
            A comissao foi registrada. Deseja criar tambem um lancamento &quot;a receber&quot; com os
            mesmos dados? Voce pode editar antes de salvar ou dispensar.
          </DialogDescription>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="space-y-3">
          <div className="space-y-1">
            <Label htmlFor="description">Descricao</Label>
            <Input id="description" name="description" defaultValue={defaultDescription} required minLength={3} />
          </div>
          <div className="grid grid-cols-2 gap-3">
            <div className="space-y-1">
              <Label htmlFor="amount">Valor (BRL)</Label>
              <Input id="amount" name="amount" type="number" step="0.01" min="0.01"
                defaultValue={defaultAmount} required />
              <p className="text-xs text-muted-foreground">Sugerido: {formatBRL(defaultAmount)}</p>
            </div>
            <div className="space-y-1">
              <Label htmlFor="due_date">Vencimento</Label>
              <Input id="due_date" name="due_date" type="date" defaultValue={todayStr} required />
            </div>
          </div>
          <div className="space-y-1">
            <Label htmlFor="notes">Observacoes (opcional)</Label>
            <Textarea id="notes" name="notes" rows={2} />
          </div>
          <div className="flex justify-end gap-2 pt-2">
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
              Agora nao
            </Button>
            <Button type="submit" disabled={submitting}>
              {submitting ? 'Criando...' : 'Criar lancamento'}
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

**3b) Modificar `src/components/seguros/mark-commission-paid-dialog.tsx`:**

1. Adicionar import do SuggestEntryDialog no topo:
   ```typescript
   import { SuggestEntryDialog } from '@/components/financeiro/suggest-entry-dialog'
   ```

2. Estender Props (interface Props):
   ```typescript
   interface Props {
     slug: string
     sourceType: 'policy' | 'quota'
     sourceId: string
     amounts: CalculatedAmounts
     // NEW — para integração com SuggestEntryDialog (D-04)
     policyId?: string
     quotaId?: string
     clientId?: string
     baseDescription?: string
     baseAmount?: number
   }
   ```

3. Aceitar nas destructuring args:
   ```typescript
   export function MarkCommissionPaidDialog({
     slug, sourceType, sourceId, amounts,
     policyId, quotaId, clientId, baseDescription, baseAmount,
   }: Props) {
   ```

4. Adicionar state:
   ```typescript
   const [suggestOpen, setSuggestOpen] = useState(false)
   ```

5. Modificar `handleConfirm` — após o caminho de sucesso (`toast.success('Comissao registrada no ledger.')`), DEPOIS do `setOpen(false)`, abrir o SuggestEntryDialog se houver dados base:
   ```typescript
   toast.success('Comissao registrada no ledger.')
   setOpen(false)
   // D-04: oferecer criar lançamento receivable após sucesso
   if (baseDescription !== undefined && baseAmount !== undefined && baseAmount > 0) {
     setSuggestOpen(true)
   }
   ```

6. No JSX do return, após o `</Dialog>` do dialog principal e antes do fechamento do componente, adicionar:
   ```tsx
   {baseDescription !== undefined && baseAmount !== undefined && (
     <SuggestEntryDialog
       slug={slug}
       open={suggestOpen}
       onOpenChange={setSuggestOpen}
       defaultDescription={baseDescription}
       defaultAmount={baseAmount}
       policyId={policyId}
       quotaId={quotaId}
       clientId={clientId}
     />
   )}
   ```

   Para isso, embrulhar o JSX existente em fragmento `<>...</>` se ainda não estiver.

**3c) Modificar `src/app/(app)/[slug]/seguros/[id]/page.tsx` (linhas ~280-290):**

Localizar o bloco:
```tsx
<MarkCommissionPaidDialog
  slug={slug}
  sourceType="policy"
  sourceId={policy.id}
  amounts={calculatedAmounts}
/>
```

Substituir por:
```tsx
<MarkCommissionPaidDialog
  slug={slug}
  sourceType="policy"
  sourceId={policy.id}
  amounts={calculatedAmounts}
  policyId={policy.id}
  clientId={policy.client?.id}
  baseDescription={`Premio ${policy.policy_number} - ${policy.client?.name ?? 'cliente'}`}
  baseAmount={Number(policy.premio_total)}
/>
```

(Verificar shape de `policy.client` no read_first — nas linhas 100-110 da página o select é `client:clients(id, name, type)`, então `policy.client?.id` e `policy.client?.name` estão disponíveis.)

**3d) Modificar `src/app/(app)/[slug]/consorcio/[id]/page.tsx` (linhas ~367-372):**

Localizar o bloco:
```tsx
<MarkCommissionPaidDialog
  slug={slug}
  sourceType="quota"
  sourceId={cr.quotaId}
  amounts={cr.amounts}
/>
```

Substituir por:
```tsx
<MarkCommissionPaidDialog
  slug={slug}
  sourceType="quota"
  sourceId={cr.quotaId}
  amounts={cr.amounts}
  quotaId={cr.quotaId}
  clientId={cr.clientId}
  baseDescription={`Cota ${cr.quotaNumber} - ${cr.clientName}`}
  baseAmount={Number(group.credit_value)}
/>
```

(Se `cr.clientId` não estiver disponível no shape atual de `commissionRows`, omitir a linha `clientId={cr.clientId}`. Ler o trecho ~330-360 da página para confirmar.)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f1=fs.readFileSync('src/components/financeiro/suggest-entry-dialog.tsx','utf8');const f2=fs.readFileSync('src/components/seguros/mark-commission-paid-dialog.tsx','utf8');const f3=fs.readFileSync('src/app/(app)/[slug]/seguros/[id]/page.tsx','utf8');const f4=fs.readFileSync('src/app/(app)/[slug]/consorcio/[id]/page.tsx','utf8');const checks=[[f1,['SuggestEntryDialog','createFinancialEntryAction','defaultDescription','defaultAmount','Agora nao','policyId','quotaId','clientId']],[f2,['SuggestEntryDialog','suggestOpen','baseDescription','baseAmount','setSuggestOpen']],[f3,['baseDescription','baseAmount','policy.policy_number']],[f4,['baseDescription','baseAmount','quotaId']]];let bad=false;for(const [c,must] of checks){const m=must.filter(s=>!c.includes(s));if(m.length){console.error('MISSING in file:',m);bad=true}};if(bad)process.exit(1);console.log('OK')" && npm run typecheck 2>&1 | tail -15