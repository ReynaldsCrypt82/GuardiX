---
phase: 05-financeiro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260420_0019_financeiro_schema.sql
  - supabase/migrations/20260420_0020_financeiro_rls.sql
  - src/lib/validations/financial-schemas.ts
  - tests/db/rls-financeiro.test.ts
  - tests/validations/financial-schemas.test.ts
autonomous: false
requirements:
  - FIN-01
  - FIN-02
must_haves:
  truths:
    - "Tabela financial_entries existe no banco com todos os campos obrigatórios (D-02) e CHECKs de domínio (entry_type, status, amount > 0)"
    - "RLS policy SELECT permite admin/financeiro verem tudo do tenant; corretor vê apenas lançamentos cujo client_id pertence a clients.assigned_to = auth.uid()"
    - "RLS policy INSERT permite somente admin e financeiro criarem lançamentos"
    - "RLS policy UPDATE permite somente admin e financeiro atualizarem lançamentos (status/paid_at) — diferente de commission_entries que é append-only"
    - "Trigger prevent_hard_delete bloqueia DELETE direto; trigger set_updated_at mantém updated_at em sync"
    - "Schemas Zod (createFinancialEntrySchema, markFinancialEntryPaidSchema, softDeleteFinancialEntrySchema) compilam com tipos inferidos exportados"
    - "Test stubs em tests/db/rls-financeiro.test.ts e tests/validations/financial-schemas.test.ts existem e o vitest run passa (it.todo permitido)"
  artifacts:
    - path: "supabase/migrations/20260420_0019_financeiro_schema.sql"
      provides: "financial_entries table + indexes + triggers"
      contains: "CREATE TABLE public.financial_entries"
    - path: "supabase/migrations/20260420_0020_financeiro_rls.sql"
      provides: "RLS policies (SELECT, INSERT, UPDATE) for financial_entries"
      contains: "CREATE POLICY \"financial_entries_select\""
    - path: "src/lib/validations/financial-schemas.ts"
      provides: "Zod schemas for create/markPaid/softDelete"
      exports: ["createFinancialEntrySchema", "markFinancialEntryPaidSchema", "softDeleteFinancialEntrySchema", "CreateFinancialEntryInput", "MarkFinancialEntryPaidInput"]
    - path: "tests/db/rls-financeiro.test.ts"
      provides: "Wave 0 RLS test stubs (it.todo)"
      contains: "describe('financial_entries RLS"
    - path: "tests/validations/financial-schemas.test.ts"
      provides: "Wave 0 validation test stubs (it.todo)"
      contains: "describe('financial-schemas"
  key_links:
    - from: "supabase/migrations/20260420_0020_financeiro_rls.sql"
      to: "supabase/migrations/20260420_0002_rls_helpers.sql"
      via: "jwt_tenant_id() and jwt_tenant_role() function calls inside policies"
      pattern: "jwt_tenant_id\\(\\)|jwt_tenant_role\\(\\)"
    - from: "supabase/migrations/20260420_0019_financeiro_schema.sql"
      to: "supabase/migrations/20260420_0005_soft_delete_triggers.sql"
      via: "prevent_hard_delete() trigger function"
      pattern: "prevent_hard_delete"
    - from: "supabase/migrations/20260420_0019_financeiro_schema.sql"
      to: "supabase/migrations/20260420_0011_seguros_schema.sql + 0013_consorcio_schema.sql + 0006_clients_schema.sql"
      via: "FK constraints to policies(id), consortium_quotas(id), clients(id), profiles(id)"
      pattern: "REFERENCES public\\.(policies|consortium_quotas|clients|profiles)"
---

<objective>
Estabelecer a fundação de dados da Phase 5 — Financeiro: tabela `financial_entries` com schema mutável (status pending/paid/cancelled), RLS policies com 3 caminhos de acesso (admin/financeiro/corretor), e schemas Zod canônicos. Diferente de `commission_entries` (Phase 4 append-only), `financial_entries` precisa de RLS UPDATE e trigger `prevent_hard_delete` (soft delete via `deleted_at`).

Purpose: Sem essa fundação nenhuma Server Action ou tela funciona. RLS mal configurada permite escalada de privilégio ou cross-tenant leak (T-5-01, T-5-02). Schemas Zod garantem validação obrigatória antes de qualquer DB call (CR-01).

Output: 2 migrations SQL aplicadas via `supabase db push`, 1 arquivo de schemas Zod com tipos inferidos, 2 arquivos de testes stub (Wave 0) que rodam no vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-financeiro/05-CONTEXT.md
@.planning/phases/05-financeiro/05-RESEARCH.md
@.planning/phases/05-financeiro/05-VALIDATION.md

<!-- Reference migrations to mirror -->
@supabase/migrations/20260420_0002_rls_helpers.sql
@supabase/migrations/20260420_0005_soft_delete_triggers.sql
@supabase/migrations/20260420_0016_corretores_schema.sql
@supabase/migrations/20260420_0018_corretores_rls.sql
@src/lib/validations/commission-schemas.ts

<interfaces>
<!-- Functions/types that downstream tasks (Plans 02, 03) will consume -->

From supabase/migrations/20260420_0002_rls_helpers.sql (existing):
```sql
public.jwt_tenant_id() RETURNS UUID  -- lê (auth.jwt() ->> 'tenant_id')::uuid
public.jwt_tenant_role() RETURNS TEXT  -- lê auth.jwt() ->> 'role'
```

From supabase/migrations/20260420_0005_soft_delete_triggers.sql (existing):
```sql
public.set_updated_at()  -- BEFORE UPDATE trigger function
public.prevent_hard_delete()  -- BEFORE DELETE trigger function (raises exception)
```

After this plan is applied, downstream code (Plans 02, 03) will use:
```typescript
import {
  createFinancialEntrySchema,
  markFinancialEntryPaidSchema,
  softDeleteFinancialEntrySchema,
  type CreateFinancialEntryInput,
  type MarkFinancialEntryPaidInput,
} from '@/lib/validations/financial-schemas'

// Table contract (no generated types — use `as any` cast on supabase client):
// public.financial_entries (
//   id UUID, tenant_id UUID, entry_type 'receivable'|'payable',
//   description TEXT, amount NUMERIC(12,2) > 0, due_date DATE,
//   status 'pending'|'paid'|'cancelled', paid_at TIMESTAMPTZ,
//   policy_id UUID?, quota_id UUID?, client_id UUID?,
//   notes TEXT?, created_by UUID, deleted_at TIMESTAMPTZ?,
//   created_at, updated_at
// )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration schema — financial_entries table + indexes + triggers</name>
  <files>supabase/migrations/20260420_0019_financeiro_schema.sql</files>
  <read_first>
    - supabase/migrations/20260420_0016_corretores_schema.sql (padrão canônico de tabela com soft delete + updated_at + prevent_hard_delete — replicar estrutura de broker_profiles/partners, NÃO de commission_entries)
    - supabase/migrations/20260420_0011_seguros_schema.sql (verificar nome da coluna de policies — confirmar `policies(id)` é a FK)
    - supabase/migrations/20260420_0013_consorcio_schema.sql (confirmar `consortium_quotas(id)` é a FK)
    - supabase/migrations/20260420_0006_clients_schema.sql (confirmar `clients(id)` é a FK)
    - supabase/migrations/20260420_0001_foundation_schema.sql (confirmar `profiles(id)` e `tenants(id)` são FKs disponíveis)
    - supabase/migrations/20260420_0005_soft_delete_triggers.sql (confirmar nomes das functions: `set_updated_at`, `prevent_hard_delete`)
    - .planning/phases/05-financeiro/05-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    - financial_entries tem TODAS as colunas listadas em D-02 com defaults e CHECKs corretos
    - amount tem CHECK (amount > 0) — Pitfall 6 do RESEARCH
    - entry_type CHECK IN ('receivable', 'payable') — D-01
    - status CHECK IN ('pending', 'paid', 'cancelled') DEFAULT 'pending' — D-02
    - 5 índices criados: tenant_id, due_date, status, client_id (parcial WHERE NOT NULL), deleted_at (parcial WHERE NULL)
    - 2 triggers: set_updated_at (BEFORE UPDATE) e prevent_hard_delete (BEFORE DELETE) — D-12 padrão Phase 1
    - SQL é idempotente — replay com `IF NOT EXISTS` onde aplicável (CREATE INDEX IF NOT EXISTS)
  </behavior>
  <action>
Criar arquivo `supabase/migrations/20260420_0019_financeiro_schema.sql` com EXATAMENTE este conteúdo:

```sql
-- Phase 05 Plan 01 — Financeiro Schema (D-01, D-02, D-03)
-- References: 05-CONTEXT.md D-01 (entry_type discriminator), D-02 (campos obrigatórios), D-03 (FKs nullable)
-- financial_entries: status mutável (pending → paid/cancelled), soft delete via deleted_at
-- Diferente de commission_entries (append-only): tem updated_at, deleted_at e RLS UPDATE

-- ========================================================================
-- financial_entries — contas a receber e a pagar (D-01 entry_type discriminator)
-- ========================================================================
CREATE TABLE public.financial_entries (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id   UUID NOT NULL REFERENCES public.tenants(id),
  entry_type  TEXT NOT NULL CHECK (entry_type IN ('receivable', 'payable')),
  description TEXT NOT NULL,
  amount      NUMERIC(12,2) NOT NULL CHECK (amount > 0),
  due_date    DATE NOT NULL,
  status      TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'cancelled')),
  paid_at     TIMESTAMPTZ,
  policy_id   UUID REFERENCES public.policies(id),
  quota_id    UUID REFERENCES public.consortium_quotas(id),
  client_id   UUID REFERENCES public.clients(id),
  notes       TEXT,
  created_by  UUID REFERENCES public.profiles(id),
  deleted_at  TIMESTAMPTZ,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_financial_entries_tenant_id  ON public.financial_entries(tenant_id);
CREATE INDEX IF NOT EXISTS idx_financial_entries_due_date   ON public.financial_entries(due_date);
CREATE INDEX IF NOT EXISTS idx_financial_entries_status     ON public.financial_entries(status);
CREATE INDEX IF NOT EXISTS idx_financial_entries_client_id  ON public.financial_entries(client_id) WHERE client_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_financial_entries_deleted_at ON public.financial_entries(deleted_at) WHERE deleted_at IS NULL;

-- updated_at trigger (padrão Phase 1)
CREATE TRIGGER trg_financial_entries_updated_at
  BEFORE UPDATE ON public.financial_entries
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

-- soft delete enforcement (padrão Phase 1 D-12)
CREATE TRIGGER trg_financial_entries_no_hard_delete
  BEFORE DELETE ON public.financial_entries
  FOR EACH ROW EXECUTE FUNCTION public.prevent_hard_delete();
```

Não criar mais nada nesse arquivo. RLS está em 0020. Não adicionar comentários inline além dos cabeçalhos.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('supabase/migrations/20260420_0019_financeiro_schema.sql','utf8');const must=['CREATE TABLE public.financial_entries','entry_type TEXT NOT NULL CHECK (entry_type IN (\\'receivable\\', \\'payable\\'))','amount      NUMERIC(12,2) NOT NULL CHECK (amount > 0)','status      TEXT NOT NULL DEFAULT \\'pending\\' CHECK (status IN (\\'pending\\', \\'paid\\', \\'cancelled\\'))','REFERENCES public.policies(id)','REFERENCES public.consortium_quotas(id)','REFERENCES public.clients(id)','REFERENCES public.profiles(id)','REFERENCES public.tenants(id)','idx_financial_entries_tenant_id','idx_financial_entries_due_date','idx_financial_entries_status','idx_financial_entries_client_id','idx_financial_entries_deleted_at','trg_financial_entries_updated_at','trg_financial_entries_no_hard_delete','public.set_updated_at()','public.prevent_hard_delete()'];const missing=must.filter(s=>!c.includes(s));if(missing.length){console.error('MISSING:',missing);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260420_0019_financeiro_schema.sql` exists
    - Contains literal string `CREATE TABLE public.financial_entries`
    - Contains `entry_type TEXT NOT NULL CHECK (entry_type IN ('receivable', 'payable'))`
    - Contains `amount      NUMERIC(12,2) NOT NULL CHECK (amount > 0)`
    - Contains `status      TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'cancelled'))`
    - Contains FK declarations for `policies(id)`, `consortium_quotas(id)`, `clients(id)`, `profiles(id)`, `tenants(id)`
    - Contains all 5 indexes: `idx_financial_entries_tenant_id`, `idx_financial_entries_due_date`, `idx_financial_entries_status`, `idx_financial_entries_client_id`, `idx_financial_entries_deleted_at`
    - Contains both triggers `trg_financial_entries_updated_at` and `trg_financial_entries_no_hard_delete`
    - Contains calls to `public.set_updated_at()` and `public.prevent_hard_delete()`
    - File does NOT contain `CREATE POLICY` (RLS lives in plan 01 task 2 file 0020)
  </acceptance_criteria>
  <done>
    Arquivo `supabase/migrations/20260420_0019_financeiro_schema.sql` criado com a tabela financial_entries, 5 índices, 2 triggers, e todas as FK constraints. SQL idempotente. Verify command passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migration RLS — financial_entries policies (SELECT/INSERT/UPDATE)</name>
  <files>supabase/migrations/20260420_0020_financeiro_rls.sql</files>
  <read_first>
    - supabase/migrations/20260420_0018_corretores_rls.sql (padrão canônico de RLS — espelhar estrutura de broker_profiles policies)
    - supabase/migrations/20260420_0002_rls_helpers.sql (confirmar assinaturas de jwt_tenant_id() e jwt_tenant_role())
    - supabase/migrations/20260420_0007_clients_rls.sql (verificar como clients.assigned_to é referenciado em subqueries — padrão para o caminho do corretor)
    - .planning/phases/05-financeiro/05-CONTEXT.md (D-05 RBAC, D-10 corretor responsável)
    - .planning/phases/05-financeiro/05-RESEARCH.md (Pattern 2 — RLS para financial_entries; Pitfall 1 — RLS UPDATE obrigatório)
  </read_first>
  <behavior>
    - ALTER TABLE habilita RLS em financial_entries
    - Policy `financial_entries_select` permite acesso a admin/financeiro (tenant inteiro) E a corretor (apenas se client_id ∈ clients onde assigned_to = auth.uid() AND deleted_at IS NULL)
    - Policy `financial_entries_insert` permite somente admin e financeiro (com `tenant_id = jwt_tenant_id()`)
    - Policy `financial_entries_update` permite somente admin e financeiro (USING + WITH CHECK)
    - SEM policy DELETE — soft delete via UPDATE deleted_at + trigger prevent_hard_delete bloqueia DELETE direto
    - Pitfall 1: RLS UPDATE é OBRIGATÓRIA (diferente de commission_entries que é append-only)
  </behavior>
  <action>
Criar arquivo `supabase/migrations/20260420_0020_financeiro_rls.sql` com EXATAMENTE este conteúdo:

```sql
-- Phase 05 Plan 01 — RLS: financial_entries
-- References: 05-CONTEXT.md D-05 (RBAC admin/financeiro), D-10 (corretor responsável vê seus próprios)
-- 05-RESEARCH.md Pattern 2 + Pitfall 1 (RLS UPDATE obrigatória — diferente de commission_entries)
-- 05-RESEARCH.md Threats T-5-01 (cross-tenant), T-5-02 (UPDATE wrong tenant), T-5-04 (badge role filter)

ALTER TABLE public.financial_entries ENABLE ROW LEVEL SECURITY;

-- ========================================================================
-- financial_entries SELECT
-- D-05: admin/financeiro veem tudo do tenant
-- D-10: corretor vê apenas lançamentos de clientes onde assigned_to = auth.uid()
-- ========================================================================
CREATE POLICY "financial_entries_select" ON public.financial_entries
  FOR SELECT TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
    AND (
      (SELECT public.jwt_tenant_role()) IN ('admin', 'financeiro')
      OR (
        (SELECT public.jwt_tenant_role()) = 'corretor'
        AND client_id IN (
          SELECT id FROM public.clients
          WHERE assigned_to = (SELECT auth.uid())
          AND deleted_at IS NULL
        )
      )
    )
  );

-- ========================================================================
-- financial_entries INSERT
-- D-05: apenas admin e financeiro podem criar lançamentos
-- ========================================================================
CREATE POLICY "financial_entries_insert" ON public.financial_entries
  FOR INSERT TO authenticated
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) IN ('admin', 'financeiro')
  );

-- ========================================================================
-- financial_entries UPDATE
-- D-05 + D-08: admin e financeiro atualizam status/paid_at/deleted_at
-- Pitfall 1 do RESEARCH: ausência desta policy bloqueia markFinancialEntryPaidAction
-- ========================================================================
CREATE POLICY "financial_entries_update" ON public.financial_entries
  FOR UPDATE TO authenticated
  USING (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND deleted_at IS NULL
    AND (SELECT public.jwt_tenant_role()) IN ('admin', 'financeiro')
  )
  WITH CHECK (
    tenant_id = (SELECT public.jwt_tenant_id())
    AND (SELECT public.jwt_tenant_role()) IN ('admin', 'financeiro')
  );

-- CRITICAL: SEM policy DELETE — soft delete via UPDATE deleted_at
-- Trigger prevent_hard_delete (em 0019) bloqueia DELETE direto
```

Não criar policy DELETE. Não adicionar revoke statements (ENABLE RLS já barra default).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('supabase/migrations/20260420_0020_financeiro_rls.sql','utf8');const must=['ALTER TABLE public.financial_entries ENABLE ROW LEVEL SECURITY','CREATE POLICY \"financial_entries_select\"','CREATE POLICY \"financial_entries_insert\"','CREATE POLICY \"financial_entries_update\"','jwt_tenant_id()','jwt_tenant_role()','IN (\\'admin\\', \\'financeiro\\')','assigned_to = (SELECT auth.uid())'];const missing=must.filter(s=>!c.includes(s));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(c.includes('CREATE POLICY \"financial_entries_delete\"')){console.error('MUST NOT have DELETE policy');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260420_0020_financeiro_rls.sql` exists
    - Contains `ALTER TABLE public.financial_entries ENABLE ROW LEVEL SECURITY`
    - Contains exactly 3 CREATE POLICY statements: `financial_entries_select`, `financial_entries_insert`, `financial_entries_update`
    - Contains NO policy named `financial_entries_delete`
    - SELECT policy contains both branches: `IN ('admin', 'financeiro')` AND `corretor` with subquery `assigned_to = (SELECT auth.uid())`
    - INSERT policy contains `(SELECT public.jwt_tenant_role()) IN ('admin', 'financeiro')`
    - UPDATE policy contains both USING and WITH CHECK clauses with role check
    - File references `jwt_tenant_id()` and `jwt_tenant_role()` helper functions
  </acceptance_criteria>
  <done>
    Arquivo `supabase/migrations/20260420_0020_financeiro_rls.sql` criado com 3 policies (SELECT/INSERT/UPDATE) e nenhuma policy DELETE. Verify command passa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Zod schemas para financial_entries + Wave 0 test stubs</name>
  <files>src/lib/validations/financial-schemas.ts, tests/db/rls-financeiro.test.ts, tests/validations/financial-schemas.test.ts</files>
  <read_first>
    - src/lib/validations/commission-schemas.ts (padrão canônico de schemas Zod — replicar estilo, exports e tipos inferidos)
    - tests/db/rls-clients.test.ts (padrão Wave 0 com `it.todo` — replicar estrutura)
    - tests/actions/commission-entries.test.ts (referência de mock de supabase para futuros testes — apenas referência, não implementar agora)
    - .planning/phases/05-financeiro/05-RESEARCH.md (Pattern 6 — Zod schemas)
    - .planning/phases/05-financeiro/05-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    Tests (it.todo — vão ficar todo até ser implementadas em planos futuros):

    tests/validations/financial-schemas.test.ts:
    - createFinancialEntrySchema rejeita amount = 0
    - createFinancialEntrySchema rejeita amount negativo
    - createFinancialEntrySchema aceita amount positivo com 2 decimais
    - createFinancialEntrySchema rejeita due_date em formato inválido
    - createFinancialEntrySchema aceita entry_type 'receivable' e 'payable'; rejeita outros valores
    - createFinancialEntrySchema aceita policy_id/quota_id/client_id null/undefined
    - markFinancialEntryPaidSchema requer entry_id UUID; paid_at é opcional ISO datetime

    tests/db/rls-financeiro.test.ts:
    - tenant A nunca vê financial_entries do tenant B (SELECT cross-tenant blocked)
    - corretor vê apenas lançamentos de clientes próprios (assigned_to = auth.uid())
    - corretor não vê lançamentos sem client_id (avulsos) — apenas admin/financeiro
    - INSERT por visualizador é bloqueado
    - UPDATE por corretor em lançamento de outro cliente é bloqueado
    - hard DELETE bloqueado pelo trigger prevent_hard_delete
  </behavior>
  <action>
**3a) Criar `src/lib/validations/financial-schemas.ts`** com EXATAMENTE este conteúdo:

```typescript
import { z } from 'zod'

/**
 * createFinancialEntrySchema — D-01, D-02, D-03
 * Validação para criação de lançamento (receivable ou payable).
 * - amount: positivo (CHECK no banco também — Pitfall 6)
 * - due_date: formato YYYY-MM-DD (DATE no PostgreSQL, sem timezone)
 * - policy_id, quota_id, client_id: nullable (D-03 lançamentos avulsos)
 */
export const createFinancialEntrySchema = z.object({
  entry_type: z.enum(['receivable', 'payable'], { message: 'Tipo invalido (receivable ou payable)' }),
  description: z.string().min(3, 'Descricao deve ter ao menos 3 caracteres'),
  amount: z.coerce.number().positive('Valor deve ser positivo'),
  due_date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Data deve estar no formato YYYY-MM-DD'),
  policy_id: z.string().uuid('ID de apolice invalido').optional().nullable(),
  quota_id: z.string().uuid('ID de cota invalido').optional().nullable(),
  client_id: z.string().uuid('ID de cliente invalido').optional().nullable(),
  notes: z.string().optional().nullable(),
})

/**
 * markFinancialEntryPaidSchema — D-08 (FIN-05)
 * paid_at default = hoje (preenchido na Server Action se não vier).
 * Idempotência (status != 'paid') é responsabilidade da action, não do schema.
 */
export const markFinancialEntryPaidSchema = z.object({
  entry_id: z.string().uuid('ID de lancamento invalido'),
  paid_at: z.string().datetime({ message: 'paid_at deve ser ISO datetime' }).optional(),
})

/**
 * softDeleteFinancialEntrySchema — D-12 padrão Phase 1
 * Soft delete via UPDATE deleted_at. Hard DELETE é bloqueado pelo trigger.
 */
export const softDeleteFinancialEntrySchema = z.object({
  entry_id: z.string().uuid('ID de lancamento invalido'),
})

export type CreateFinancialEntryInput = z.infer<typeof createFinancialEntrySchema>
export type MarkFinancialEntryPaidInput = z.infer<typeof markFinancialEntryPaidSchema>
export type SoftDeleteFinancialEntryInput = z.infer<typeof softDeleteFinancialEntrySchema>
```

**3b) Criar `tests/validations/financial-schemas.test.ts`** com EXATAMENTE este conteúdo:

```typescript
import { describe, it, expect } from 'vitest'
import {
  createFinancialEntrySchema,
  markFinancialEntryPaidSchema,
  softDeleteFinancialEntrySchema,
} from '@/lib/validations/financial-schemas'

describe('financial-schemas — createFinancialEntrySchema', () => {
  it('aceita receivable com campos minimos validos', () => {
    const r = createFinancialEntrySchema.safeParse({
      entry_type: 'receivable',
      description: 'Premio Auto Bradesco',
      amount: 1500.5,
      due_date: '2026-05-15',
    })
    expect(r.success).toBe(true)
  })

  it('aceita payable com vinculos opcionais explicitamente null', () => {
    const r = createFinancialEntrySchema.safeParse({
      entry_type: 'payable',
      description: 'Repasse seguradora',
      amount: 100,
      due_date: '2026-05-20',
      policy_id: null,
      quota_id: null,
      client_id: null,
    })
    expect(r.success).toBe(true)
  })

  it.todo('rejeita amount = 0')
  it.todo('rejeita amount negativo')
  it.todo('rejeita entry_type fora de receivable/payable')
  it.todo('rejeita due_date em formato dd/mm/yyyy')
  it.todo('rejeita description com menos de 3 chars')
  it.todo('aceita amount com 2 casas decimais')
  it.todo('aceita policy_id como UUID valido')
})

describe('financial-schemas — markFinancialEntryPaidSchema', () => {
  it('aceita entry_id UUID sem paid_at', () => {
    const r = markFinancialEntryPaidSchema.safeParse({
      entry_id: '11111111-1111-1111-1111-111111111111',
    })
    expect(r.success).toBe(true)
  })

  it.todo('aceita entry_id UUID com paid_at ISO datetime')
  it.todo('rejeita entry_id nao-UUID')
  it.todo('rejeita paid_at em formato YYYY-MM-DD (deve ser ISO datetime)')
})

describe('financial-schemas — softDeleteFinancialEntrySchema', () => {
  it('aceita entry_id UUID', () => {
    const r = softDeleteFinancialEntrySchema.safeParse({
      entry_id: '22222222-2222-2222-2222-222222222222',
    })
    expect(r.success).toBe(true)
  })

  it.todo('rejeita entry_id ausente')
})
```

**3c) Criar `tests/db/rls-financeiro.test.ts`** com EXATAMENTE este conteúdo:

```typescript
import { describe, it } from 'vitest'

describe('financial_entries RLS — tenant isolation', () => {
  it.todo('tenant A nunca vê financial_entries do tenant B (SELECT)')
  it.todo('INSERT de financial_entry com tenant_id mismatch é bloqueado')
  it.todo('hard DELETE é bloqueado por trigger prevent_hard_delete')
})

describe('financial_entries RLS — RBAC admin/financeiro', () => {
  it.todo('admin vê todos lançamentos do tenant (com e sem client_id)')
  it.todo('financeiro vê todos lançamentos do tenant (com e sem client_id)')
  it.todo('admin pode INSERT lançamento receivable e payable')
  it.todo('financeiro pode INSERT lançamento receivable e payable')
  it.todo('admin pode UPDATE status pending → paid')
  it.todo('financeiro pode UPDATE status pending → paid')
})

describe('financial_entries RLS — RBAC corretor (D-10)', () => {
  it.todo('corretor vê lançamentos de clientes próprios (clients.assigned_to = auth.uid())')
  it.todo('corretor NÃO vê lançamentos de clientes de outro corretor')
  it.todo('corretor NÃO vê lançamentos avulsos sem client_id (apenas admin/financeiro)')
  it.todo('corretor NÃO pode INSERT (FOR INSERT WITH CHECK falha)')
  it.todo('corretor NÃO pode UPDATE (FOR UPDATE USING/WITH CHECK falha)')
})

describe('financial_entries RLS — RBAC visualizador', () => {
  it.todo('visualizador NÃO vê lançamentos (não está em admin/financeiro/corretor)')
  it.todo('visualizador NÃO pode INSERT')
})
```
  </action>
  <verify>
    <automated>npx vitest run tests/validations/financial-schemas.test.ts tests/db/rls-financeiro.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/validations/financial-schemas.ts` exists and exports `createFinancialEntrySchema`, `markFinancialEntryPaidSchema`, `softDeleteFinancialEntrySchema`, `CreateFinancialEntryInput`, `MarkFinancialEntryPaidInput`, `SoftDeleteFinancialEntryInput`
    - `createFinancialEntrySchema` definition contains literal `z.enum(['receivable', 'payable']` and `z.coerce.number().positive(`
    - `tests/validations/financial-schemas.test.ts` exists and contains 3 `describe(` blocks
    - `tests/db/rls-financeiro.test.ts` exists and contains 4 `describe(` blocks (tenant isolation, admin/financeiro, corretor, visualizador)
    - `npx vitest run tests/validations/financial-schemas.test.ts tests/db/rls-financeiro.test.ts` exits with code 0 (passing tests + todos)
  </acceptance_criteria>
  <done>
    3 arquivos criados: schemas + 2 test stubs. `npx vitest run tests/validations/financial-schemas.test.ts tests/db/rls-financeiro.test.ts` passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: [BLOCKING] Apply migrations via supabase db push</name>
  <files>(no files modified — applies migrations to live database)</files>
  <read_first>
    - supabase/migrations/20260420_0019_financeiro_schema.sql (verificar arquivo existe e está correto)
    - supabase/migrations/20260420_0020_financeiro_rls.sql (verificar arquivo existe e está correto)
    - .planning/STATE.md (Phase 03 decision: "supabase db push contornado via supabase db query --linked -f por versioning collision" — usar mesmo workaround se necessário)
  </read_first>
  <behavior>
    - Migrations 0019 e 0020 aplicadas ao banco linked
    - Tabela financial_entries existe e responde a `\d public.financial_entries`
    - Policies financial_entries_select/insert/update existem (verificáveis via pg_policies)
    - Se `supabase db push` falhar com versioning collision (mesmo erro do Phase 03), aplicar via `supabase db query --linked -f supabase/migrations/20260420_0019_financeiro_schema.sql` e idem para 0020
  </behavior>
  <action>
1. Tentar `supabase db push --linked --include-all` primeiro:
   ```bash
   supabase db push --linked --include-all
   ```

2. Se a versão `20260420` colidir com phases anteriores (mesma date key), usar workaround documentado no STATE.md:
   ```bash
   supabase db query --linked -f supabase/migrations/20260420_0019_financeiro_schema.sql
   supabase db query --linked -f supabase/migrations/20260420_0020_financeiro_rls.sql
   ```

3. Verificar que a tabela existe com query de smoke test:
   ```bash
   supabase db query --linked --execute "SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='public' AND table_name='financial_entries' ORDER BY ordinal_position;"
   ```

4. Verificar policies aplicadas:
   ```bash
   supabase db query --linked --execute "SELECT policyname, cmd FROM pg_policies WHERE tablename='financial_entries' ORDER BY policyname;"
   ```

5. Se SUPABASE_ACCESS_TOKEN não estiver setado e o comando precisar de auth interativo, exportar o token:
   ```bash
   export SUPABASE_ACCESS_TOKEN=<from .env or supabase login>
   ```

NOTA: Se o ambiente não tiver acesso a `supabase` CLI (modo offline), reportar bloqueio explicitamente — NÃO marcar a task como done sem confirmação de que o banco recebeu a migration.
  </action>
  <verify>
    <automated>supabase db query --linked --execute "SELECT count(*) AS col_count FROM information_schema.columns WHERE table_schema='public' AND table_name='financial_entries';" 2>&1 | grep -E "[0-9]+" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Comando `supabase db query --linked --execute "SELECT count(*) FROM information_schema.columns WHERE table_schema='public' AND table_name='financial_entries';"` retorna count > 0 (pelo menos 16 colunas — id, tenant_id, entry_type, description, amount, due_date, status, paid_at, policy_id, quota_id, client_id, notes, created_by, deleted_at, created_at, updated_at)
    - Comando `supabase db query --linked --execute "SELECT count(*) FROM pg_policies WHERE tablename='financial_entries';"` retorna count = 3 (select, insert, update)
    - Triggers `trg_financial_entries_updated_at` e `trg_financial_entries_no_hard_delete` existem (verificável via `SELECT trigger_name FROM information_schema.triggers WHERE event_object_table='financial_entries';`)
  </acceptance_criteria>
  <done>
    Migrations 0019 e 0020 aplicadas. financial_entries tem 16 colunas e 3 policies. Triggers ativos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Untrusted user input (FormData, body) cruza para Server Action; Zod safeParse + auth role check necessários |
| Server Action → Postgres | Auth via @supabase/ssr cookies → JWT → claims (tenant_id, role); RLS policies enforcement |
| Cross-tenant access via JWT | tenant_id no JWT é imutável (`app_metadata`); RLS lê via `jwt_tenant_id()` helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | financial_entries SELECT | mitigate | RLS SELECT policy com `tenant_id = jwt_tenant_id()` + 3-way role check (admin/financeiro/corretor-via-clients.assigned_to) |
| T-5-02 | Tampering | financial_entries UPDATE/INSERT | mitigate | RLS INSERT/UPDATE com `tenant_id = jwt_tenant_id()` + role IN ('admin','financeiro'); WITH CHECK previne pivot de tenant em UPDATE |
| T-5-03 | Tampering | DELETE direto via API | mitigate | Trigger `prevent_hard_delete` BEFORE DELETE + ausência de RLS DELETE policy (defesa em profundidade) |
| T-5-04 | Information Disclosure | corretor vendo lançamentos avulsos | mitigate | RLS SELECT corretor branch exige `client_id IN (...)` — lançamento sem client_id é invisível ao corretor (apenas admin/financeiro) |
| T-5-05 | Tampering | amount negativo via API | mitigate | DB CHECK `amount > 0` + Zod `.positive()` em createFinancialEntrySchema (defesa em profundidade) |
| T-5-06 | Elevation of Privilege | corretor INSERT em financial_entries | mitigate | RLS INSERT WITH CHECK exclui role 'corretor' explicitamente |
| T-5-07 | Information Disclosure | description/notes contendo PII | accept | description e notes são livres; LGPD audit via `created_by` UUID resolve; sem necessidade de criptografia em v1 |
</threat_model>

<verification>
- [ ] Arquivo `supabase/migrations/20260420_0019_financeiro_schema.sql` existe e contém `CREATE TABLE public.financial_entries`
- [ ] Arquivo `supabase/migrations/20260420_0020_financeiro_rls.sql` existe e contém 3 CREATE POLICY (SELECT, INSERT, UPDATE) e ZERO DELETE policy
- [ ] Arquivo `src/lib/validations/financial-schemas.ts` exporta `createFinancialEntrySchema`, `markFinancialEntryPaidSchema`, `softDeleteFinancialEntrySchema` com tipos inferidos
- [ ] `npx vitest run tests/validations/financial-schemas.test.ts tests/db/rls-financeiro.test.ts` passa (tests + todos)
- [ ] `supabase db push` aplicou ambas as migrations (ou workaround `supabase db query --linked -f`)
- [ ] `pg_policies` mostra 3 policies para `financial_entries`; `information_schema.triggers` mostra 2 triggers
- [ ] `npm run typecheck` passa (financial-schemas.ts compila sem erros)
</verification>

<success_criteria>
1. Tabela `public.financial_entries` existe no banco linked com 16 colunas, 5 indexes, 2 triggers, 3 RLS policies
2. RLS UPDATE policy permite admin/financeiro fazer UPDATE — confirmado via `pg_policies WHERE policyname = 'financial_entries_update'`
3. Schemas Zod compilam e safeParse funciona conforme testes positivos (3 testes implementados, demais it.todo)
4. Vitest run dos arquivos de teste deste plano passa sem erro (incluindo it.todo)
5. Trigger `prevent_hard_delete` existe e DELETE direto retorna erro
</success_criteria>

<output>
Após completar todas as tasks, criar `.planning/phases/05-financeiro/05-01-SUMMARY.md` com:
- Migrations aplicadas (0019 schema + 0020 RLS)
- Schemas Zod exportados (3) e tipos inferidos (3)
- Test stubs criados (2 arquivos)
- Decisões emergentes (workaround usado para `supabase db push` se aplicável)
- Notas para Plan 02 (assinaturas exatas dos schemas Zod a serem importados pelas Server Actions)
</output>
