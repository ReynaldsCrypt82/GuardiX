---
phase: 02-crm-clientes
plan: 04
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/lib/actions/pipeline.ts
  - src/lib/validations/pipeline-schemas.ts
  - src/app/(app)/[slug]/configuracoes/pipeline/page.tsx
  - src/app/(app)/[slug]/configuracoes/pipeline/stages-manager.tsx
  - src/app/(app)/[slug]/configuracoes/pipeline/stage-delete-dialog.tsx
  - src/app/(app)/[slug]/clientes/client-stage-selector.tsx
  - src/components/auth/sidebar-shell.tsx
  - tests/actions/pipeline.test.ts
  - tests/actions/clients.test.ts
autonomous: true
requirements:
  - CRM-05
must_haves:
  truths:
    - "Admin acessa /[slug]/configuracoes/pipeline e vê os estágios atuais ordenados por position"
    - "Admin pode criar novo estágio com nome, cor e flag is_closed — position = max+1"
    - "Admin pode remover estágio; se houver clientes nele, dialog confirma realocação para o estágio default (posição 1)"
    - "Corretor/Visualizador NÃO acessa a tela de configurações de pipeline (403 ou redirect)"
    - "Na tabela de clientes, cada row mostra o badge de estágio como dropdown (admin e corretor responsável)"
    - "Trocar estágio via dropdown inline atualiza imediatamente sem navegar (optimistic + revalidate)"
    - "Corretor só pode mudar estágio dos próprios clientes (enforcement na RLS + no Server Action)"
    - "Trocar estágio persiste em clients.stage_id e retorna sucesso"
    - "Ao deletar estágio com N clientes, esses N clientes ganham stage_id do primeiro estágio não-deletado"
  artifacts:
    - path: "src/lib/actions/pipeline.ts"
      provides: "updateClientStage, createStage, deleteStage Server Actions"
      exports: ["updateClientStage", "createStage", "deleteStage"]
    - path: "src/lib/validations/pipeline-schemas.ts"
      provides: "Zod schemas"
      exports: ["createStageSchema", "updateStageSchema"]
    - path: "src/app/(app)/[slug]/configuracoes/pipeline/page.tsx"
      provides: "Tela admin de gestão de estágios"
    - path: "src/app/(app)/[slug]/configuracoes/pipeline/stages-manager.tsx"
      provides: "Client Component CRUD de estágios"
    - path: "src/app/(app)/[slug]/clientes/client-stage-selector.tsx"
      provides: "Dropdown inline para mudar estágio do cliente"
  key_links:
    - from: "stages-manager.tsx"
      to: "createStage / deleteStage"
      via: "Server Actions"
      pattern: "createStage|deleteStage"
    - from: "client-stage-selector.tsx"
      to: "updateClientStage"
      via: "Server Action optimistic"
      pattern: "updateClientStage"
    - from: "deleteStage"
      to: "UPDATE clients SET stage_id = defaultStageId WHERE stage_id = deletedId"
      via: "realocação antes de soft-delete"
      pattern: "stage_id.*default"
---

<objective>
Entregar CRM-05 (mover cliente pelo pipeline): tela admin em `/[slug]/configuracoes/pipeline` para criar/remover estágios com realocação segura, e dropdown inline na tabela de clientes para mudar estágio sem navegar.

Purpose: Implementa D-08 e D-09. Pipeline configurável é diferencial competitivo — cada corretora organiza sua operação.
Output: 3 Server Actions, 1 schema Zod, 1 página admin, 2 client components (stages-manager + stage-delete-dialog), 1 client component reutilizável (client-stage-selector), link na sidebar, testes unitários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-crm-clientes/02-CONTEXT.md
@.planning/phases/02-crm-clientes/02-RESEARCH.md
@src/lib/actions/auth.ts
@src/lib/actions/invites.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/components/auth/sidebar-shell.tsx
@src/components/ui/dialog.tsx
@src/components/ui/alert-dialog.tsx
@src/components/ui/dropdown-menu.tsx
@src/components/ui/badge.tsx
@supabase/migrations/20260420_0007_clients_rls.sql

<interfaces>
Schema (from Plan 01):
```sql
public.pipeline_stages: id UUID, tenant_id, name TEXT, color TEXT, position INT, is_closed BOOL, deleted_at
  UNIQUE (tenant_id, position) WHERE deleted_at IS NULL
public.clients.stage_id UUID REFERENCES pipeline_stages(id) ON DELETE SET NULL
```

RLS (from Plan 01):
```sql
pipeline_stages_admin_manage: FOR ALL apenas se role='admin'
clients_update: admin OR (corretor AND assigned_to = auth.uid())
```

Pattern Server Action (src/lib/actions/auth.ts):
```typescript
'use server'
return { error: {...} } | void
revalidatePath on success
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions pipeline.ts (createStage, deleteStage, updateClientStage) + schema + testes</name>
  <read_first>
    - src/lib/actions/auth.ts (padrão Server Action)
    - src/lib/actions/invites.ts
    - supabase/migrations/20260420_0007_clients_rls.sql (entender RLS aplicável)
    - supabase/migrations/20260420_0010_pipeline_defaults_and_soft_delete.sql (defaults por tenant)
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Pitfall 3 (realocação ao deletar estágio) + §Pitfall 7 (position unique)
    - tests/actions/clients.test.ts (converter it.todo de updateClientStage)
  </read_first>
  <behavior>
    - createStage({ name, color, is_closed }): INSERT com position = MAX(position) + 1 dentro do tenant; unique violation nunca ocorre pois calculamos server-side
    - createStage: name vazio → error `{ name: ['Nome obrigatório'] }`
    - createStage: color não é hex válido → error `{ color: ['Cor inválida'] }`
    - createStage: role != 'admin' → error `{ _form: ['Apenas admin pode criar estágios'] }`
    - deleteStage(id): se é o ÚNICO estágio não-deletado → error `{ _form: ['É necessário manter ao menos um estágio ativo'] }`
    - deleteStage: busca default (primeiro estágio não-deletado com `id != targetId`, ordenado por position); UPDATE clientes movendo `stage_id` de target para default; depois soft-delete target (UPDATE `deleted_at = NOW()`)
    - deleteStage: retorna contagem de clientes realocados para UI informar
    - updateClientStage({ clientId, stageId }): corretor só muda dos próprios (enforcement via guard + RLS); admin muda qualquer; sucesso retorna `{ ok: true }`
    - updateClientStage: stageId inexistente no tenant → error `{ _form: ['Estágio inválido'] }`
    - updateClientStage: clientId que não pertence ao tenant → RLS retorna 0 rows → `{ error: { _form: ['Cliente não encontrado'] } }`
  </behavior>
  <action>
    **1. Criar `src/lib/validations/pipeline-schemas.ts`:**

    ```typescript
    import { z } from 'zod'

    const hexColor = z.string().regex(/^#[0-9a-fA-F]{6}$/, 'Cor inválida (use formato hex #RRGGBB)')

    export const createStageSchema = z.object({
      name: z.string().min(1, 'Nome obrigatório').max(50, 'Nome muito longo'),
      color: hexColor,
      is_closed: z.boolean().default(false),
    })

    export const updateClientStageSchema = z.object({
      clientId: z.string().uuid(),
      stageId: z.string().uuid(),
    })

    export type CreateStageInput = z.infer<typeof createStageSchema>
    export type UpdateClientStageInput = z.infer<typeof updateClientStageSchema>
    ```

    **2. Criar `src/lib/actions/pipeline.ts`:**

    ```typescript
    'use server'
    import { revalidatePath } from 'next/cache'
    import { createClient } from '@/lib/supabase/server'
    import {
      createStageSchema,
      updateClientStageSchema,
    } from '@/lib/validations/pipeline-schemas'

    async function getAuthContext() {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) return { supabase, user: null, tenantId: null, role: null }
      const meta = user.app_metadata as { tenant_id?: string; role?: string }
      return { supabase, user, tenantId: meta.tenant_id ?? null, role: meta.role ?? null }
    }

    export async function createStage(
      slug: string,
      formData: FormData,
    ): Promise<{ error?: Record<string, string[]>; id?: string }> {
      const raw = Object.fromEntries(formData) as Record<string, unknown>
      raw.is_closed = raw.is_closed === 'true' || raw.is_closed === 'on' || raw.is_closed === true
      const parsed = createStageSchema.safeParse(raw)
      if (!parsed.success) return { error: parsed.error.flatten().fieldErrors }

      const { supabase, tenantId, role } = await getAuthContext()
      if (!tenantId) return { error: { _form: ['Sessão inválida'] } }
      if (role !== 'admin') return { error: { _form: ['Apenas admin pode criar estágios'] } }

      // Calcular próxima position
      const { data: maxRow } = await supabase
        .from('pipeline_stages')
        .select('position')
        .is('deleted_at', null)
        .order('position', { ascending: false })
        .limit(1)
        .maybeSingle()
      const nextPosition = (maxRow?.position ?? 0) + 1

      const { data, error } = await supabase
        .from('pipeline_stages')
        .insert({
          tenant_id: tenantId,
          name: parsed.data.name,
          color: parsed.data.color,
          is_closed: parsed.data.is_closed,
          position: nextPosition,
        })
        .select('id')
        .single()

      if (error) return { error: { _form: ['Erro ao criar estágio'] } }
      revalidatePath(`/${slug}/configuracoes/pipeline`)
      return { id: data.id }
    }

    export async function deleteStage(
      slug: string,
      stageId: string,
    ): Promise<{ error?: Record<string, string[]>; relocated?: number }> {
      const { supabase, role } = await getAuthContext()
      if (role !== 'admin') return { error: { _form: ['Apenas admin pode remover estágios'] } }

      // Listar estágios não-deletados do tenant (RLS filtra por tenant)
      const { data: stages } = await supabase
        .from('pipeline_stages')
        .select('id, position')
        .is('deleted_at', null)
        .order('position')

      if (!stages || stages.length <= 1) {
        return { error: { _form: ['É necessário manter ao menos um estágio ativo'] } }
      }
      const target = stages.find((s) => s.id === stageId)
      if (!target) return { error: { _form: ['Estágio não encontrado'] } }
      const defaultStage = stages.find((s) => s.id !== stageId) // primeiro por order ASC de position
      if (!defaultStage) return { error: { _form: ['Sem estágio default disponível'] } }

      // Contar e realocar clientes
      const { count } = await supabase
        .from('clients')
        .select('id', { count: 'exact', head: true })
        .eq('stage_id', stageId)
        .is('deleted_at', null)

      if ((count ?? 0) > 0) {
        const { error: relocErr } = await supabase
          .from('clients')
          .update({ stage_id: defaultStage.id })
          .eq('stage_id', stageId)
          .is('deleted_at', null)
        if (relocErr) return { error: { _form: ['Erro ao realocar clientes'] } }
      }

      const { error: delErr } = await supabase
        .from('pipeline_stages')
        .update({ deleted_at: new Date().toISOString() })
        .eq('id', stageId)
      if (delErr) return { error: { _form: ['Erro ao remover estágio'] } }

      revalidatePath(`/${slug}/configuracoes/pipeline`)
      revalidatePath(`/${slug}/clientes`)
      return { relocated: count ?? 0 }
    }

    export async function updateClientStage(
      slug: string,
      input: { clientId: string; stageId: string },
    ): Promise<{ error?: Record<string, string[]>; ok?: true }> {
      const parsed = updateClientStageSchema.safeParse(input)
      if (!parsed.success) return { error: parsed.error.flatten().fieldErrors }

      const { supabase, user, role } = await getAuthContext()
      if (!user) return { error: { _form: ['Sessão expirada'] } }

      // Validar que stage pertence ao tenant (RLS de pipeline_stages bloqueia cross-tenant)
      const { data: stage } = await supabase
        .from('pipeline_stages')
        .select('id')
        .eq('id', parsed.data.stageId)
        .is('deleted_at', null)
        .maybeSingle()
      if (!stage) return { error: { _form: ['Estágio inválido'] } }

      // Defesa em profundidade: corretor só pode mudar stage de clientes próprios
      if (role === 'corretor') {
        const { data: own } = await supabase
          .from('clients')
          .select('id, assigned_to')
          .eq('id', parsed.data.clientId)
          .is('deleted_at', null)
          .maybeSingle()
        if (!own || own.assigned_to !== user.id) {
          return { error: { _form: ['Cliente não encontrado'] } }
        }
      }

      const { error, data } = await supabase
        .from('clients')
        .update({ stage_id: parsed.data.stageId })
        .eq('id', parsed.data.clientId)
        .is('deleted_at', null)
        .select('id')
        .maybeSingle()

      if (error || !data) return { error: { _form: ['Cliente não encontrado'] } }

      revalidatePath(`/${slug}/clientes`)
      revalidatePath(`/${slug}/clientes/${parsed.data.clientId}`)
      return { ok: true }
    }
    ```

    **3. Criar `tests/actions/pipeline.test.ts`** cobrindo pelo menos:
    - `createStage` valida name/color/is_closed
    - `createStage` rejeita role != admin
    - `createStage` usa MAX(position) + 1
    - `deleteStage` bloqueia quando há só 1 estágio
    - `deleteStage` realoca clientes para o primeiro não-deletado
    - `deleteStage` retorna `relocated` com count
    - `updateClientStage` valida UUIDs
    - `updateClientStage` rejeita stage de outro tenant (mock retorna null)
    - `updateClientStage` bloqueia corretor tentando mudar cliente de outro

    Mock pattern: reutilizar o padrão de `tests/actions/clients.test.ts` (Task 1 do Plan 02) para mockar `createClient` e `auth.getUser()`.

    **4. Atualizar `tests/actions/clients.test.ts`** — substituir os 3 `it.todo` da seção `updateClientStage (CRM-05)` por testes reais, ou simplesmente marcá-los como duplicados (prefira remover e deixar cobertura em `tests/actions/pipeline.test.ts`). Usar comentário:
    ```typescript
    // NOTE: updateClientStage coverage lives in tests/actions/pipeline.test.ts (Plan 04)
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/actions/pipeline.test.ts tests/actions/clients.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validations/pipeline-schemas.ts` existe e exporta `createStageSchema`, `updateClientStageSchema`
    - `src/lib/actions/pipeline.ts` existe com `'use server'`
    - grep `export async function createStage`, `deleteStage`, `updateClientStage` — 3 matches
    - grep `MAX\|maxRow?.position` confirma cálculo de next position
    - grep `realocação|defaultStage|stage_id.*defaultStage` confirma realocação de clientes
    - grep `corretor.*assigned_to !== user.id` ou similar confirma guard de corretor
    - `tests/actions/pipeline.test.ts` existe com pelo menos 9 `it(...)` (não `it.todo`)
    - `npx vitest run tests/actions/pipeline.test.ts` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    3 Server Actions funcionais com validação Zod, enforcement de role, realocação segura de clientes ao deletar estágio, e testes unitários cobrindo edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tela admin de pipeline + dropdown inline de estágio na tabela de clientes</name>
  <read_first>
    - src/lib/actions/pipeline.ts (Task 1)
    - src/components/ui/dialog.tsx, alert-dialog.tsx, dropdown-menu.tsx
    - src/components/auth/sidebar-shell.tsx (adicionar link)
    - src/app/(app)/[slug]/clientes/clients-table.tsx (Plan 03 — integrar seletor inline)
    - .planning/phases/02-crm-clientes/02-CONTEXT.md D-08, D-09
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Pitfall 3
  </read_first>
  <action>
    **1. Criar `src/app/(app)/[slug]/configuracoes/pipeline/page.tsx` (Server Component):**

    - Verificar role do user; se != 'admin', `notFound()` ou redirect para `/${slug}/dashboard`
    - Fetch estágios via `supabase.from('pipeline_stages').select('id, name, color, position, is_closed').is('deleted_at', null).order('position')`
    - Para cada estágio, fetch count de clientes: `supabase.from('clients').select('id', { count: 'exact', head: true }).eq('stage_id', stage.id).is('deleted_at', null)` — pode fazer em batch via RPC ou loop simples (4-6 estágios, 4-6 queries rápidas é aceitável)
    - Renderizar `<StagesManager stages={stages} />` passando `slug` e counts

    **2. Criar `src/app/(app)/[slug]/configuracoes/pipeline/stages-manager.tsx` (Client Component):**

    - `'use client'`
    - Props: `slug`, `stages: Array<{ id, name, color, position, is_closed, clientCount }>`
    - Lista ordenada de estágios (não drag-and-drop em v1) — cada item: swatch de cor (div com background-color inline), nome, position, badge "Fechado" se is_closed, count de clientes, botão remover (disabled se count > 0 sem confirmação — abre dialog)
    - Form "Adicionar estágio": input name, color picker (input type="color" nativo), checkbox is_closed, botão "Adicionar"
    - Submit → `createStage(slug, formData)`; em erro exibe field errors via sonner toast; em sucesso `router.refresh()`
    - Remover → abrir `<StageDeleteDialog stage={s} />` — se `clientCount > 0`, dialog exibe mensagem "X clientes serão movidos para {defaultStage.name}. Confirmar?"; confirmação chama `deleteStage(slug, stage.id)`

    **3. Criar `src/app/(app)/[slug]/configuracoes/pipeline/stage-delete-dialog.tsx`:**

    - `'use client'`
    - Props: `stage: { id, name, clientCount }`, `defaultStageName: string`, `slug: string`, `disabled?: boolean`
    - Usa `<AlertDialog>` do shadcn
    - Texto condicional:
      - `clientCount === 0` → "Tem certeza que deseja remover o estágio '{name}'?"
      - `clientCount > 0` → "O estágio '{name}' possui {clientCount} cliente(s). Eles serão movidos para '{defaultStageName}'. Deseja continuar?"
    - Botão Confirmar chama `deleteStage(slug, stage.id)` e exibe toast com count relocated; Cancelar fecha dialog

    **4. Criar `src/app/(app)/[slug]/clientes/client-stage-selector.tsx`:**

    - `'use client'`
    - Props: `slug: string`, `clientId: string`, `currentStage: { id, name, color } | null`, `stages: Array<{ id, name, color }>`, `canEdit: boolean`
    - Renderiza badge com cor atual; se `canEdit`, envolve em `<DropdownMenu>` (shadcn)
    - Ao clicar em uma opção do dropdown, chama `updateClientStage(slug, { clientId, stageId })` via useTransition para mostrar pending state
    - Se `canEdit === false`, renderiza apenas o badge (não dropdown)
    - Optimistic: ao clicar, imediatamente atualiza o state local; em erro, reverte e mostra toast

    **5. Integrar `ClientStageSelector` em `src/app/(app)/[slug]/clientes/clients-table.tsx` (do Plan 03):**

    Trocar o badge estático de stage por `<ClientStageSelector slug={slug} clientId={c.id} currentStage={c.stage} stages={stages} canEdit={...} />`.

    Para determinar `canEdit`: o Server Component `page.tsx` (Plan 03) deve passar `userRole` e `userId` para a tabela, e `canEdit = userRole === 'admin' || (userRole === 'corretor' && c.assigned_to?.id === userId)`.

    Isto implica em pequena alteração em `src/app/(app)/[slug]/clientes/page.tsx` do Plan 03 — adicionar passagem de `userRole`, `userId` e `stages` para `<ClientsTable>` e `<ClientsTable>` passa adiante para o seletor. DOCUMENTAR essa alteração no summary.

    **6. Atualizar `src/components/auth/sidebar-shell.tsx`** — adicionar child "Pipeline" ao item "Configurações":
    ```tsx
    {
      label: 'Configurações',
      href: `/${slug}/configuracoes`,
      icon: <Settings size={16} />,
      children: [
        { label: 'Usuários', href: `/${slug}/configuracoes/usuarios` },
        { label: 'Pipeline',  href: `/${slug}/configuracoes/pipeline` },
      ],
    }
    ```

    NOTA: apenas admins veem a tela funcional — o link fica visível para todos mas a página faz redirect/404 para não-admin (defesa em profundidade: RLS de pipeline_stages_admin_manage já bloqueia writes no banco).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "src/app/(app)/[slug]/configuracoes/pipeline/page.tsx" && test -f "src/app/(app)/[slug]/configuracoes/pipeline/stages-manager.tsx" && test -f "src/app/(app)/[slug]/clientes/client-stage-selector.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos de UI existem nos paths indicados
    - grep `'use client'` em stages-manager.tsx, stage-delete-dialog.tsx, client-stage-selector.tsx — 3 matches
    - grep `createStage` em stages-manager.tsx retorna match
    - grep `deleteStage` em stage-delete-dialog.tsx retorna match
    - grep `updateClientStage` em client-stage-selector.tsx retorna match
    - grep `clientCount > 0` em stage-delete-dialog.tsx ou similar confirma lógica condicional
    - grep `canEdit` em clients-table.tsx (modificado) retorna match
    - grep `'Pipeline'` em `src/components/auth/sidebar-shell.tsx` retorna match
    - grep `notFound|redirect.*dashboard` em `pipeline/page.tsx` confirma guard de role
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Admin gerencia estágios via tela dedicada; realocação segura ao deletar; corretor e admin podem mudar estágio inline na tabela de clientes (corretor só nos próprios). Estados loading/erro tratados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → updateClientStage | client-side pode enviar qualquer stageId/clientId |
| Server Action → Postgres | RLS clients_update + pipeline_stages_admin_manage são as linhas finais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Elevation of Privilege | Não-admin tentando createStage/deleteStage via curl direto ao Server Action | mitigate | Server Action faz `if (role !== 'admin') return error` ANTES de chamar DB; RLS `pipeline_stages_admin_manage` é defense-in-depth |
| T-02-22 | Tampering | Corretor tenta mudar stage de cliente de outro via form forjado | mitigate | Server Action `updateClientStage` tem guard explícito para role='corretor' confirmando assigned_to = auth.uid(); RLS `clients_update` bloqueia no banco |
| T-02-23 | Information Disclosure | updateClientStage com stageId de outro tenant revela existência | mitigate | Consulta `pipeline_stages` passa por RLS; stage de outro tenant retorna null → erro genérico "Estágio inválido" |
| T-02-24 | Denial of Service | deleteStage do único estágio ativo quebra CRM | mitigate | Guard `stages.length <= 1` retorna erro antes de deletar |
| T-02-25 | Tampering | Pipe de SQL injection via name/color | mitigate | Zod regex para color (`^#[0-9a-fA-F]{6}$`); name passa como parâmetro (não concatenado) |
| T-02-26 | Race Condition | Dois admins criando estágio simultâneo com mesma position | accept | UNIQUE index em (tenant_id, position) force rejection; segundo admin recebe erro e precisa retry. Aceitável para v1 (baixa probabilidade) |
</threat_model>

<verification>
1. `npx vitest run tests/actions/pipeline.test.ts tests/actions/clients.test.ts` → 0 failures
2. `npx tsc --noEmit` → exit 0
3. Manual smoke (admin): criar estágio "Teste" → aparece na lista; remover estágio com 2 clientes → dialog confirma; confirmar → clients agora têm stage default
4. Manual smoke (corretor): tentar acessar `/[slug]/configuracoes/pipeline` → 404
</verification>

<success_criteria>
- Admin gerencia estágios: criar (position = max+1), remover (realoca clientes para primeiro restante)
- Corretor/Visualizador/Financeiro não acessam a tela de configurações de pipeline
- Dropdown inline na tabela permite admin mover qualquer cliente e corretor apenas os próprios
- Testes cobrem RBAC, realocação e validações de schema
- Link "Pipeline" aparece em Configurações na sidebar
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-crm-clientes/02-04-SUMMARY.md` com arquivos criados, alterações em `clients-table.tsx`/`page.tsx` do Plan 03 (passagem de userRole/userId/stages), e qualquer decisão de discretion.
</output>
