---
phase: 02-crm-clientes
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/app/(app)/[slug]/clientes/page.tsx
  - src/app/(app)/[slug]/clientes/clients-search.tsx
  - src/app/(app)/[slug]/clientes/clients-filters.tsx
  - src/app/(app)/[slug]/clientes/clients-table.tsx
  - src/app/(app)/[slug]/clientes/clients-pagination.tsx
  - src/lib/utils/format-document.ts
  - tests/actions/clients.test.ts
autonomous: true
requirements:
  - CRM-08
  - CRM-09
must_haves:
  truths:
    - "GET /[slug]/clientes lista clientes paginados (25 por página) ordenados por created_at DESC"
    - "Busca digitada no topo (debounced 400ms) filtra por nome, CPF ou CNPJ simultaneamente"
    - "Busca por CPF com máscara '111.444.777-35' encontra cliente com document='11144477735'"
    - "Filtros Corretor, Estágio, Tipo visíveis acima da tabela (sem drawer)"
    - "Aplicar filtro reseta page=1 (Pitfall 4)"
    - "Tabela tem colunas: Nome, Tipo (badge PF/PJ), Documento (formatado), Corretor, Estágio (badge colorido), Data"
    - "Corretor autenticado vê APENAS seus clientes; admin/financeiro/visualizador veem todos do tenant (via RLS)"
    - "URL reflete filtros via searchParams (?q=, ?corretor=, ?stage=, ?type=, ?page=)"
    - "Paginação exibe total de resultados e permite navegar até última página"
    - "Estado vazio (zero clientes) exibe call-to-action 'Cadastrar primeiro cliente' → /novo"
  artifacts:
    - path: "src/app/(app)/[slug]/clientes/page.tsx"
      provides: "Listagem (Server Component) com fetch filtrado + paginação"
    - path: "src/app/(app)/[slug]/clientes/clients-search.tsx"
      provides: "Input de busca debounced com URL state"
    - path: "src/app/(app)/[slug]/clientes/clients-filters.tsx"
      provides: "3 dropdowns (Corretor/Estágio/Tipo) + contador de filtros ativos"
    - path: "src/app/(app)/[slug]/clientes/clients-table.tsx"
      provides: "Tabela shadcn com colunas + link para detalhes"
    - path: "src/app/(app)/[slug]/clientes/clients-pagination.tsx"
      provides: "Navegação de páginas com preservação de searchParams"
    - path: "src/lib/utils/format-document.ts"
      provides: "formatDocument(document: string, type: 'pf'|'pj')"
      exports: ["formatDocument"]
  key_links:
    - from: "page.tsx"
      to: "supabase.from('clients').select(...).or(ilike name OR document)"
      via: "Server Component fetch"
      pattern: "\\.or\\(.*ilike"
    - from: "clients-search.tsx"
      to: "router.replace(?q=...)"
      via: "debounced URL update"
      pattern: "router\\.replace"
    - from: "clients-filters.tsx"
      to: "page=1"
      via: "reset on filter change"
      pattern: "page.*1"
---

<objective>
Entregar CRM-08 (busca por nome/CPF/CNPJ) e CRM-09 (filtros por corretor/estágio/tipo): página de listagem em `/[slug]/clientes` com busca debounced, 3 dropdowns de filtros inline, tabela paginada e RLS-safe (corretor vê só os próprios).

Purpose: Implementa D-05, D-06, D-07. É o hub principal do CRM — onde o usuário consome todos os clientes cadastrados.
Output: 1 página server component + 4 client components (search, filters, table, pagination) + 1 utilitário de formatação + 3 testes reais para searchClients e filtros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-crm-clientes/02-CONTEXT.md
@.planning/phases/02-crm-clientes/02-RESEARCH.md
@.planning/phases/02-crm-clientes/02-VALIDATION.md
@src/components/ui/table.tsx
@src/components/ui/select.tsx
@src/components/ui/input.tsx
@src/components/ui/badge.tsx
@src/components/ui/button.tsx
@src/lib/supabase/server.ts
@src/lib/validations/cpf.ts
@src/lib/validations/cnpj.ts

<interfaces>
Supabase client (SSR):
```typescript
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()
const { data, count } = await supabase.from('clients')
  .select('id, name, type, document, created_at, assigned_to:profiles!clients_assigned_to_fkey(id, full_name), stage:pipeline_stages(id, name, color)', { count: 'exact' })
  .is('deleted_at', null)
  .order('created_at', { ascending: false })
  .range(offset, offset + pageSize - 1)
```

Next.js App Router searchParams (Next.js 15 — Promise):
```typescript
interface Props {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ q?: string; page?: string; corretor?: string; stage?: string; type?: string }>
}
```

URL state pattern:
```typescript
'use client'
import { useRouter, useSearchParams, usePathname } from 'next/navigation'
const params = new URLSearchParams(searchParams.toString())
params.set('q', value); params.set('page', '1')
router.replace(`${pathname}?${params.toString()}`)
```

Strip helpers (Plan 01 output):
```typescript
import { stripCPF, formatCPF } from '@/lib/validations/cpf'
import { stripCNPJ, formatCNPJ } from '@/lib/validations/cnpj'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utilitário formatDocument + Server Component listagem</name>
  <read_first>
    - src/lib/validations/cpf.ts, cnpj.ts (formatCPF/formatCNPJ)
    - src/app/(app)/[slug]/dashboard/page.tsx (padrão Server Component + await params)
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Server Component de Listagem
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Pitfall 2 (busca por documento com máscara)
  </read_first>
  <action>
    **1. Criar `src/lib/utils/format-document.ts`:**

    ```typescript
    import { formatCPF } from '@/lib/validations/cpf'
    import { formatCNPJ } from '@/lib/validations/cnpj'

    /**
     * Formats a 11-digit CPF or 14-digit CNPJ stored without mask back to display form.
     * Falls back to raw input if length doesn't match expected type.
     */
    export function formatDocument(document: string, type: 'pf' | 'pj'): string {
      const digits = (document ?? '').replace(/\D/g, '')
      if (type === 'pf' && digits.length === 11) return formatCPF(digits)
      if (type === 'pj' && digits.length === 14) return formatCNPJ(digits)
      return document ?? ''
    }
    ```

    **2. Criar `src/app/(app)/[slug]/clientes/page.tsx` (Server Component):**

    ```typescript
    import Link from 'next/link'
    import { notFound } from 'next/navigation'
    import { createClient } from '@/lib/supabase/server'
    import { Button } from '@/components/ui/button'
    import { ClientsSearch } from './clients-search'
    import { ClientsFilters } from './clients-filters'
    import { ClientsTable } from './clients-table'
    import { ClientsPagination } from './clients-pagination'

    const PAGE_SIZE = 25 // Claude discretion — see RESEARCH Open Question 2

    interface Props {
      params: Promise<{ slug: string }>
      searchParams: Promise<{
        q?: string
        page?: string
        corretor?: string
        stage?: string
        type?: string
      }>
    }

    export default async function ClientesPage({ params, searchParams }: Props) {
      const { slug } = await params
      const sp = await searchParams
      const supabase = await createClient()

      const { data: { user } } = await supabase.auth.getUser()
      if (!user) notFound()

      const pageNum = Math.max(1, parseInt(sp.page ?? '1', 10))
      const offset = (pageNum - 1) * PAGE_SIZE

      // Carregar corretores e stages para os filtros (RLS limita ao tenant)
      const [corretoresRes, stagesRes] = await Promise.all([
        supabase.from('profiles')
          .select('id, full_name')
          .in('role', ['admin', 'corretor'])
          .eq('active', true)
          .is('deleted_at', null)
          .order('full_name'),
        supabase.from('pipeline_stages')
          .select('id, name, color, position')
          .is('deleted_at', null)
          .order('position'),
      ])

      // Query de clientes com filtros aplicados
      let query = supabase
        .from('clients')
        .select(
          `id, name, type, document, created_at,
           assigned_to:profiles!clients_assigned_to_fkey(id, full_name),
           stage:pipeline_stages(id, name, color)`,
          { count: 'exact' },
        )
        .is('deleted_at', null)
        .order('created_at', { ascending: false })
        .range(offset, offset + PAGE_SIZE - 1)

      if (sp.q) {
        const stripped = sp.q.replace(/\D/g, '')
        if (stripped.length >= 3) {
          query = query.or(`name.ilike.%${sp.q}%,document.ilike.%${stripped}%`)
        } else {
          query = query.ilike('name', `%${sp.q}%`)
        }
      }
      if (sp.corretor) query = query.eq('assigned_to', sp.corretor)
      if (sp.stage)    query = query.eq('stage_id', sp.stage)
      if (sp.type && (sp.type === 'pf' || sp.type === 'pj')) query = query.eq('type', sp.type)

      const { data: clients, count } = await query

      const totalPages = Math.max(1, Math.ceil((count ?? 0) / PAGE_SIZE))

      return (
        <div className="mx-auto max-w-7xl space-y-4 p-6">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-semibold">Clientes</h1>
              <p className="text-sm text-muted-foreground">{count ?? 0} cliente(s) encontrado(s)</p>
            </div>
            <Button asChild>
              <Link href={`/${slug}/clientes/novo`}>+ Novo cliente</Link>
            </Button>
          </div>

          <ClientsSearch />

          <ClientsFilters
            corretores={corretoresRes.data ?? []}
            stages={stagesRes.data ?? []}
          />

          {count === 0 && !sp.q && !sp.corretor && !sp.stage && !sp.type ? (
            <div className="flex flex-col items-center gap-3 rounded-lg border border-dashed p-12">
              <p className="text-muted-foreground">Nenhum cliente cadastrado ainda.</p>
              <Button asChild>
                <Link href={`/${slug}/clientes/novo`}>Cadastrar primeiro cliente</Link>
              </Button>
            </div>
          ) : (
            <>
              <ClientsTable slug={slug} clients={clients ?? []} />
              <ClientsPagination page={pageNum} totalPages={totalPages} />
            </>
          )}
        </div>
      )
    }
    ```

    A query condicional para `q`: se `stripped.length >= 3` usa OR ilike em name + document; caso contrário (usuário digitou só letras) usa apenas name. Isto evita matches falsos em `document` quando o input não parece ser número.

    IMPORTANTE: o uso de `OR` com `ilike` em Supabase JS tem caveat — valores com vírgula ou parêntese quebram a sintaxe. Como CPF/CNPJ stripped é puramente dígito e nome humano raramente tem vírgula, aceitamos o risco; para v1 é suficiente. Se necessário escape: `encodeURIComponent` do valor antes do `.or()`.
  </action>
  <verify>
    <automated>test -f "src/app/(app)/[slug]/clientes/page.tsx" && test -f src/lib/utils/format-document.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/format-document.ts` existe com `export function formatDocument`
    - grep `formatCPF` e `formatCNPJ` em `format-document.ts` — 2 matches
    - `src/app/(app)/[slug]/clientes/page.tsx` existe
    - grep `range\(offset, offset \+ PAGE_SIZE - 1\)` confirma paginação
    - grep `.or\(`name.ilike.` em `page.tsx` confirma busca multi-campo (escape dos backticks ignorado pelo grep; pode usar `grep -F "name.ilike"`)
    - grep `sp.corretor.*\.eq\('assigned_to'` confirma filtro corretor
    - grep `sp.stage.*\.eq\('stage_id'` confirma filtro stage
    - grep `PAGE_SIZE = 25` confirma tamanho de página escolhido
    - grep `count: 'exact'` confirma count para paginação
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Página de listagem renderiza clientes do tenant via RLS, aplica busca e filtros via searchParams, exibe estado vazio quando não há clientes, paginação funcional com 25 itens/página.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client Components de search, filters, table, pagination + testes</name>
  <read_first>
    - src/components/ui/table.tsx
    - src/components/ui/select.tsx
    - src/components/ui/input.tsx
    - src/components/ui/badge.tsx
    - tests/actions/clients.test.ts (converter it.todo de searchClients e filters)
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Busca Debounced
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Pitfall 4 (reset page=1 no filtro)
  </read_first>
  <behavior>
    - ClientsSearch: digitar no input dispara router.replace após 400ms, setando `?q=<value>&page=1` — valor vazio remove `?q=`
    - ClientsFilters: trocar qualquer Select atualiza searchParams e reseta `page=1`; badge "N filtros ativos" aparece quando > 0
    - ClientsTable: cada row é `<Link href={/[slug]/clientes/[id]}>`; badge type (PF azul-outline / PJ violet-outline); badge stage com `background-color: stage.color` inline style; documento formatado via formatDocument
    - ClientsPagination: botões "Anterior" / "Próximo" desabilitam nas bordas; exibe "Página X de Y"; preserva todos os outros searchParams ao mudar page
    - Helper searchClients: normalize(query) retorna o que é passado para ilike — strip de dígitos se input parece número, senão literal
  </behavior>
  <action>
    **1. `src/app/(app)/[slug]/clientes/clients-search.tsx`** (Client Component):

    - `'use client'`
    - Usar `useRouter`, `useSearchParams`, `usePathname` de `next/navigation`
    - Helper `debounce(fn, 400)` inline (conforme RESEARCH §Busca Debounced — NÃO adicionar `use-debounce` como dependência nova)
    - `defaultValue={searchParams.get('q') ?? ''}`
    - onChange atualiza URL: `params.set('q', value)` ou `params.delete('q')`; sempre `params.set('page','1')`; `router.replace(\`${pathname}?${params.toString()}\`)`
    - Placeholder: "Buscar por nome, CPF ou CNPJ…"

    **2. `src/app/(app)/[slug]/clientes/clients-filters.tsx`:**

    - Props: `corretores: { id: string; full_name: string | null }[]`, `stages: { id: string; name: string; color: string; position: number }[]`
    - 3 selects shadcn: Corretor, Estágio, Tipo (PF/PJ)
    - Cada um inclui opção "Todos" (value=""); valor "Todos" → `params.delete(key)`
    - Ao mudar: `params.set('page','1')`; `router.replace(...)`
    - Botão "Limpar filtros" aparece quando algum filtro ativo; limpa todos os filtros E `q`
    - Badge "N filtros ativos" ao lado do botão Limpar (contagem ignora `q` e `page`)

    **3. `src/app/(app)/[slug]/clientes/clients-table.tsx`:**

    - Props: `slug: string`, `clients: Array<{ id, name, type: 'pf'|'pj', document, created_at, assigned_to: {id, full_name}|null, stage: {id, name, color}|null }>`
    - Renderiza `<Table>` shadcn com header: Nome | Tipo | Documento | Corretor | Estágio | Cadastrado em
    - Cada linha: `<Link href={\`/${slug}/clientes/${c.id}\`}>`  — hover visual
    - Badge Tipo: `<Badge variant={c.type === 'pf' ? 'secondary' : 'outline'}>{c.type === 'pf' ? 'PF' : 'PJ'}</Badge>`
    - Documento: `formatDocument(c.document, c.type)`
    - Badge Estágio: `<Badge style={{ backgroundColor: c.stage?.color, color: '#fff', border: 'none' }}>{c.stage?.name ?? '—'}</Badge>`
    - Data: `format(new Date(c.created_at), 'dd/MM/yyyy', { locale: ptBR })` — usar date-fns v4 (já instalado)
    - Estado vazio (clients empty + tem filtros ativos): "Nenhum cliente corresponde aos filtros."

    **4. `src/app/(app)/[slug]/clientes/clients-pagination.tsx`:**

    - Props: `page: number`, `totalPages: number`
    - Preserva searchParams ao mudar página
    - Botões Anterior/Próximo (shadcn Button variant="outline")
    - Label: "Página {page} de {totalPages}"
    - NÃO renderiza se `totalPages <= 1`

    **5. Atualizar `tests/actions/clients.test.ts`:**

    Substituir os `it.todo` de `listClients filters (CRM-09)` e `searchClients (CRM-08)` por testes unitários reais da função utilitária de construção de query. Para evitar testar Supabase SDK, extrair o helper pure de normalização de query:

    Criar `src/lib/utils/clients-query.ts`:
    ```typescript
    /**
     * Decides how to interpret a free-form search input.
     * If the input contains 3+ digits after stripping non-digits → search by name OR document (stripped).
     * Otherwise → search by name only (literal).
     */
    export function buildSearchClause(query: string): { type: 'or'; name: string; document: string } | { type: 'name'; name: string } {
      const stripped = query.replace(/\D/g, '')
      if (stripped.length >= 3) {
        return { type: 'or', name: query, document: stripped }
      }
      return { type: 'name', name: query }
    }
    ```

    E no `page.tsx`, substituir o bloco inline por `const clause = buildSearchClause(sp.q)`.

    Testar `buildSearchClause` em `tests/actions/clients.test.ts` na seção `searchClients (CRM-08)`:
    - Input "Maria" → `{ type: 'name', name: 'Maria' }`
    - Input "111.444.777-35" → `{ type: 'or', name: '111.444.777-35', document: '11144477735' }`
    - Input "11" → `{ type: 'name', name: '11' }` (menos que 3 dígitos)
    - Input "12.345.678/0001-90" → `{ type: 'or', ..., document: '12345678000190' }`

    Na seção `listClients filters (CRM-09)`, converter `it.todo('paginação reseta para page=1 ao aplicar filtro novo (Pitfall 4)')` em teste unitário de um helper `resetPageOnFilterChange(currentParams, filterKey, filterValue)`:

    ```typescript
    // src/lib/utils/clients-query.ts
    export function resetPageOnFilterChange(
      currentParams: URLSearchParams,
      filterKey: string,
      filterValue: string | null,
    ): URLSearchParams {
      const next = new URLSearchParams(currentParams)
      if (filterValue) next.set(filterKey, filterValue)
      else next.delete(filterKey)
      next.set('page', '1')
      return next
    }
    ```

    Usar este helper em `clients-filters.tsx` e `clients-search.tsx`.

    Manter outros `it.todo` (RBAC stage change — cobertura vem em Plan 04).
  </action>
  <verify>
    <automated>npx vitest run tests/actions/clients.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos client (`clients-search.tsx`, `clients-filters.tsx`, `clients-table.tsx`, `clients-pagination.tsx`) existem
    - Todos têm `'use client'` na primeira linha — grep `'use client'` em cada um
    - `src/lib/utils/clients-query.ts` existe e exporta `buildSearchClause` e `resetPageOnFilterChange`
    - grep `router.replace` em `clients-search.tsx` e `clients-filters.tsx` — matches em ambos
    - grep `page.*1` em `clients-search.tsx` confirma reset de paginação
    - grep `formatDocument` em `clients-table.tsx` retorna match
    - grep `locale: ptBR` em `clients-table.tsx` retorna match
    - `tests/actions/clients.test.ts` tem pelo menos 4 testes reais de `buildSearchClause` (grep `buildSearchClause`) e pelo menos 2 de `resetPageOnFilterChange`
    - `npx vitest run tests/actions/clients.test.ts` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Listagem funcional com busca debounced, 3 filtros inline, tabela paginada, estado vazio. CPF com máscara encontra cliente armazenado sem máscara. Helpers puros testados unitariamente. Filtros sempre resetam page=1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → Supabase query | `?q=`, `?corretor=`, `?stage=`, `?type=` chegam não confiáveis |
| Server render → RLS | Query passa por RLS — corretor não vê clientes de outro corretor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | Corretor acessa `?corretor=<outro_id>` tentando ver clientes de outros | mitigate | RLS policy `clients_select` já filtra por `assigned_to = (SELECT auth.uid())` para role corretor — mesmo que o filtro `?corretor=otherId` seja aplicado, o banco retorna 0 rows |
| T-02-18 | Tampering | SQL injection via `sp.q` no `.or()` | mitigate | Supabase JS client escapa valores como parâmetros; `ilike` usa padrão %value%. Validar length (>= 1 char) e reject casos absurdos (> 100 chars); no client, `sp.q.slice(0, 100)` antes de aplicar |
| T-02-19 | Information Disclosure | `?type=<malicious>` forçando comportamento inesperado | mitigate | Guard `sp.type && (sp.type === 'pf' \|\| sp.type === 'pj')` — ignora valores fora da whitelist |
| T-02-20 | DoS | Range com offset muito alto (`?page=999999`) | accept | RLS ainda filtra; Postgres trata eficientemente via offset+limit em índice. Opcional: cap `pageNum` em `Math.min(pageNum, totalPages)` — para v1, accept risk |
</threat_model>

<verification>
1. `npx vitest run tests/actions/clients.test.ts` → 0 failures (incluindo novos testes de buildSearchClause e resetPageOnFilterChange)
2. `npx tsc --noEmit` → exit 0
3. Manual smoke: `/[slug]/clientes` renderiza lista; digitar no input debounce 400ms; selecionar filtro reseta para page=1
</verification>

<success_criteria>
- Página `/[slug]/clientes` funcional com busca + 3 filtros + tabela + paginação
- Helpers puros `buildSearchClause` e `resetPageOnFilterChange` testados
- `formatDocument` renderiza CPF/CNPJ com máscara na tabela
- Estado vazio (sem filtros) convida a cadastrar; estado vazio (com filtros) informa que não há resultados
- URL reflete todos os filtros — bookmark e back/forward funcionam
- 25 itens por página, ordenado por created_at DESC
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-crm-clientes/02-03-SUMMARY.md` com arquivos criados, decisões de discretion (page size = 25, busca com 3+ dígitos ativa OR), e desvios.
</output>
