---
phase: 02-crm-clientes
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/lib/validations/client-schemas.ts
  - src/lib/actions/clients.ts
  - src/app/(app)/[slug]/clientes/novo/page.tsx
  - src/app/(app)/[slug]/clientes/novo/new-client-form.tsx
  - src/components/auth/sidebar-shell.tsx
  - tests/actions/clients.test.ts
autonomous: true
requirements:
  - CRM-01
  - CRM-02
must_haves:
  truths:
    - "Usuário autenticado vê link 'Clientes' na sidebar"
    - "Usuário acessa /[slug]/clientes/novo e vê formulário com toggle PF/PJ"
    - "Ao selecionar PF, campo document aparece como CPF (11 dígitos) com máscara"
    - "Ao selecionar PJ, campos document (CNPJ), razão social e responsável aparecem"
    - "Ao alternar PF↔PJ, erros e valores do campo document são limpos"
    - "Campo 'Corretor responsável' é um select obrigatório com corretores ativos do tenant"
    - "CPF inválido (all-same ou checksum errado) exibe erro antes de submit"
    - "Submit cria cliente e redireciona para listagem com toast de sucesso"
    - "Documento duplicado no mesmo tenant retorna erro 'Este documento já está cadastrado nesta corretora.'"
    - "Corretor que não seja admin só pode atribuir cliente a si mesmo"
  artifacts:
    - path: "src/lib/validations/client-schemas.ts"
      provides: "Zod discriminated union PF/PJ + tipos"
      exports: ["createClientSchema", "CreateClientInput"]
    - path: "src/lib/actions/clients.ts"
      provides: "createClientAction Server Action"
      exports: ["createClientAction"]
    - path: "src/app/(app)/[slug]/clientes/novo/page.tsx"
      provides: "Rota /[slug]/clientes/novo — Server Component que carrega corretores e renderiza form"
    - path: "src/app/(app)/[slug]/clientes/novo/new-client-form.tsx"
      provides: "Client Component com toggle PF/PJ + RHF + Zod"
  key_links:
    - from: "new-client-form.tsx"
      to: "createClientAction"
      via: "form action (Server Action)"
      pattern: "createClientAction"
    - from: "createClientAction"
      to: "public.clients INSERT"
      via: "supabase.from('clients').insert"
      pattern: "from\\('clients'\\)\\.insert"
    - from: "sidebar-shell.tsx"
      to: "/[slug]/clientes"
      via: "nav link"
      pattern: "/clientes"
---

<objective>
Entregar CRM-01 (cadastro PF) e CRM-02 (cadastro PJ): formulário único em `/[slug]/clientes/novo` com toggle PF/PJ, validação CPF/CNPJ, campo obrigatório "Corretor responsável", persistência via Server Action com erro de duplicidade amigável, e link "Clientes" na sidebar.

Purpose: Implementa D-01 a D-04 do CONTEXT.md. Sem este plano, não há como cadastrar cliente — é a porta de entrada do CRM.
Output: 1 schema Zod, 1 Server Action, 1 rota server component, 1 client component de formulário, atualização da sidebar, e transformação dos `it.todo` do arquivo `tests/actions/clients.test.ts` em testes reais para as partes implementadas aqui (createClientAction PF/PJ).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-crm-clientes/02-CONTEXT.md
@.planning/phases/02-crm-clientes/02-RESEARCH.md
@.planning/phases/02-crm-clientes/02-VALIDATION.md
@src/lib/validations/cnpj.ts
@src/lib/validations/auth-schemas.ts
@src/lib/actions/auth.ts
@src/lib/actions/invites.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/components/auth/sidebar-shell.tsx

<interfaces>
Server Action pattern (src/lib/actions/auth.ts):
```typescript
export async function registerTenant(formData: FormData): Promise<{ error?: FormError } | void>
// Zod safeParse → return { error: parsed.error.flatten().fieldErrors } on fail
// Map DB error codes: 23505 → friendly duplicate message
// revalidatePath on success (not redirect — client handles navigation)
```

Supabase clients:
```typescript
import { createClient } from '@/lib/supabase/server'       // SSR cookies
import { createAdminClient } from '@/lib/supabase/admin'   // service_role — Server Actions only
```

Validations:
```typescript
import { validateCPF, stripCPF, formatCPF } from '@/lib/validations/cpf'       // from Plan 01
import { validateCNPJ, stripCNPJ, formatCNPJ } from '@/lib/validations/cnpj'   // existing
```

Profiles schema (para carregar corretores):
```sql
public.profiles: id, tenant_id, full_name, role, active, deleted_at
-- corretores ativos: role='corretor' AND active=true AND deleted_at IS NULL
-- (admin também pode ser atribuído como assigned_to)
```

Sidebar nav item pattern (src/components/auth/sidebar-shell.tsx):
```typescript
interface NavItem { label: string; href: string; icon: React.ReactNode; children?: { label: string; href: string }[] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar schema Zod + Server Action createClientAction</name>
  <read_first>
    - src/lib/validations/auth-schemas.ts (padrão de schemas Zod)
    - src/lib/actions/auth.ts (padrão FormError + safeParse + 23505 handling)
    - src/lib/actions/invites.ts (padrão adicional Server Action)
    - src/lib/validations/cnpj.ts
    - src/lib/validations/cpf.ts (from Plan 01)
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Server Action: Criar Cliente
    - .planning/phases/02-crm-clientes/02-CONTEXT.md D-01..D-04
    - tests/actions/clients.test.ts (substituir it.todo por testes reais para PF/PJ create)
  </read_first>
  <behavior>
    - createClientAction recebe FormData com type='pf' + document + name + assigned_to → INSERT sucesso, document sem máscara
    - createClientAction recebe FormData com type='pj' + document + name + assigned_to (+ responsible opcional) → INSERT sucesso
    - CPF inválido (ex: "111.111.111-11") → `{ error: { document: ['CPF inválido'] } }`
    - CNPJ inválido → `{ error: { document: ['CNPJ inválido'] } }`
    - name vazio → `{ error: { name: ['Nome obrigatório'] } }` (PF) ou `{ error: { name: ['Razão social obrigatória'] } }` (PJ)
    - assigned_to vazio ou não-UUID → `{ error: { assigned_to: ['Corretor responsável obrigatório'] } }`
    - Duplicata no tenant (erro PG 23505) → `{ error: { document: ['Este documento já está cadastrado nesta corretora.'] } }`
    - type inválido (ex: 'xx') → `{ error: { type: [...] } }`
    - email fornecido mas inválido → `{ error: { email: [...] } }`
    - email vazio ('') → aceito (opcional)
  </behavior>
  <action>
    **1. Criar `src/lib/validations/client-schemas.ts`:**

    ```typescript
    import { z } from 'zod'
    import { validateCPF } from './cpf'
    import { validateCNPJ } from './cnpj'

    const baseFields = {
      assigned_to: z.string().uuid('Corretor responsável obrigatório'),
      email: z.string().email('E-mail inválido').optional().or(z.literal('')),
      phone: z.string().optional().or(z.literal('')),
    }

    export const createClientSchema = z.discriminatedUnion('type', [
      z.object({
        type: z.literal('pf'),
        document: z.string().refine((v) => validateCPF(v), 'CPF inválido'),
        name: z.string().min(2, 'Nome obrigatório'),
        ...baseFields,
      }),
      z.object({
        type: z.literal('pj'),
        document: z.string().refine((v) => validateCNPJ(v), 'CNPJ inválido'),
        name: z.string().min(2, 'Razão social obrigatória'),
        responsible: z.string().optional().or(z.literal('')),
        ...baseFields,
      }),
    ])

    export type CreateClientInput = z.infer<typeof createClientSchema>
    export type ClientFormError = Record<string, string[]>
    ```

    **2. Criar `src/lib/actions/clients.ts`:**

    ```typescript
    'use server'
    import { revalidatePath } from 'next/cache'
    import { createClient } from '@/lib/supabase/server'
    import { stripCPF } from '@/lib/validations/cpf'
    import { stripCNPJ } from '@/lib/validations/cnpj'
    import { createClientSchema, type ClientFormError } from '@/lib/validations/client-schemas'

    export async function createClientAction(
      slug: string,
      formData: FormData,
    ): Promise<{ error?: ClientFormError; id?: string } | void> {
      const raw = Object.fromEntries(formData) as Record<string, unknown>
      const parsed = createClientSchema.safeParse(raw)
      if (!parsed.success) {
        return { error: parsed.error.flatten().fieldErrors as ClientFormError }
      }

      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) {
        return { error: { _form: ['Sessão expirada. Faça login novamente.'] } }
      }

      // Enforce: corretor só pode atribuir cliente a si mesmo (defesa em profundidade; RLS já bloqueia no banco)
      const role = (user.app_metadata as { role?: string })?.role
      if (role === 'corretor' && parsed.data.assigned_to !== user.id) {
        return { error: { assigned_to: ['Corretor só pode atribuir cliente a si mesmo.'] } }
      }

      const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
      if (!tenantId) {
        return { error: { _form: ['Tenant não identificado na sessão.'] } }
      }

      const strip = parsed.data.type === 'pf' ? stripCPF : stripCNPJ
      const base = {
        tenant_id: tenantId,
        type: parsed.data.type,
        document: strip(parsed.data.document),
        name: parsed.data.name,
        email: parsed.data.email || null,
        phone: parsed.data.phone || null,
        assigned_to: parsed.data.assigned_to,
      }
      const insertRow = parsed.data.type === 'pj'
        ? { ...base, responsible: parsed.data.responsible || null }
        : base

      const { data, error } = await supabase
        .from('clients')
        .insert(insertRow)
        .select('id')
        .single()

      if (error) {
        const code = (error as { code?: string }).code
        if (code === '23505') {
          return { error: { document: ['Este documento já está cadastrado nesta corretora.'] } }
        }
        return { error: { _form: ['Erro ao salvar cliente. Tente novamente.'] } }
      }

      revalidatePath(`/${slug}/clientes`)
      return { id: data.id }
    }
    ```

    **3. Atualizar `tests/actions/clients.test.ts`** — substituir os `it.todo` das seções `createClientAction (PF)` e `createClientAction (PJ)` por testes reais usando mocks do Supabase.

    Os testes devem:
    - Mockar `createClient` de `@/lib/supabase/server` (reutilizar padrão de `tests/__mocks__/` se existir; caso contrário, mockar inline via `vi.mock`)
    - Mockar `supabase.auth.getUser()` retornando `{ data: { user: { id: 'user-1', app_metadata: { tenant_id: 't1', role: 'admin' } } } }`
    - Mockar `supabase.from('clients').insert(...).select('id').single()` para retornar sucesso ou erro 23505
    - Assert `parsed.error.fieldErrors` para inputs inválidos
    - Manter todos os demais `it.todo` (updateClientStage, listClients, searchClients) — eles serão convertidos em outros plans

    NÃO deletar os `it.todo` de outras seções — apenas converter PF e PJ.
  </action>
  <verify>
    <automated>npx vitest run tests/actions/clients.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validations/client-schemas.ts` existe e exporta `createClientSchema` e tipos
    - grep `z.discriminatedUnion\('type'` confirma union PF/PJ
    - grep `validateCPF` e `validateCNPJ` importados em `client-schemas.ts`
    - `src/lib/actions/clients.ts` existe com `'use server'` na primeira linha
    - grep `export async function createClientAction` retorna 1 match
    - grep `revalidatePath` em `src/lib/actions/clients.ts` retorna 1 match
    - grep `'23505'` em `src/lib/actions/clients.ts` confirma tratamento de duplicata
    - grep `corretor só pode atribuir` em `src/lib/actions/clients.ts` retorna match
    - `tests/actions/clients.test.ts` tem pelo menos 8 casos reais para createClientAction (grep `await createClientAction`)
    - `npx vitest run tests/actions/clients.test.ts` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Server Action createClientAction funciona para PF e PJ, valida via Zod+validateCPF/CNPJ, trata duplicata PG 23505 com mensagem genérica (T-02-05), bloqueia corretor atribuindo a outro, e revalida `/[slug]/clientes`. Testes unitários verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rota /[slug]/clientes/novo + Client Form com toggle PF/PJ</name>
  <read_first>
    - src/app/(app)/[slug]/dashboard/page.tsx (padrão Server Component + params)
    - src/components/ui/form.tsx, input.tsx, select.tsx, button.tsx, dialog.tsx
    - src/lib/supabase/server.ts
    - .planning/phases/02-crm-clientes/02-CONTEXT.md D-01..D-04
    - .planning/phases/02-crm-clientes/02-RESEARCH.md §Pitfall 5 (toggle limpar validações)
    - src/lib/validations/client-schemas.ts (criado Task 1)
    - src/lib/actions/clients.ts (criado Task 1)
  </read_first>
  <behavior>
    - Rota `/[slug]/clientes/novo` retorna 200 quando autenticado
    - Form tem toggle `<Tabs>` com valores "pf" e "pj" — trocar limpa `document`, `name`, `responsible` e errors
    - PF: campos exibidos = Nome completo, CPF (mask 000.000.000-00), Email, Telefone, Corretor responsável
    - PJ: campos exibidos = Razão social, CNPJ (mask 00.000.000/0000-00), Responsável, Email, Telefone, Corretor responsável
    - Campo "Corretor responsável" é select populado server-side com profiles do tenant onde role IN ('admin','corretor') AND active=true AND deleted_at IS NULL
    - Submit chama `createClientAction(slug, formData)` — em erro, exibe mensagem no campo; em sucesso, toast "Cliente cadastrado" + `router.push(/${slug}/clientes)`
    - Máscaras aplicadas em onChange (left-to-right fill, mesma lógica de CNPJ da Phase 1 — ver FIX do commit d367571)
  </behavior>
  <action>
    **1. Criar `src/app/(app)/[slug]/clientes/novo/page.tsx`** (Server Component):

    ```typescript
    import { notFound } from 'next/navigation'
    import { createClient } from '@/lib/supabase/server'
    import { NewClientForm } from './new-client-form'

    interface Props { params: Promise<{ slug: string }> }

    export default async function NovoClientePage({ params }: Props) {
      const { slug } = await params
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) notFound()

      // Carregar corretores disponíveis do tenant corrente
      const { data: corretores } = await supabase
        .from('profiles')
        .select('id, full_name, role')
        .in('role', ['admin', 'corretor'])
        .eq('active', true)
        .is('deleted_at', null)
        .order('full_name')

      const userRole = (user.app_metadata as { role?: string })?.role
      const defaultAssignedTo = userRole === 'corretor' ? user.id : (corretores?.[0]?.id ?? '')
      const corretorLockedToSelf = userRole === 'corretor'

      return (
        <div className="mx-auto max-w-2xl p-6">
          <h1 className="mb-6 text-2xl font-semibold">Novo Cliente</h1>
          <NewClientForm
            slug={slug}
            corretores={corretores ?? []}
            defaultAssignedTo={defaultAssignedTo}
            lockAssignedToSelf={corretorLockedToSelf}
          />
        </div>
      )
    }
    ```

    **2. Criar `src/app/(app)/[slug]/clientes/novo/new-client-form.tsx`** (Client Component) — deve:

    - Usar `'use client'`
    - Usar React Hook Form + `zodResolver(createClientSchema)`
    - Toggle PF/PJ via `<Tabs value={type} onValueChange={...}>` — no `onValueChange`:
      - `form.resetField('document')`
      - `form.resetField('name')`
      - `form.resetField('responsible')`
      - `form.clearErrors(['document','name','responsible'])`
      - `form.setValue('type', newValue)`
    - Campo `document` com máscara aplicada via `onChange` do Input:
      - PF: aplicar mask CPF `000.000.000-00` — lógica: `onChange = (e) => { const d = e.target.value.replace(/\D/g,'').slice(0,11); let out = d.slice(0,3); if(d.length>3) out += '.' + d.slice(3,6); if(d.length>6) out += '.' + d.slice(6,9); if(d.length>9) out += '-' + d.slice(9,11); field.onChange(out) }`
      - PJ: aplicar mask CNPJ `00.000.000/0000-00` — mesma lógica, 14 dígitos
    - Campo "Corretor responsável" = `<Select disabled={lockAssignedToSelf}>` com opções populadas por prop `corretores`
    - `onSubmit = async (data) => { const fd = new FormData(); Object.entries(data).forEach(([k,v]) => fd.append(k, String(v))); const res = await createClientAction(slug, fd); if (res?.error) { Object.entries(res.error).forEach(([field, msgs]) => form.setError(field as any, { message: msgs[0] })); toast.error(res.error._form?.[0] ?? 'Verifique os campos'); } else { toast.success('Cliente cadastrado'); router.push(`/${slug}/clientes`); } }`
    - Botão "Cancelar" volta para `/${slug}/clientes`

    Importar: `useRouter` de `next/navigation`, `toast` de `sonner`, `Tabs/TabsList/TabsTrigger` de `@/components/ui/tabs`, `Form/FormField/FormItem/FormLabel/FormControl/FormMessage` de `@/components/ui/form`, `Input`, `Select/SelectTrigger/SelectContent/SelectItem/SelectValue`, `Button`.

    **3. Adicionar nav item "Clientes" em `src/components/auth/sidebar-shell.tsx`:**

    Adicionar ao array `navItems` — entre Dashboard e Configurações:
    ```tsx
    {
      label: 'Clientes',
      href: `/${slug}/clientes`,
      icon: <Users size={16} />,
    },
    ```

    Renomear o ícone atual de "Usuários" (children de Configurações) para `UserCog size={14}` ou `UsersRound` para não duplicar. Importar de `lucide-react`.

    NÃO criar `/[slug]/clientes/page.tsx` aqui — essa é a listagem, Plan 03. Link funcionará 404 até Plan 03 ser implementado, mas isso não bloqueia Plan 02.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "src/app/(app)/[slug]/clientes/novo/page.tsx" && test -f "src/app/(app)/[slug]/clientes/novo/new-client-form.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/[slug]/clientes/novo/page.tsx` existe
    - `src/app/(app)/[slug]/clientes/novo/new-client-form.tsx` existe e tem `'use client'` na primeira linha
    - grep `'use client'` em `new-client-form.tsx` retorna match
    - grep `Tabs` e `TabsList` em `new-client-form.tsx` retorna match
    - grep `resetField\('document'\)` e `clearErrors` em `new-client-form.tsx` confirma Pitfall 5 mitigation
    - grep `createClientAction` em `new-client-form.tsx` retorna match
    - grep `zodResolver\(createClientSchema\)` em `new-client-form.tsx` retorna match
    - grep `Clientes` em `src/components/auth/sidebar-shell.tsx` retorna match
    - grep `href: \`/\${slug}/clientes\`` em `src/components/auth/sidebar-shell.tsx` retorna match
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Usuário autenticado clica em "Clientes" na sidebar (vai para 404 até Plan 03), e pode acessar `/[slug]/clientes/novo` diretamente. Form funciona com toggle PF/PJ, máscaras aplicadas, submit cria cliente. Corretor vê seu nome no select travado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → Server Action | FormData não confiável; type, document, name, assigned_to devem ser validados |
| Server Action → Postgres | supabase.from('clients').insert sofre RLS; tentativa de cross-tenant bloqueada |
| session JWT → tenant_id | `app_metadata.tenant_id` NUNCA vem do formulário; obtido via `auth.getUser()` e derivado no servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | Cliente submete `tenant_id` forjado no FormData | mitigate | Server Action ignora qualquer `tenant_id` do input; usa EXCLUSIVAMENTE `user.app_metadata.tenant_id` derivado do JWT |
| T-02-12 | Elevation of Privilege | Corretor atribui cliente a outro corretor via FormData | mitigate | Guard no Server Action: `if (role==='corretor' && assigned_to !== user.id) return error`. RLS `clients_insert` é defense-in-depth (bloqueia no banco) |
| T-02-13 | Tampering | CPF/CNPJ inválido bypassando validação client-side | mitigate | Zod `.refine(validateCPF/CNPJ)` no Server Action (executa no servidor) + CHECK constraints no banco |
| T-02-14 | Information Disclosure | Erro "documento já cadastrado" em outro tenant | accept | Constraint `UNIQUE(tenant_id, document)` é escopada ao tenant; colisão inter-tenant fisicamente impossível — mensagem sempre fala "nesta corretora" |
| T-02-15 | Tampering | XSS via campo `name` ou `responsible` | mitigate | React renderiza como texto (não dangerouslySetInnerHTML); JSON-B `address` nunca renderizado inline |
| T-02-16 | Info Disclosure | Select de corretores expondo usuários de outro tenant | mitigate | Query `profiles` no Server Component passa por RLS de profiles (já estabelecida Phase 1); só retorna do tenant corrente |
</threat_model>

<verification>
1. Rodar suite de actions: `npx vitest run tests/actions/clients.test.ts` → 0 failures
2. Type-check: `npx tsc --noEmit` → exit 0
3. Lint: `npm run lint` (se existir) → 0 warnings new
4. Manual smoke (post-execute): visitar `/[slug]/clientes/novo` autenticado → form renderiza; tentar CPF "000.000.000-00" → erro inline; cadastrar PJ válido → redireciona para `/[slug]/clientes` com toast
</verification>

<success_criteria>
- `src/lib/validations/client-schemas.ts` criado com discriminatedUnion PF/PJ
- `src/lib/actions/clients.ts` criado com `createClientAction` retornando `{ error?, id? }`
- Rota `/[slug]/clientes/novo` funcional (server + client components)
- Toggle PF/PJ limpa document/name/responsible e errors (Pitfall 5)
- Máscaras CPF/CNPJ aplicadas left-to-right (herda fix do commit d367571)
- Sidebar contém link "Clientes"
- Testes PF e PJ de createClientAction convertidos de it.todo para testes reais — passam
- Erro 23505 traduzido para mensagem amigável sem vazar info inter-tenant
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-crm-clientes/02-02-SUMMARY.md` com arquivos criados/modificados, qualquer desvio, e decisão sobre usar libs de máscara ou implementação inline.
</output>
