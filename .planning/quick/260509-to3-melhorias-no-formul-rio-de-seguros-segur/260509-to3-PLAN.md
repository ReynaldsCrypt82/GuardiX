---
phase: quick-260509-to3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/command.tsx
  - src/lib/constants/seguradoras.ts
  - src/lib/actions/clients-search.ts
  - src/app/(app)/[slug]/seguros/nova/page.tsx
  - src/app/(app)/[slug]/seguros/nova/policy-form.tsx
autonomous: true
requirements:
  - QUICK-260509-to3-M1  # Seguradora combobox estatico
  - QUICK-260509-to3-M2  # Cliente combobox server-side
  - QUICK-260509-to3-M3  # Marca/Modelo via FIPE API

must_haves:
  truths:
    - "Campo Seguradora exibe Combobox pesquisavel com 50 seguradoras brasileiras pre-cadastradas"
    - "Campo Cliente busca clientes do tenant em tempo real via Server Action ao digitar 2+ caracteres"
    - "Campos Marca e Modelo sao Comboboxes que carregam dados da FIPE API (parallelum.com.br)"
    - "Modelo fica desabilitado ate Marca ser selecionada; trocar Marca limpa Modelo"
    - "FormData enviado ao createPolicyAction contem 'insurer' (string), 'client_id' (UUID), 'marca_modelo' (string concatenada Marca+Modelo) — schema Zod existente nao precisa mudar"
    - "createPolicyAction continua aceitando o formulario sem alteracoes (prova: criar apolice 'auto' completa retorna sucesso)"
  artifacts:
    - path: "src/components/ui/command.tsx"
      provides: "shadcn Command component (cmdk wrapper)"
      contains: "export.*Command"
    - path: "src/lib/constants/seguradoras.ts"
      provides: "Lista estatica de 50 seguradoras BR"
      exports: ["SEGURADORAS"]
    - path: "src/lib/actions/clients-search.ts"
      provides: "Server Action de busca server-side de clientes por tenant"
      exports: ["searchClientsAction"]
    - path: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      provides: "Form com 3 comboboxes (seguradora, cliente, marca/modelo)"
      contains: "SEGURADORAS|searchClientsAction|fipe/api"
    - path: "src/app/(app)/[slug]/seguros/nova/page.tsx"
      provides: "Pagina sem prop 'clientes' (removida — busca passa a ser server-side)"
      contains: "PolicyForm"
  key_links:
    - from: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      to: "src/lib/constants/seguradoras.ts"
      via: "import { SEGURADORAS }"
      pattern: "from ['\"]@/lib/constants/seguradoras"
    - from: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      to: "src/lib/actions/clients-search.ts"
      via: "import { searchClientsAction }"
      pattern: "searchClientsAction\\("
    - from: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      to: "https://parallelum.com.br/fipe/api/v1/carros/marcas"
      via: "fetch em useEffect"
      pattern: "parallelum\\.com\\.br/fipe"
    - from: "src/app/(app)/[slug]/seguros/nova/policy-form.tsx"
      to: "src/lib/actions/policies.ts (createPolicyAction)"
      via: "FormData com insurer/client_id/marca_modelo (sem mudanca de contrato)"
      pattern: "fd\\.set\\(['\"](insurer|client_id|marca_modelo)['\"]"
---

<objective>
Tres melhorias de UX no formulario de cadastro de apolice (`/[slug]/seguros/nova`):
1. **Seguradora**: Input livre -> Combobox pesquisavel com 50 seguradoras BR (lista estatica)
2. **Cliente**: Select com lista pre-carregada -> Combobox com busca server-side (Server Action)
3. **Marca/Modelo**: Input livre -> dois Comboboxes encadeados consumindo a FIPE API (parallelum.com.br)

Purpose: Reduzir erros de digitacao (seguradora padronizada), eliminar lentidao em corretoras com muitos clientes (busca incremental ao inves de carregar tudo) e garantir veiculo valido (FIPE como fonte da verdade).

Output:
- Novo componente shadcn `Command` instalado
- Constante `SEGURADORAS` com lista de 50 cias
- Server Action `searchClientsAction(slug, query)`
- `policy-form.tsx` com 3 Comboboxes integrados ao FormData existente
- `page.tsx` sem prop `clientes` (busca passa a ser sob demanda)

**Constraint absoluto:** NAO modificar `src/lib/validations/policy-schemas.ts` nem `src/lib/actions/policies.ts`. O contrato do FormData (`insurer`, `client_id`, `marca_modelo` como strings) permanece identico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/app/(app)/[slug]/seguros/nova/policy-form.tsx
@src/app/(app)/[slug]/seguros/nova/page.tsx
@src/lib/validations/policy-schemas.ts
@src/lib/actions/policies.ts
@src/lib/supabase/server.ts
@package.json

<interfaces>
<!-- Contratos relevantes — usar diretamente, sem explorar codebase -->

# Server Action createPolicyAction (NAO MODIFICAR)
File: src/lib/actions/policies.ts

```typescript
// Recebe slug + FormData. FormData precisa conter (para type='auto'):
//   insurer (string min 2)
//   client_id (UUID)
//   marca_modelo (string min 2)
//   policy_number, vigencia_inicio, vigencia_fim, premio_total,
//   assigned_to, type='auto', placa, ano, valor_fipe, cobertura
// Retorna { id } ou { error: { campo: [msg] } }
export async function createPolicyAction(slug: string, formData: FormData)
```

# Schema Zod relevante (NAO MODIFICAR)
File: src/lib/validations/policy-schemas.ts

```typescript
// type='auto' valida marca_modelo: z.string().min(2)
// Todos os tipos validam:
//   insurer: z.string().min(2)
//   client_id: z.string().uuid()
```

# Supabase server client
File: src/lib/supabase/server.ts

```typescript
export async function createClient(): Promise<SupabaseClient>
// Cliente RLS-aware: respeita auth.uid() e tenant_id em policies
```

# Tabela `clients` (relevante para busca)
Colunas usadas: id (uuid), name (text), document (text), tenant_id (uuid), deleted_at (timestamptz nullable)
RLS ja garante isolamento por tenant_id, mas filtramos explicitamente para evitar surpresas.

# FIPE API (parallelum.com.br) — endpoints publicos, sem auth
GET https://parallelum.com.br/fipe/api/v1/carros/marcas
  -> [{ codigo: string, nome: string }, ...]   (~80 marcas)

GET https://parallelum.com.br/fipe/api/v1/carros/marcas/{codigo}/modelos
  -> { modelos: [{ codigo: number, nome: string }, ...], anos: [...] }

# shadcn Command (a ser adicionado)
File: src/components/ui/command.tsx (criado pela CLI)
Exports: Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandSeparator

# Padrao de uso Combobox (Popover + Command):
<Popover>
  <PopoverTrigger asChild><Button variant="outline" role="combobox">{label ?? 'Selecionar'}</Button></PopoverTrigger>
  <PopoverContent className="w-[--radix-popover-trigger-width] p-0">
    <Command>
      <CommandInput placeholder="Buscar..." onValueChange={...} />
      <CommandList>
        <CommandEmpty>Nada encontrado.</CommandEmpty>
        <CommandGroup>
          {items.map(i => <CommandItem key={i.id} onSelect={() => ...}>{i.label}</CommandItem>)}
        </CommandGroup>
      </CommandList>
    </Command>
  </PopoverContent>
</Popover>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar shadcn Command + criar SEGURADORAS + Server Action de busca</name>
  <files>
    package.json,
    src/components/ui/command.tsx,
    src/lib/constants/seguradoras.ts,
    src/lib/actions/clients-search.ts
  </files>
  <action>
**Passo 1.1 — Instalar componente Command (cmdk wrapper):**
Rodar `npx shadcn@latest add command --yes` na raiz do projeto. Isso:
- Adiciona dependencia `cmdk` ao package.json
- Cria `src/components/ui/command.tsx` com exports: Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandSeparator, CommandDialog, CommandShortcut

Se a CLI falhar (ex: prompt interativo), fallback manual:
1. `npm install cmdk`
2. Criar `src/components/ui/command.tsx` copiando do registry oficial: https://ui.shadcn.com/r/styles/new-york/command.json (ou use o conteudo padrao do shadcn — usa `cmdk` + `Dialog` ja existente em `@/components/ui/dialog` se houver, senao apenas o componente raw).

**Passo 1.2 — Criar `src/lib/constants/seguradoras.ts`:**
```typescript
export const SEGURADORAS = [
  'Porto Seguro',
  'SulAmérica',
  'Bradesco Seguros',
  'Itaú Seguros',
  'Mapfre',
  'Allianz Seguros',
  'Zurich Seguros',
  'HDI Seguros',
  'Liberty Seguros',
  'Tokio Marine',
  'AIG Seguros',
  'Chubb',
  'BB Seguridade',
  'Icatu Seguros',
  'Mongeral Aegon',
  'Sompo Seguros',
  'Generali Brasil',
  'AXA Seguros',
  'Capemisa Seguradora',
  'PAN Seguros',
  'Cardif',
  'Mitsui Sumitomo',
  'Azul Seguros',
  'Previsul',
  'Indiana Seguros',
  'Excelsior Seguros',
  'Nobre Seguradora',
  'Sancor Seguros',
  'Argo Seguros',
  'Berkley Brasil',
  'Markel Seguros',
  'QBE Seguros',
  'Austral Seguradora',
  'Junto Seguros',
  'Youse Seguros',
  'Kovr Seguros',
  'Porto Saúde',
  'SulAmérica Saúde',
  'Bradesco Saúde',
  'Amil',
  'Hapvida',
  'Notre Dame Intermédica',
  'Golden Cross',
  'Omint',
  'Prevent Senior',
  'São Francisco Saúde',
  'Seguros Unimed',
  'Unimed Nacional',
  'MetLife',
  'Prudential do Brasil',
] as const

export type Seguradora = (typeof SEGURADORAS)[number]
```

**Passo 1.3 — Criar `src/lib/actions/clients-search.ts`:**
```typescript
'use server'
import { createClient } from '@/lib/supabase/server'

// eslint-disable-next-line @typescript-eslint/no-explicit-any
type AnySupabase = any

export interface ClientSearchResult {
  id: string
  name: string
  document: string | null
}

export async function searchClientsAction(
  slug: string,
  query: string,
): Promise<ClientSearchResult[]> {
  const trimmed = query.trim()
  if (trimmed.length < 2) return []

  const supabase = (await createClient()) as AnySupabase
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return []

  const tenantId = (user.app_metadata as { tenant_id?: string })?.tenant_id
  if (!tenantId) return []

  // Escape % e _ para evitar wildcard injection no ilike
  const safe = trimmed.replace(/[%_]/g, (c: string) => `\\${c}`)

  const { data, error } = await supabase
    .from('clients')
    .select('id, name, document')
    .eq('tenant_id', tenantId)
    .ilike('name', `%${safe}%`)
    .is('deleted_at', null)
    .order('name')
    .limit(20)

  if (error || !data) return []
  return data as ClientSearchResult[]

  // Nota: parametro `slug` mantido na assinatura (compat com chamadas que ja
  // passam slug do contexto de tenant). RLS + tenant_id do JWT ja isolam o
  // tenant correto independente do slug.
}
```

**Passo 1.4 — Verificar:** rodar `npm run typecheck` (deve passar) e confirmar:
- `package.json` contem `"cmdk"` em dependencies
- `src/components/ui/command.tsx` existe e exporta `Command`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`
- `SEGURADORAS` tem exatamente 50 entradas
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
- `cmdk` instalado e listado em package.json
- `src/components/ui/command.tsx` existe com os 6 exports principais
- `SEGURADORAS` exportado com 50 strings (`grep -c "^  '" src/lib/constants/seguradoras.ts` retorna 50)
- `searchClientsAction` exportado, retorna `Promise<ClientSearchResult[]>`, faz fallback `[]` em qualquer erro (sem throw)
- `npm run typecheck` passa sem erros novos
  </done>
</task>

<task type="auto">
  <name>Task 2: Refatorar policy-form.tsx com 3 Comboboxes + atualizar page.tsx</name>
  <files>
    src/app/(app)/[slug]/seguros/nova/policy-form.tsx,
    src/app/(app)/[slug]/seguros/nova/page.tsx
  </files>
  <action>
**Passo 2.1 — Atualizar `src/app/(app)/[slug]/seguros/nova/page.tsx`:**

Remover query de `clientes` e remover prop `clientes` passada ao `<PolicyForm>` (busca passa a ser server-side via Server Action). Manter `corretores` (lista pequena, fixa).

```typescript
import { notFound } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { PolicyForm } from './policy-form'

interface Props {
  params: Promise<{ slug: string }>
}

export default async function NovaPolicePage({ params }: Props) {
  const { slug } = await params
  const supabase = await createClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) notFound()

  const role = (user.app_metadata as { role?: string })?.role ?? ''

  const { data: corretores } = await supabase
    .from('profiles')
    .select('id, full_name, role')
    .in('role', ['admin', 'corretor'])
    .eq('active', true)
    .is('deleted_at', null)
    .order('full_name')

  const defaultAssignedTo = role === 'corretor' ? user.id : ''
  const lockAssignedToSelf = role === 'corretor'

  return (
    <div className="mx-auto max-w-2xl space-y-6 p-6">
      <div>
        <h1 className="text-2xl font-semibold">Nova Apólice</h1>
        <p className="text-sm text-muted-foreground">Preencha os dados da apólice de seguro.</p>
      </div>
      <PolicyForm
        slug={slug}
        corretores={corretores ?? []}
        defaultAssignedTo={defaultAssignedTo}
        lockAssignedToSelf={lockAssignedToSelf}
      />
    </div>
  )
}
```

**Passo 2.2 — Refatorar `src/app/(app)/[slug]/seguros/nova/policy-form.tsx`:**

Adicionar imports:
```typescript
import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover'
import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from '@/components/ui/command'
import { Check, ChevronsUpDown } from 'lucide-react'
import { cn } from '@/lib/utils'
import { SEGURADORAS } from '@/lib/constants/seguradoras'
import { searchClientsAction, type ClientSearchResult } from '@/lib/actions/clients-search'
```

Mudancas na interface `Props`:
- Remover `clientes: Cliente[]`
- Remover interface `Cliente` (nao mais usada)

Remover do `FormValues`:
- `marca_modelo: string` (continua existindo no FormData mas via hidden input controlado)
- `insurer: string` (idem)

NOTA IMPORTANTE: manter os campos no FormValues nao quebra nada (RHF apenas nao os registra). Mas como vamos parar de chamar `register('insurer')` e `register('marca_modelo')`, e necessario REMOVER `{...register('insurer')}` e `{...register('marca_modelo')}` dos JSX (substituidos por hidden inputs controlados).

**Estados novos a adicionar (apos `acomodacao`):**
```typescript
// Seguradora (M1)
const [insurerOpen, setInsurerOpen] = useState(false)
const [insurer, setInsurer] = useState('')

// Cliente (M2) — substitui clientId existente
// (renomear: ja existe `clientId`. Adicionar clientName + clientQuery + clientResults)
const [clientName, setClientName] = useState('')
const [clientQuery, setClientQuery] = useState('')
const [clientResults, setClientResults] = useState<ClientSearchResult[]>([])
const [clientOpen, setClientOpen] = useState(false)
const [clientLoading, setClientLoading] = useState(false)

// FIPE Marca/Modelo (M3)
interface FipeMarca { codigo: string; nome: string }
interface FipeModelo { codigo: number; nome: string }
const [fipeMarcas, setFipeMarcas] = useState<FipeMarca[]>([])
const [fipeModelos, setFipeModelos] = useState<FipeModelo[]>([])
const [marcaSelecionada, setMarcaSelecionada] = useState<FipeMarca | null>(null)
const [modeloSelecionado, setModeloSelecionado] = useState<FipeModelo | null>(null)
const [loadingMarcas, setLoadingMarcas] = useState(false)
const [loadingModelos, setLoadingModelos] = useState(false)
const [marcaOpen, setMarcaOpen] = useState(false)
const [modeloOpen, setModeloOpen] = useState(false)
```

**Effect — debounced search de clientes:**
```typescript
useEffect(() => {
  if (clientQuery.trim().length < 2) {
    setClientResults([])
    return
  }
  setClientLoading(true)
  const handle = setTimeout(async () => {
    const results = await searchClientsAction(slug, clientQuery)
    setClientResults(results)
    setClientLoading(false)
  }, 250)
  return () => clearTimeout(handle)
}, [clientQuery, slug])
```

**Effect — carregar marcas FIPE quando type='auto':**
```typescript
useEffect(() => {
  if (selectedType !== 'auto') return
  if (fipeMarcas.length > 0) return // cache em memoria
  setLoadingMarcas(true)
  fetch('https://parallelum.com.br/fipe/api/v1/carros/marcas')
    .then((r) => (r.ok ? r.json() : []))
    .then((data: FipeMarca[]) => setFipeMarcas(Array.isArray(data) ? data : []))
    .catch(() => setFipeMarcas([]))
    .finally(() => setLoadingMarcas(false))
}, [selectedType, fipeMarcas.length])
```

**Effect — carregar modelos quando marca muda:**
```typescript
useEffect(() => {
  if (!marcaSelecionada) {
    setFipeModelos([])
    setModeloSelecionado(null)
    return
  }
  setLoadingModelos(true)
  setModeloSelecionado(null) // limpa modelo ao trocar marca
  fetch(
    `https://parallelum.com.br/fipe/api/v1/carros/marcas/${marcaSelecionada.codigo}/modelos`,
  )
    .then((r) => (r.ok ? r.json() : { modelos: [] }))
    .then((data: { modelos?: FipeModelo[] }) =>
      setFipeModelos(Array.isArray(data?.modelos) ? data.modelos : []),
    )
    .catch(() => setFipeModelos([]))
    .finally(() => setLoadingModelos(false))
}, [marcaSelecionada])
```

**Reset ao trocar tipo (atualizar effect existente):**
Adicionar dentro do `useEffect([selectedType])` existente:
```typescript
setMarcaSelecionada(null)
setModeloSelecionado(null)
setFipeModelos([])
```
(NAO limpar `insurer`, `clientId`, `clientName` — esses sao independentes do type.)

**Valor combinado marca_modelo:**
```typescript
const marcaModeloCombinado =
  marcaSelecionada && modeloSelecionado
    ? `${marcaSelecionada.nome} ${modeloSelecionado.nome}`
    : ''
```

**`onSubmit` — atualizar `fd.set` para incluir os novos controlled values:**
```typescript
fd.set('insurer', insurer)
fd.set('client_id', clientId)
fd.set('marca_modelo', marcaModeloCombinado)
// (manter os existentes: type, assigned_to, cobertura/tipo_imovel/acomodacao)
```

**JSX — substituir 3 campos:**

(a) Seguradora (substitui `<Input id="insurer" {...register('insurer')} />`):
```tsx
<div className="space-y-2">
  <Label>Seguradora <span className="text-destructive">*</span></Label>
  <Popover open={insurerOpen} onOpenChange={setInsurerOpen}>
    <PopoverTrigger asChild>
      <Button
        type="button"
        variant="outline"
        role="combobox"
        aria-expanded={insurerOpen}
        className="w-full justify-between font-normal"
      >
        {insurer || 'Selecione a seguradora'}
        <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
      </Button>
    </PopoverTrigger>
    <PopoverContent className="w-[--radix-popover-trigger-width] p-0" align="start">
      <Command>
        <CommandInput placeholder="Buscar seguradora..." />
        <CommandList>
          <CommandEmpty>Nenhuma seguradora encontrada.</CommandEmpty>
          <CommandGroup>
            {SEGURADORAS.map((s) => (
              <CommandItem
                key={s}
                value={s}
                onSelect={(v) => {
                  setInsurer(v)
                  setInsurerOpen(false)
                }}
              >
                <Check className={cn('mr-2 h-4 w-4', insurer === s ? 'opacity-100' : 'opacity-0')} />
                {s}
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </Command>
    </PopoverContent>
  </Popover>
</div>
```

(b) Cliente (substitui o `<Select>` de cliente):
```tsx
<div className="space-y-2">
  <Label>Cliente <span className="text-destructive">*</span></Label>
  <Popover open={clientOpen} onOpenChange={setClientOpen}>
    <PopoverTrigger asChild>
      <Button
        type="button"
        variant="outline"
        role="combobox"
        aria-expanded={clientOpen}
        className="w-full justify-between font-normal"
      >
        {clientName || 'Buscar cliente'}
        <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
      </Button>
    </PopoverTrigger>
    <PopoverContent className="w-[--radix-popover-trigger-width] p-0" align="start">
      <Command shouldFilter={false}>
        <CommandInput
          placeholder="Digite ao menos 2 letras..."
          value={clientQuery}
          onValueChange={setClientQuery}
        />
        <CommandList>
          <CommandEmpty>
            {clientLoading
              ? 'Buscando...'
              : clientQuery.trim().length < 2
                ? 'Digite ao menos 2 letras.'
                : 'Nenhum cliente encontrado.'}
          </CommandEmpty>
          <CommandGroup>
            {clientResults.map((c) => (
              <CommandItem
                key={c.id}
                value={c.id}
                onSelect={() => {
                  setClientId(c.id)
                  setClientName(c.name)
                  setClientOpen(false)
                }}
              >
                <Check className={cn('mr-2 h-4 w-4', clientId === c.id ? 'opacity-100' : 'opacity-0')} />
                <span>{c.name}{c.document ? ` — ${c.document}` : ''}</span>
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </Command>
    </PopoverContent>
  </Popover>
</div>
```

(c) Marca/Modelo (dentro do bloco `selectedType === 'auto'`, substituir o `<Input id="marca_modelo" ... />`):
```tsx
<div className="space-y-2">
  <Label>Marca <span className="text-destructive">*</span></Label>
  <Popover open={marcaOpen} onOpenChange={setMarcaOpen}>
    <PopoverTrigger asChild>
      <Button
        type="button"
        variant="outline"
        role="combobox"
        aria-expanded={marcaOpen}
        disabled={loadingMarcas}
        className="w-full justify-between font-normal"
      >
        {marcaSelecionada?.nome ?? (loadingMarcas ? 'Carregando...' : 'Selecione a marca')}
        <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
      </Button>
    </PopoverTrigger>
    <PopoverContent className="w-[--radix-popover-trigger-width] p-0" align="start">
      <Command>
        <CommandInput placeholder="Buscar marca..." />
        <CommandList>
          <CommandEmpty>Nenhuma marca encontrada.</CommandEmpty>
          <CommandGroup>
            {fipeMarcas.map((m) => (
              <CommandItem
                key={m.codigo}
                value={m.nome}
                onSelect={() => {
                  setMarcaSelecionada(m)
                  setMarcaOpen(false)
                }}
              >
                <Check className={cn('mr-2 h-4 w-4', marcaSelecionada?.codigo === m.codigo ? 'opacity-100' : 'opacity-0')} />
                {m.nome}
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </Command>
    </PopoverContent>
  </Popover>
</div>
<div className="space-y-2">
  <Label>Modelo <span className="text-destructive">*</span></Label>
  <Popover open={modeloOpen} onOpenChange={setModeloOpen}>
    <PopoverTrigger asChild>
      <Button
        type="button"
        variant="outline"
        role="combobox"
        aria-expanded={modeloOpen}
        disabled={!marcaSelecionada || loadingModelos}
        className="w-full justify-between font-normal"
      >
        {modeloSelecionado?.nome ??
          (loadingModelos
            ? 'Carregando...'
            : !marcaSelecionada
              ? 'Selecione uma marca primeiro'
              : 'Selecione o modelo')}
        <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
      </Button>
    </PopoverTrigger>
    <PopoverContent className="w-[--radix-popover-trigger-width] p-0" align="start">
      <Command>
        <CommandInput placeholder="Buscar modelo..." />
        <CommandList>
          <CommandEmpty>Nenhum modelo encontrado.</CommandEmpty>
          <CommandGroup>
            {fipeModelos.map((m) => (
              <CommandItem
                key={m.codigo}
                value={m.nome}
                onSelect={() => {
                  setModeloSelecionado(m)
                  setModeloOpen(false)
                }}
              >
                <Check className={cn('mr-2 h-4 w-4', modeloSelecionado?.codigo === m.codigo ? 'opacity-100' : 'opacity-0')} />
                {m.nome}
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </Command>
    </PopoverContent>
  </Popover>
  {selectedType === 'auto' && marcaModeloCombinado === '' && (
    <p className="text-xs text-muted-foreground">Selecione marca e modelo para continuar.</p>
  )}
</div>
```

**Limpeza final:**
- Remover `interface Cliente` e a prop `clientes` da assinatura `PolicyForm({ slug, clientes, ...})` -> `PolicyForm({ slug, corretores, defaultAssignedTo, lockAssignedToSelf })`
- Remover `insurer: string` e `marca_modelo: string` do tipo `FormValues` (nao mais registrados via RHF)
- Remover `errors.insurer` e `errors.marca_modelo` (validacao agora server-side; erro vem via `setError` do retorno da action — manter compatibilidade no bloco de erro existente)
- NAO mexer em nenhum outro campo (placa, chassi, ano, valor_fipe, cobertura permanecem identicos)

**Validacao de tipos:** rodar `npm run typecheck`. Erros tipicos esperados:
- `Cannot find name 'cn'` -> import de `@/lib/utils`
- Property 'insurer' / 'marca_modelo' on FormValues -> remover dessas duas linhas no tipo
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/app/\(app\)/\[slug\]/seguros/nova/policy-form.tsx src/app/\(app\)/\[slug\]/seguros/nova/page.tsx src/lib/actions/clients-search.ts src/lib/constants/seguradoras.ts</automated>
  </verify>
  <done>
- `policy-form.tsx` nao contem mais `register('insurer')` nem `register('marca_modelo')` (`grep` retorna 0)
- `policy-form.tsx` contem 3 blocos `<Popover>...<Command>` (seguradora, cliente, marca/modelo) — `grep -c "<Command>" src/app/\(app\)/\[slug\]/seguros/nova/policy-form.tsx` retorna pelo menos 4 (3 Command + possivelmente CommandInput etc — alvo: presenca dos 3 Comboboxes)
- `policy-form.tsx` contem `parallelum.com.br/fipe` (2 ocorrencias: marcas + modelos)
- `policy-form.tsx` contem `searchClientsAction(slug, clientQuery)`
- `page.tsx` NAO carrega mais `clients` (`grep -c "from('clients')" src/app/\(app\)/\[slug\]/seguros/nova/page.tsx` retorna 0)
- `page.tsx` NAO passa mais prop `clientes` para `<PolicyForm>` (`grep "clientes=" src/app/\(app\)/\[slug\]/seguros/nova/page.tsx` retorna nada)
- `npm run typecheck` passa sem erros novos
- `npm run lint` nos arquivos modificados passa sem erros
- Verificacao manual (apos task 2 — rodar `npm run dev` e abrir `/[slug]/seguros/nova`):
  - Combobox seguradora abre, filtra digitando "porto", seleciona "Porto Seguro"
  - Combobox cliente: digitar 2 letras dispara busca, resultados aparecem com nome + documento
  - Type=auto: combobox marca carrega lista FIPE, selecionar marca habilita combobox modelo
  - Submeter formulario completo com type=auto cria apolice (verificar via toast "Apolice cadastrada com sucesso" + redirect para `/[slug]/seguros`)
  </done>
</task>

</tasks>

<verification>
**Smoke test ao final:**
1. `npm run typecheck` -> 0 erros novos
2. `npm run lint` -> 0 erros novos
3. `npm run dev` e visitar `http://localhost:3000/[slug]/seguros/nova`:
   - Tipo de seguro = Auto (default)
   - Seguradora: digitar "bra" deve mostrar Bradesco Seguros, Bradesco Saude
   - Cliente: digitar 2 letras dispara busca server-side (verificar Network tab — chamada `searchClientsAction`)
   - Marca: dropdown carrega lista FIPE (verificar Network tab — `parallelum.com.br/fipe/api/v1/carros/marcas`)
   - Selecionar marca habilita combobox de modelo
   - Submeter formulario completo -> toast de sucesso + redirect

**Contrato preservado (NAO MUDAR):**
- `createPolicyAction` ainda recebe `insurer`, `client_id`, `marca_modelo` no FormData -> `grep "fd.set('insurer'\|fd.set('client_id'\|fd.set('marca_modelo')" src/app/\(app\)/\[slug\]/seguros/nova/policy-form.tsx` retorna 3 ocorrencias
- `src/lib/validations/policy-schemas.ts` nao foi modificado (`git diff` vazio para esse arquivo)
- `src/lib/actions/policies.ts` nao foi modificado (`git diff` vazio para esse arquivo)
</verification>

<success_criteria>
**Funcional (observavel pelo usuario):**
- [ ] Combobox de seguradora exibe 50 cias e filtra ao digitar
- [ ] Combobox de cliente busca server-side ao digitar 2+ chars (verificar via dev tools que ha chamada de Server Action)
- [ ] Combobox de marca carrega da FIPE; modelo desabilitado ate marca selecionada
- [ ] Trocar marca limpa modelo selecionado (testar: selecionar Marca A -> Modelo X, trocar para Marca B -> Modelo deve estar vazio)
- [ ] Submit completo de apolice 'auto' funciona (toast verde + redirect para `/seguros`)

**Tecnico (constraints):**
- [ ] `policy-schemas.ts` e `policies.ts` permanecem byte-identicos (`git diff` vazio)
- [ ] FormData enviado contem `insurer` (string), `client_id` (UUID), `marca_modelo` (string concatenada) — campos esperados pelo schema Zod existente
- [ ] `cmdk` em `package.json` dependencies
- [ ] `npm run typecheck` passa
- [ ] `npm run lint` passa nos arquivos modificados

**Reachability check:**
- M1 Seguradora: `SEGURADORAS` constante -> import em `policy-form.tsx` -> render no Combobox -> selecao popula `insurer` state -> `fd.set('insurer', insurer)` no submit -> chega no `createPolicySchema.insurer.min(2)` PASS
- M2 Cliente: input do usuario -> `setClientQuery` -> useEffect debounced -> `searchClientsAction(slug, query)` -> Supabase RLS por `tenant_id` -> resultados em `clientResults` -> selecao popula `clientId` (UUID) -> `fd.set('client_id', clientId)` -> chega no `createPolicySchema.client_id.uuid()` PASS
- M3 Marca/Modelo: useEffect em `selectedType==='auto'` -> fetch FIPE marcas -> selecao popula `marcaSelecionada` -> useEffect dispara fetch FIPE modelos -> selecao popula `modeloSelecionado` -> `marcaModeloCombinado = "${marca.nome} ${modelo.nome}"` -> `fd.set('marca_modelo', marcaModeloCombinado)` -> chega no `createPolicySchema (auto).marca_modelo.min(2)` PASS
</success_criteria>

<output>
After completion, create `.planning/quick/260509-to3-melhorias-no-formul-rio-de-seguros-segur/260509-to3-SUMMARY.md`
</output>
