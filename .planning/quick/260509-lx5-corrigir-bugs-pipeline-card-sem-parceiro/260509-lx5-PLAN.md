---
phase: quick
plan: 260509-lx5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/[slug]/pipeline/page.tsx
  - src/app/(app)/[slug]/pipeline/kanban-board.tsx
  - src/lib/actions/partners.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "KanbanCard exibe nome do parceiro quando assigned_to é null"
    - "Soft delete de parceiro retorna mensagem de erro real do Supabase no toast"
  artifacts:
    - path: "src/app/(app)/[slug]/pipeline/page.tsx"
      provides: "Query com join de parceiro"
    - path: "src/app/(app)/[slug]/pipeline/kanban-board.tsx"
      provides: "Interface Client com partner + renderização condicional"
    - path: "src/lib/actions/partners.ts"
      provides: "softDeletePartnerAction com erro real e detecção de 0 rows"
  key_links:
    - from: "pipeline/page.tsx"
      to: "kanban-board.tsx"
      via: "Client[] passado como prop com partner incluído"
    - from: "softDeletePartnerAction"
      to: "toast no cliente"
      via: "error.message retornado na resposta"
---

<objective>
Corrigir dois bugs no módulo de pipeline e parceiros:
1. KanbanCard não exibe nome do parceiro quando assigned_to é null — faltava join na query e campo na interface.
2. Soft delete de parceiro engole o erro do Supabase e retorna mensagem genérica — impossível diagnosticar falha.

Purpose: Corretores precisam ver quem é o parceiro responsável pelo cliente diretamente no card do pipeline. Admins precisam ver o erro real ao tentar excluir um parceiro.
Output: Query corrigida, interface atualizada, renderização condicional, error handling explícito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar join de parceiro na query do pipeline e atualizar interface + renderização no KanbanCard</name>
  <files>
    src/app/(app)/[slug]/pipeline/page.tsx
    src/app/(app)/[slug]/pipeline/kanban-board.tsx
  </files>
  <action>
**Em `src/app/(app)/[slug]/pipeline/page.tsx` — linha ~29:**

Alterar o `.select(...)` de:
```ts
.select('id, name, type, document, stage_id, assigned_to:profiles!clients_assigned_to_fkey(full_name)')
```
Para:
```ts
.select('id, name, type, document, stage_id, assigned_to:profiles!clients_assigned_to_fkey(full_name), partner:partners!clients_partner_id_fkey(name)')
```

**Em `src/app/(app)/[slug]/pipeline/kanban-board.tsx`:**

1. Interface `Client` (linhas 15-22) — adicionar campo `partner`:
```ts
interface Client {
  id: string
  name: string
  type: string
  document: string
  stage_id: string | null
  assigned_to: { full_name: string } | null
  partner: { name: string } | null
}
```

2. Bloco de renderização do corretor no `KanbanCard` (linhas ~161-165) — alterar para exibir parceiro como fallback quando `assigned_to` é null:
```tsx
{(client.assigned_to || client.partner) && (
  <div className="flex items-center gap-1 text-xs text-muted-foreground">
    <User size={11} />
    <span className="truncate">
      {client.assigned_to?.full_name ?? client.partner?.name}
    </span>
  </div>
)}
```

Não alterar nenhuma outra lógica do componente. Não adicionar importações desnecessárias.
  </action>
  <verify>
    <automated>cd "C:\Users\Reinaldo - Local\Desktop\Cowork\NEXUS-AGENT" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compila sem erros nos arquivos modificados. Interface Client tem campo `partner`. KanbanCard renderiza `partner.name` quando `assigned_to` é null.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir softDeletePartnerAction para retornar erro real e detectar 0 rows afetados</name>
  <files>src/lib/actions/partners.ts</files>
  <action>
**Em `src/lib/actions/partners.ts` — função `softDeletePartnerAction` (linhas ~102-119):**

Substituir o bloco de update:
```ts
const { error } = await supabase
  .from('partners')
  .update({ deleted_at: new Date().toISOString() })
  .eq('id', partnerId)

if (error) return { error: 'Erro ao excluir parceiro.' }
```

Por:
```ts
const { data, error } = await supabase
  .from('partners')
  .update({ deleted_at: new Date().toISOString() })
  .eq('id', partnerId)
  .select('id')

if (error) return { error: `Erro ao excluir parceiro: ${error.message}` }
if (!data || data.length === 0) return { error: 'Parceiro não encontrado ou já excluído.' }
```

Manter o resto da função intacto (verificação de user, verificação de role, revalidatePath, return success).

Não alterar nenhuma outra função no arquivo.
  </action>
  <verify>
    <automated>cd "C:\Users\Reinaldo - Local\Desktop\Cowork\NEXUS-AGENT" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compila sem erros. `softDeletePartnerAction` retorna `error.message` real do Supabase. Retorna mensagem específica quando 0 rows afetados.</done>
</task>

</tasks>

<verification>
Após as duas tasks:
1. `npx tsc --noEmit` sem erros nos arquivos modificados
2. Navegar até o pipeline — cards de clientes com parceiro (sem corretor) devem exibir o nome do parceiro
3. Tentar excluir um parceiro como admin — erro deve aparecer no toast com mensagem real (não genérica)
</verification>

<success_criteria>
- KanbanCard exibe `partner.name` como fallback quando `assigned_to` é null
- Toast de erro ao excluir parceiro mostra a mensagem real do Supabase
- Zero regressões de TypeScript nos arquivos modificados
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260509-lx5-corrigir-bugs-pipeline-card-sem-parceiro/260509-lx5-SUMMARY.md` com os arquivos modificados, decisões tomadas e resultado.
</output>
